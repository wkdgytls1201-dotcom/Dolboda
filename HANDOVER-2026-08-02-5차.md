# 돌보다(Dolboda) 인수인계 — 5차 세션

> 최종 갱신: **2026-08-02** · 이 문서만 읽고 바로 이어서 작업할 수 있게 작성했습니다.
> **새 세션은 반드시 `C:\Users\linea\요양` 폴더에서 열어주세요.**
> 이전 문서: [HANDOVER-2026-08-02.md](HANDOVER-2026-08-02.md)(4차) → [HANDOVER-2026-08-01.md](HANDOVER-2026-08-01.md)(3차) → [HANDOVER.md](HANDOVER.md)(2차)

- 배포: https://dolboda-rho.vercel.app
- 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (`main`)
- 이 세션 커밋: **29개** (`1e42c28` ~ `7c16982`) — 전부 배포됨

### 기술서(별도 문서, 수정 시 함께 갱신할 것)

| 문서 | 대상 | 성격 |
|---|---|---|
| [docs/ai-score-spec.md](docs/ai-score-spec.md) | 안심지수 | ★ 핵심 자산 — 가중치 변경은 사용자 승인 필수 |
| [docs/care-agreement-spec.md](docs/care-agreement-spec.md) | 돌봄 합의서 | ⚠️ 법적 포지션 — 조항 수정 전 반드시 §1 읽을 것 |
| [docs/work-index-spec.md](docs/work-index-spec.md) | 근무환경 지수 | 매니저용 지표, 구간은 실제 분포 기반 |
| [docs/program-taxonomy-spec.md](docs/program-taxonomy-spec.md) | 프로그램 태그 | 공단 12만 건 분류 규칙 |

---

## 0. ★ 사용자가 해야 할 일 (이것부터 확인)

### 0-1. 🔴 GitHub 시크릿 등록 — 안 하면 매일 데이터가 영구 손실된다

이번 세션에 만든 **공단 데이터 일일 자동 수집**(§2-1)이 시크릿 없이는 매일 새벽 실패한다.
시계열은 지나간 날짜를 소급 생성할 수 없어서, 등록이 늦어진 만큼 그대로 손실이다.

저장소 → Settings → Secrets and variables → Actions → New repository secret

| 이름 | 값 |
|---|---|
| `DIRECT_URL` | **필수** — `.env.local`의 DIRECT_URL 그대로 |
| `TELEGRAM_BOT_TOKEN` | 선택 — @BotFather `/newbot`으로 발급 |
| `TELEGRAM_CHAT_ID` | 선택 — 봇에 메시지 후 `api.telegram.org/bot<토큰>/getUpdates`에서 확인 |

등록 후 Actions 탭 → `daily-nhis-sync` → "Run workflow"로 즉시 테스트 가능.

### 0-2. 🟡 공단 사진 zip 재다운로드
10GB 파일이 손상돼(EOCD 없음) 삭제함. 스키마·갤러리 UI는 이미 배포됨(§2-6).
받으면 → 구조 확인 → WebP 최적화(카드 640px/갤러리 1280px) → Supabase Storage 업로드.
`SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`를 `.env.local`에 추가해야 함.
시설의 93%가 `instCode`(공단 기관기호 11자리)를 갖고 있어 기관기호 기준이면 매핑이 정확하다.

### 0-3. 🟡 정식 도메인 결정 (3차부터 이월)
상담신청 자동 이메일(코드 완성·발신 도메인 인증만 남음)과 SEO canonical이 여기 막혀 있다.

### 0-4. 🟢 pg_cron (급하지 않음)
스냅샷은 지금 GitHub Actions로 적재되므로 당장은 불필요.

---

## 1. 이 세션에서 새로 배운 것 — 다음 세션이 반드시 알아야 할 것

### 1-1. 로컬에서는 카카오 SDK가 아예 차단된다 (검증 불가 구간)

`autoPort` 때문에 dev 서버가 임의 포트(예: 50032)로 뜨는데, 그 포트는 카카오 앱에
등록돼 있지 않아 **SDK 요청이 `status 0, size 0`으로 차단**된다. 지도·로드뷰가 실제로
그려지는지는 **로컬에서 확인할 방법이 없다.**

→ **카카오 관련 변경은 "배포 후 프로덕션 확인"까지를 한 세트로 잡을 것.**
이번에 이걸 안 지켜서 실제로 사고가 났다(§1-2).

### 1-2. effect에 가드를 넣으면 의존성 배열도 반드시 같이 본다

`KakaoMap`에 `if (!inView) return;`을 넣고 의존성 배열은 `[lat, lng]` 그대로 뒀다.
마운트 시 `inView=false`로 한 번 돌고 끝나서 **지도가 영영 안 그려졌다.** 프로덕션
실측(로드뷰는 나침반까지 정상인데 지도 컨테이너만 자식 0개)으로 발견해 `c649258`로 수정.

### 1-3. 이 자동화 환경은 프레임을 그리지 않는다 — IO·rAF가 죽는다

브라우저 패널이 표시되지 않으면 **IntersectionObserver도 requestAnimationFrame도
콜백이 발화하지 않는다**(실측 확인). 그래서:
- 스크롤·등장 관련 코드를 rAF로 짜면 이 환경에서 검증이 불가능하고, 백그라운드 탭에서도 밀린다
  → **setTimeout을 쓸 것** (등급테스트 자동 스크롤·`useInViewOnce` 스로틀 모두 이 이유로 교체)
- IO에만 의존하면 검증도 못 하고 특이 웹뷰에서 기능이 죽을 수 있다
  → `lib/useInViewOnce.ts`는 IO + 스크롤 좌표 계산 **이중 감지**로 만들었다

### 1-4. `position: fixed`는 조상의 `backdrop-blur`에 갇힌다

필터 바텀시트가 모바일에서 화면 밖으로 밀려 안 보였다. 원인은 상단 필터 바 조상의
`backdrop-blur`가 새 containing block을 만들어 `fixed inset-0`이 뷰포트가 아니라
그 작은 바 크기로 잡힌 것. → **모달·시트는 `createPortal(document.body)`로 그린다.**

### 1-5. 민감정보 동의는 미리 체크해두지 않는다

"동의 체크박스를 디폴트로 켜달라"는 요청이 있었으나, 건강 정보는 민감정보라
사전 체크된 동의는 개인정보 보호법상 유효한 명시적 동의로 인정받기 어렵다.
(필수) 표시와 가독성 개선만 하고 디폴트 체크는 넣지 않았다.
→ 광고성 이메일 건(3차)과 같은 계열의 판단이니 앞으로도 이 기준을 유지할 것.

### 1-6. 실측 없이는 "개선"인지 알 수 없다

- 홈이 추천 6곳을 뽑으려고 전국 200건(**149KB**)을 받고 있었다 → 전용 API로 11.6KB
- 시설 상세가 열자마자 카카오 요청 **15건**(SDK·타일 6장·로드뷰) → 0건
- 공단 데이터 갱신 주기를 "예측"하는 대신 `sourceDate` 분포를 뽑아 **평일 200~300곳/일**
  갱신, 주말 급감을 확인 → "매일 1회 수집"의 근거가 됨

---

## 2. 이 세션에서 완료한 것 (커밋 29개)

### 2-0. 돌봄 합의서 + 전자서명 — `3efa821`, `e75982a` ★ 법적 포지션이 걸린 기능

**반드시 [docs/care-agreement-spec.md](docs/care-agreement-spec.md)를 먼저 읽을 것.**

보호자·매니저가 구두 합의만으로 진행하다 분쟁이 나는 걸 막되, 회사가 계약 당사자가
되지 않는 선을 지켰다(계약을 "체결"시키면 직업안정법상 유료직업소개사업 등록 문제).

| 하는 것 | 하지 않는 것 |
|---|---|
| 표준 양식 제공 | 계약 체결·중개 |
| 서명 사실 기록·보관 | 사례비 결정·지급·정산 |
| 사본을 양측 이메일로 발송 | 상대방 신원·자격 보증 |
| 위변조 방지(SHA-256) | 이행 감독·분쟁 개입 |

- `/care-request/agreement` — 보호자·매니저 공용 열람·서명 화면
- `components/SignaturePad.tsx` — 손글씨 서명(포인터 이벤트로 마우스·터치·펜 통합,
  DPR 반영해 서명이 뭉개지지 않게, `touch-action:none`으로 획이 스크롤로 새지 않게)
- **봉인 구조**: 첫 서명 시점의 내용을 `content(jsonb)`에 통째로 넣고 SHA-256을 저장.
  원본 요청이 나중에 바뀌어도 합의서는 그대로이고, 조회 시 해시를 다시 계산해
  `hashValid`로 위변조를 검증한다
- **서명 증거**: 이름·서명 이미지·서버 시각·접속 IP·기기 정보·계정 이메일
- `CareAgreement`는 User·CareRequest에 **관계를 걸지 않았다** — Cascade로 묶으면 한쪽
  탈퇴 시 합의서가 사라져 남은 당사자가 권리를 증명할 수 없다
- 정책 반영: 이용약관 제12조 2개 항 신설, 개인정보처리방침에 서명 기록 항목 + **3년 보관** 근거

⚠️ 조항은 참고용 표준 문안이며 법률 자문이 아니다 — 서비스 확대 전 변호사 검토 필요.

### 2-0-2. 돌봄 흐름 상태 전이 버그 4건 — `3e8feab`, `7c16982`

화면에서는 버튼을 숨겨 막고 있었지만 **API에 상태 가드가 없어** 순서가 어긋난 요청이
들어오면 두 사람이 서로 다른 화면을 보게 됐다.

1. **요청 취소 시 지원자를 방치** (가장 큰 문제) — 취소해도 지원은 "지원완료"·"매칭확정"
   으로 남아 매니저는 계속 답을 기다렸다 → "요청취소"로 정리하고 매니저 화면에
   "보호자가 요청을 취소했어요"로 표시(탈락과 구분)
2. 완료 처리에 가드 없음 — OPEN·취소된 건도 완료돼 매니저 실적이 어긋날 수 있었다 → MATCHED만
3. 확정 후에도 요청 수정 가능 — 매니저가 동의한 조건이 바뀔 수 있었다 → OPEN만
4. 확정된 지원을 매니저가 삭제 가능 — 요청은 확정인데 지원자만 사라졌다 → "지원완료"만 철회
5. 취소 확인 팝업 신설 + **확정 후 취소 경로 신설**(사정이 생겨도 화면에 방법이 없었다)

실측 검증: 임시 스크립트로 DB에 시나리오 3종을 재현해 전부 확인 후 삭제.

### 2-0-3. 로그인 세션 database → JWT — `75dcaee`

인증 없는 API는 20~30ms인데 **인증 필요 API는 570~1,660ms**였다(실측). `database`
전략은 `auth()`마다 세션 테이블을 조회해 로그인 사용자의 DB 왕복이 두 배로 든다.
전환 후 세션 조회 **221~264ms로 4~5배 개선**.

- PrismaAdapter는 유지 — 카카오 계정 연결·사용자 생성·`talk_message` access_token 저장
- 맞바꾼 것: 기존 세션 전부 무효화(재로그인 필요), 강제 로그아웃 불가,
  탈퇴 후 토큰 만료 전까지 유효(단 User 행이 지워져 조회는 비고 생성은 FK에 막힘)

### 2-0-4. 마이페이지 대시보드 API 4개 → 1개 — `b8b763c`

`/api/mypage/summary` 신설. 세션 검사 1회 + 쿼리 병렬로 묶어 왕복 4번 → 1번.
(마이페이지 API 호출 8개 → 5개)

### 2-1. 공단 데이터 일일 자동 수집 + 시계열 스냅샷 — `022d9a6` ★ 이번 세션 최대 성과

"시계열 스냅샷을 쌓자"는 계획의 전제(데이터가 자동 갱신되는가?)를 사용자가 짚어서
확인해보니, 현원·대기 데이터는 **손으로 받은 엑셀 16개**가 원천이라 멈춰 있었다.
그래서 수집 자동화부터 만들었다.

- **다운로드 구조 실측**: `GET /npbs/r/a/201/selectXLtcoSrch.web?siDoCd={코드}`로 세션을
  만들고 → `POST /npbs/r/a/201/selectLtcoSrchMapExcel.web`이면 xlsx가 바로 온다.
  로그인·캡차·토큰 없음. 16개 시도 코드는 스크립트에 있음.
- `scripts/daily-nhis-sync.mjs`: 다운로드 → 기존 임포트와 **동일 규칙 파싱**(name+address
  해시 id라 매칭이 어긋날 수 없음) → 변경 시설만 UPDATE → `FacilitySnapshot` 적재
- **안전장치**: 엑셀 아닌 응답 거부, 고유 시설 2만 미만이면 반영 중단(사이트 개편 감지),
  지역당 3초 간격
- **텔레그램 알림**: 변경 수·자리 난 시설·등급 변동·신규/폐업 감지 요약 + 실패 경고
- `.github/workflows/daily-nhis-sync.yml`: 매일 04:00 KST + 수동 실행 버튼
- **첫 실행 완료**: 45,261행 → 27,672곳, 변경 1,681곳 반영, **스냅샷 기준선 27,493행 적재**

`FacilitySnapshot`은 변경분만 저장한다(용량 절약). 특정 날짜 값 = 그 시설의
`date <= D` 중 최신 행.

### 2-2. 성능 3건

| 대상 | 전 | 후 | 커밋 |
|---|---|---|---|
| 홈 첫 화면 페이로드 | 149KB(200건) | **11.6KB** | `013bda7` |
| 시설 상세 카카오 요청 | 15건 | **0건**(스크롤 시 로드) | `645d62d`+`c649258` |
| 시설 목록 API | 매번 DB 스캔 | CDN 1시간 캐시 | `d6ff04b` |

- 홈: `/api/facilities/home` 신설(추천·최신설립·등급순 각 6곳). 겸사 발견 — 기존 추천은
  `createdAt` 동점이 수천 건이라 순서가 흔들릴 수 있었다(id 2차 정렬로 고정)
- 상세: `lib/useInViewOnce.ts` 신설, KakaoMap·KakaoRoadview에 적용

### 2-3. 시설찾기 개편 — `ffc5117`, `4368a2c`, `07bde50`, `40ba708`

- **안심지수를 전면 활용**: 목록 카드 배지, "안심지수 우수만" 필터, "안심지수순" 정렬
  (기존 "평점순"은 실제 평점 데이터가 없는 스텁이라 교체 — 사용자 승인)
  점수는 서버(`toCardFacility`)가 계산해 숫자 하나만 실어 보낸다
- **필터를 케어닥식 단일 버튼 + 바텀시트로** 재구성(가로 스크롤 7개 드롭다운 → "필터" 1개)
- 모바일 버그: 시트가 헤더에 가려지던 것(§1-4), 배경 스크롤 새어나감(body fixed 잠금),
  터치 타겟 44px 보강

### 2-4. 검색 복귀 시 보던 자리 복원 — `f749124`
스크롤 위치·펼친 카드 수를 저장했다가 상세에서 돌아오면 복원. 실측: 2,400px·90장 정확 복원.

### 2-5. 상담 신청 내역 화면 — `ee312a3`
API(`/api/consult/mine`)만 있고 화면이 없던 것. `/mypage/consults` 신설.
진행 상황(신청함→연락받음→입소결정)을 보호자가 직접 표시. **탈중개 방지 원칙 적용 —
"전화 걸기" 버튼 없이 다음 행동을 전부 플랫폼 안(다시 보기·비교·관심시설)으로 유도.**
진입점 3곳: 마이페이지 메뉴, 모바일 빠른 메뉴, 대시보드 카드(입소 미결정 건이 있을 때만).

### 2-6. 시설 사진 영역 스키마·갤러리 — `5529d39`
`FacilityPhotoArea` 9종(외관/입구·로비/거실·공용/생활실/프로그램/식당/재활/마당/기타).
**메인은 반드시 외관 사진**(찾아갈 때 알아볼 수 있어야 함). 갤러리는 영역 라벨 표시 +
외관→생활공간→활동→부대시설 순 정렬, 첫 장만 eager.

### 2-7. 모바일 앱 패키징 대비 — `4aa5ece`
웹 앱 매니페스트(안드로이드 TWA 필수), iOS `appleWebApp` 메타,
`--safe-top` CSS 변수로 헤더·sticky 바 4곳 노치 대응(브라우저에선 0이라 화면 변화 없음).

### 2-8. QA 피드백 19건 — `1e42c28`(11건), `bc12dfb`(8건)
- 워딩: "돌봄 프로필" → **"보호자 프로필"** 전면 변경
- 등급테스트: 답하면 **다음 문항으로 자동 스크롤**, AI도우미 배너를 시작 전에만 노출,
  결과가 나오면 "예상 등급"부터 보이게 스크롤
- 마이페이지: 인사 카드 신설, 보호자/매니저 섹션 헤더 확대, 지원·매칭·완료 숫자 링크화,
  카드 강조 띠("돌보다 단독"·"꿀팁"), 서브 화면에서 매니저 메뉴 숨김(과스크롤 해소)
- 버그: 매니저 가입 1/5 "다음" 버튼이 탭바에 가려짐, 돌봄 완료 즉시 종료(확인 팝업 추가),
  대시보드 ISO 날짜 노출, **"전남"·"광주" 활동 지역의 주소 접두어 매핑 누락**(전라남도
  시설이 통째로 빠지고 있었음)
- 일하기 좋은 시설: 지역 탭을 전국 17개로(기존엔 본인 활동 지역 10개만 나와 나머지를
  고를 수 없었음)

### 2-9. 모바일 가독성·터치 타겟 전수 점검 — `986e65a`, `b8b763c`, `720bb03`
- **시설 카드 주소가 잘리고 있었다**(실측: 325px 주소가 226px만 보임 — 보호자가 가장
  먼저 보는 "어느 동네인가"를 알 수 없었다). `lib/shortAddress.ts`로 시/도 정식명칭만
  줄이고(구·동은 그대로) 두 줄 허용 → 잘림 5건 → 0건
- 44px 미만 터치 타겟 일괄 보강: 기간 빠른선택 36px, 달력 화살표·헤더 버튼·푸터 링크
  26px, 지원자 확정 버튼 42px, 알림 토글 등
- 푸터의 SEO용 지역 링크(15px)는 **의도적으로 존재감을 낮춘 것이라 그대로 둠**

### 2-10. 매칭 확정 경쟁 조건 + 계좌번호 마스킹 — `908b6e2`, `f2d8071`
- 서로 다른 지원자를 거의 동시에 확정하면 둘 다 검사를 통과해 **두 명이 매칭확정**될 수
  있었다 → 트랜잭션 안에서 조건부 `updateMany`로 요청을 선점한 쪽만 진행(실측 검증)
- 매니저 계좌번호를 본인 화면에서도 전체 노출하지 않도록 마스킹(`lib/maskAccount.ts`)

---

## 3. 확정된 규칙 (이번 세션 신규 — 3·4차 규칙에 추가)

1. **카카오 관련 변경은 프로덕션 확인까지가 한 세트** (§1-1, 로컬 검증 불가)
2. **스크롤·등장 코드에 rAF 대신 setTimeout** (§1-3)
3. **모달·바텀시트는 createPortal(document.body)** (§1-4)
4. **민감정보 동의는 사전 체크하지 않는다** (§1-5)
5. **effect에 가드를 넣으면 의존성 배열을 반드시 함께 본다** (§1-2)
6. 새 인터랙션 요소는 **44px 터치 타겟** 확인 (4차에서 이어짐, 이번에도 여러 건 걸림)
7. **회사가 계약 당사자가 되는 방향으로 가지 않는다** — 합의서·매칭 문구를 고칠 때
   "양식 제공·기록 보관"까지가 선(docs/care-agreement-spec.md §1)
8. **상태를 바꾸는 API에는 현재 상태 가드를 넣는다** — 화면에서 버튼을 숨기는 것만으로는
   막히지 않는다(이번에 유령 매칭·유령 지원 4건이 여기서 나왔다)
9. **서버 전용 모듈(node:crypto 등)을 클라이언트가 import하는 파일에 두지 않는다**
   — 합의서 화면 번들이 236KB로 부풀었던 원인(파일 분리로 106KB 정상화)

---

## 4. 다음 할 일

### 4-1. 검증이 안 끝난 것
- **돌봄 합의서 전 흐름을 실제로 한 번 밟아볼 것** — 매칭 확정 → 양측 서명 →
  이메일 사본 수신까지. 특히 이메일은 발신 도메인이 `onboarding@resend.dev`(테스트
  전용)라 **실제로는 발송이 거부될 가능성이 크다**(§0-3 도메인 결정에 막혀 있음).
  화면·해시·서명 기록은 정상 동작하므로 도메인만 정하면 바로 열린다.
- 돌봄 요청 5단계 마법사 완주는 실데이터가 생겨(전국 매니저에게 공고 노출) 보류함.

### 4-2. 안심지수 고도화 후보
| 항목 | 상태 |
|---|---|
| 행정처분 공표 반영 | 스크립트·설계(weight 4) 완성, **파일만 있으면 즉시** |
| 실사진 보유 여부(투명성) | 사진 파이프라인 후 |
| CareReview(자체 매칭 실적) | 표본 0건 — 시설당 최소 3건 게이팅 필요 |
| 시계열 기반 신호(등급 추세 등) | 스냅샷이 쌓이면 가능 |

### 4-3. 기능 후보
1. **카카오톡 알림** — `talk_message` 권한은 이미 받고 있음, 발송 인프라만 필요.
   리텐션 논의에서 최우선.
2. **찜한 시설 자리 알림** — 스냅샷 3개월치면 실체를 가짐(지금 알림 화면은 껍데기)
3. 홈 "이 지역 안심지수 TOP" 배지 — 이미 받은 목록으로 계산만 하면 됨(추가 요청 없음)
4. 옵트인 구독 링크(합법적 마케팅 리스트)

---

## 5. 이번 세션 신규·주요 변경 파일

```
── 합의서·전자서명 (법적 포지션 — docs/care-agreement-spec.md 먼저 읽을 것)
lib/careAgreement.ts                 ★ 신설 — 표준 조항 10개·정규화·문서번호
lib/careAgreementHash.ts             ★ 신설 — SHA-256(서버 전용, 번들 분리 이유는 §3-9)
app/api/care-agreement/route.ts      ★ 신설 — 조회·서명·사본 발송
app/care-request/agreement/page.tsx  ★ 신설 — 열람·서명 화면(보호자·매니저 공용)
components/SignaturePad.tsx          ★ 신설 — 손글씨 서명(포인터 이벤트·DPR)
prisma/schema.prisma                  CareAgreement 신설(관계 미설정이 의도)
app/terms/page.tsx · app/privacy/page.tsx   정책 반영

── 자동 수집·시계열
scripts/daily-nhis-sync.mjs          ★ 신설 — 공단 일일 자동 수집(§2-1)
.github/workflows/daily-nhis-sync.yml ★ 신설 — 매일 04:00 KST
prisma/schema.prisma                  FacilitySnapshot 신설

── 성능·구조
auth.ts                               database → JWT 전략(§2-0-3)
app/api/mypage/summary/route.ts      ★ 신설 — 대시보드 4개 API 통합
app/api/facilities/home/route.ts     ★ 신설 — 홈 전용 경량 목록
lib/useInViewOnce.ts                 ★ 신설 — IO+스크롤 이중 감지(§1-3)
lib/shortAddress.ts                  ★ 신설 — 카드 주소 잘림 해소
lib/facilityRepo.ts                  toCardFacility가 안심지수 총점 계산

── 흐름·화면
app/api/care-requests/[id]/route.ts  상태 가드(완료·수정·취소) + 트랜잭션
app/api/care-request-applications/[id]/route.ts  확정 경쟁 조건·철회 가드
components/CareRequestDetail.tsx     완료·취소 확인 팝업, 합의서 진입점
app/mypage/consults/page.tsx         ★ 신설 — 상담 신청 내역
app/manifest.ts                      ★ 신설 — 웹 앱 매니페스트
components/FilterBar.tsx             바텀시트로 전면 재구성
components/MyPageShell.tsx           인사 카드·섹션 헤더·상담 내역 메뉴
components/KakaoMap.tsx / KakaoRoadview.tsx   지연 로드
app/HomeClient.tsx                   200건 요청 제거 → 경량 API
app/search/SearchPageClient.tsx      안심지수 정렬·필터, 스크롤 복원
```

## 6. DB 현황 (2026-08-02 기준)

| 항목 | 수치 |
|---|---|
| Facility | 28,844 (실데이터 28,834) |
| **FacilitySnapshot** | **27,493 (8/2 기준선, 오늘부터 매일 적재)** |
| 시설 이메일 보유 | 23,290 (81%) |
| instCode 보유 | 26,717 (93%) — 사진 매핑 키 |
| CareProfile / CareReview | 신규 기능, 실사용 시작 단계 |
| CareAgreement | 신설(이번 세션) — 아직 실사용 서명 없음 |

---

## 7. 돌봄 흐름 상태 전이 지도 (수정 후 기준)

새 기능을 붙이거나 상태를 건드릴 때 이 표와 어긋나지 않는지 먼저 확인할 것.

| 단계 | CareRequest | Application | 누가 | 가드 |
|---|---|---|---|---|
| 요청 등록 | `OPEN` | — | 보호자 | — |
| 매니저 지원 | `OPEN` | `지원완료` | 매니저 | OPEN일 때만, 중복 불가(unique) |
| 지원 철회 | `OPEN` | (삭제) | 매니저 | **`지원완료`일 때만** |
| 매칭 확정 | `MATCHED` | `매칭확정` / 나머지 `미선정` | 보호자 | OPEN일 때만, **조건부 updateMany로 선점** |
| 합의서 서명 | `MATCHED` | `매칭확정` | 양측 | 각자 1회, 첫 서명에 내용 봉인 |
| 요청 수정 | `OPEN` | — | 보호자 | **OPEN일 때만** |
| 요청 취소 | `CANCELLED` | `요청취소` | 보호자 | OPEN·MATCHED일 때만, **지원자 상태 함께 정리** |
| 돌봄 완료 | `COMPLETED` | `돌봄완료` | 보호자 | **MATCHED일 때만** |
| 후기 작성 | `COMPLETED` | `돌봄완료` | 보호자 | 요청당 1건, 실제 완료된 매니저만 |

굵게 표시한 가드가 이번 세션에 추가된 것들이다(§2-0-2).
