# 돌보다 인수인계 — 17차 세션 (2026-08-06)

이 문서만 읽고 이어서 작업할 수 있게 썼습니다. 16차 직후 이어진 **24커밋 세션**
(`40c1111` → `84c6fb3`, 전부 푸시·Vercel 배포 라인).

몸통은 넷입니다:
① **시설찾기 로딩 최적화**(사용자 요청) ② **전수 감사 — 실버그·구조 결함 다수**
③ **안심지수 방문요양 편향 수정**(6,270곳이 "확인 필요"에서 벗어남) ④ **사진 5개 지역 처리 + 매칭 개선**(32%→72%) ⑤ **검색엔진 등록**

이전: [16차](HANDOVER-2026-08-06-16차.md). 15차 §2 방침, 13차(사진 파이프라인),
12차(B2B), 10차(알림), 5차 §1·§3 여전히 유효.

---

## ⚡ 다음 세션에서 바로 시작할 일

1. **🔴 다음 사진 zip이 오면 처리.** 남은 지역은 **충청·전북·전남(광주 제외)·제주·세종·울산**
   정도다. 순서는 13차 그대로(inventory → convert → extras → upload), 검증 후 **6개 조건**으로
   삭제 판단(§3-1). 매칭 2차 시도가 들어가 있어 **유실 없이 한 번에 끝난다** — 회수 작업 불필요.
   ⚠️ **extras 전에 upload를 돌리지 말 것**(여분 사진이 조용히 누락된다).
2. 개발 순위(사용자 합의): ① 사진 zip ② **GA4/네이버 애널리틱스**(측정 ID만 받으면 코드는
   바로 붙는다) ③ B2B 첫 고객 전 실사용 검증 ④ **카카오톡은 마지막**.
3. 사진이 **72%**가 됐으니 16차의 사진 활용 3종(캡션 SEO·실사진 필터·영역 분류)이 제 값을 한다.
   **실사진 필터가 더는 "크게 좁히는 필터"가 아니다**(32%일 땐 결과가 1/3로 줄었다).
4. **검색엔진 등록이 이 세션에서 끝났다**(§1-9). 며칠 뒤 서치콘솔 **페이지** 화면에서
   색인 숫자가 오르는지 확인할 것. 신규 도메인은 색인까지 **2~6주** 걸린다.

> ✅ **경북·강원, 전남광주·대전 사진은 이 세션에서 전부 끝났다**(§1-7). 두 지역 원본 zip은
> 6개 조건을 전부 충족해 **삭제 가능**하다고 사용자에게 알렸다.

## 0. ★ 사용자가 직접 해야 할 일

- 🔴 **`MAIL_FROM`을 GitHub Secrets에서도 `noreply@send.dolboda.kr`로.** 로컬 `.env.local`은
  이 세션에서 바꿨다(백업 `.env.local.bak-20260806`). **실제 배치 발송은 Actions에서 도니
  거기 값을 바꿔야 SPF가 맞는다.** 지금은 DKIM으로 도착하지만 DMARC를 올리면 전부 스팸행.
- 🟡 GA4 계정 생성 + 측정 ID(`G-XXXXXXX`) — 16차에서 이월.
- 🟡 서치콘솔 canonical · 네이버 로그인 프로덕션 · `ADMIN_USER_IDS` · R2 env 5종 — 이월.
- 🟡 **기관 마스터 재임포트 시** `scripts/build-fee-benchmarks.mjs`도 같이 돌릴 것.

---

## 1. 이 세션에서 한 일

### 1-1. 시설찾기 로딩 최적화 (`e3a1b5a`)

사용자가 "뜨는 게 느리다"고 해서 **재보고** 고쳤다(5차 §1-6 — 실측 없이는 개선인지 모른다).

| 문제 | 실측 | 수정 |
|---|---|---|
| `ORDER BY createdAt` 인덱스 없음 | Seq Scan **28,834행** + 정렬 | `@@index([createdAt])` → **Index Scan 310행, 2.9ms** |
| 카드가 안 쓰는 `extra`를 통째로 가져와 버림 | DB→서버 **1,628KB / 259ms** | SQL 투영 → **741KB / 168ms** |
| **클라이언트 캐시가 아예 없었다** | 뒤로가기마다 300건 재요청 | 세션 캐시(5분 TTL·LRU 12) → **복귀 시 네트워크 0** |

- `lib/facilityCardQuery.ts` 신설 — 점수 계산에 필요한 만큼만 SQL에서 남긴다
  (사진 첫 장만 · programs는 카테고리 종류만 · tenure는 합계 한 줄 · institutionInfo 5필드 ·
  programTags.samples 제거). 검색·주변 두 API가 공유.
- **`scripts/verify-card-projection.mjs` 신설** — 추린 페이로드가 "점수가 읽는 입력값"을
  하나도 바꾸지 않는지 **28,844곳 전수 대조**. 점수 함수가 TS라 못 부르므로 **입력값 동일성**을 본다.
  ⚠️ `lib/dolbodaScore.ts`가 새 `extra` 항목을 읽게 되면 투영 SQL과 이 스크립트를 같이 늘릴 것.
- **더 안 깎은 이유**: 응답이 brotli로 **22KB**라 JSON을 더 줄여도 체감이 안 바뀐다.

**★ 첫 화면 서버 렌더까지 마쳤다**(`9011b5f`). 위 셋을 하고도 **첫 방문의 순서**는
그대로였다 — HTML → 번들 → 하이드레이션 → 그제서야 300건 요청 → 카드. 저가형 폰에서
체감이 가장 큰 왕복이 이것이다. 이제 서버가 **첫 페이지 분량(30장)**을 들고 내려온다.

- 30장만 보내는 이유: 300건을 HTML에 실으면 190KB가 붙는다. 첫 화면에 보이는 건
  `PAGE_SIZE`(30)장뿐이라 그만큼만 보내고 나머지는 기존 요청에 맡긴다.
- **하이드레이션을 깨지 않는 요령**: 서버와 클라이언트의 **첫 렌더 출력이 같아야** 한다.
  복원(`useLayoutEffect`)은 하이드레이션 뒤에 도니 첫 렌더는 양쪽 다 기본값이고,
  그래서 양쪽 모두 `initialFacilities`를 그린다. 복원 조건이 기본과 다르면 곧바로
  요청이 나가 대체된다(뼈대를 안 거쳐 깜빡임도 없다).
- 검증: HTML에 **시설 링크 30개** 포함 · **Suspense 바일아웃 없음**(`useSearchParams`가
  있어 이게 관건이었다) · 하이드레이션 오류 0 · 번들 87.4kB 그대로.
- `unstable_cache` 1시간. 서버 조회가 실패해도 빈 배열로 떨어져 기존 경로로 간다.

⚠️ **dev에서 `/api/auth/session` 500이 보이면** `loadManifest` JSON 파싱 실패인지 보라 —
**다른 세션 dev 서버와 `.next`를 공유**해서 나는 환경 문제다(코드 문제가 아니다).

### 1-2. B2B 판매 전 실태 점검 (`40c1111`)

유료 기능 테이블이 **전부 0행**(실측) — 승인→입금확인→노출→해지가 프로덕션에서 한 번도
안 돌았다. 첫 고객 전에 셋을 고쳤다.

1. **지역 슬롯 오버셀** — 승인이 만드는 슬롯은 입금 대기라 `active=false`인데 상한 검사는
   `active=true`만 셌다. 3자리에 3곳을 승인해도 "남은 자리 3"으로 보였고, 노출 조회의
   `take` 상한에 걸려 **나중 계약 시설이 돈 내고 노출 0**이 된다.
   → 자리 점유 기준(입금대기·일시중지 포함, 해지·만료 제외) + **활성화 시점 재검사**.
2. **구독 하나를 건드리면 그 시설 광고가 전부 켜지고 꺼짐** — `updateMany`가 시설 단위였다.
   → `SponsorPlacement`·`FacilityBanner`에 `subscriptionId`.
3. **`endsAt`이 지워지고 해지가 되살아남** → 해지일 때만 쓰고, 해지는 되돌릴 수 없다(409).

검증: 슬롯 시나리오 12케이스(실제 DB에 5상태 심고 대조, 테스트 행 정리). 기능서
[docs/business-console-spec.md](docs/business-console-spec.md) §2-1~2-3 신설.

### 1-3. 근속을 검색 전면으로 (`9724402`)

시니어 플랫폼 조사에서 나온 것 — **새로 만들 것보다 "가진 걸 안 쓰는 것"이 컸다.**
국내 보호자 체크리스트와 미국 CMS(2022년 이직률을 요양원 별점에 편입)가 독립적으로 같은
신호를 지목하는데, 우리는 근속 분포를 **26,235곳(91%)** 갖고도 상세 안쪽에만 뒀다.

- `lib/tenureSignal.ts` 신설. 임계값은 **실제 분포의 3사분위수** — 요양원 60% · 주야간 62% ·
  방문요양 52%. 표본 가드 5명. 실측 통과율 25%·25%·21%(안심지수 "우수"와 같은 상위 25% 기준선).
- 카드에 근속 배지 + **돌봄직원 1명당 어르신 수**. 배지는 상위 25%에만 — 낮은 곳에 표시를
  달면 낙인이 된다.
- ⚠️ **요양병원은 조건에서 제외**(심평원 자료에 근속 항목이 없어 1,282곳 전부 데이터 없음).
- 알림 스크립트의 저장검색 매칭에도 같은 조건을 넣었다. **실사진 조건도 같은 구멍이 있어
  함께 막았다**(16차 때 누락).

### 1-4. ★ 전수 감사 3건 (`b2afbcc`)

사용자가 "인간이 하기 힘든 것"을 물어 **전수·교차대조**를 돌린 결과.

- **좌표 없는 1,163곳 중 970곳 복구**(`scripts/geocode-missing.mjs` 신설). 좌표가 없으면
  지도·"내 주변"·거리순에서 **통째로 안 보인다** — 주야간보호는 전체의 10%가 그 상태였다
  (543곳 → 76곳). 기존 `geocode-nhis.mjs`로는 안 됐다(옛 JSON 대상 + 상세주소 제거 규칙이
  `123층` 형태만 다룸). 실제 실패 주소는 `5,6층501호`·`601,702호`·`3· 4층`이었고,
  **"층·호 지우기"를 "건물번호까지만 남기기"로 뒤집으니 표본 3/15 → 14/15**가 됐다.
  가드 셋: 대한민국 위경도 범위 · 카카오 시도와 주소 시도 일치(111건이 여기서 걸림) ·
  이미 좌표 있는 행은 안 건드림. 남은 193곳은 주소가 `(영광읍)`뿐인 원본 결손.
- **죽은 필터 조합 2개**: 요양병원+실사진 = 항상 0곳(요양병원 1,282곳 중 실사진 **0곳** —
  공단 사진은 장기요양기관만), 빈자리 필터가 방문요양·요양병원에서 무효(정원 개념 없음).
  전자는 유형 제외, 후자는 필터 시트에서 미리 밝힌다.
- **안심지수 vs 등급 교차 대조**(`scripts/audit-score-vs-grade.mjs` 신설, 18,136곳).
  스피어만 **−0.530** — 방향 정상이고 등급의 복사본도 아니다. 1등급인데 하위20%는 1곳,
  5등급인데 상위20%는 0곳. **지표 자체는 건강하다.**

### 1-5. ★ 안심지수 — 방문요양 편향 수정 (`08d15aa`)

위 교차 대조에서 나온 것. **방문요양 15,677곳(전체의 54%) 중 "우수"가 0.5%뿐이고
47.3%가 "확인 필요"**였다.

원인은 한 줄이었다 — **간호인력 0명 = 30점 감점**. 그런데 방문요양센터에 간호사가 없는 건
결격이 아니다(간호는 방문간호라는 별도 급여, 방문요양은 요양보호사가 가정을 찾는 서비스).
의료 영역 평균이 30점(타 유형 64~86)에 눌렸고 그 영역 가중치가 23%라, **유형 간 격차의
대부분이 이 한 항목에서 나왔다.**

→ 해당 없음이므로 감점도 가점도 하지 않고 `PRIOR_SCORE`(65) 중립값.

| 유형 | "우수" 이상 | "확인 필요" |
|---|---|---|
| 요양병원 | 58.9% → 58.9% | 1.0% → 1.0% |
| 요양원 | 72.4% → 72.4% | 1.2% → 0.9% |
| 주야간보호 | 38.9% → 38.9% | 3.1% → 2.6% |
| **방문요양** | **0.5% → 13.4%** | **47.3% → 7.3%** |

**다른 유형은 한 곳도 내려가지 않는다.** 조건은 정원 값이 아니라 **유형**으로 판정한다
(요양원 16곳·주야간 13곳은 정원이 0인 데이터 결손이라 평균 수렴 보정을 받는 게 맞다).

### 1-6. 포인트 24개월 소멸 구현 (`9ce0685`)

약관 제12조의2와 `/mypage/points`는 "24개월 뒤 사라진다"고 하는데 **잔액 계산에 구현이
없었다.** 사용자 결정으로 고지대로 구현.

- **단순히 "소멸분 빼기"가 아니다.** 원장은 어느 적립을 썼는지 기록하지 않아, 그냥 빼면
  이미 쓴 포인트를 두 번 빼서 이용자가 손해를 본다. → **오래된 적립부터 쓰인 것으로 본다(FIFO).**
- **잔액 조회와 사용 차감이 같은 함수를 쓴다** — 어긋나면 "화면엔 없는데 결제는 되는" 상태.
- 잔액 카드에 "N P가 YYYY년 M월 D일에 사라져요" 표시.
- 검증: 시나리오 14케이스 + 실데이터 대조(10,000P → 소멸예정 2028-08-05).

### 1-7. 사진 — 두 지역 완료, 매칭 개선 (`cd0b711` · `c14cdea`)

| 지역 | inventory | convert | extras | upload |
|---|---|---|---|---|
| 경북·강원 | 2,789곳/30,214장 | 2,618곳/15,836장 | 1,224곳/12,286장 | ✅ **2,618곳** |
| 전남광주·대전 | 2,954곳/30,929장 | **2,730곳**/15,605장 | 1,122곳/13,309장 | ✅ **2,730곳** |

**둘 다 6개 조건 충족 → 원본 zip 삭제 가능**(사용자에게 알림 완료).

| 지역 | 사진 보유율 |
|---|---|
| 강원 | 91.7% |
| 대전 | 85.5% |
| 경북 | 87.3% |
| 전남광주 | 82.5% |

**전국 사진 보유율 32% → 41.1% → 50.5% → 61.6% → 72.0%**(20,751/28,834곳).

검증 방식: R2 무작위 4곳 HEAD 200 + **상세페이지 실제 렌더**(이미지 로드·캡션 alt).
전남광주·대전은 일부러 **재매칭으로 새로 붙은 시설**(`참사랑노인복지센터`, 기관기호 불일치였던
곳)로 검증했다 — 사진 11장 로드, alt "참사랑노인복지센터 교육장", **주소가 대전 서구로
zip 지역과 일치**. 매칭이 옳았다는 증거다.

**★ 매칭 2차 시도 신설** — §3-1 참조. 전남광주·대전에서 **63곳 / 223장 회수**.

**경남·부산**: inventory 3,455곳/35,022장 · 매칭 3,196곳(이름·시도 31 포함) ·
convert 3,196곳/18,776장 · extras 1,390곳/14,183장 · **upload 3,196곳 완료**.
**대구·인천**: inventory 3,192곳/30,593장 · 매칭 2,991곳(이름·시도 41 포함) ·
convert 2,991곳/17,037장 · extras 1,245곳/11,647장 · **upload 2,991곳 완료**.
둘 다 6개 조건 충족 → 원본 zip 삭제 가능(사용자에게 알림 완료).
보유율: 경남 87.2% · 부산 84.0% · 대구 87.5% · 인천 88.2%.

### 1-9. ★ 검색엔진 등록 — 진짜 문제는 여기였다 (`c8867bd`~`84c6fb3`)

외부 SEO 진단이 **"30/100 이하 · 크롤러 수집 실패 · 색인 확인 안 됨"**으로 나왔다.
16차 §1-1이 이미 겪은 것과 같아서, 받아들이기 전에 **프로덕션을 직접 대조했다.**

진단서의 1~7순위가 **거의 다 이미 돼 있었다**(실측):

| 진단서 주장 | 실제 |
|---|---|
| 크롤러가 HTML을 못 가져옴 | 200 OK, 69KB HTML 정상 |
| 도메인 리디렉션 필요 | `dolboda.kr → 308 → www` 이미 그 구조 |
| noindex 의심 | 메타 `index, follow`, X-Robots-Tag 없음 |
| robots.txt·sitemap 확인 실패 | 둘 다 200(사이트맵은 인덱스 분리까지) |
| SPA 앱 셸 의심 | title·description·h1·JSON-LD·시설 링크가 HTML에 있음 |
| 시설 고유 URL·지역 랜딩 필요 | `/facility/[id]` 28,844 · `/region/…` 897개 이미 존재 |

**즉 진단 도구가 사이트를 못 읽은 것**이지 사이트 문제가 아니었다(어제 80점이었다가
하루 만에 도메인·robots·SSR이 통째로 사라질 수는 없다 — 봇 차단·레이트리밋으로 보인다).

**진짜로 빠져 있던 것은 두 가지뿐이었고 이 세션에서 채웠다:**
1. **메인에만 canonical이 없었다** — 주요 10개 페이지를 훑어보니 나머지는 다 갖고 있었다.
   ⚠️ 루트 레이아웃이 아니라 `app/page.tsx`에 넣었다 — Next는 `alternates.canonical`을
   자식이 **상속**하므로, 레이아웃에 두면 canonical 없는 페이지가 전부 메인을 가리킨다.
2. **검색엔진에 등록이 안 돼 있었다.** 구글·네이버 **소유 확인 + 사이트맵 제출 완료**.
   - 확인 코드는 **여러 개를 함께 싣는다**(`verification.google`/`naver`가 배열).
     하나 지우면 그 코드로 인증해 둔 속성의 소유 확인이 풀린다. env도 덮어쓰지 않고 합친다.
   - 구글 파일 확인은 `public/`에 두면 그대로 서빙된다. **확인 후에도 지우지 말 것.**
   - ⚠️ **토큰은 시도할 때마다 바뀐다** — 화면을 새로고침하면 새 파일명이 발급되므로,
     코드를 받으면 그대로 두고 배포가 끝난 뒤에 [확인]을 눌러야 한다(이번에 두 번 헛돌았다).
   - ⚠️ **"도메인" 속성은 DNS TXT만 받는다**(HTML 태그·파일 안 됨). dolboda.kr 네임서버는
     **가비아**다. "URL 접두어" 속성으로 만들면 코드로 해결된다.
3. **사이트 설명이 89자였다** — 네이버가 "사이트 설명"·"Open Graph 설명" 두 항목에서
   80자 초과를 경고한다. 공단·심평원 기관명을 빼고 **64자**로 줄였다(유형 키워드는 유지).
   `SITE_DESCRIPTION` 주석에 80자 상한을 못박아 뒀다.

이후는 기다리는 일이다 — 신규 도메인은 색인까지 **2~6주**.

### 1-8. 부가급여 필터 + 메인 H1 (`2a8b304` · `adc743a`)

**부가급여 필터** — `otherServices`가 12,146곳(42%)에 있는데 시설 상세의 한 줄로만
쓰이고 있었다. 근속과 같은 구조의 낭비다. 필터로 낸 값은 셋: **방문목욕(10,348)** ·
방문요양(2,693) · 방문간호(735). 단기보호 47곳은 표본이 얇아 뺐고, 복지용구는 별도 화면이 있다.

> ★ **근속 필터와 처리를 달리한 지점** — 둘 다 "유형에 값이 없다"인데 성격이 반대다.
> 근속은 요양병원에 **자료 항목이 없어서** 판단 불가라 통과시켰다. 부가급여는 입소시설에
> 값이 없는 게 **실제로 방문 서비스를 안 하기 때문**이라(실측: 입소 0% · 재가 58.8% ·
> 주야간 20%) 걸러지는 게 맞다. 대신 필터 화면에서 이유를 미리 밝힌다.
> **"데이터가 없다"와 "그런 게 없다"를 구분하라** — 이 세션에서 안심지수(§1-5)와
> 필터 두 곳에서 같은 판단이 나왔다.

검증: 300곳 → **89곳**(API 집계와 일치), 결과가 방문요양 28 + 주야간 2뿐이고 **입소시설 0곳**.
카드 페이로드 189KB → 204KB(반복되는 짧은 문자열이라 압축이 잘 먹는다).

**메인 H1을 의미 단위 세 줄로**(사용자 요청): 수식("부모님께 딱 맞는") → 대상
("요양원·요양병원") → 질문("어디에 모시겠어요?"). 핵심 검색어가 한 줄을 독차지해
눈에 먼저 들어오고 16차의 SEO 목적도 지킨다. 375px·800px 양쪽에서 세 줄 유지 확인.

---

## 2. 사용자 방침 (이 세션 신규)

1. **포인트는 24개월 소멸시킨다**(2026-08-06 결정 — 한 차례 "소멸 안 시켜도 될 것 같다"
   했다가 뒤집음. 최종은 소멸).
2. **지표를 고칠 땐 "많은 시설이 득보고 유형 간 차이가 크지 않은" 방향**(2026-08-06).
   강등을 만드는 안은 채택하지 않는다 — §3-2의 막다른 길 참조.
3. 15·16차 방침 전부 유효: 매니저 법적 거리 · 별 상단 노출 허용 · 카카오톡은 마지막.

## 3. 이 세션에서 배운 함정 (다음 세션 필독)

### 3-1. 🔴 원본 zip 삭제 조건은 5개가 아니라 **6개**다

13차 §3-1의 5개 조건에 **"미매칭 시설 재매칭 완료"**가 빠져 있었다.

**매칭에 실패한 시설은 convert 자체를 안 한다.** 그 사진은 zip에만 있는데, 원본을 지우는
순간 영영 사라진다. 5개 지역 실측에서 **361곳**이 "사진은 있는데 기관기호가 안 맞아"
버려지고 있었고, 그중 **254곳(서울 51·경기 152·경북강원 51)은 zip을 이미 지워 복구 불가**다.
매칭률 90~94%를 계속 보고도 "나머지 사진은 어디로 가는가"를 묻지 않은 결과다.

→ `matchFacilities()`에 **2차 시도**를 넣었다(`scripts/photo-import.mjs`):
```
1차: extra.instCode 정확 일치
2차: 실패 시 이름 + 시도 — 세 조건을 모두 만족할 때만
     ① 같은 시도에 동명이 정확히 1곳 (2곳 이상이면 포기)
     ② 그 시설에 아직 사진이 없다
     ③ 이미 다른 zip 시설이 그 id를 가져가지 않았다
```
시도는 zip 최상위 폴더명에서 뽑는다(`47_경상북도` → `경상북도`, DB 주소 첫 토큰과 같은 표기).
전남광주·대전 실측: **+63곳 / 223장 회수**, 위험한 39곳(동명 2곳 이상)은 설계대로 제외.
눈으로 확인: 시도 분포가 zip 지역과 정확히 일치, "**장흥**재가방문간호센터 → 장흥군"처럼
이름 속 지역이 주소와 맞음.

### 3-2. 🔴 안심지수에서 **막다른 길 두 개** — 다시 시도하지 말 것

방문요양이 낮은 것을 고치려다 두 번 틀렸다. 둘 다 실측으로 되돌렸고
[docs/ai-score-spec.md](docs/ai-score-spec.md) §4에도 적어뒀다.

1. **유형별 라벨 문턱** — 각 유형을 기계적으로 상위 25%로 자르면 요양원 등 **약 3,800곳이
   "우수"에서 강등**된다. 모든 유형의 품질 분포가 같다는 가정에 근거가 없다.
2. **유형상 없는 영역을 분모에서 제외** — 방문요양 중앙값이 67 → **62로 오히려 내려갔다.**
   `PRIOR_SCORE`가 재가를 끌어내린 게 아니라 **떠받치고 있었기 때문**(재가의 실제 영역
   평균이 65보다 낮았다). 산출 보류까지 겹쳐 **약 5,900곳이 점수를 잃었다.**

교훈: **"원인을 안다"고 생각해도 고친 뒤 반드시 전수로 다시 재라.** 두 번 다 진단이
그럴듯했는데 방향이 반대였다.

### 3-3. progress 파일은 "변환된 것"이 아니라 "시도한 것"이다

매칭을 고치고 재실행했더니 **"처리 0곳"**이 나왔다. progress에 **매칭 실패로 건너뛴 시설도
"처리됨"으로** 들어가 있어서다. `manifest.jsonl`이 실제 변환의 진실이다.
매칭 규칙을 바꾼 뒤에는 **manifest에 없는데 inventory에는 facilityId가 있는 instCode**를
convert·extras progress 양쪽에서 빼야 한다(이제 convert가 그 간극을 시작 시 알려준다).

### 3-4. 비교할 때 **모수가 같은지** 먼저 봐라

주야간 중앙값이 "80 → 76으로 떨어졌다"고 보고할 뻔했다. 이전 값은 **등급 있는 시설만**
(18,136곳), 이후 값은 **전체**(28,834곳)라 애초에 비교 불가였다. 라벨 비율은 같은 기준이라
유효했다.

### 3-5. 자기 가설을 수치로 확인하고 말할 것 (16차 §3-15의 재발)

좌표 결손의 원인을 "주소의 공백 2칸"으로 짚었는데, **좌표가 있는 곳도 95.8%가 공백 2칸**이었다.
말하기 전에 재서 다행이었다.

### 3-6. 기타

- **`git checkout --`는 이 환경에서 차단된다**(변경 폐기). 되돌릴 땐 Edit로 명시적으로.
- **bash에서 백틱이 든 문자열을 node -e로 넘기면 명령치환으로 먹힌다.** 백틱·`@'...'@`
  (PowerShell here-string)를 bash에 쓰지 말 것 — 커밋 메시지 앞에 `@`가 붙거나 코드가
  잘린다(이 세션에서 둘 다 겪음). 파일로 쓰고 `-F`/스크립트 파일로 넘기는 게 안전하다.
- **Edit 도구가 템플릿 문자열의 공백을 NUL 바이트로 넣은 적이 있다.** `node --check`와
  `readFileSync(...).includes(0)`으로 확인하면 잡힌다.

## 4. 커밋·배포 상태

**미커밋 0. 24커밋(`40c1111`→`84c6fb3`) 전부 푸시** — Vercel 자동 배포.
36파일 +1,560줄 −150줄. **매 커밋 전 프로덕션 빌드 통과 확인**(다른 세션 dev 서버를 내리고).

> ⚠️ 빌드 전 dev 서버 정리는 이번에도 필수였다. 다른 세션 dev 2개를 내리고 빌드했다
> (포트로 특정 — **사진 업로드 node 프로세스와 헷갈리지 말 것**).

## 5. DB 변경 (전부 db push로 프로덕션 반영, 가산형)

- 필드 추가: `SponsorPlacement.subscriptionId` · `FacilityBanner.subscriptionId`·`endsAt`
- 인덱스 추가: **`Facility.createdAt`** (목록 API 정렬 — Seq Scan 28,834행 → Index Scan 310행)
- 데이터:
  - **좌표 970곳 신규 기입**(lat/lng, 검증 통과분만)
  - 경북·강원 사진 **2,618곳**, 전남광주·대전 **2,730곳**(진행 중) `extra.photos`/`photoItems`
  - 스키마 변경 후 **dev 서버 재시작 필요**(Prisma 클라이언트)

## 6. 검증 자산 (레포에 있음 — 재활용할 것)

| 스크립트 | 무엇을 |
|---|---|
| `scripts/verify-card-projection.mjs` | 카드 투영이 점수 입력을 안 바꾸는지 **28,844곳 전수** |
| `scripts/audit-score-vs-grade.mjs` | 안심지수 vs 등급 상관·이상치·유형 편향 (dev 서버 필요) |
| `scripts/geocode-missing.mjs` | 좌표 결손 복구(미리보기 기본, `--write`로 반영) |

스크래치로만 돌린 것(레포에 없음, 필요하면 다시 만들 가치 있음): 슬롯 카운트 12케이스 ·
포인트 소멸 14케이스.

## 7. 로컬 환경 메모

- 사진 zip은 `C:/Users/linea/Desktop/건강보험 중요자료/`에 온다.
  **`05_전남광주통합특별시_대전광역시.zip`은 업로드·검증 끝나기 전엔 지우지 말 것.**
- `photo-data/{지역}/`의 **jsonl 3종은 영구 보관**. `files/`는 업로드 후 삭제 가능
  (이 세션에서 경기·서울 4.13GB 회수).
- 전남광주·대전 progress 파일은 `.bak` 백업이 있다(63곳 회수 작업).
- 테스트 계정 2개 각 10,000P 보유. 소멸 예정 2028-08-05.
- 카카오 로그인은 `localhost:3000`에서만(KOE006). 카카오 지도는 프로덕션 도메인에서만.
