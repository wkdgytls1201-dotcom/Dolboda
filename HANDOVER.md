# 돌보다(Dolboda) 인수인계 문서

> 최종 갱신: 2026-07-31 (2차 세션 종료 시점) · 이 문서만 읽고 바로 이어서 작업할 수 있도록 작성했습니다.
> **새 세션은 반드시 `C:\Users\linea\요양` 폴더에서 열어주세요.** 코드 전체가 여기 있고 GitHub에도 푸시돼 있습니다.

---

## 1. 프로젝트 목표와 배경

### 무엇을 만드는가
**돌보다** — 요양시설 검색 + 돌봄 인력 매칭 플랫폼. 벤치마킹 대상은 **케어닥(caredoc.kr)**.

부모님 요양시설을 알아보는 보호자는 지금 정보가 흩어져 있어 힘듭니다. 국민건강보험공단 사이트에 시설 목록은 있지만 비교가 어렵고, 평가등급이 뭘 뜻하는지도 알기 어렵습니다. 그래서 두 가지를 합쳤습니다.

1. **시설 정보** — 전국 28,860개 요양시설을 평가등급·비급여비용·인력현황과 함께 검색·비교
2. **돌봄 매칭** — 보호자가 돌봄 요청을 올리면 **돌보다 매니저**(돌봄 인력)가 지원하고 보호자가 고르는 구조

### 회사 소개서상의 큰 그림 (2026-07-31 확인)
사용자가 `C:\Users\linea\Desktop\돌보다 0731.docx` (회사 소개서)를 공유했습니다. **현재 구현된 범위보다 훨씬 큰 그림**이니 방향 판단 시 참고하세요. (사용자는 "참고만 해"라고 했으므로 이걸 근거로 임의 구현하지 말 것)

- **비전**: 정보 비대칭 없는 투명한 돌봄 생태계
- **슬로건**: "제대로 보다, 정성껏 돌보다 — See Clearly, Care Deeply."
- **브랜드 스토리**: "돌보다"에 ① 보살피다 ② 돌아보다(자세히 들여다보다) 두 뜻을 겹침
- **핵심 가치**: 투명성 · 신뢰 · 데이터 · 존엄
- **서비스 5단계 흐름**: ① 시설 탐색(유입) → ② 보호자 가입(전환) → ③ 돌봄 매칭(거래) → ④ **안전 정산(에스크로)** → ⑤ 생태계 확장
- **선순환 구조**: 돌봄 경험이 있는 **보호자가 돌보다 매니저로 전환**되는 설계 (수요자→공급자)
- **BM**: ① 돌봄 매칭 수수료(에스크로에서 공제, 핵심) ② 시설 우선노출 광고 ③ 실버타운 프리미엄 광고 ④ 복지용구 커머스
- **확장**: 등급 신청 컨설팅 / 시설 B2B SaaS / 보험 제휴 / 매니저 교육 / 병원동행 / 시니어 하우징 중개
- **오프라인**: 프랜차이즈(가볍게 시작) → 직영 케어홈 → 실버타운 제휴
- **로드맵 5단계**: 신뢰 구축 → 거래 활성화 → 수익 다각화 → 오프라인 진출 → 생태계 완성

> ⚠️ **에스크로 정산은 문서상 핵심 BM이지만 현재 미구현**이며, 아래 4-1의 "우리가 계산한 가격 표시 금지" 규칙과 충돌할 수 있습니다. 결제 인프라를 붙일 때 사용자와 다시 정리해야 합니다.

### 서비스 대상
- **보호자**: 부모님 시설을 알아보거나 간병인이 필요한 자녀 세대
- **돌보다 매니저**: 돌봄 일자리를 찾는 요양보호사·간병인 (나중에 별도 앱으로 확장 예정)

### 운영 정보
- 운영자: 장성권 / wkdgytls1201@gmail.com / 010-2222-9943
- 개인 프로젝트 (사업자 등록은 "나중에 추가 예정")
- 배포: https://dolboda-rho.vercel.app
- 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (브랜치 `main`)

### 중요한 맥락
- **사용자는 거의 모바일로 확인합니다.** 모든 작업은 모바일 우선으로 검증해야 합니다.
- 사용자가 케어닥 화면을 스크린샷으로 계속 공유하며 "이것보다 더 좋게" 요구합니다. 스크린샷 위치는 아래 12번 참고.
- 사용자는 **워딩·구성을 케어닥과 똑같이 베끼는 것을 명시적으로 반대**합니다. 참고만 하고 우리 식으로 다시 짜야 합니다.
- 사용자는 **짧게 여러 건씩 요청**합니다. 작업 중에도 새 요청이 끼어드니, 하던 것부터 끝내고 순서대로 처리하세요.
- **작업 후 "커밋 배포 ㄱㄱ" 요청이 잦습니다.** 검증 → 커밋 → push까지 한 번에 진행하면 됩니다.

---

## 2. 기술 스택과 실행 방법

| 항목 | 내용 |
|---|---|
| 프레임워크 | Next.js 14 (App Router), React 18, TypeScript |
| 스타일 | Tailwind CSS — 커스텀 토큰: `primary`(코랄 #FF6250), `royal`(보라 #7C42BD), `ivory`(배경 #FFFBF3), `mint`, `peach`, `accent`(골드), `ink`(텍스트 회색) |
| DB | Supabase Postgres. `DATABASE_URL`=풀링(6543, 런타임), `DIRECT_URL`=직결(5432, 스크립트/마이그레이션) |
| ORM | Prisma v7 (driver-adapter, `@prisma/adapter-pg`). **마이그레이션 파일 없이 `db push`만 사용 중** |
| 인증 | NextAuth v5 (Auth.js) + `@auth/prisma-adapter`, **카카오 로그인만**, database 세션. 쿠키명 `authjs.session-token` |
| 지도 | Kakao Maps JS SDK |
| 이메일 | Resend (`lib/resend.ts`) — 상담 신청 접수 |
| 아이콘 | lucide-react |

```bash
npm install        # postinstall에서 prisma generate 자동 실행
npm run dev        # http://localhost:3000
npx tsc --noEmit   # 타입체크 (커밋 전 항상)
npx prisma db push # 스키마 변경 반영
```

`.env.local` 필요 키 (기존 폴더에 이미 있음, Vercel Production에도 등록 완료):
`DATABASE_URL`, `DIRECT_URL`, `AUTH_SECRET`, `AUTH_KAKAO_ID`, `AUTH_KAKAO_SECRET`, `KAKAO_REST_API_KEY`, `NEXT_PUBLIC_KAKAO_JS_KEY`, `RESEND_API_KEY`, `HIRA_SERVICE_KEY`, `HIRA_HOSP_INFO_BASE`, `HIRA_HOSP_EVAL_BASE`, `HIRA_HOSP_DETAIL_BASE`, `HIRA_NONPAYMENT_BASE`

---

## 3. 지금까지 완료한 것 (결과물 전부)

### 3-1. 시설 검색 (기반 기능)
- 전국 시설 검색·필터(유형/평가등급/거리/진료과목/빈자리/공공데이터)·정렬(거리순·등급순·평점순)
- 시설 상세: 평가등급, 인력현황, 비급여비용, 본인부담금 계산기, 카카오 지도·로드뷰, 사진 갤러리
- 최대 3곳 비교, 관심시설, 상담 신청(DB 저장 + Resend 메일 발송)
- **검색 상태 유지**: 상세 갔다 뒤로가기해도 검색어·필터·정렬·**지도/목록 보기 상태·지도 확대위치**까지 남음

### 3-2. 데이터 (DB 현황 · 2026-07-31 실측)

| 시설 유형 | 개수 |
|---|---|
| 방문요양센터 (HOME_CARE) | 15,678 |
| 요양원 (NURSING_HOME) | 6,246 |
| 주야간보호 (DAY_NIGHT_CARE) | 5,632 |
| **요양병원 (NURSING_HOSPITAL)** | **1,303** |
| 실버타운 (SILVER_TOWN) | 1 |
| **합계** | **28,860** |

- 좌표 확보: **27,697건** (실패 1,163건 — 카카오맵 미등록 주소)
- 평가등급 보유: **17,886건**
- **평가 세부점수(evaluationDetail) 보유: 17,639건** (2026-07-31 반영 — 공단 평가결과 파일, 시설당 최신 평가 1건, 시군구 3곳 이상이면 지역 평균도 포함)
- 그 외: CareRequest 5건, SitterProfile 1건, User 4건 (전부 테스트 수준)

### 3-3. 돌봄 요청 시스템 (보호자 ↔ 돌보다 매니저 매칭)

**보호자 흐름**: `/care-request`
- **5단계 마법사** — ① 돌봄 유형(병원/가사돌봄/자택) ② 언제·어디서 ③ 누구를 돌보나 ④ 어떤 도움 ⑤ 조건 확인
- 요청서 필드: 지역·장소·기간·시간대·24시간 상주 여부 / 돌봄 받는 분(관계·성별·연령대·체중대) / 거동·식사·배변 보조 4단계 / 질환 / 집안일 체크리스트 / 방문 주기 / 병원 입원 정보(출입절차·병실·입원사유·수술예정) / 선호 성별 / 특별 요청 / 희망 사례비 / 전달사항
- **커스텀 날짜 달력** (`DateRangeCalendar`) — 두 달 동시 표시, 범위 띠 강조, 1주/2주/1개월/3개월 빠른 선택
- **병원 간병 선택 시 "자세한 장소" 자동완성** — 타이핑하면 등록된 요양병원을 실시간 검색해 후보 표시, 클릭하면 채워짐 (2026-07-31 추가)
- 중복 요청 방지(409), 등록 직후 축하 배너 + 다음 단계 안내
- 지원자 목록 확인 → 확정 → 돌봄 완료 처리
- **비로그인 안내 화면** — 이용 절차 3단계, 로그인이 필요한 이유 3가지, 대안 링크

**돌보다 매니저 흐름**: `/mypage/sitter/*`
- 등록 5단계 마법사(약관 전체동의 토글 / 기본정보 / 경력·자격증 / 활동지역 / **정산계좌(선택)**)
- 자기소개 작성 가이드·예시·글자수 카운터 (200자)
- **일자리 관리 4탭**: 새 일자리 / 지원 현황 / 매칭된 돌봄 / 완료
- 정렬(최신·시작임박·사례비 높은순) + 유형 필터 + 마감 제외
- **일자리 목록은 현재 전국 노출** (지역 필터 해제 — 아래 4-3 참고)
- **지원 확인 시트** — 바로 지원되지 않고 "보호자에게 내 프로필이 이렇게 보여요" 미리보기 후 확정
- 프로필 관리 / 정산 관리 / 알림 설정

**공통**
- **돌봄 확인서** `/care-request/confirmation` — 매칭 확정 시 양측이 보는 요약 문서, `window.print()`로 PDF 저장
- 상태 전이: 지원완료 → 매칭확정 → 돌봄완료

### 3-4. 장기요양등급 예상 테스트 `/grade-test`
- 실제 **장기요양인정조사표 5개 영역 52항목** 그대로 반영 (신체기능12·인지기능7·행동변화14·간호처치9·재활10)
- 영역별 5화면 (케어닥은 문항마다 화면 전환 — 우리가 훨씬 빠름)
- 결과: 예상 등급 + 영역별 도움 필요도 막대 + 이용 가능 서비스 + 신청 5단계 + 제도 안내
- **비로그인 시 결과 블러 처리** + 로그인 유도, 답변 52개는 sessionStorage에 보존돼 로그인 후 복원

### 3-5. 돌봄 서비스 소개 `/services`
- 4종(병원 간병 / 집에서 돌봄 / 가사 돌봄 / 가족 간병) 접이식 카드
- SVG 일러스트(외부 이미지 없음), 4종 비교표, 이용 절차 3단계
- **예시 공고 카드 6개 + 자동 캐러셀** (마우스 hover 시 정지, 2.8초 간격)
- 각 카드 CTA가 `?type=`을 넘겨 마법사 1단계를 건너뜀

### 3-6. SEO / GEO
- 루트 메타데이터(키워드·OG·트위터카드), `robots.ts`, `sitemap.ts`
- **지역 랜딩 761개**: `/region/[시도]` 13개 + `/region/[시도]/[시군구]` 182개 + `/region/[시도]/[시군구]/[유형]` 566개
- 구조화 데이터: WebSite·Organization·SearchAction / BreadcrumbList / ItemList / FAQPage / HowTo / LocalBusiness·Hospital(geo 좌표 포함)
- 시설 상세 28,000여 페이지 개별 title·description 자동 생성
- OG 이미지 1200×630 (`public/og.png`), AI 검색용 `public/llms.txt`
- 구글 서치콘솔·네이버 서치어드바이저 소유확인 완료 + 사이트맵 제출 완료
- 푸터 지역 링크는 **10px / 투명도 50%** (완전히 숨기면 검색엔진 페널티라 이 선은 유지)

### 3-7. 성능
- 목록 API에 `view=card` 모드 — 카드에 필요한 필드만 전송. **300건 289KB → 113KB (61% 감소)**
- 히어로 배너: 로컬 데이터로 즉시 렌더(API 대기 제거)
- **시터 프로필 중복 요청 제거** — `SitterProfileProvider`를 `app/mypage/layout.tsx`에 두어 마이페이지 안에서 화면을 옮겨도 `/api/sitter-profile`를 다시 부르지 않음

### 3-8. 모바일 최적화
- **하단 탭바 신설** (`components/MobileTabBar.tsx`) — 홈 · 시설찾기 · 등급테스트 · 마이페이지
- 검색 필터바 360px → 189px (칩 가로 스크롤)
- 44px 미만 터치 타겟 전수 확대
- sticky 오프셋을 헤더 높이와 정합, safe-area 대응, 입력 글자 16px(iOS 자동확대 방지)

---

## 3-9. ⭐ 이번(2차) 세션에서 한 작업 전부

### 데이터
1. **인천·전북·경북 데이터 누락 해결** — 원인은 코드가 아니라 **엑셀 파일 라벨링 오류**였음. `인천광역시.xlsx`·`전북특별자치도.xlsx`·`경상북도.xlsx`가 각각 **대구·강원·충남 데이터**로 채워져 있었음. 사용자가 올바른 파일 재다운로드 → **5,017건 신규 추가** → 지오코딩(성공 4,840 / 실패 1,163). DB 23,843 → **28,860건**
2. **`scripts/add-new-nhis-facilities.mjs` 신규 작성** — 기존 좌표를 보존하면서 신규 시설만 추가. ⚠️ `prisma/seed.ts`의 `seedNhisV2()`는 delete-then-insert라 그대로 돌리면 좌표가 전부 null이 됨

### 홈
3. "가장 최근 설립" 로직 변경 — 설립연도는 요양병원에만 있어서 요양병원을 먼저 채우고 남는 자리는 나머지 유형을 등급순으로
4. "점수 높은 시설" — 항상 내 위치(미허용 시 서울 기본값) **100km 이내**로 좁힌 뒤 등급·총점순
5. 히어로 배너 사진 크롭 버그 — 5장 중 1장이 **세로형(800×1200)**이라 가로 배너에서 잘렸음. 해당 사진 제외
6. 히어로 배너 로딩 버그 — 가로 캐러셀에서 `loading="lazy"`가 제대로 트리거되지 않아 첫 장 외 안 뜸. 전부 즉시 로드로 변경
7. 히어로 배너와 검색창이 겹쳐 캡션이 가려지던 문제 — 모바일에서만 `-mt-6` 제거

### 시설찾기 / 지도
8. 등급순 정렬 시 위치가 있으면 **100km 이내**로 제한 + 동점이면 평가 총점 비교
9. 지도 기본 중심을 "첫 마커" → **내 위치**로 변경, 내 위치도 bounds에 포함
10. **내 위치 파란 점 표시** + **"내 위치로 이동" 버튼** 추가
11. **핀 클릭 시 정보창** — 시설명·유형·등급 + "자세히 보기" 링크 (카카오맵 기본 InfoWindow 재사용)
12. 지도가 **마커 순서만 바뀌어도 통째로 재생성**되며 확대/중심이 초기화되던 버그 수정 (좌표를 정렬 후 비교)
13. **지도 확대/이동 위치를 sessionStorage에 기억** — 시설 상세 갔다 뒤로가기해도 복원
14. 지도/목록 보기 상태도 검색 상태 저장에 포함
15. **모바일에서 "내 위치" 버튼이 한 화면에 안 보이던 문제** — 지도가 화면 575px 아래에서 시작했음. 지도 보기일 때만 지도를 위로 올리고 "총 N개"·위치안내를 아래로 이동(**삭제 아님, 순서만 변경**) → 343px. 모바일 지도 높이 52vh, 버튼은 하단 탭바 위로
16. 검색 첫 로딩 시 "총 0개 시설"이 잠깐 뜨던 것 → 로더로 교체, 이후 스켈레톤 바도 제거

### 내비게이션 / 모바일 앱화
17. **하단 탭바 신설** — 홈·시설찾기·등급테스트·마이페이지 (비로그인 시 마이페이지 탭 → 로그인 모달)
18. 자체 하단 CTA가 있는 화면(돌봄요청·등급테스트·시설상세)에서는 탭바 자동 숨김
19. 시설비교 플로팅 바를 탭바 위로 띄움
20. 햄버거 메뉴 정리 — 탭바와 겹치는 항목 제거, **로그아웃은 복원**
21. 로그인 시 상단에 **"돌봄 서비스"(코랄)** 버튼, 마이페이지에서는 **"돌봄 요청"(보라)** 버튼 (배타적 노출)
22. 헤더 로고 크기 확대
23. 비회원 상단 로그인 배너 추가 → **사용자 요청으로 삭제** (`components/GuestLoginBanner.tsx` 삭제됨)

### 로딩 / UX
24. **`components/PageLoader.tsx` 신규** — 실제 로고 이미지를 회전시키는 로더. `app/loading.tsx`로 라우트 전환에도 자동 적용
25. `return null`로 빈 화면만 보이던 화면 전부 로더로 교체 (돌봄요청·확인서·마이페이지·정보수정·시설상세·비교하기·프로필관리·일자리관리·정산관리·시터등록)
26. 검색 결과 영역용 `compact` 모드 추가 (화면 아래로 밀려나 안 보이던 문제)
27. 비교하기 모바일 카드 너비 72vw — 다음 카드가 살짝 걸쳐 보이게

### 마이페이지 / 매니저
28. 이미 등록된 매니저에게 **"등록하기" 버튼이 깜빡이던 버그** — `isSitter` 초기값 `null`을 `false`처럼 취급하던 문제
29. **`lib/sitterProfileContext.tsx` 신규** — 프로필 데이터를 레이아웃에서 한 번만 불러 공유 (중복 요청 제거)
30. 알림설정 **토글 스위치가 꺼짐/켜짐 위치가 어긋나던 버그** — `translate-x` 계산값 대신 `left`/`right` 포지셔닝으로 변경
31. 돌봄요청 마법사 **단계 이동 시 화면 최상단으로** (등급테스트와 같은 패턴)
32. 프로필 사진 URL이 깨졌을 때 기본 아바타로 대체 (`onError`)
33. "한 줄 소개" → **"자기소개"** 로 변경 (200자 유지)
34. 정산 계좌에 **"(선택)"** 표시
35. **일자리 목록 전국 노출** — 요청이 아직 적어 지역 좁게 등록한 매니저에게 화면이 비어 보였음

### 기타
36. **상담신청 알림메일 접수시각 타임존 버그** — `toLocaleString("ko-KR")`에 timeZone 미지정으로 Vercel(UTC) 기준이라 9시간 어긋남. `Asia/Seoul` 명시
37. 돌봄 서비스 페이지 **상단 일러스트 교체** — 집·병원·방문돌봄 파노라마 → 손으로 하트를 감싸는 그림, 높이 260→150
38. **"모심시터" → "돌보다 매니저" 전체 워딩 변경** (커밋 5566dd3)
39. **`CLAUDE.md` 신설** — 아래 4-2의 규칙 명시

---

## 4. 확정된 결정사항과 규칙 ⚠️ 반드시 지킬 것

### 4-1. 절대 하지 않기로 한 것

| 금지 | 이유 |
|---|---|
| **주민등록번호 수집** | 케어닥은 지원 시 요구하지만, 실제 원천징수·배상보험 가입이라는 법적 근거가 있어서임. 우리는 그 인프라가 없어 개인정보보호법 위반 소지 |
| **"계약서" 표현** | 법적 구속력 있는 것처럼 보이면 안 됨. **"돌봄 확인서"**로 부르고 "법적 계약서가 아님" 명시 |
| **우리가 계산한 가격 표시** | 결제 인프라 없음. 단, **보호자가 직접 입력한 희망 사례비**는 그대로 전달 OK |
| **매년 바뀌는 금액 안내** | 등급별 월 한도액·본인부담률은 고시로 변경됨. 구조만 설명하고 금액은 공식 홈페이지로 |
| **없는 데이터를 지어내기** | 평가 세부점수가 없으면 "아직 공개되지 않았어요"로 표시. 가짜 점수 생성 금지 |
| **케어닥 워딩·구성 그대로 베끼기** | 사용자가 여러 번 명시적으로 요구 |
| **푸터 SEO 링크 완전히 숨기기** | 검색엔진이 "숨긴 텍스트"로 판단해 페널티. 10px/투명도 50%가 하한선 |

### 4-2. `CLAUDE.md`에 명시된 최우선 규칙 (2026-07-31 추가)
1. **기존 기능 제거는 반드시 확인받고 진행할 것.** 이 규칙은 다른 모든 판단보다 우선한다.
2. **앱 성능 최적화가 최우선 목표 중 하나.** 만들 때부터 가볍게.
3. **SEO·GEO(AI 검색 노출)도 최우선 목표 중 하나.** 새 페이지엔 메타데이터·JSON-LD·sitemap을 기본 작업 범위로.
4. **최대한 가볍게.** 불필요한 의존성·큰 번들·과도한 전송 회피.

### 4-3. 지켜야 할 방향
- **모바일 최우선.** 터치 타겟 최소 44px, 하단 고정 CTA, 가로 스크롤 금지. 눈으로 짐작하지 말고 **브라우저에서 실제 측정**할 것.
- **모바일 웹이지만 앱처럼 느껴지게.** 하단 탭바가 그 축.
- **톤앤매너**: 존댓말 + "~해요체". 어르신·보호자가 읽기 쉽게.
- **파괴적 액션은 숨긴다.** 회원탈퇴는 화면 한참 아래 10px 흐린 글씨, 확인 단계에서 큰 버튼 = 취소.
- **정보 제공 시 정확성 우선.** 모르면 모른다고 하고 공식 출처로 연결.
- **로그인 게이트는 가치를 먼저 보여주고 걸 것.** 빈 화면 금지.
- **매니저는 정보 중개만** (고용관계 아님) — `/terms/sitter`에 명시됨.
- **위치 권한**: 커스텀 모달 → 브라우저 네이티브 순서. 허용=localStorage(영구), "다음에"=sessionStorage. 한 번 제거했다가 사용자가 되돌리라고 했으니 다시 없애지 말 것.
- **로그아웃 시 로컬 데이터 삭제** (비교함·관심시설·검색조건·위치).
- **로딩 중 빈 화면 금지** — 반드시 `PageLoader` 사용.
- **일자리 목록 전국 노출은 임시 조치.** 요청이 충분히 쌓이면 지역 필터를 되살릴 것 (`app/api/care-requests/open/route.ts`에 주석으로 남겨둠).

### 4-4. 사용자와 합의된 기타
- 사업자등록번호는 **형식 검증만**
- 시설 사진: `facility.photos`가 있으면 로드뷰보다 자동 우선. 크롤링은 저작권 문제로 계속 거부
- 추천 시설은 `lib/promotedFacilities.ts` 배열로 수동 제어
- 하단 탭바는 **4개**로 확정 (사용자가 "보통 4개쯤 되는 것 같다"며 결정)
- 자기소개는 **200자** (1000자로 늘렸다가 "너무 많다"고 해서 되돌림)

### 4-5. ⭐ 돌보다 AI기반 안심지수 — 특별 관리 자산 (2026-07-31 신설)

`lib/dolbodaScore.ts`의 돌보다 AI기반 안심지수는 **돌보다만의 핵심 차별화 데이터**로, 사용자가 특별 관리를 지시했다.

- **명세는 [docs/ai-score-spec.md](docs/ai-score-spec.md)가 단일 기준.** 코드와 문서가 어긋나면 안 된다. 로직을 바꾸면 매핑 표 + 변경 이력을 반드시 같이 갱신할 것.
- 영역 가중치(안전·신뢰 27 / 의료·간호 23 / 돌봄·운영 20 / 환경 10 / 투명성 10 / 이용가능성 10)는 사용자가 확정한 값 — **임의 변경 금지, 변경은 사용자 승인 필수.**
- 신뢰 장치 3종은 항상 유지: ① 미확보 신호 제외+공개 ② 커버리지 50% 미만 산출 보류 ③ 미확보 가중치만큼 전국 평균(65점) 수렴.
- 데이터가 새로 들어오면(평가 세부점수·행정처분·인력현황) 기술서 5장의 고도화 후보 표에 따라 신호를 추가한다.

---

## 5. 아직 안 끝난 작업과 다음 할 일

### 5-1. 사용자가 직접 "나중에 하자"고 한 것
**① 프로필 사진 실제 업로드 기능** ⭐ 가장 최근 요청
- 현재는 **URL 텍스트 입력칸**만 있음 (`app/mypage/sitter/register/page.tsx`, `profile/page.tsx`)
- 깨진 URL일 때 기본 아바타로 대체하는 처리만 해둔 상태
- 사용자 요청: "동그랗게 사진 수정해서 올릴 수 있어야 할 것 같다" → **"실제 업로드 기능을 만들어보자 나중에"**
- 필요한 작업: Supabase Storage 버킷 생성 → 업로드 API(`/api/upload`) → 용량 제한 → **WebP 리사이즈**(성능 규칙상 필수) → 원형 크롭 UI
- ⚠️ 현재 프로젝트에 `@supabase/supabase-js` 미설치. "가볍게" 규칙상 의존성 추가 전 사용자 확인 권장

### 5-2. 우선순위 높음
**② ~~시설 평가 세부점수(evaluationDetail)~~ → 2026-07-31 완료**
- 공단 평가결과 파일(`국민건강보험공단_장기요양기관 평가 결과_20260625.csv`)을 `scripts/import-evaluations.mjs`로 반영 → **17,639건** 확보 (28,860곳 중)
- 시설 상세의 **오각형 레이더 차트**·**전국 평균 대비 막대**·**같은 시군구 평균 대비 막대**(신규)가 이제 대부분 시설에서 뜸
- 매칭 안 된 것: 이름 못 찾음 2,810건 / 동명이인이라 모호해서 건너뜀 675건 — 안전을 위해 잘못 매칭하느니 건너뛰는 쪽을 택함
- 파일에 구지표(5영역: 기관운영·환경및안전·수급자권리보장·급여제공과정·급여제공결과)와 2025년 개편 신지표(4영역: 기관운영·수급자존중·서비스제공·서비스결과)가 섞여 있어, 구지표가 없으면 신지표로 대체하도록 스크립트에서 처리함
- 시설당 여러 평가연도가 섞여 있어 **최신 연도 1건만** 남김(연도는 "2021년 정기평가" 같은 문자열에서 파싱)
- 지역 평균은 같은 시군구에 **평가 보유 시설이 3곳 이상**일 때만 계산해 `regionAverage`/`regionName`으로 저장 (표본이 너무 적으면 왜곡되므로)

**②-2 행정처분(위반사실 공표) 데이터 — 같은 패턴, 파일 대기 중 (2026-07-31)**
- UI(상세 경고 배너·카드 배지·돌보다 AI기반 안심지수 안전 감점)는 전부 완성돼 있고, 데이터가 없으면 아무것도 안 뜨는 구조. 사용자가 "나중에 데이터 넣으면 표시되게만" 하기로 확정
- 파일을 `C:\Users\linea\Desktop\장기요양 행정처분` 폴더에 넣고 `node scripts/import-admin-actions.mjs`(미리보기) → `--write`(반영)
- 출처: 공공데이터포털 "장기요양기관 행정처분" 검색, 또는 노인장기요양보험 홈페이지 공표 명단

**③ 업체(기업) 가입 + 업체 구인글**
- 케어닥 스크린샷 폴더의 `기업가입1~3`에 해당. 미구현
- 회사 소개서의 BM2(시설 우선노출 광고)와 직결

### 5-3. 중간 우선순위
4. **에스크로 정산 인프라** — 회사 소개서의 핵심 BM. 결제 PG 연동 필요. 4-1의 가격 표시 규칙과 함께 재정리 필요
5. **Prisma 정식 마이그레이션 도입** — 지금까지 `db push`만 사용, `prisma/migrations` 폴더 없음
6. **AUTH_SECRET 프로덕션 로테이션** — 로컬 개발용 값을 그대로 사용 중
7. **커스텀 도메인 연결** — 현재 `dolboda-rho.vercel.app` 서브도메인만
8. **알림 실제 발송 인프라** — `/notifications`는 UI만 있고 실제 발송 없음. OAuth scope에 `talk_message`는 받아둠
9. **SEO 색인 상태 확인** — 서치콘솔에서 색인 수·사이트맵 상태 점검

### 5-4. 낮은 우선순위 / 데이터 품질
10. 지오코딩 실패 **1,163건** 재시도 (주소 정제 필요)
11. HIRA 등급코드 완전 매핑 (`asmGrd10`만 확인)
12. SILVER_TOWN 실데이터 1건뿐 — NHIS 대상이 아니라 별도 소스 필요
13. HOME_CARE 정원 개념 없음 — UI에서 "입소 가능" 대신 "정원 개념 없음"으로 구분 표시하면 좋음
14. 시설 실사진 확보 — **원본 그대로 올리면 안 되고 WebP 리사이즈 → CDN 파이프라인 필요**
15. 하이브리드 앱 전환 — 트래픽·전환 확정 전이라 보류

---

## 6. 파일 구조 (주요 파일 역할)

```
CLAUDE.md                         ★ 최우선 작업 규칙 (기능제거 확인 / 성능 / SEO / 경량화)
HANDOVER.md                       이 문서

app/
  layout.tsx                      루트 레이아웃 (Header/Footer/MobileTabBar + 메타데이터 + JSON-LD)
  loading.tsx                     ★ 라우트 전환 시 자동 로더
  page.tsx                        홈 (히어로배너·내주변·추천·점수높은시설·최근설립)
  services/page.tsx               돌봄 서비스 소개 (4종 비교 + 예시 캐러셀)
  grade-test/page.tsx             장기요양등급 예상 테스트
  search/page.tsx                 시설 찾기 (필터·정렬·지도) ★ 지도 보기 시 순서 재배치
  facility/[id]/page.tsx          시설 상세
  facility/[id]/layout.tsx        시설 SEO 메타 + JSON-LD (서버)
  region/[sido]/...               지역 SEO 랜딩 3단계
  care-request/page.tsx           돌봄 요청 (보호자)
  care-request/confirmation/      돌봄 확인서 (인쇄용)
  compare/ favorites/ notifications/
  mypage/layout.tsx               ★ SitterProfileProvider 위치 (중복 요청 방지의 핵심)
  mypage/                         계정 설정 + 회원탈퇴
  mypage/sitter/register|profile|jobs|settlements|notifications
  api/facilities/                 목록(view=card)·상세·통계·nearby(서버 거리계산)
  api/care-requests/              요청 CRUD + open(매니저용 목록, ★현재 전국 노출)
  api/care-request-applications/  지원·확정·철회·mine
  api/sitter-profile/             매니저 프로필·자격증
  api/consult/                    상담신청 (Resend 메일, ★타임존 Asia/Seoul 명시됨)
  robots.ts  sitemap.ts

components/
  Header Footer Logo HeroBanner SearchHero
  MobileTabBar.tsx                ★ 모바일 하단 탭바 (홈·시설찾기·등급테스트·마이페이지)
  PageLoader.tsx                  ★ 로고 회전 로더 (compact 옵션 있음)
  Switch.tsx                      ★ 토글 (left/right 포지셔닝 — translate-x 쓰지 말 것)
  KakaoMap.tsx                    ★ 내위치 점·버튼·핀 정보창·확대위치 기억(persistKey)
  FacilityCard FacilityThumbnail FacilityPhotoGallery FilterBar
  KakaoRoadview DetailVisuals(GradeScaleBar·DomainRadar·ScoreCompareBar)
  CareRequestWizard.tsx           ★ 병원 자동완성(HospitalLocationInput) + 단계별 스크롤 최상단
  CareRequestDetail CareRequestSignedOut CareFormFields DateRangeCalendar
  GradeTest ServicesShowcase.tsx(★자동 캐러셀) CareServicesHero.tsx(★새 일러스트)
  MyPageShell AuthModal LocationConsentModal CompareSelectBar InfoTooltip ApplyConfirmSheet

lib/
  sitterProfileContext.tsx        ★ 매니저 프로필 공유 컨텍스트
  types.ts facilityRepo.ts useFacilities.ts prisma.ts stockPhotos.ts(★세로사진 금지)
  careLocationTypes.ts careOptions.ts careRequestTypes.ts careRequestValidation.ts
  careServices.ts(★예시 6개) gradeTest.ts householdTasks.ts
  regionSeo.ts regionData.ts facilityTypeSeo.ts siteConfig.ts
  userLocation.ts signOutAndClear.ts distance.ts copay.ts viewLimit.ts
  compareContext favoritesContext viewGateContext alertPreferencesContext

scripts/
  import-nhis-v2.mjs              요양시설(엑셀 16개) → lib/realNhisDataV2.json
  add-new-nhis-facilities.mjs     ★ 신규 시설만 추가 (좌표 보존) — 부분 갱신은 반드시 이걸로
  import-hira-all.mjs             전국 요양병원
  import-evaluations.mjs          평가 세부점수 (2026-07-31 실행 완료, 17,639건. 파일 갱신되면 재실행)
  import-admin-actions.mjs        행정처분 공표 (파일 받으면 실행)
  geocode-db.mjs                  카카오 지오코딩 (lat IS NULL만, 재실행하면 이어짐)
```

---

## 7. 개발 시 주의점 (실제로 겪은 함정들)

1. **`prisma generate` 후 dev 서버를 완전히 재시작**해야 합니다. `lib/prisma.ts`가 HMR용 글로벌 캐싱을 해서 재시작 전까지 새 필드를 못 봅니다.
2. **`ON CONFLICT ... DO UPDATE`에 컬럼을 빠뜨리지 마세요.**
3. **`html { scroll-behavior: smooth }`가 전역입니다.** `window.scrollTo`에 `behavior: "instant"`를 명시하세요.
4. **스크롤 앵커링** — 화면 높이가 바뀌면 브라우저가 스크롤을 되돌립니다. `useEffect` + `requestAnimationFrame`으로 **두 번** 맞춰야 정확히 0이 됩니다. (GradeTest·CareRequestWizard에 같은 패턴)
5. **Next.js `route.ts`에서는 핸들러 외 export가 금지**됩니다.
6. **루트 폰트가 18.5px**입니다(`globals.css`). ⚠️ **Tailwind의 px 고정 임의값(`translate-x-[22px]` 등)을 쓰면 다른 요소와 비율이 안 맞습니다.** 토글 스위치 버그의 원인이었습니다. rem이나 `left/right` 포지셔닝을 쓰세요.
7. 장시간 DB 스크립트는 Supabase 풀링 연결이 가끔 끊깁니다. 행 단위 저장 설계라 재실행하면 이어집니다.
8. 카카오 지오코딩을 병렬로 때리면 rate-limit으로 대량 실패합니다. 단일 워커로만.
9. `scripts/`의 수동 env 파서는 따옴표 제거가 필요합니다.
10. **`dotenv`가 실행 시 광고성 문구를 출력합니다** (`vestauth.com` 등 외부 링크 포함). 공급망 침해가 아니라 패키지 자체 기능이니 놀라지 마세요.
11. **가로 스크롤 캐러셀에서 `loading="lazy"`는 신뢰할 수 없습니다.** 브라우저가 "화면 밖"으로 잘못 판단해 로드하지 않습니다.
12. **`KakaoMultiMap`의 useEffect 의존성**은 좌표를 **정렬한 뒤** 문자열로 만드세요. 순서만 바뀌어도 지도가 재생성돼 확대가 초기화됩니다.
13. **stockPhotos에는 가로형 사진만** 넣으세요. 세로형은 `object-cover`로 대부분 잘려나갑니다.

---

## 8. 로그인 없이 세션 기능을 테스트하는 방법 (검증 필수 기법)

```js
// 1) DB에 Session 행 직접 삽입 (scripts/에 임시 _test-*.mjs 만들어 실행)
await prisma.user.upsert({ where: { id: "test-user-x" }, create: { id: "test-user-x", name: "장테스트", email: "x@example.com" }, update: {} });
await prisma.session.upsert({
  where: { id: "test-session-x" },
  create: { id: "test-session-x", sessionToken: "testtoken-x", userId: "test-user-x", expires: new Date("2027-01-01") },
  update: {},
});
// 매니저 기능을 보려면 sitterProfile도 upsert (nickname/nationality/regions/agreedAt 필수)

// 2) 브라우저에서 쿠키 설정
document.cookie = "authjs.session-token=testtoken-x; path=/; max-age=86400";

// 3) 검증이 끝나면 반드시 정리 (cascade로 전부 삭제됨)
await prisma.user.delete({ where: { id: "test-user-x" } });
```

**주의**: 같은 브라우저에서 **두 번째 세션으로 전환은 안 됩니다**(HttpOnly 쿠키 재발급으로 JS가 못 덮음). 보호자·매니저 양쪽이 필요하면 **같은 테스트 유저에게 SitterProfile을 추가**해서 한 계정으로 검증하세요.

---

## 9. 배포·인프라 메모

- **Vercel 환경변수**는 Settings → Environments → **Production** 안에 있습니다
- `postinstall: prisma generate`가 package.json에 있어야 빌드 성공합니다
- **카카오 콘솔**: Redirect URI는 "플랫폼 키 → REST API 키" 아래, JS SDK 도메인은 "플랫폼 키 → JavaScript 키" 아래. 비즈앱 전환 완료
- **중복 Vercel 프로젝트 주의**: 빌드 실패 메일이 오면 **프로젝트명을 먼저 확인**하세요 — `dolboda-rho`가 아니면 중복 프로젝트입니다
- `git push`는 Git Credential Manager가 브라우저 인증을 요구할 때가 있습니다

---

## 10. 검증 절차 (커밋 전 체크리스트)

1. `npx tsc --noEmit` 통과
2. 브라우저(모바일 뷰포트 375px)에서 실제 동작 확인 — **짐작 금지, 반드시 측정**
3. 가로 스크롤 없음: `document.documentElement.scrollWidth > window.innerWidth` 가 false
4. 44px 미만 터치 타겟 없음
5. **하단 탭바(64px)에 가려지는 요소가 없는지** 확인 — `getBoundingClientRect()`로 탭바 top과 비교
6. 세션 기능은 8번 기법으로 흐름 전체 검증 후 **테스트 데이터 삭제**
7. 커밋 메시지는 한국어로 무엇을·왜 바꿨는지. 끝에 `Co-Authored-By: Claude <noreply@anthropic.com>`

---

## 11. 이번(2차) 세션의 커밋 이력 (최신순)

```
9202ee1 지도 내 위치 버튼 노출 수정, 자기소개 200자로 환원
63ab407 프로필 사진 깨짐 대비, 자기소개 장문 입력, 정산계좌 선택 표시, 서비스 일러스트 교체
5566dd3 "모심시터" 워딩을 "돌보다 매니저"로 전체 변경
65619ed 토글 스위치 위치 오류, 마법사 스크롤, 시터 등록 1단계 버튼, 일자리 전국노출 수정
39c3859 검색 로딩 스켈레톤 제거, 돌봄서비스 예시 자동 캐러셀, 병원 간병 장소 자동완성
6efd247 마이페이지 시터 화면 빈 페이지 수정, 프로필 데이터 중복 요청 제거, 로그인 배너 삭제
c304baa 시설찾기 로딩 애니메이션이 화면 아래로 밀려나 안 보이던 문제 수정
e2074e4 헤더 로고 크기 확대
c6a8b15 로그인 시 상단에 돌봄서비스 버튼(코랄) 추가, 로더는 실제 로고 이미지를 회전
5db1971 로더를 실제 로고와 같은 4장 클로버 꽃잎 모양으로 재작업, 로딩 중 푸터 안 보이게
a13b2cd 로더 꽃잎 애니메이션으로 교체, 시설찾기/비교하기 로딩 공백 수정, SEO 링크 더 흐리게
a3a87b6 비교하기 모바일 카드 너비 조정 — 다음 카드가 살짝 보이게
16f495f 마이페이지에서 이미 등록된 시터에게 등록하기 버튼이 잠깐 떴다 사라지던 버그 수정
b1c27ea 화면 전환 로딩 시 빈 화면 대신 가벼운 로더 표시
48a3c92 햄버거 로그아웃 복원, 돌봄요청 버튼 위치·홈 배너 겹침 수정
5161f6b 지도 확대 위치 기억, 탭바 크기 확대, 마이페이지 돌봄요청 버튼 추가
d02a2a3 상담신청 알림메일 접수시각 타임존 버그 수정
ea703ad 모바일 하단 탭바·비회원 로그인 배너 추가, 히어로 배너 로딩 버그 수정
6d408b0 홈 추천 로직·지도 UX 개선, 인천/전북/경북 데이터 보강
```

---

## 12. 참고자료

### 케어닥 스크린샷 (재전송 불필요)
`C:\Users\linea\Desktop\케어닥 모바일 구인구직\` 아래에 있습니다.

| 폴더 | 내용 |
|---|---|
| `1-30`, `31-50`, `51-55` | 보호자 간병 신청 흐름 55장 |
| `케어닥` | 케어코디 등록·프로필·일자리 관리 + **기업가입 3장(미구현)** |
| `모바일 필터, 병원` | 공고 목록·필터·정렬 14장 |
| `모바일 가사돌봄` / `모바일 집에서` | 가사돌봄 8장 / 자택 돌봄 6장 |
| `모바일 모심시터 신청서 계약서 PDF 자동생성` | 계약서 자동생성 16장 (우리는 "돌봄 확인서") |
| `지원하기 했을떄 주민번호 필요` | 주민번호 요구 8장 (우리는 수집 안 함) |

### 회사 소개서
`C:\Users\linea\Desktop\돌보다 0731.docx` — 위 1번의 큰 그림 원본

### 원본 데이터
`C:\Users\linea\Desktop\지역 장기\` — 시도별 장기요양기관 엑셀 16개
⚠️ `*_old_dup.xlsx` 3개는 라벨링이 잘못됐던 파일의 백업입니다. import 대상이 아닙니다.

---

## 13. 새 세션 시작 시 권장 첫 마디

> "HANDOVER.md 읽고 이어서 작업해줘. 오늘은 [하고 싶은 일]부터."

우선순위를 정해달라고 하면 **5-1(사진 업로드) → 5-2의 ②③** 순서로 제안하면 됩니다.
