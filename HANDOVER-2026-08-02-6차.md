# 돌보다(Dolboda) 인수인계 — 6차 세션

> 최종 갱신: **2026-08-02** · 이 문서만 읽고 바로 이어서 작업할 수 있게 작성했습니다.
> **새 세션은 반드시 `C:\Users\linea\요양` 폴더에서 열어주세요.**
> 이전 문서: [5차](HANDOVER-2026-08-02-5차.md) → [4차](HANDOVER-2026-08-02.md) → [3차](HANDOVER-2026-08-01.md) → [2차](HANDOVER.md)
> 5차 문서의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)은 **여전히 유효**하니 그 세 절은 같이 읽으세요.

- 배포: **https://www.dolboda.kr** (구 주소 `dolboda-rho.vercel.app`도 살아 있음)
- 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (`main`)
- 이 세션 커밋: **27개** (`2aeac86` ~ `c9cc468`) — 전부 배포됨

### 기술서(별도 문서, 수정 시 함께 갱신할 것)

| 문서 | 대상 | 성격 |
|---|---|---|
| [docs/ai-score-spec.md](docs/ai-score-spec.md) | 안심지수 | ★ 핵심 자산 — 가중치 변경은 사용자 승인 필수 |
| [docs/care-agreement-spec.md](docs/care-agreement-spec.md) | 돌봄 합의서 | ⚠️ 법적 포지션 — 조항 수정 전 반드시 §1 읽을 것 |
| [docs/work-index-spec.md](docs/work-index-spec.md) | 근무환경 지수 | 매니저용 지표 |
| [docs/program-taxonomy-spec.md](docs/program-taxonomy-spec.md) | 프로그램 태그 | 공단 12만 건 분류 규칙 |

---

## 0. ★ 사용자가 해야 할 일

### 0-1. 🟡 공단 사진 zip 재다운로드 — 유일하게 남은 미결 항목

10GB 파일이 손상돼(EOCD 없음) 삭제함. 스키마·갤러리 UI는 이미 배포됨(5차 §2-6).
받으면 → 구조 확인 → WebP 최적화(카드 640px/갤러리 1280px) → Supabase Storage 업로드.
시설의 93%가 `instCode`(공단 기관기호 11자리)를 갖고 있어 기관기호 기준이면 매핑이 정확하다.
스토리지 키는 이미 로컬·Vercel 양쪽에 등록돼 있다(§1-5).

### 0-2. 🟢 로그인해야 보이는 화면 눈으로 확인 (코드·타입 검사만 마친 것)

- 매니저 프로필 **사진 원형 크롭**(§2-6) — 원 안에서 얼굴 맞추기·확대 슬라이더
- 등급테스트 결과의 저장 CTA(워딩·화살표 nudge), 마이페이지 **"지원 중"** 숫자,
  매니저 가이드 펼치기, 시설 상세 햄버거의 **마이페이지**

### 0-3. 🟢 방문간호 164곳 추가 여부 (보류 중)

경쟁사와 시설 수가 다른 이유를 실측했다(§3). 우리가 안 다루는 건 **복지용구 1,683곳 ·
방문간호 164곳** 뿐이다. 복지용구는 판매·대여 사업소라 시설 목록과 성격이 다르지만,
**방문간호는 실제 장기요양 급여 서비스라 넣을 만하다.** 유형 하나 추가 + 임포트·필터·라벨.

---

## 1. 운영 환경 — 오늘 전부 연결됨 (다음 세션이 알아야 할 현재 상태)

### 1-1. 도메인 `www.dolboda.kr`

- DNS(가비아): `CNAME www → 20e1c7021ce4a375.vercel-dns-017.com.`, `A @ → 216.198.79.1`
- `dolboda.kr` → `www.dolboda.kr` **308 영구 리다이렉트**
- 카카오: JavaScript SDK 도메인·로그인 Redirect URI에 새 주소 등록(기존 vercel.app 유지)
- 실측: canonical·og:url·sitemap·robots 전부 새 도메인, 프로덕션에서 카카오 SDK 로드 확인

### 1-2. 메일 발송 (Resend, `dolboda.kr` 인증 완료)

가비아 DNS에 MX(`send`, 우선순위 10)·TXT SPF(`send`)·TXT DKIM(`resend._domainkey`)·
TXT DMARC(`_dmarc`) 네 줄. 발신 `noreply@dolboda.kr`, 회신은 `CONSULT_NOTIFY_EMAIL`로 간다.

### 1-3. 공단 일일 자동 수집 + 텔레그램

`DIRECT_URL`·`TELEGRAM_BOT_TOKEN`·`TELEGRAM_CHAT_ID` 등록 완료. 수동 실행 성공(5분 24초),
텔레그램 요약 수신 확인. 매일 04:00 KST 자동 실행.
스냅샷은 **8/3부터** 하루치씩 쌓인다(같은 날짜에는 새 행이 안 생기는 게 정상).

### 1-4. Vercel 환경변수 (Production)

```
NEXT_PUBLIC_SITE_URL  AUTH_URL          → https://www.dolboda.kr
MAIL_FROM             → noreply@dolboda.kr
TELEGRAM_BOT_TOKEN    TELEGRAM_CHAT_ID  → 앱 런타임 알림용(GitHub Actions 시크릿과 별개)
SUPABASE_URL          SUPABASE_SERVICE_ROLE_KEY → 프로필 사진 업로드
```

⚠️ **환경변수는 넣은 뒤 재배포해야 반영된다.** 오늘 `MAIL_FROM`은 됐는데 텔레그램만 안 왔던
원인이 이것이었다(넣은 시점이 마지막 배포보다 뒤).

### 1-5. 상담 신청이 들어오면 나가는 세 갈래

| 대상 | 내용 | 조건 |
|---|---|---|
| 시설 담당자 | 전달 메일(§2-2) | 시설 이메일 있음(81%) + 수신거부 안 함 |
| 운영자 메일 | 접수 요약 + 자동안내 성공/실패 | 항상 |
| 운영자 텔레그램 | 폰 알림 | 항상 |

프로덕션 실측 완료(이메일 없는 시설로 통과시켜 요양시설에는 아무것도 안 가게 함).

---

## 2. 이 세션에서 한 일 (커밋 27개)

### 2-1. 홈 "이 지역 안심지수 TOP" — `2aeac86`, `62327c0`

통계 바로 아래에 배치하고 "돌보다 AI" 배지·설명·계산방식 링크를 붙였다. 주변 300건은 이미
받아오고 서버가 점수를 실어주므로 **추가 요청 0**.

⚠️ **처음엔 카드 리본만 붙였다가 실측에서 한 건도 안 걸린 것을 발견했다** — 300건 중 상위
3곳이 6장짜리 기존 섹션에 우연히 들어올 확률이 낮았다. "붙였으니 됐다"가 아니라 **화면에
실제로 나타나는지**까지 봐야 한다.

### 2-2. 상담 전달 메일 전면 개편 — `2b5e30a` 외 8개

시설 담당자가 처음 받는 메일이 곧 우리 서비스의 첫인상이라 여러 번 다듬었다.

- 헤더: 큰 배너 → **흰 바탕 한 줄**(44px 로고 + 서비스명 + 주소). 배너형은 광고로 읽힌다
- 본문: 서비스 소개(고령화 시대 돌봄 정보 격차…) → 정정 요청 → 수신거부 → 맺음 인사
- `replyTo`를 운영자 주소로 — 발신 `noreply@`는 수신함이 없어 "회신해 주세요"가 무의미했다
- `word-break:keep-all` — 없으면 한글이 단어 중간("오늘 도/착한")에서 잘린다

⚠️ **소개 문구에 "순위를 임의로 바꾸지 않습니다"가 들어 있다.** 지금은 사실이다
(`PROMOTED_FACILITY_IDS`가 비어 있음). **유료 상위 노출 상품을 팔게 되면 이 문장을 반드시
함께 고쳐야 한다** — 안 고치면 거짓이 된다.
광고성 문구는 넣지 않는다(정보통신망법 제50조, 5차 §3과 동일 기준).

### 2-3. 수신거부 자동화 — `d7d0aa9`

사람이 기억해서 빼는 방식은 언젠가 반드시 새어나간다.

- `EmailOptOut` 테이블(이메일이 키 — 한 법인이 여러 시설에 같은 주소를 쓰기 때문)
- `lib/emailOptOut.ts` — 주소별 HMAC 토큰(`AUTH_SECRET` 재사용), `timingSafeEqual` 비교
- `app/api/email/unsubscribe` — POST(원클릭 RFC 8058) + GET(사람이 눌렀을 때 결과 화면)
- 발송 직전 `isOptedOut` 확인 → 건너뜀
- 메일 헤더 `List-Unsubscribe` + `List-Unsubscribe-Post`

실측: 위조 토큰 400 / 원클릭 200 + DB 기록 / 브라우저 완료 화면.

### 2-4. 안심지수 라벨 문턱 조정 — `728c235` ★ 핵심 자산

**가중치·산식은 그대로. 부르는 이름만 바뀜.** 실측(28,118곳): p25=62 · p50=70 · p75=79 · p90=86.
예전 기준(70+=우수)은 **전체의 50.1%가 우수**라 사실상 중앙값이었고, 3등급 시설의 50.8%가
"우수"로 표시돼 "국가는 3등급인데 돌보다는 우수?"로 읽혔다.
→ **86/79/62**(상위 10%/25%, 하위 25%). 같은 문턱을 쓰는 네 곳을 `SCORE_LEVEL_THRESHOLDS`
하나로 묶었다(상세 라벨·카드 배지·"안심지수 우수만" 필터·홈 TOP 진입선).

평가등급과 지수가 엇갈릴 때 상세에 이유 한 줄을 붙였다. 근거 영역은 **비중이 큰 영역 중
75점 이상**에서 고른다 — 점수만 보면 보조 반영인 "정보 투명성 100점"이 뽑혀 정작 핵심인
"의료·간호 대응 94점"을 놔두게 된다.

### 2-5. 시설찾기 — 정렬 버그 + 첫 진입 최적화 — `62327c0`

- **안심지수순이 전국 등록순 300건 안에서 정렬**돼, 부산에서도 서울 시설이 1위로 올라왔다
  → 거리·등급순과 같은 최근접 목록을 쓰고 100km로 좁힌다(실측: 부산 상위 5곳 전부 부산)
- 저장된 검색상태·위치가 정해지기 전에 목록을 부르지 않게 해서 **150~190KB 요청 2~3회 → 1회**
- nearby의 "총 N개"는 사용자 위치와 무관한데 요청마다 28,000행을 세고 있어 하루 캐시로

### 2-6. 매니저 프로필 사진 — `69394df`, `bbc9661`, `20694dc`

`photoUrl` 필드와 완성도 항목은 있었는데 올릴 방법이 없었다.

- 브라우저에서 512px WebP로 축소(30~60KB) → 서버는 저장만. sharp 같은 의존성 없음
- `lib/supabaseStorage.ts` — SDK 없이 REST만 fetch로 부르는 최소 클라이언트
- **원형 크롭 창**(`components/PhotoCropModal.tsx`) — 끌어서 얼굴 맞추고 슬라이더로 확대.
  기계적으로 가운데를 자르면 이마가 잘리거나 배경만 남았다
- 30MB 초과는 미리 끊고, HEIC처럼 못 여는 형식은 무엇을 해야 하는지 알려준다

### 2-7. 흐름·화면 정리

- **비회원 무료 열람 2곳 → 5곳**(`c9cc468`) — 보호자는 원래 서너 곳을 나란히 비교한다
- **상담 신청에 로그인 게이트**(`6ea1234`) — 이름·연락처를 남기는 일이라 계정이 있어야
  마이페이지에서 진행 상황을 되짚을 수 있다
- **시설 상세에서 마이페이지로 갈 방법이 없던 것**(`f2e8691`) — 그 화면은 자체 하단 CTA 바가
  있어 탭바가 숨겨지는데(`MobileTabBar`의 `HIDDEN_PREFIXES`) 햄버거에도 없었다
- 햄버거를 연 채 40px 이상 스크롤하면 자동으로 닫힘
- **전화 걸기 버튼 정리**(`cff7787`) — 상담 신청과 역할이 겹치고 전화로 새면 이력이 안 남는다.
  단 **시설 이메일이 없는 곳(19%)은 남겼다** — 상담이 자동 전달되지 않아 운영자가 손으로
  옮겨야 해서 하루 이상 걸린다
- 카드의 전화번호 줄 → **프로그램 태그 옅은 한 줄**, 빈자리는 기존 정보 상자에 합침
  (`정원 25명 · 현원 24명 · 빈자리 1`). 색을 늘리지 않는다 — 배지 줄에 이미 색이 세 개다

### 2-8. 공유 카드(OG) — `fa25784`

- 사이트 설명의 "22,000여 개"가 실제 집계(28,834곳)와 어긋나 있었다 → **28,000여 개**
- Next.js metadata는 하위 페이지가 `openGraph`를 정의하면 루트의 `images`를 **물려받지 않고
  통째로 교체**한다. 시설·지역·가이드·서비스 상세 9개가 썸네일 없이 공유되고 있었다
  → `lib/siteConfig.ts`의 `OG_IMAGE`를 각 페이지에 넣어 복구

---

## 3. 경쟁사와 시설 수가 다른 이유 (실측 결론)

carebalance.co.kr은 32,580곳, 우리는 28,834곳. **데이터를 빠뜨린 게 아니라 세는 단위가 다르다.**

| 기준 | 개수 |
|---|---|
| 공단 원본 **급여종류 행** | 43,943 |
| 공단 원본 **고유 기관(instCode)** | 29,103 |
| 돌보다 표시 | 28,834 |
| 상대 사이트 | 32,580 (유형별 합계는 **34,773** — 자기들끼리 2,193 안 맞는다) |

상대는 한 기관이 방문요양·방문목욕·복지용구를 함께 하면 **여러 곳으로 센다.**
우리가 실제로 안 다루는 건 **복지용구 1,683 · 방문간호 164 · 방문목욕 전용 37 = 1,886곳**.

→ 숫자를 부풀리지 않는다. 대신 §0-3처럼 방문간호만 추가하는 쪽을 검토한다.

---

## 4. 이번 세션에서 새로 배운 함정 (5차 §1에 추가)

1. **워크플로 env는 `postinstall`이 도는 설치 단계까지 넣어야 한다** — `npm ci`의 postinstall이
   `prisma generate`를 돌리고 `prisma.config.ts`가 `env("DIRECT_URL")`을 평가해서, 뒤 단계에만
   넣었더니 첫 실행이 17초 만에 죽었다(`bb0475d`)
2. **Supabase는 "버킷 없음"을 404가 아니라 400 + 본문 `NoSuchBucket`으로 준다** — 상태코드만
   보고 자동 생성을 걸어둬서 첫 업로드가 실패했다(`bbc9661`)
3. **`prisma db push` 후에는 dev 서버를 재시작해야 한다** — 이미 떠 있는 서버는 옛 클라이언트를
   물고 있어 `prisma.<새모델>`이 undefined가 되고 500이 난다
4. **Next.js metadata의 `openGraph`는 병합이 아니라 교체다**(§2-8)
5. **환경변수는 넣은 뒤 재배포해야 반영된다.** 특히 `NEXT_PUBLIC_*`은 빌드 시 코드에 박히므로
   **빌드 캐시를 끄고** 재배포해야 한다
6. **PowerShell/curl로 한글 JSON을 POST하면 인코딩이 깨진다**(CP949). 테스트로 넣은 한글이
   `???`로 저장돼 앱 문제로 오해했다 — `[System.Text.Encoding]::UTF8.GetBytes`로 보낼 것

---

## 5. 이번 세션 신규·주요 변경 파일

```
── 메일·알림
lib/resend.ts                        상담 전달 메일 전면 개편(헤더·소개·수신거부·맺음말)
lib/telegram.ts                     ★ 신설 — 앱 런타임 텔레그램 발송
lib/emailOptOut.ts                  ★ 신설 — HMAC 토큰·발송 전 조회·차단 등록
app/api/email/unsubscribe/route.ts  ★ 신설 — 원클릭(POST)+브라우저(GET)
app/api/consult/route.ts             수신거부 확인·List-Unsubscribe·텔레그램 알림
prisma/schema.prisma                 EmailOptOut 신설
app/privacy/page.tsx                 수탁업체에 Resend·Telegram 추가

── 사진
components/PhotoCropModal.tsx       ★ 신설 — 원형 크롭(포인터 이벤트·createPortal)
lib/squareImage.ts                  ★ 신설 — 브라우저 축소(크롭 미사용 경로용)
lib/supabaseStorage.ts              ★ 신설 — SDK 없는 REST 클라이언트
app/api/sitter-profile/photo/route.ts ★ 신설 — 업로드·삭제

── 안심지수·목록
lib/dolbodaScore.ts                  SCORE_LEVEL_THRESHOLDS(86/79/62)
components/DolbodaScoreCard.tsx      평가등급과 엇갈릴 때 이유 한 줄
components/FacilityCard.tsx          순위 리본·빈자리·프로그램 줄, 전화번호 제거
app/HomeClient.tsx                   "이 지역 안심지수 TOP" 섹션
app/search/SearchPageClient.tsx      안심지수순 지역 버그·첫 진입 요청 1회
app/api/facilities/nearby/route.ts   총 건수 하루 캐시

── 흐름
lib/viewLimit.ts                     무료 열람 5곳
components/Header.tsx                햄버거에 마이페이지·스크롤 시 닫힘
app/facility/[id]/FacilityDetailClient.tsx  로그인 게이트·전화 버튼 조건부
lib/siteConfig.ts                    MAIL_FROM·OG_IMAGE
```

## 6. DB 현황 (2026-08-02 기준)

| 항목 | 수치 |
|---|---|
| Facility | 28,834 (HOME_CARE 15,677 / NURSING_HOME 6,244 / DAY_NIGHT_CARE 5,631 / 요양병원 1,282) |
| Institution(공단 원본) | 고유 기관 29,103 · 급여종류 행 43,943 |
| FacilitySnapshot | 27,493 (8/2 기준선) — 8/3부터 변경분만 적재 |
| ConsultRequest | 3건 (실제 접수) |
| EmailOptOut | 0건 (신설) |
