# 18차 인수인계 (2026-08-08)

**이번 세션 성격: SEO 마감 + 전면 감사(모바일·보안·안정성).**
새 기능은 거의 없고, **이미 있던 것들이 실제로 제대로 도는지 실측으로 확인**한 세션입니다.
그래서 이 문서의 값어치는 "무엇을 만들었나"보다 **"무엇을 이미 확인했으니 다시 하지 말 것인가"**에 있습니다.

---

## 0. 다음 세션에서 바로 시작할 일

① ~~GA4 붙이기~~ ✅ **연동 완료(2026-08-08).** 측정 ID `G-Z3YY2B2EED`,
   Vercel `NEXT_PUBLIC_GA_ID`(Production). 라이브 실측으로 `gtag/js?id=G-Z3YY2B2EED`와
   `send_page_view: false`가 실린 것, GA4 실시간에 사용자가 잡히는 것까지 확인.
   GA 관리화면의 "브라우저 방문 기록 기반 페이지 변경"도 껐고(중복 집계 방지),
   이벤트 데이터 보관은 14개월로 바꿔뒀다.
   → **다음은 데이터가 쌓이길 기다리는 것**: 실시간 외 보고서는 24~48시간 뒤부터 채워진다.
   일주일 뒤 **획득 → 트래픽 획득**의 `Organic Search`와
   **참여도 → 이벤트 → `view_search_results`**(검색어)를 볼 것. 검색어는
   `/search?q=`가 GA 기본 파라미터라 별도 설정 없이 자동 수집된다 — 다음에 어떤
   지역·가이드를 보강할지 정하는 근거가 된다.

② **사진은 끝났다. 더 받을 zip이 없으면 더 할 일이 없다**(§1-1). 17차의
   "남은 지역: 충청·전북·전남·제주·세종·울산"은 이번 세션에 전부 처리됐다.

③ **앱 패키징 시점까지 미뤄둔 것 2개**(§4) — 필터 시트 뒤로가기, WebView OAuth.
   **웹에서 검증이 불가능해서 미룬 것이지 빠뜨린 게 아니다.** 실기기 붙는 날 함께 한다.

④ 서치콘솔 **페이지** 화면에서 색인 숫자 확인(신규 도메인은 2~6주 걸린다 — 17차부터 대기 중).

⑤ 🔴 **중단된 작업이 있다 — §7 "돌봄 흐름 실사용 검증(중단)"부터 읽고 이어서 할 것.**
   실마리를 하나 잡아둔 채로 멈췄다. 재개 방법과 실행할 스크립트를 그대로 적어뒀다.

**미커밋 0 · 이번 세션 10커밋 전부 배포 라인.**

---

## 1. 이번 세션에 확정된 사실 (다시 조사하지 말 것)

### 1-1. 사진 — 전국 86.6%로 완료

| 지역 | 보유율 | 지역 | 보유율 |
|---|---|---|---|
| 세종 92.2% | 강원 91.7% | 제주 90.5% | 충남 89.0% |
| 전북 88.9% | 충북 88.9% | 서울 88.5% | 인천 88.2% |
| 대구 87.5% | 경북 87.3% | 경남 87.2% | 대전 85.5% |
| 경기 84.6% | 울산 84.3% | 부산 84.0% | 전남광주 82.5% |

**전국 24,929 / 28,795 = 86.6%.** 17개 지역 전부 82~92% 구간.

⚠️ **남은 13%는 "처리 안 한 지역"이 아니다.** 공단 zip에 사진이 아예 없거나 매칭이
안 된 시설들이라, **새 zip을 받지 않는 한 코드로 올릴 방법이 없다.** 17차 문서의
"전국 72%"와 "남은 지역 목록"은 이 세션 작업 전 기준이라 이제 유효하지 않다.

확인 방법(읽기 전용, 필요할 때만):
```sql
SELECT split_part(address,' ',1) AS sido, count(*) AS total,
       count(*) FILTER (WHERE extra->'photos'->0 IS NOT NULL) AS with_photo
FROM "Facility" WHERE "dataSource" <> 'mock' GROUP BY 1 ORDER BY 2 DESC;
```

### 1-2. 감사 결과 — 이미 안전한 것들 (전수 확인함)

아래는 **실제로 열어보고 확인한 것**이다. 같은 조사를 반복하지 말 것.

| 영역 | 상태 | 확인 방법 |
|---|---|---|
| 시크릿 노출 | 안전 | 빌드된 클라이언트 번들 전수 grep — 서버 키 0건. `.env` 미추적, git 히스토리에도 없음 |
| API 인가·IDOR | 안전 | 전 라우트 가드 맵 작성. `requireAdmin`/`requireFacilityOwner`가 세션+소유권 행을 확인 |
| XSS | 안전 | 모든 `dangerouslySetInnerHTML`이 `jsonLdHtml()` 하나를 거치고 거기서 `<` `>` `&` 이스케이프 |
| SQL injection | 안전 | `queryRawUnsafe`/`executeRawUnsafe` 0건. `Prisma.raw`는 사용자 입력 없는 고정 SQL |
| 파일 업로드 | 안전 | MIME 화이트리스트 + 크기 상한. 확장자를 검증된 MIME에서 유도(사용자 파일명 미사용) |
| open redirect | 안전 | 사용자 입력이 리다이렉트에 들어가는 곳 없음 |
| 메모리 누수 | 안전 | interval·리스너·observer cleanup 전수 스캔 — 누락 0건 |
| 중복 클릭 | 안전 | 전 폼에 가드. 기업 입점 신청은 `if (sending) return`으로 함수 진입까지 막음 |
| 상태 유지 | 구현됨 | 검색 조건·스크롤을 sessionStorage로 복원 |
| 이미지 최적화 | 적절 | `loading="lazy"` + `decoding="async"` + aspect로 자리 확보. **`next/image`를 안 쓰는 게 맞다** — 사진 파이프라인이 이미 WebP로 변환해 R2에 올리는데 얹으면 재처리 비용만 는다 |
| 로그인 후 복귀 | 정상 | next-auth 클라이언트가 `redirectTo = callbackUrl ?? window.location.href`(react.js:128). `signIn(provider)`만 불러도 현재 페이지로 돌아온다 |
| scroll lock | 정상 | 언마운트 cleanup에서 무조건 복원. 중첩돼도 이전 값 저장/복원이라 안 깨짐 |
| 검색 race | 안전 | 300ms 디바운스 + `cancelled` 플래그. `AbortController`는 없지만 **결과 정확성은 동일**(네트워크 절약 차이만) |
| 딥링크 | 적합 | 라우트가 전부 경로 기반(`/facility/[id]`) — 앱 만들 때 그대로 쓰면 됨 |

---

## 2. 이번 세션에 만든 것

### 2-1. SEO 마감 (커밋 `10093f2`)
- `/search`가 크롤러에게 **"총 0개 시설"**로 보이던 SSR 버그 수정.
  원인은 `useFacilities` 훅의 하드코딩 `total: 0` — 서버에서 캐시된 통계를 `initialTotal`로 시딩.
- 시설 상세에 **시각적 breadcrumb** 추가. 기존 JSON-LD BreadcrumbList와 **같은 배열**을 공유해
  화면과 구조화 데이터가 어긋나지 않게 했다.
- 관련 시설 캐러셀 "더 보기"가 범용 `/search` 대신 **지역+유형 허브**로.
- 안심지수 문구에서 "산출 방식을 전부 공개" → "산출 원칙을 투명하게 공개"(과장 제거).

### 2-2. 모바일·태블릿 레이아웃 (커밋 `dcd2c1f`)
🔴 **가장 임팩트 큰 수정.**
- **640~899px 전 구간에서 페이지 전체가 가로 스크롤**됐다. 데스크톱 nav가 `sm:`(640px)부터
  펼쳐지는데 실제 필요 폭은 약 899px. 실측: 700px에서 199px, 아이패드 세로(768px) 131px 밀림.
  경계를 `lg:`(1024px)로 옮겼다.
  ⚠️ **세 곳을 함께 옮겨야 한다** — Header의 nav(`lg:flex`)·햄버거 그룹(`lg:hidden`)과
  **MobileMenuSheet(`lg:hidden`)**. 시트를 빠뜨리면 그 구간에서 메뉴 버튼이 먹통이 된다.
- **아이폰에서 페이지 맨 아래 33px이 탭바에 가려졌다.** 탭바는 safe-area 패딩이 있는데
  자리를 확보하는 스페이서가 `h-[60px]` 고정이었다. 브라우저(safe-area=0)에선 59<60이라
  멀쩡해 보이고 **홈 인디케이터가 있는 기기에서만** 터진다.
  → `calc(60px + env(safe-area-inset-bottom))`.

### 2-3. 대량 수집 방어 (커밋 `eb844a2`)
`/api/facilities`의 `ids=`가 limit을 무시하고 넘긴 id를 **전부** 돌려줬다.
실측: **id 400개 → 2.05MB를 0.27초**. 카드 축약본이 아니라 비급여·인력·근속·프로그램·
정원/대기·좌표·사진 URL이 전부 담긴 전체 레코드(시설당 6KB). 전국 175MB를 **72번이면** 끝.
→ `MAX_IDS = 200` + 분당 120회 rate limit.

**SEO는 안 해친다**: 이 API는 CDN 1시간 캐시라 정상 사용자는 대부분 캐시에서 끝나고,
매번 새 조합을 던지는 수집기만 캐시 미스로 원본에 닿는다. 시설 페이지 크롤링은 그대로.
찜이 200개를 넘는 사용자를 위해 `useFacilitiesByIds`가 200개씩 나눠 보낸다.

### 2-4. race condition 3건 (커밋 `d3fc44d`)
시/도를 빠르게 바꾸면(서울→부산) 먼저 보낸 응답이 나중에 도착해 **부산 화면에 서울
시군구가** 채워졌다. 그 상태로 고르면 (부산, 서울시군구)로 조회해 결과가 비고,
사용자에겐 "이 지역엔 사업소가 없다"로 보인다. 복지용구 찾기·**상담 신청서**·매니저
일터 탭 3곳. 상담 신청서는 잘못된 지역으로 접수되면 실제 전화 연결까지 이어진다.

### 2-5. 에러 경계 3종 (커밋 `2168219`)
경계 파일이 **하나도 없었다**(`loading.tsx`는 12개인데).
`not-found.tsx`(404 + 내부링크 4개), `error.tsx`(헤더·푸터 살린 채 본문만 교체),
`global-error.tsx`(루트 레이아웃이 터진 최후 상황 — 여기만 인라인 스타일).

### 2-6. 뒤로가기로 모달 닫기 (커밋 `bbec891`)
`lib/useBackToClose.ts` — FacilityPhotoModal이 쓰던 패턴을 공용 훅으로.
적용: AuthModal, ConsultModal.

### 2-8. B2B 첫 고객 이탈 지점 2건 (커밋 `351a93f`)
🔴 **첫 유료 고객이 걸리면 손해가 큰 자리.**
- **승인된 기업회원이 "당신은 승인 안 됐다"는 화면을 봤다.** `myFacilities()`는 세션이
  없으면 조회도 안 하고 빈 배열을 돌려주는데(`lib/facilityOwner.ts`), 콘솔·월간보고서가
  이를 구분하지 않았다. 그래서 승인 끝난 고객이 다른 기기·세션 만료 후 들어오면
  콘솔은 "아직 연결된 시설이 없어요", 월간보고서는 **"지역 프리미엄 이상에서 볼 수
  있어요"** — 돈 낸 고객에게 "당신 요금제로는 안 된다"고 말하는 셈이었다.
- **입점 신청 폼에 클라이언트 검증이 없어 오타가 한도를 깎았다.**
  `/api/business-inquiry`는 IP당 **시간당 5회**인데 **검증 실패 요청도 함께 센다.**
  기관기호(11자리)는 사업자등록번호(10자리)와 헷갈리기 쉬워 몇 번 틀리는 게 정상인데,
  다섯 번이면 한 시간 잠겼다. 서버와 같은 규칙을 클라이언트에도 뒀다(서버는 그대로).

### 2-9. 매니저 화면 무한 로딩 (커밋 `1ebb820`)
🔴 **`/mypage/sitter/jobs`가 로그아웃 상태에서 영영 로더만 돌았다.**
`loadJobs()`가 404(매니저 아님)만 `isSitter=false`로 확정하고 **401에서는 그냥
return**해서 `isSitter`가 null로 남고, `null → 로더` 분기에 갇혔다.
실측: 비로그인 진입 4초 뒤에도 헤더·푸터뿐 본문이 아예 없었다.
`/mypage/sitter/settlements`는 같은 원인으로 등록된 매니저에게 "아직 등록하지
않으셨어요"를 띄웠다(정산=돈 화면이라 더 나쁘다).
⚠️ **401을 `isSitter=false`로 두는 방향은 틀렸다** — 그러면 이미 등록한 매니저에게
"등록 안 하셨어요"가 뜬다. 로그인 안내를 **따로** 내보내는 게 맞다.

### 2-7. GA4 (이번 세션)
`components/Analytics.tsx` + 루트 레이아웃 연결. **`NEXT_PUBLIC_GA_ID`가 없으면
아무것도 렌더하지 않는다** — ID 없이 배포해도 무해하고, 넣는 순간 켜진다.

---

## 3. 함정 (코드 작업 전 읽을 것)

**3-1. JSX 표현식 위치에 `{/* */}` 주석을 넣으면 빌드가 깨진다.**
`return (` 바로 뒤나 `{cond && (` 바로 뒤는 **JS 표현식 자리**라 JSX 주석 문법이 안 된다.
`//` 줄 주석을 써야 한다. 이번 세션에 실제로 두 파일을 동시에 깨뜨렸고, 증상이
"전 페이지 500"이라 원인 파악이 오래 걸린다. **콘솔 에러를 먼저 볼 것.**

**3-2. 개발 환경에서 브라우저 창이 숨겨져 있으면 `requestAnimationFrame`이 0프레임이다.**
실측으로 확인했다(1.6초 동안 0회). BottomSheet는 **닫힘 애니메이션이 끝나야**
`setRendered(false)`가 불리는 구조라, 이 상태에서는 **시트가 영영 안 닫힌다.**
→ 이걸 "scroll lock 누수"로 오진하기 쉽다. 실제로 오진했다가 rAF를 직접 재서 잡았다.
**애니메이션에 의존하는 UI는 이 환경에서 검증 불가**라고 인정하고 넘어갈 것.

**3-3. 없는 시설 URL이 HTTP 200을 반환한다(soft 404).**
프로덕션 빌드로도 확인. 매니저 페이지가 쓰는 해법(스트리밍 전 `generateMetadata`에서
`notFound()`)을 시설에 적용해봤으나 **상태코드가 바뀌지 않아 되돌렸다** — 스트리밍
라우트는 200 헤더가 이미 나간 뒤다. **다시 시도하지 말 것.**
다만 그 응답에 Next가 `robots=noindex`를 자동으로 붙이므로 **색인은 안 된다**(SEO 피해 없음).
진짜 404로 만들려면 시설 상세의 `<Suspense>` 스트리밍을 걷어내야 하는데 의도적인
성능 구조라 맞바꿀 값어치가 없다.

**3-4. 뒤로가기 훅(`useBackToClose`)을 아무 오버레이에나 붙이면 안 된다.**
히스토리를 한 칸 쌓는 방식이라, **이동이 목적인 오버레이**(MobileMenuSheet,
ApplyConfirmSheet)에 붙이면 링크로 나간 뒤 뒤로가기가 그 빈 칸을 먼저 밟아
**한 번 헛도는 증상**이 쓸 때마다 생긴다. 순수 모달에만 쓴다. 코드에 판단 근거를 남겨뒀다.

**3-5. 17차 §3의 함정 둘은 여전히 유효하다** — 원본 zip 삭제 6조건, 안심지수 막다른 길 둘.

**3-6. `grep`으로 "로그인 게이트가 있는가"를 판정하지 말 것.**
이번 세션에 판별식을 세 번 다르게 짰는데 세 번 다 놓치는 게 있었다 —
표현이 `!== "authenticated"` / `data === null` / `profile === null` / `!session?.user`로
제각각이라 어떤 패턴을 써도 오탐·누락이 난다. 실제로 "게이트 없음"으로 잡힌 곳 중
절반이 정상이었다(consults·points 등). **후보를 좁히는 데까지만 grep을 쓰고,
판정은 반드시 브라우저로 열어 실제 글자를 확인한다.** §7-5도 같은 얘기.

**3-7. "로그인 안 함"과 "데이터 없음"을 같은 빈 상태로 처리하지 말 것.**
이번 세션 결함 4건 중 4건이 전부 이것이었다(§2-8·§2-9). 서버 API는 전부 올바르게
401/404를 돌려주고 있었고, **화면이 그걸 잘못 해석**한 것이다.
새 화면을 만들 때 `fetch` 실패 처리에서 이 둘을 반드시 갈라라 —
안 그러면 **이미 등록·승인된 사용자에게 "당신은 등록 안 됐다"**가 뜬다.

---

## 4. 앱 패키징 시점까지 미뤄둔 것 (빠뜨린 게 아님)

**4-1. 필터 시트(BottomSheet) 뒤로가기.**
적용 가치는 가장 크나 닫기 경로가 넷(배경 탭·ESC·아래 스와이프·부모의 "적용")이고
검색의 핵심 UI다. §3-2 때문에 **개발 환경에서 개폐 검증 자체가 불가능**해서 손대지 않았다.
실기기에서 확인하며 진행할 것.

**4-2. WebView OAuth.**
카카오·네이버가 앱 내장 WebView 로그인을 막는 정책이 있어 시스템 브라우저로 띄워야 할
수 있다. 지금 구조(`signIn(provider)`)는 확장 가능하지만, **웹에서 검증할 수 없는 코드를
미리 넣으면 안 된다.** Capacitor 붙이는 날 함께.

---

## 5. 사용자 조치 대기

- ~~GA4 측정 ID~~ ✅ 완료(2026-08-08).
- **`MAIL_FROM`을 GitHub Secrets에서도 `noreply@send.dolboda.kr`로**(17차부터 대기 —
  로컬은 반영됐지만 실제 배치는 Actions에서 돌아 거기가 진짜다).
- **`ADMIN_EMAILS` 명시 설정 권장** — 비어 있으면 `CONSULT_NOTIFY_EMAIL`로 폴백한다
  (`lib/admin.ts:25`). 그 주소 계정을 가진 사람이 곧 운영자가 되므로, 공용/외주와
  공유되는 주소라면 명시하는 편이 안전하다. 코드 문제가 아니라 운영 설정이라 손대지 않았다.

---

## 7. 🔴 중단된 작업 — 돌봄 흐름 실사용 검증 (여기서 이어서)

### 7-1. 무엇을 하던 중이었나

이번 세션에서 가장 성과가 좋았던 방법을 **돌봄 요청 흐름**(보호자 요청 → 매니저 지원 →
확정 → 합의서 → 돌봄일지)에 적용하던 중이었다. 5개 화면 중 **3개까지 보고 멈췄다.**

**방법(이게 이 세션의 진짜 자산이다):**
"서버 가드가 맞는가"가 아니라 **"사용자가 그 화면에서 실제로 뭘 보는가"**를 본다.
오늘 찾은 결함 4건이 전부 이 방법에서 나왔다. 서버 API는 전부 올바르게 401/404를
돌려주고 있었는데, **화면이 그걸 잘못 해석**하고 있었던 것이다.

> 공통 원인: `fetch`가 실패했을 때 "로그인 안 함"과 "데이터 없음"을 구분하지 않는다.
> 그래서 **이미 등록/승인된 사용자가 로그아웃 상태로 들어오면 "당신은 등록 안 됐다"**를
> 보거나(§2-8·§2-9), 아예 **화면이 영영 안 뜬다**(무한 로더).

### 7-2. 어디까지 확인했나

비로그인 상태에서 각 화면이 무엇을 보여주는지 실측한 결과:

| 화면 | 결과 | 판정 |
|---|---|---|
| `/care-request` | 요청 등록 폼이 정상 렌더(652자) | ✅ 정상 |
| `/care-request/care-log` | "로그인이 필요해요" | ✅ 정상 |
| `/care-request/care-log/join` | "초대 링크가 올바르지 않아요 / 보호자께 초대 링크를 다시 요청해주세요" | ✅ 정상(토큰 없이 접근한 경우라 맞는 안내) |
| `/care-request/confirmation` | **`<main>` 요소 자체가 없음** | ⚠️ **여기서 멈췄다** |
| `/care-request/agreement` | **`<main>` 요소 자체가 없음** | ⚠️ **여기서 멈췄다** |

### 7-3. 다음 사람이 바로 할 일

**`<main>`이 없다는 건 아직 "버그"가 아니라 "신호"다.** 그 두 화면이
(가) `<main>` 대신 `<div>`로 감싸 정상 안내를 띄우는 중인지,
(나) 정말 아무것도 안 그리고 빈 화면인지
**를 아직 구분하지 못했다.** (나)라면 jobs 무한 로딩과 같은 급의 결함이다.

⚠️ **(가)일 가능성이 꽤 있다.** 오늘 이 방법으로 5번 넘게 "찾았다" 싶었다가
실제로는 내 판별식이 좁아서 오탐인 경우가 있었다(§3-6). 반드시 실제 화면 글자를
확인한 뒤 판단할 것.

**재현 절차:**

```bash
# 1) dev 서버를 띄운다(포트는 자동 할당될 수 있으니 출력값 확인)
#    ⚠️ CLAUDE.md 규칙: dev 켠 채로 npm run build 금지
```

그 다음 브라우저에서 아래를 실행한다(로그아웃 상태여야 한다 — 시크릿 창 권장):

```js
// 비로그인 화면 실측 — <main>이 아니라 body 전체에서 헤더·푸터를 뺀 실제 내용을 본다
(async () => {
  const pages = ['/care-request/confirmation', '/care-request/agreement'];
  const fr = document.createElement('iframe');
  fr.style.cssText = 'position:fixed;left:-9999px;top:0;width:420px;height:900px;border:0;';
  document.body.appendChild(fr);
  const out = [];
  for (const p of pages) {
    await new Promise(r => { fr.onload = () => setTimeout(r, 4000); fr.src = p; });
    const d = fr.contentDocument;
    const header = d.querySelector('header')?.innerText ?? '';
    const footer = d.querySelector('footer')?.innerText ?? '';
    let body = d.body.innerText;
    // 헤더·푸터를 걷어내야 "본문이 진짜 비었는지"가 보인다
    body = body.replace(header, '').replace(footer, '').replace(/\s+/g, ' ').trim();
    out.push({ p, 본문길이: body.length, 본문: body.slice(0, 200),
               로더잔존: !!d.querySelector('[aria-busy="true"]') });
  }
  fr.remove();
  return JSON.stringify(out, null, 1);
})()
```

**판정 기준:**
- 본문에 "로그인이 필요" 류 안내가 있으면 → ✅ 정상, 넘어간다
- 본문이 사실상 비어 있거나 로더만 남아 있으면 → 🔴 `app/mypage/sitter/jobs/page.tsx`의
  2026-08-08 수정(커밋 `1ebb820`)과 **같은 방식**으로 고친다:
  `useSession()`으로 로그인 여부를 **데이터 상태보다 먼저** 보고, 비로그인이면
  "로그인이 필요해요" 화면을 따로 낸다.

### 7-4. 그 다음에 볼 것 (아직 손도 안 댐)

돌봄 흐름에서 **로그인 여부 말고도** 확인이 필요한 것들:

1. **흐름 중간의 뒤로가기.** 요청 등록 마법사(`CareRequestWizard`)는 여러 단계인데,
   중간에 뒤로가기를 누르면 입력이 날아가는지 / 단계만 뒤로 가는지 확인 안 됨.
   (모달에는 `useBackToClose`를 붙였지만 **마법사 단계에는 안 붙였다** — §3-4 참고,
   붙일지 말지도 판단이 필요하다.)
2. **합의서 서명 도중 세션 만료.** 서명은 SHA-256 봉인이 걸린 법적 기능이다
   (docs/care-agreement-spec.md). 첫 서명 직후 만료되면 화면이 어떻게 되는지 미확인.
3. **돌봄일지 날짜 창 경계.** 종료 후 7일 유예가 지난 시점에 화면이 무엇을 보여주는지
   (서버는 막지만 화면 안내가 있는지) 미확인.

### 7-5. 이 방법을 다른 곳에도 쓸 때

**공용 판별식을 만들지 말 것.** 오늘 `grep`으로 "로그인 분기가 있는가"를 세 번 다르게
짰는데 세 번 다 놓치는 게 있었다(`!== "authenticated"`, `data === null`,
`profile === null` 등 표현이 제각각이다). **결국 브라우저로 직접 열어보는 것만이 확실했다.**
코드를 훑는 건 후보를 좁히는 데까지만 쓰고, 판정은 반드시 실측으로 한다.

---

## 6. 이전 문서

[17차](HANDOVER-2026-08-06-17차.md) 사진 5개 지역·검색엔진 등록 ·
[16차](HANDOVER-2026-08-06-16차.md) 폐업 추적·데이터 자산 상품화(§3 함정 15개 유효) ·
[15차](HANDOVER-2026-08-06-15차.md) 돌봄 포인트·지도 앱형 ·
[14차](HANDOVER-2026-08-05-14차.md) 메인 고급화·역경매 ·
[13차](HANDOVER-2026-08-05-13차.md) 사진 파이프라인 상세 ·
[12차](HANDOVER-2026-08-04-12차.md) B2B 전면 개편 ·
[11차](HANDOVER-2026-08-04-11차.md) 체감 속도·UI ·
[10차](HANDOVER-2026-08-04-10차.md) 알림 시스템 ·
[5차](HANDOVER-2026-08-02-5차.md) §1 함정·§3 규칙(여전히 유효)
