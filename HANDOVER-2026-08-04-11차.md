# 돌보다 인수인계 — 11차 세션 (2026-08-04)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다.
> 10차와 같은 날 이어진 세션이지만 성격이 다릅니다 — 10차는 **알림 시스템**,
> 이번은 **체감 속도·UI 다듬기·버그 수정**입니다.
> 이전: [10차](HANDOVER-2026-08-04-10차.md) · 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)는 여전히 유효합니다.

---

## 0. ★ 사용자가 직접 해야 할 일

10차 §0에서 이월된 것들이 **그대로 남아 있습니다.** 이번 세션에서는 확인하지
못했습니다.

- 🔴 **네이버 로그인 프로덕션 반영** — Vercel 환경변수(`AUTH_NAVER_ID`·
  `AUTH_NAVER_SECRET`) 등록 + 네이버 개발자센터 검수. 안 하면 배포본에서
  네이버 버튼이 죽습니다.
- 🟡 **GitHub Actions 시크릿**(`DIRECT_URL`·`RESEND_API_KEY`·`MAIL_FROM`) —
  알림 워크플로 4개가 전부 이것에 걸려 있습니다.
- 🟡 Vercel `ADMIN_USER_IDS`, 공공데이터 제공신청, 공단 사진 zip 재다운로드,
  서치콘솔 canonical 확인(8/6 예약)

---

## 1. 이 세션에서 한 일

### 1-A. ⭐ 전환 애니메이션 도입 → 되돌림 (`632813d`·`ff36b35` → `53baa3b`에서 revert)

시설 카드를 누르면 카드가 상세 페이지로 "변형되는" Shared Element Transition을
View Transitions API로 두 차례에 걸쳐 만들었다가(사진·제목·배지 3쌍 짝짓기,
시차 타이밍, 뒤로가기 복원, 접근성까지) **사용자가 실기기에서 보고 "느려지는
것 같다"고 해서 전부 되돌렸다.**

**되돌린 것이 옳았다.** 전환은 420ms 동안 새 화면을 붙잡고 있는 구조라 실제로
기다림을 만든다. 애니메이션으로 "빨라 보이게" 하려던 것이 오히려 느리게 만든
셈이다.

**다음에 또 이 주제가 나오면 알아둘 것:**
- `next-view-transitions` 라이브러리는 제거했다(package.json에서도 빠짐).
- View Transitions API는 **보안 컨텍스트에서만 동작한다** — HTTPS 또는
  `localhost`. 휴대폰에서 `http://192.168.x.x:3000`으로 접속하면 API 자체가
  없는 것으로 취급돼 전환이 안 보인다. 로컬 IP로 테스트하면 "안 된다"는
  잘못된 결론을 내리기 쉽다.
- **이 자동화 환경(Claude 브라우저 창)에서는 View Transition을 눈으로 확인할
  수 없다.** 프레임 합성이 안 돼 스냅샷을 못 뜬다 — 빈 div 크기만 바꾸는
  최소 테스트도 `InvalidStateError`로 실패한다. 코드 문제와 헷갈리지 말 것.
- `view-transition-name`은 문서 안에서 **유일**해야 한다. 둘 이상이면 브라우저가
  전환을 통째로 취소한다(`InvalidStateError`). 목록처럼 같은 요소가 여럿인
  화면에서는 "누른 것에만" 이름을 붙이고, 붙이기 전에 기존 것을 지워야 한다.

### 1-B. ⭐⭐ 체감 속도 — `loading.tsx` 두 개 (이번 세션의 핵심)

사용자가 지적한 서로 다른 두 증상이 **같은 원인**이었다:
1. "시설찾기를 누르면 전국 요양병원·요양원… 이런 게 먼저 뜬다"
2. "카드를 눌러 상세로 가는 게 느리다"

원인: `/search`와 `/facility/[id]`에 `loading.tsx`가 없었다. App Router는
`loading.tsx`가 없으면 서버 응답이 올 때까지 **이전 화면을 그대로 붙잡고
있는다.** 그래서 (1)은 본문이 빈 동안 푸터가 화면 위로 올라온 것이고,
(2)는 눌러도 아무 반응이 없던 것이다.

- `app/search/loading.tsx` — `PageLoader`(뷰포트를 채워 푸터를 밀어냄)
- `app/facility/[id]/loading.tsx` — **실제 레이아웃 모양의 뼈대**. 로고 로더가
  아니라 뼈대인 이유: 곧 나올 화면과 같은 모양이 보이면 이미 도착한 것처럼
  느껴진다. 돌아가는 로고는 "지금부터 기다려라"는 신호다.

**실측: 클릭 → 화면 반응 258ms** (기존에는 응답 올 때까지 무반응).

> 다른 라우트에도 같은 문제가 있을 수 있다. 현재 `loading.tsx`가 있는 곳:
> care-request, compare, favorites, mypage, notifications, **search(신규)**,
> **facility/[id](신규)**. 새 라우트를 만들 때 기본 작업 범위에 넣을 것.

### 1-C. 상세 페이지 서버 조회 병렬화 + 남은 병목

소개·소식·비슷한시설 세 조회가 **순차**였다. 서로 무관한 조회라 `Promise.all`로
묶었다. **실측 165ms → 135ms.**

⚠️ **남은 병목: "비슷한 시설" 조회 98ms.** `address: { contains: sigungu }`는
LIKE `%사직로%` 형태라 **인덱스를 못 타고 2만 8천 행을 전부 훑는다**(정작
얻는 건 28행). 병렬화해도 이 하나가 전체 시간을 지배한다.

앞으로 손볼 수 있는 방향(아직 안 함):
1. `pg_trgm` GIN 인덱스 — `contains` 조회를 인덱스로 태울 수 있다
2. 주소에서 시/군/구를 별도 컬럼으로 빼서 정확 일치(=)로 바꾸기 — 인덱스 직행
3. 이 섹션을 `<Suspense>`로 스트리밍 — 본문이 98ms 먼저 뜬다(SEO는 유지된다,
   RSC 스트리밍은 크롤러에게도 최종 HTML을 준다). 페이지 하단이라 늦게 와도
   사용자가 느끼지 못한다. **셋 중 가장 손이 덜 가고 효과가 확실하다.**

### 1-D. 워딩 — "AI기반 안심지수" → "공공데이터 기반 안심지수" (전면)

실제로 AI가 아니라 공공데이터 가중평균이라 이쪽이 정확하다. 사용자 지시로
**코드·문서 전체**를 바꿨다: 상세페이지·약관·개인정보처리방침·데이터출처·
요양가이드·`CLAUDE.md`·`docs/ai-score-spec.md`·소스 주석까지.

- 남겨둔 곳: **과거 인수인계 문서**(HANDOVER-2026-08-01.md, HANDOVER.md) —
  그때의 기록이라 바꾸지 않았다.
- `docs/ai-score-spec.md`는 **파일명은 그대로**고 제목만 바뀌었다(경로를
  참조하는 곳이 여럿이라). 헷갈리지 말 것.

### 1-E. 복지용구 혜택 — 주인공을 바꿈

"지금 바로 상담받고 싶다면"을 앞세우던 것을 **"예상 등급 확인"이 주가 되게**
뒤집었다(사용자 지시).

근거는 `WelfareConsultGate.tsx`에 이미 적혀 있던 판단과 같다 — 복지용구는
장기요양등급이 있어야 받는 제도인데, 검색으로 처음 들어오는 사람 대부분은
아직 등급이 없다. 그 사람들에게 상담 신청을 앞세우면 연락처만 남고 운영자가
해줄 수 있는 게 없다.

상담은 없애지 않고 "이미 요양인정번호가 있어요 · 상담 신청"으로 뒤에 물렸다
(기존 기능 제거 아님). 로그인 상태면 그 자리에서 폼이 펼쳐진다.

### 1-F. 등급테스트 결과 화면

- **결과 카드**: 흰 카드라 아래 섹션들과 구분이 안 돼 밋밋했다 → 등급대별
  색(`TONE_STYLES.hero`)으로 채우고, 등급 문구는 등장 시 한 번 `animate-pop`.
  점수 눈금은 왼쪽에서 차례로 차오른다(`grade-fill` 키프레임 신설,
  `transform`만 사용해 저사양 기기 부담 없음).
- **순서 교체**: "이 결과를 저장하기"를 "이런 서비스를 주로 이용해요"보다 앞으로.
- 저장 버튼에 `animate-bob-slow`(4px·3초). hover 시엔 멈춘다(hover의
  `-translate-y`와 겹쳐 어긋나 보이므로).

### 1-G. 🐛 버그 — 등급테스트 하이드레이션 깨짐

`GradeTest.tsx`가 **렌더 도중** `sessionStorage`를 읽어 초기 상태를 정하고
있었다. 서버에는 sessionStorage가 없으니 서버는 "인트로", 클라이언트는
"결과"를 그려 하이드레이션이 깨지고 React가 화면을 통째로 다시 그렸다.

**로그인하고 돌아오는 흐름 = 이 화면의 주 사용 경로**에서 매번 일어나던
문제다(52문항을 풀고 로그인하면 저장분이 항상 있으니까).

고친 방법: 초기값은 서버와 같게("인트로") 두고, `useLayoutEffect`로 복원한다.
`useEffect`가 아닌 이유는 그리기 **뒤**에 실행돼 인트로가 한 번 번쩍이기
때문. 서버에서는 `useEffect`로 바꿔치기해 경고를 피한다
(`useIsomorphicLayoutEffect`).

⚠️ 함께 넣은 가드: 복원이 끝나기 전에는 저장 effect가 돌지 않게 막았다.
안 막으면 첫 렌더의 초기값(빈 답변)이 저장분을 덮어써 **답변 52개가 날아간다.**

> **같은 유형의 버그를 조심할 것.** `app/search/SearchPageClient.tsx`도 예전에
> 같은 문제를 겪고 고친 전례가 있다(그쪽 주석 참고). 렌더 도중
> `localStorage`/`sessionStorage`/`window`를 읽는 코드는 전부 후보다.

### 1-H. 🐛 버그 — 마이페이지 "매니저 시작"이 잠깐 떴다 "매니저"로 바뀜

`MyPageRoleCard`가 `canSwitch`(= `isSitter === true`)로 분기하는데, 로딩 중
`isSitter`는 `null`이다. `null === true`는 `false`라 **로딩 중엔 무조건
미등록으로 취급**돼 "매니저 시작"이 먼저 그려졌다.

고친 방법: `isSitter === null`(확인 중)이면 글자를 정하지 않고 같은 크기의
자리만 잡아둔다. 확정된 뒤에도 레이아웃이 흔들리지 않는다.

> `MyPageShell`은 이미 `isSitter === false`로 올바르게 처리하고 있었고 주석에
> 같은 이유가 적혀 있었다 — `MyPageRoleCard`만 빠져 있었다.
> **3항 불리언(`true`/`false`/`null=로딩중`)을 이항으로 좁힐 때 항상 이 함정이
> 있다.** `=== true`로 좁히면 로딩 중이 false 쪽에 붙는다.

### 1-I. 푸터 정리 + 마이페이지 순서

- **푸터**: 링크 9개를 한 줄에 흘려 4/4/1로 어긋나게 접히던 것을
  **서비스·돌보다·이용안내 세 묶음**으로. 개수가 안 맞아도 의도된 배치로 읽힌다.
  저작권 문구는 아래로 내렸다.
- **전국 유형 링크**(전국 요양원 등)를 아래 지역 링크와 같은 무게로 낮췄다
  (`text-[10px] text-ink-300/50`). 검색엔진 크롤 경로라 숨기면 스팸 신호가
  되지만, 본문 링크와 같은 크기면 정작 위쪽 메뉴가 묻힌다.
- **마이페이지**: 메뉴의 얇은 줄(등급 테스트·상담 신청 내역)이 대시보드의
  "진행 중인 돌봄 요청" 카드보다 **위에** 나왔다. 지금 진행 중인 일이 먼저고
  가끔 들춰보는 기록이 그다음이라, 얇은 줄을 대시보드 아래로 내렸다.
  데스크톱 사이드바에는 그대로 있어 접근성은 줄지 않는다.

---

## 2. 다음에 할 일

### 2-1. "비슷한 시설" 조회 98ms — §1-C의 3번(Suspense 스트리밍) 추천

가장 손이 덜 가고 효과가 확실하다. 페이지 하단 섹션이라 늦게 와도 사용자가
느끼지 못하고, 본문은 98ms 먼저 뜬다.

### 2-2. 10차에서 이월 — 취소 알림

보호자가 요청을 취소하면(`DELETE /api/care-requests/[id]`) 지원자 상태는
"요청취소"로 바뀌는데 이메일이 안 나간다. 10차 §1-J와 같은 자리·같은 패턴으로
붙일 수 있다.

### 2-3. 나머지 라우트 `loading.tsx` 점검

§1-B 참고. 없는 라우트에서 같은 증상이 난다.

### 2-4. 리텐션 로드맵

1. **입소 대기 예측** — 여전히 1순위, 아직 손 안 댐
2. 돌봄일지 2단계 — 매칭 데이터가 쌓여야 검증 가능
3. 근무환경지수 변화 감지 알림 — 즉석 계산 지표라 일별 스냅샷이 없으면
   값싸게 못 만든다(10차 §1-H)

---

## 3. 이 세션에서 배운 함정

1. **애니메이션은 체감 속도를 낮추기도 한다.** 전환 시간만큼 새 화면이 늦게
   나온다. "빨라 보이게" 하려면 애니메이션보다 **즉각 반응**(loading.tsx)이
   훨씬 효과적이다.
2. **`loading.tsx`가 없으면 화면이 이전 상태로 멈춰 있는다.** 사용자에게는
   "눌렀는데 아무 일도 안 일어남"으로 보인다. 새 라우트의 기본 작업 범위.
3. **로딩 상태를 이항으로 좁히면 잘못된 쪽에 붙는다.** `isSitter === true`처럼
   좁히면 `null`(로딩 중)이 false 취급돼 잘못된 UI가 번쩍인다.
4. **렌더 도중 브라우저 저장소를 읽으면 하이드레이션이 깨진다.** 서버에는 그
   값이 없기 때문. `useLayoutEffect`로 복원하면 깜빡임 없이 해결된다.
5. **View Transitions API는 보안 컨텍스트 전용**이고, **이 자동화 환경에서는
   눈으로 검증할 수 없다**(§1-A).
6. **JSX에서 `{cond && (` 바로 뒤에 `{/* 주석 */}`을 넣으면 문법 오류가 난다.**
   조건식 밖으로 빼야 한다(이번에 한 번 밟았다).

---

## 4. ⚠️ 미커밋 상태 (계속 보류)

B2B 입점 절차 개편 코드가 여전히 working tree에만 있습니다:
`app/admin/AdminClient.tsx` · `app/api/admin/inquiries/route.ts` ·
`app/api/business-inquiry/route.ts` · `app/business/BusinessInquiryForm.tsx` ·
`app/business/console/page.tsx` · `app/business/page.tsx` · `lib/businessPlans.ts` ·
`app/business/apply/`(미추적)

**사용자가 "나중에 다시 할게"로 보류한 상태.** 이번 세션에서도 건드리지
않았습니다. 임의로 커밋·배포하지 말 것.

---

## 5. DB 변경

**없음.** 이번 세션은 화면·성능 작업이라 스키마를 건드리지 않았습니다.
(10차까지의 모델은 전부 프로덕션 반영 완료 — 10차 §5 참고)
