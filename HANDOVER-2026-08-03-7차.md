# 돌보다(Dolboda) 인수인계 — 7차 세션

> 최종 갱신: **2026-08-03** · 이 문서만 읽고 바로 이어서 작업할 수 있게 작성했습니다.
> 이전 문서: [6차](HANDOVER-2026-08-02-6차.md) → [5차](HANDOVER-2026-08-02-5차.md) → …
> 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)는 여전히 유효합니다.

- 배포: https://www.dolboda.kr · 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (`main`)
- **⚠️ 이 세션의 변경은 전부 미커밋·미배포 상태다.** `git status`에 수정 20여 파일 + 신규 15개.

---

## 0. ★ 배포 전 반드시 할 일 (순서대로)

### 0-1. 🔴 `npx prisma db push` — 안 하면 신청 폼·관리자 콘솔이 500

이 세션에서 스키마에 모델 **5개**가 추가됐다:
`BusinessInquiry`(입점 신청) · `FacilityOwner`(시설-계정 연결) ·
`FacilitySubscription`(구독) · `SponsorPlacement`(광고 슬롯) · `FacilityContent`(시설 작성 소개·사진).
push 후 **dev 서버 재시작** 필수(떠 있는 서버는 옛 Prisma 클라이언트를 물고 있다 — 4차 함정).
시설 상세·지역 페이지는 push 전에도 죽지 않게 방어돼 있다(§2).

### 0-2. 🔴 Vercel 환경변수 `ADMIN_EMAILS` 등록

`/admin`(운영자 콘솔) 접근 판별용. 쉼표 구분 이메일 목록.
없으면 기본값으로 `CONSULT_NOTIFY_EMAIL`(wkdgytls1201@gmail.com)이 쓰인다.
**카카오 로그인 계정에 이메일이 있어야 통과된다.** 환경변수는 넣고 재배포해야 반영(6차 함정 5).

### 0-3. 🟡 배포 후 서치콘솔 확인 (SEO 작업분)

vercel.app → www 308 리다이렉트가 이번 배포에 포함된다. 배포 후
`/`, `/region/경남`, `/facility/nhis2-1f2njjz`의 "Google이 선택한 canonical"이
www.dolboda.kr로 잡히는지 확인. 새 페이지: `/요양원` `/요양병원` `/주야간보호` `/방문요양` `/business`.

### 0-4. 🟡 공단 사진 zip 재다운로드 (6차에서 이월, 여전히 미결)

---

## 1. 이 세션에서 한 일 (크게 세 덩어리)

### 1-A. SEO 점검·수정 (사용자가 외부 SEO 진단을 가져옴)

1. **vercel.app 중복 도메인 차단** — `next.config.mjs`에 host 기반 전 경로 308 리다이렉트.
   `public/llms.txt`(통째로 옛 도메인이었음)·`lib/siteConfig.ts` 기본값도 www로 교체
2. **홈 제목·설명 교체** — `SITE_TITLE` 상수 신설("전국 요양원·요양병원 찾기·비교 | 돌보다"),
   설명은 "2만8천여 개" 자릿수 표기 (정확한 숫자는 데이터 갱신마다 어긋난다)
3. **지역+유형 페이지 FAQ를 유형별 전용으로** — `lib/facilityTypeSeo.ts`에 유형마다 faq 배열,
   `{지역}` 치환 + 받침 조사 처리(`lib/korean.ts`). "김해시에는 요양시설이 몇 곳?"에 요양원
   수를 답하던 오답 해소
4. **전국 유형 허브 4페이지 신설** — `/요양원` 등 한글 URL(`app/[typeSlug]/page.tsx`).
   전국 통계·등급 분포·시도별·상위 지역 30곳·FAQ. 사이트맵·푸터·가이드 CTA 연결.
   ⚠️ `dynamicParams=false`는 한글 slug에서 전부 404를 냈다(실측) — notFound() 방식으로 대체
5. **본문이 푸터보다 뒤에 나오던 문제** — 루트 `app/loading.tsx`가 원인(전 페이지가 Suspense로
   감싸져 본문이 `<div hidden>`으로 밀림). 삭제하고 `/mypage` 등 앱 화면 5곳에만 재배치.
   실측: main 7.6k → h1 9.6k → footer 36k로 정상화
6. **/search에 h1이 없던 것** — 하단 SEO 섹션 h2→h1
7. 소개 페이지: "가공 없이 그대로" → "왜곡하지 않고 정제·표준화" (실제와 맞는 표현),
   이메일/전화 버튼에 라벨+값 병기 (크롤러가 "이메일 문의010-…"으로 읽던 문제)

### 1-B. 코드 점검에서 잡은 버그 2건

- `lib/emailOptOut.ts` — 토큰 길이를 글자 수로 비교해 한글 32자(96바이트)가 통과 후
  `timingSafeEqual`에서 500. 바이트 비교로 수정(재현 확인)
- `lib/viewLimit.ts` — localStorage 값이 배열 아니면 시설 상세가 통째로 죽음. 배열 검사 추가

### 1-C. 시설 운영자(B2B) 기능 — 신규 사업

**요금제** (`lib/businessPlans.ts` — 단일 출처, 전부 부가세 별도):

| plan id | 상품 | 월 | 비고 |
|---|---|---|---|
| free | 시설 기본 등록 | 0 | 인증+페이지 관리 |
| plus | 비즈니스 플러스 | 49,000 | 사진 30장·소식·통계 |
| sponsor | 지역 스폰서 | 149,000 | **시·군·구당 3곳 상한**(`SPONSOR_SLOTS_PER_SIGUNGU`) |
| premium | 지역 프리미엄 | 299,000 | plus+sponsor+배너 (★추천 표시) |
| silvertown | 실버타운 프리미엄 | 790,000~ | 전국·광역 |

**⚠️ 가격·유료 노출 방침 (사용자 확정)**: 앞단 /business에는 **유료의 흔적 자체를 싣지
않는다** — 가격·요금·유료·확장 상품·스폰서·상품명·부가세 전부 금지(전수검사로 0회 확인).
일반 보호자도 보는 페이지라서다. 요금표(BUSINESS_PLANS)는 인증된 담당자만 보는
시설 콘솔(#plans 섹션)에서만 보여준다. 신청 폼도 상품 선택 없이 항상 free로 접수.
앞단에 유료 문구를 다시 넣는 건 사용자 결정 사항이며, 넣게 되면 JSON-LD Offer도 함께 맞출 것.

**만든 것 (전부 타입검사·프로덕션 빌드 통과):**

```
lib/businessPlans.ts        요금제·가입절차 단일 출처
lib/admin.ts                운영자 판별 (ADMIN_EMAILS)
lib/facilityOwner.ts        시설 소유권·구독 조회, capabilityOf(plan별 기능 상한)
lib/sponsor.ts              스폰서 슬롯 조회(5분 캐시)·잔여 슬롯
lib/korean.ts               받침 조사 처리
components/SponsorSlots.tsx 광고 표시 컴포넌트 (분리 섹션 + '광고' 라벨)
app/business/page.tsx       입점 안내 (정적, JSON-LD Service+Offer+FAQ)
app/business/BusinessInquiryForm.tsx  신청 폼 (동의 사전체크 없음)
app/api/business-inquiry/route.ts     신청 접수 (검증·레이트리밋·시설 자동매칭·알림)
app/admin/page.tsx + AdminClient.tsx  운영자 콘솔 (신청 처리 + 구독·광고 관리)
app/api/admin/inquiries/route.ts      승인 트랜잭션 (아래 §2)
app/api/admin/subscriptions/route.ts  입금확인·중지·해지 (슬롯 연동)
prisma/schema.prisma        모델 4개 신설
```

**운영 흐름 (돈이 도는 길):**

```
시설이 /business에서 신청 (최소 7항목, 서류 없음)
  → BusinessInquiry 접수 + 운영자 메일·텔레그램
  → 운영자가 공단 공개 대표번호로 확인 전화 (/admin에서 "확인전화 완료")
  → 서류 1건 수령, 담당자 카카오 로그인 → 그 계정 id를 /admin 승인란에 입력
  → [승인] = 한 트랜잭션: FacilityOwner + FacilitySubscription(pending) + SponsorPlacement(비활성)
  → 입금 확인 → /admin 구독 탭 [입금 확인·노출 시작] = 구독 active + 광고 슬롯 켜짐
  → 해지 → 구독 cancelled + 슬롯 즉시 꺼짐 (대기 시설에 자리)
```

승인 API가 지키는 것: 이미 승인된 건 되돌리기 금지(유령 권한 방지), 스폰서 상품은
**슬롯 잔여 확인 후에만 승인**(상한 3곳을 코드가 지킨다), 시설 미매칭 신청은 시설 id 지정 요구.

### 1-D. "순위를 팔지 않는다" 계열 문구 **완전 삭제** (사용자 최종 지시)

처음엔 사실 서술로 다듬었으나 사용자가 "그냥 없애자"로 확정 → **전부 들어냈다.**
- `/business`의 "광고에 대한 우리의 원칙" 섹션 통째 삭제 → 그 자리에 **콘솔 미리보기**(CSS 목업)
- FAQ에서 순위·공정성 언급 제거, 노출 위치·리포트 설명만 남김
- `app/about/page.tsx` 원칙 카드: "광고비로 순위를 바꾸지 않습니다" → "광고와 정보를 구분해 보여드립니다"
- 시설 전달 메일(`lib/resend.ts`·`app/api/consult/route.ts`): 순위 문장 삭제, "광고로 표시" 사실만
- 실측: /business·/about 렌더 HTML에 "순위" 0회
- **남긴 것**: '광고' 라벨 자체(표시광고법상 필수)와 "지역당 3곳·차면 대기"(판매 수량 안내)

### 1-E. 케어닥 B2B 구조 분석 → [docs/competitor-caredoc-b2b.md](docs/competitor-caredoc-b2b.md)

기업회원 = 프랜차이즈(company.caredoc.kr) + 케어옵스 SaaS(careops) + MRO 폐쇄몰(mro) +
직영시설 4개 사업의 묶음. 상세 비교·약관 정리·돌보다 차별점(가격 공개, 페이지 클레임 통로,
광고 분리 표시, 리드 기록)은 그 문서에. **다음 단계 후보 로드맵도 그 문서 §4에 있다.**

---

## 2. ⚠️ 구현 상태 — 페이지가 파는 것 vs 실제 (세션 말 기준)

| /business가 약속 | 상태 |
|---|---|
| 무료 인증 + 신청 접수 | ✅ 완성 (승인까지 운영자 수동) |
| 스폰서 노출 (광고 슬롯) | ✅ 지역 페이지 연결 완료 — 시군구·시군구+유형 페이지 목록 위 분리 섹션('광고' 라벨). 검색(/search) 화면은 미연결(지역 특정이 안 되는 화면이라 보류) |
| 시설 콘솔 | ✅ **/business/console** — 소개글·사진(무료 5/유료 30)·**조회수(7일/30일)**·**상담 신청 내역 목록(연락처+바로 전화)**·**시설 소식 발행(plus 이상, API도 plan 가드)**·**정보 정정 요청(운영자 메일·텔레그램 알림)**·광고 노출 중 배지·확장 상품 요금표 |
| 콘솔 작성분의 보호자 노출 | ✅ 시설 상세: "시설이 직접 소개해요"(소개·사진) + **"시설 소식"(최신 3개)** — 둘 다 '시설 제공' 라벨 |
| 페이지 조회수 집계 | ✅ **신설** — 상세 진입 시 sendBeacon → `/api/facilities/view` → `FacilityDailyView`(시설·**KST 날짜**별 +1, 개인정보 없음, IP당 시간 120회 상한, 실재 시설만) |
| 영상 발행 | 🔴 없음 |
| 지역 배너·월간 성과 보고서 | 🔴 없음 (조회수·상담 데이터가 쌓이면 보고서 재료는 준비됨) |
| 결제 | 🔴 없음 — 계좌이체+운영자 입금확인 설계 (pending→active) |

**콘솔 관련 스키마 (2차 push 완료)**: `FacilityPost`(소식, 시설당 20개 상한) ·
`FacilityDailyView`(일일 조회수, PK=시설+날짜) · `FacilityCorrection`(정정 요청 접수).
정정 요청은 시설이 공공데이터를 직접 못 고치게 하는 설계다 — 안심지수 입력값이라
시설 손에 맡기면 지표 신뢰가 무너진다. 접수 → 운영자 확인 → 반영.

**/admin에 "정정 요청" 탭 추가** — 시설 콘솔에서 들어온 정정 요청을 확인 필요/반영
완료/반영 불가로 처리한다. `app/api/admin/corrections/route.ts`. 상태 전환 API만
있고, "반영 완료"를 눌러도 실제 공공데이터를 자동으로 고쳐주지는 않는다 — 운영자가
원본과 대조해 수동 반영한 뒤 상태만 기록하는 용도다(재적재 파이프라인과는 별개).

### 1-F. 지역 배너 (지역 프리미엄 이상 전용) — 신규

`FacilityBanner` 모델. **시·군·구당 1곳**(`BANNER_SLOTS_PER_SIGUNGU`, 스폰서의
3곳보다 희소하게 — 프리미엄이 "가장 눈에 띄는 자리"여야 값어치가 있다는 판단).

- `/admin` 입점 승인 시 premium·silvertown이면 **스폰서와 같은 트랜잭션에서** 배너
  행도 함께 예약(이미지는 아직 없음, `active:false`). regionKey는 스폰서와 동일 키를
  공유 — 프리미엄 승인 한 번으로 스폰서 카드 노출 + 배너 자리가 같이 확보된다
- 슬롯 부족 시 승인 자체가 400/409로 막힌다(스폰서 슬롯 검사와 같은 자리에서 함께 검사)
- 시설 콘솔(`/business/console`)에 "지역 배너" 섹션 — `lib/wideImage.ts`로 브라우저에서
  4:1 비율(1200×300)로 미리 크롭 후 업로드. `premium`·`silvertown` 아니면 섹션 자체가
  안 보임(`capabilityOf(plan).hasBanner`)
- 자가 치유: 요금제 변경 등으로 승인 시점에 배너 행이 안 만들어진 옛 계정도, 콘솔에서
  처음 업로드할 때 스폰서 지역을 재사용해 새로 만든다(이때도 슬롯 재검사)
- 구독 상태 토글(`/api/admin/subscriptions`)이 스폰서 배치와 배너를 **같은 트랜잭션**에서
  함께 켜고 끈다 — "입금 확인했는데 배너만 안 나온다" 같은 어긋남 방지
- 노출: 시군구 지역 페이지(`app/region/[sido]/[sigungu]/page.tsx`) 상단, 스폰서 목록보다
  **위**에 배치(`components/RegionBanner.tsx`). 이미지 위에 '광고' 라벨 오버레이 — 값비싼
  자리라고 표시 의무가 없어지지 않는다
- **DB 레벨 전체 생명주기 검증 완료**(예약→이미지→활성화→상한 도달 거부→해지→슬롯 반환→
  비노출) — 스크립트로 실행 후 삭제, 결과는 이 세션 대화 기록에 있음
- 관리자 콘솔 구독 탭에도 배너 상태 표시("이미지 없음"/"노출 중"/"꺼짐")

지역+유형 페이지(`[type]`)에는 배너를 넣지 않았다 — 배너는 지역 전체를 대표하는
자리라 시군구 메인 페이지 하나에만 두는 것이 "가장 눈에 띄는 자리"라는 약속에 맞는다.

### 1-H. 복지용구 혜택 안내 — 신규 (공개 랜딩 + 마이페이지 보호자 전용 화면)

돌봄일지 vs 복지용구 중 무엇을 먼저 만들지 DB로 실측(진행 중 매칭 0건 vs 복지용구
사업소 데이터 이미 보유 1,683곳)해 복지용구를 먼저 선택했다. 케어닥 유사 화면
스크린샷(`C:\Users\linea\Desktop\복지용구\`)을 레퍼런스로 워딩·UX를 새로 짰다.

- `/welfare-equipment` — 공개 마케팅 랜딩. 그라데이션 히어로 + 카운트업 숫자
  (`lib/welfareEquipment.ts`의 `ANNUAL_LIMIT`) + 본인부담금 비교 막대(4구간) +
  구입/대여 품목 탭 + 절차 타임라인 + FAQ. 여기서는 등록·신청을 하지 않는다 —
  로그인 전 이탈을 막기 위해 "혜택이 있다"만 보여주고 CTA는 전부
  `/mypage/welfare-benefit`로 보낸다
- `/mypage/welfare-benefit` — 로그인 + 보호자 프로필 필요. 흐름:
  프로필 없음 → 프로필 만들기 유도 / 등급 없음(`NoGradeState`) → 등급테스트 유도 /
  등급 있음 → **항상 혜택 대시보드**(한도·비교·품목·사업소 찾기)
- ⚠️ **요양인정번호는 게이트가 아니다** — 처음엔 "번호 등록해야 혜택 화면이 열린다"로
  만들었다가 사용자가 "대부분 이 번호를 모른다"고 지적해 구조를 바꿨다. 지금은
  등급만 있으면 번호 없이도 전체 대시보드가 보이고, 등록은 그 안의 눈에 띄는
  권유 카드(보라색, `ShieldCheck` 아이콘) — 등록해도 실시간 조회와 연동되지 않는다는
  걸 화면에 계속 밝힌다(공단 API 연동 없음, 정보 확인용)
- `CareProfile.ltcCertNumber` 필드 추가. **API 사이드 이펙트를 하나 고쳤다**:
  `app/api/care-profile/route.ts`의 `cleanFields()`가 body에 없는 키도 전부 null로
  채우는 구조라, 번호만 보내는 부분 업데이트(`{id, ltcCertNumber}`)가 나머지 프로필
  필드(성별·거동수준 등)를 전부 지워버리는 버그가 될 뻔했다 — "키가 body에 있을
  때만 pick()" 방식으로 바꿔 기존 전체-폼 호출과 새 부분-업데이트 호출이 둘 다
  안전하게 동작하도록 수정
- `lib/welfareProviders.ts` — 복지용구 사업소(공단 급여종류 B06·C06)는 Facility
  테이블에 없다(원래 시설 타입 체계 밖으로 제외됨). `Institution` 원본 테이블을
  지역 페이지와 같은 방식(시도 접두어+시군구 포함)으로 직접 조회. 전화번호 보유율
  100%(1,978건 중 0건 null, 실측) — 상담 신청 DB플로우 대신 `tel:` 링크만 제공
  (계약 당사자가 되지 않는다는 기존 원칙 재사용)
- `/api/welfare-providers` — 시도→시군구 2단계 지역 선택기가 부르는 API
- 애니메이션은 `requestAnimationFrame`이 아니라 `setInterval`/`setTimeout` 계단식
  증가로 만들었다(5차 §1-3 — 이 자동화 환경은 화면이 합성 안 되면 rAF가 아예 안
  불린다). `components/WelfareBenefitVisuals.tsx`의 `CountUpNumber`·`CopayCompareBars`
- 색은 새로 만들지 않고 기존 브랜드 토큰만 재사용(primary=금액 강조, royal=등록
  유도 카드, mint=긍정 배지) — dataviz 스킬 기준으로 새 카테고리 컬러를 쓰지
  않았으므로 팔레트 검증 스크립트는 돌리지 않았다
- `/privacy`에 `ltcCertNumber` 수집 항목 추가, 부칙에 8/3 개정 기록

### 1-G. 월간 성과 보고서 (지역 프리미엄 이상) — 신규

사용자가 "내용이 허접하면 안 하는 것만 못하다"고 짚었다. 그래서 **없는 지표는
0으로 채우지 않고 필드 자체를 숨긴다**는 원칙을 세우고, 그 전제로 먼저
스폰서·배너의 노출·클릭 추적 인프라부터 만들었다(이게 없으면 요금제 설명의
"노출수·클릭수 리포트"가 실제로는 존재하지 않는 기능이었다).

- `AdEvent` 모델 — `FacilityDailyView`와 같은 설계(시설·종류·날짜 하나로 뭉친 카운터).
  kind는 `sponsor_impression`·`sponsor_click`·`banner_impression`·`banner_click`
- `components/AdTracking.tsx` — `SponsorSlots`·`RegionBanner`는 서버 컴포넌트로 두고
  이 파일만 클라이언트 섬으로 분리(`AdImpressionBeacon`=마운트 시 노출 기록,
  `TrackedLink`=클릭 시 기록 후 정상 이동). 지역 페이지가 하루 단위 ISR이라
  서버 렌더 시점에 세면 캐시가 갱신될 때만 올라가 실측을 크게 밑돈다 — 반드시
  브라우저에서 실제로 그려질 때 비컨을 쏴야 한다
- `app/api/ads/track/route.ts` — 조회수 비컨과 같은 패턴(IP 상한, 실재 시설만,
  KST 날짜). **실측**: 유효 kind만 기록되고 잘못된 kind·없는 시설은 조용히
  무시되는 것을 DB에서 직접 확인(테스트 행은 삭제함)
- `lib/monthlyReport.ts` — 이달·지난달 조회수/상담/광고 지표 + **지역 평균 대비
  배수**("우리 시설은 지역 평균의 1.8배"). 지역 평균은 전 시설 공통으로 수집되는
  조회수 데이터를 재사용한다(유료 시설이 아니어도 비교 대상이 됨). 같은 시군구
  비교 대상이 2곳 미만이면 `null`(비교 안 함) — 0으로 만들지 않는다.
  스폰서/배너를 안 산 요금제는 그 섹션 자체가 `null`
- `app/business/console/report/page.tsx` — 순수 서버 렌더, 새 차트 라이브러리
  없이 기존 브랜드 토큰(primary=조회수 막대, mint=긍정 델타, primary=부정 델타)만
  재사용. 30일 스파크라인은 얇은 막대 나열(범례 없음 — 단일 시계열 규칙)
- **실측 검증**: `tsx`로 `getMonthlyReport()`를 실제 DB에 직접 돌려 확인 —
  8/2·8/3 조회 2건이 정확히 잡히고, 지역 평균 0(진짜 계산값)과 null(비교 불가)이
  구분되고, 미가입 시설의 sponsor/banner가 정확히 `null`로 나옴
  ⚠️ 이 검증 중 함정을 하나 새로 발견했다 — 아래 §3에 추가
- 콘솔 메인 화면에 "이번 달 성과 보고서 보기" 진입 버튼(비자격 시설엔 다른 문구)
- **문구 정합성도 맞췄다**: `lib/businessPlans.ts`의 프리미엄 설명에 있던
  "유입 검색어"는 만들지 않아서 문구에서 뺐다(정확히 이 세션의 원칙 그대로 적용).
  다만 같은 목록의 **"상담 버튼 강조 표시"는 여전히 미구현**이다 — 시설 상세
  페이지에 그런 강조 처리가 없다. 다음에 만들거나 문구에서 빼야 한다

### 1-H. 마이페이지 역할 전환 (보호자 ↔ 돌보다 매니저) — 신규

카드 뒤집기 인터랙션으로 한 계정의 두 얼굴을 전환한다. 사용자 요청 그대로:
"카드 뒤집는 느낌 + 기존 UX/UI 최대한 유지".

- `lib/mypageRoleContext.tsx` — 역할 상태. **localStorage에만 저장**(서버 X) —
  "누구인가"가 아니라 "지금 어느 화면을 보고 싶은가"라서다. 첫 가입자는 무조건
  보호자. 매니저 미등록·탈퇴 계정이 저장된 manager로 남아 빈 화면에 갇히지 않게
  isSitter가 **false로 확정**됐을 때만 guardian으로 되돌린다(null 로딩 중에 되돌리면
  매니저가 열 때마다 튕긴다)
- `components/MyPageRoleCard.tsx` — 세그먼트(전환 수단+현재 표시) 위, 신분증 카드
  (뒤집히는 피드백) 아래. 카드 자체를 눌러 뒤집게 하지 않은 이유: 발견 불가능.
  앞뒷면은 **같은 골격**(아바타+버튼2, h-[168px] 고정) — 뒷면이 absolute라 높이를
  못 만들기 때문. 미등록자에겐 오른쪽 칸이 전환 대신 /mypage/sitter/register 링크
- `globals.css`의 `.flip-scene`(perspective 1400px)·`.flip-card`(preserve-3d, 0.5s)·
  `.flip-face`(backface-hidden). perspective를 카드가 아니라 무대에 주는 게 핵심
  (카드에 주면 종이 접히듯 보인다). `prefers-reduced-motion`이면 즉시 전환
- `MyPageShell` — 역할에 따라 보호자/매니저 영역 중 하나만. 보호자 메뉴를 매니저와
  **같은 문법**(대표카드→2×2 그리드→얇은 줄)으로 재구성: 상담내역(대표)·프로필·
  관심시설·시설비교·등급테스트·정보수정. 색으로 모드 구분(보호자=royal, 매니저=코랄)
- `MyPageDashboard` — 카드가 역할을 따른다. **기존 버그도 함께 고침**: 매니저로
  등록하면 보호자 카드(돌봄 프로필·시작 안내)가 영영 안 보였다(`isSitter === false`
  조건). 매니저도 부모를 모시는 보호자일 수 있다 — 이제 역할 전환으로 양쪽을 다 본다
- **실측**: CSS 3D 기제(perspective·preserve-3d·backface·앞뒷면 168px 일치·m11=-1
  회전 완료), 모바일 375px 레이아웃, 거울 글자 없음, 가로 넘침 없음(기존 헤더 넘침은
  이 변경과 무관한 별건) 전부 브라우저에서 확인

### 1-I. 입점 절차 개편 — 통화 전면 삭제 + 신청 전용 페이지 (사용자 결정)

- **전화 통화가 절차에서 완전히 빠졌다.** SIGNUP_STEPS 4단계→3단계(신청→이메일 서류
  확인→계정 연결). 사칭 방지는 대표번호 역전화 대신 **서류의 기관기호·상호가 신청
  내용과 일치하는지 대조**로 바뀌었다. /business·apply·콘솔 빈 화면·운영자 알림 메일
  전부에서 통화 문구 제거(실측: 남은 '통화'는 "전화 통화 없음" 안내문뿐)
- **신청 폼을 /business/apply 전용 페이지로 분리.** /business 하단에는 큰 CTA 배너만.
  이유: 동의 문서까지 소개 페이지에 펼치면 소개도 신청도 무거워진다. apply는 noindex
- **동의 체계**: 필수(개인정보 수집·이용) + 선택(마케팅 수신). 마케팅 동의는
  `BusinessInquiry.marketingConsentAt`(DateTime? — **언제** 동의했는지가 법적으로
  중요)로 저장. 실측: 동의 시 시각, 미동의 시 null 확인 후 테스트 행 삭제.
  ⚠️ 광고성 메일·문자는 이 값이 있는 신청자에게만 보낼 것
- **보유 기간 2년**(기존 6개월에서 변경, 사용자 지시). 신청 페이지 하단에 법정 고지
  4요소(항목·목적·기간·거부권)+파기 방법+제3자 미제공을 접지 않고 전부 펼쳐 적었다.
  서류 사본만은 여전히 "확인 즉시 파기". privacy 페이지도 같은 내용으로 갱신
- /admin: "확인전화 완료" 라벨 → "서류 안내 발송"(상태 키 contacted는 데이터 호환 위해
  유지), "담당자에게 전화" 버튼 → **서류 요청 메일 쓰기**(본문까지 미리 채운 mailto)

### 모바일·앱(WebView) 전환 대비 상태

사용자가 "나중에 구글/애플 앱으로 배포할 예정"이라 함. 이미 돼 있는 것:
`app/manifest.ts`(웹 앱 매니페스트) · `viewportFit: cover` + safe-area 패딩(탭바) ·
JWT 세션(웹뷰 쿠키 이슈에 강함) · 카카오 로그인 하이브리드 대비 코드(4차에서 보류
결정된 항목 중 일부는 이미 반영) · 이번 역할 전환도 GPU 합성 transform만 사용.
**아직 안 된 것**: 푸시 알림(FCM), 앱 스킴/딥링크, 스토어 등록 자산(아이콘·스크린샷).
하이브리드 앱 전환 자체는 4차에서 "트래픽·전환 확정 전 보류"로 결정된 상태 유지.

### 스폰서 지역 키 규칙 (어긋나면 노출·상한이 깨진다)

`SponsorPlacement.regionKey`는 **`lib/regionSeo.ts`의 `canonicalRegionKey(주소)`가 만드는
"시도slug 시군구"** (예: `경남 김해시`) 하나만 쓴다. 저장(관리자 승인)·조회(지역 페이지)·
슬롯 집계(AdminClient)가 전부 이 함수를 공유한다. 주소 표기("경상남도")를 그대로 쓰면
같은 지역이 두 키로 갈라진다 — 처음에 실제로 그렇게 어긋날 뻔했다.

### 콘솔 데이터 격리

시설 작성분은 `FacilityContent` 별도 테이블이다. `Facility.extra`에 넣지 않은 건 의도 —
extra는 공단 일일 수집이 소유한 영역이라 재적재 때 시설이 쓴 내용이 덮일 수 있다.

### 부가 조회의 방어

스폰서·FacilityContent 조회는 실패해도(테이블 미생성 등) **빈 값으로 조용히 떨어진다** —
부가 기능이 시설 상세 2만 8천 장·지역 페이지를 500으로 만들면 안 되기 때문.
단 `/admin`은 방어하지 않았다 — db push 전에 열면 500이 나는 게 정상이고, 그게 신호다.

---

## 3. 이 세션에서 새로 배운 함정

1. **루트 app/loading.tsx는 전 페이지를 Suspense로 감싼다** — 서버 HTML이
   "헤더→스피너→푸터"가 되고 본문은 `<div hidden>`으로 밀린다. JS 없는 크롤러(AI 봇)에겐
   본문이 안 보인다. 로더는 세그먼트별로만 둘 것
2. **`dynamicParams = false` + 한글 slug = 전부 404** (Next 14.2 실측).
   한글 URL 동적 라우트는 resolve→notFound() 방식으로 막을 것
3. **`.next`를 dev 서버와 `next build`가 동시에 쓰면 둘 다 깨진다** — 빌드 전 dev 중지
4. **Prisma 새 모델 추가 후 `npx prisma generate` 전까지 tsc가 전부 빨갛다** — 스키마
   수정→generate→typecheck 순서
5. **PowerShell 5.1에서 `$hash + @{같은키=…}`는 예외** — 덮어쓰기가 아니라 중복 키 에러.
   테스트 페이로드 변형은 Clone 후 인덱서 대입으로
6. **PowerShell here-string(`@'…'@`)에 큰따옴표(`"`)가 들어가면 native exe 인자가 쪼개진다**
   — `git commit -m @'... "가장 눈에 띄는" ...'@`가 pathspec 에러로 실패했다. Win32
   argv 파서가 문자열 중간의 `"`를 인자 구분자로 다시 해석하는 탓. 커밋 메시지에
   큰따옴표를 넣지 말 것(작은따옴표나 「」로 대체)
7. **tsx/ts-node로 `.ts` 스크립트를 실행할 때 정적 import는 dotenv.config()보다
   먼저 평가된다** — ESM은 모듈 그래프를 바닥부터 평가하므로, `import {config} from
   "dotenv"; config(...); import {x} from "../lib/y"` 순서로 써도 lib/y가 끌고 오는
   lib/prisma.ts의 `new PrismaPg(...)`가 먼저 실행돼 DATABASE_URL이 빈 채로 어댑터가
   만들어진다(ECONNREFUSED로 나타남). config() 호출 뒤 `await import(...)`로 동적
   임포트해야 순서가 보장된다. .mjs를 plain node로 돌릴 때는 이 문제가 없다
   (그래서 이전 세션의 .mjs 검증 스크립트들은 멀쩡했다)

---

## 4. 문서 지도 (이 세션 이후)

| 문서 | 내용 |
|---|---|
| [docs/competitor-caredoc-b2b.md](docs/competitor-caredoc-b2b.md) | ★ 신규 — 케어닥 B2B 분석·전략 비교·로드맵 |
| docs/ai-score-spec.md · work-index-spec.md · care-agreement-spec.md · program-taxonomy-spec.md | 기존 유지 (이 세션 변경 없음) |

이 세션에서 안심지수 가중치·합의서 조항·근무환경 지수는 **건드리지 않았다.**

## 5. DB 현황 변화

기존 수치는 6차 §6과 동일. 신설 테이블 4개는 **아직 push 전**이라 프로덕션 DB에 없다(§0-1).
