// 홈 "추천 시설" 섹션 맨 앞에 고정 노출할 시설 id를 순서대로 적어두는 곳.
// 비어 있으면 등급 기준 추천으로만 채워진다.
//
// ⚠️ 여기에 유료 시설을 넣으려면 **반드시 '광고' 표시와 함께** 일반 목록과 구분해서
// 그려야 한다. /business·소개 페이지·시설 전달 메일이 "광고는 분리된 자리에 광고로
// 표시한다"고 안내하고 있어서, 표시 없이 섞으면 그 세 곳이 한꺼번에 거짓이 된다.
// 유료 노출은 이 배열이 아니라 SponsorPlacement(components/SponsorSlots.tsx)로 구현돼 있다.
export const PROMOTED_FACILITY_IDS: string[] = [];
