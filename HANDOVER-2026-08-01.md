# 돌보다(Dolboda) 인수인계 문서 — 3차 세션

> 최종 갱신: **2026-08-01** · 이 문서만 읽고 바로 이어서 작업할 수 있게 작성했습니다.
> **새 세션은 반드시 `C:\Users\linea\요양` 폴더에서 열어주세요.**
> 이전 문서([HANDOVER.md](HANDOVER.md))는 2차 세션까지의 내용이며, 이 문서가 최신입니다.

- 배포: https://dolboda-rho.vercel.app
- 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (브랜치 `main`)
- 이 세션 마지막 커밋: **`01d606d`** (2026-08-01 19:21)

---

## 1. 개발할 때 절대 잊으면 안 되는 것 ⚠️

### 1-1. 코드 최적화는 "만들 때부터"

"일단 되게 만들고 나중에 최적화"는 하지 않습니다. 이번 세션에서 실제로 겪은 사례:

| 문제 | 원인 | 결과 |
|---|---|---|
| 홈 0.78초 | 전체 스캔 3번을 방문마다 실행 | 한 쿼리로 합치고 하루 캐시 → **0.048초** |
| 지역 페이지 1.84초 | 시/도 수천 행을 읽어 JS에서 셈 | 이미 캐시된 집계에서 파생 → **0.15초** |
| 상세페이지 낭비 | 24행을 조회·전송하고 `.length`만 사용 | 서버 렌더링에 실제로 활용 |

**반드시 지킬 것**
- 새 쿼리를 짤 때 "이게 전체 스캔인가?" 먼저 확인
- 집계는 SQL에서 끝낸다 (행을 다 읽어와 JS에서 세지 않는다)
- 자주 안 바뀌는 데이터는 `unstable_cache`로 하루 캐시
- 목록 API는 `view=card` 슬림 응답 사용 (전체 응답은 2배 이상 무겁다)
- 큰 목록은 점진 렌더링 (검색 결과 300장 → 30장씩)

### 1-2. 모바일 사용자 최우선

**주 사용자는 40~60대 보호자이고, 거의 모바일로 봅니다.** 사용자 본인도 모바일로 확인합니다.

- **모든 작업은 375px에서 실제로 측정해서 검증합니다.** 눈으로 짐작 금지
  - 가로 스크롤: `document.documentElement.scrollWidth > window.innerWidth`가 `false`여야 함
  - 터치 타겟 44px 이상
  - 하단 탭바(64px)에 가려지는 요소 없는지 `getBoundingClientRect()`로 확인
- 가로 스크롤 메뉴는 **위험합니다.** 이번에 매니저 메뉴가 975px인데 화면이 338px이라
  6개 중 2개만 보이고 나머지는 존재조차 몰랐습니다 → 2열 그리드로 교체
- 카드 캐러셀은 78vw로 다음 카드가 살짝 보이게 (더 있다는 신호)
- 아이콘만 두면 누를 수 있는 줄 모릅니다 → **글자를 함께** ("펼치기"/"접기")

### 1-3. 가독성 — 글자 크기와 들여쓰기

**40~60대에게 작은 글씨는 치명적입니다.** 화면마다 클래스를 고치면 레이아웃이 깨지므로
`app/globals.css`에서 크기 정의를 한 번에 올려두었습니다.

```css
.text-[10px] → 13px      .text-xs → 0.87rem (≈16.1px)
.text-[11px] → 14.5px    .text-sm → 0.96rem (≈17.8px)
.text-[12px] → 15px      /* 줄간격 1.5~1.7도 함께 */
.text-[13px] → 15.5px
.text-[14px] → 16px
.text-[15px] → 17px
```

**문장이 길어지면 한 줄에 붙이지 말고 아래로 들여씁니다.**

```
❌ 돌보다가 자체 설계한 6개 영역 지표 · 공공데이터 100% 확보 기준
   (좁은 화면에서 어중간하게 접힘)

✅ 돌보다가 자체 설계한 6개 영역 지표
     └ 공공데이터 100% 확보 기준
   (둘째 줄은 들여쓰고 색을 한 단계 옅게 → 부연임이 드러남)
```

### 1-4. 개발 환경 함정 (이번 세션에서 반복해서 당함)

- **dev 서버가 켜진 상태에서 `npm run build`를 하면 안 됩니다.**
  프로덕션 빌드가 `.next`를 덮어써서 dev 서버가 `Cannot find module './vendor-chunks/@prisma.js'`
  로 죽고, 페이지가 3KB 빈 HTML로 나옵니다.
  → 빌드가 필요하면 **dev 서버를 먼저 끄고**, 빌드 후 `rm -rf .next` 하고 재시작
- HMR이 자주 꼬입니다. 화면이 이상하면 코드를 의심하기 전에 **dev 서버 재시작**부터
- 브라우저 검증 시 뷰포트를 375px로 바꿨으면 **끝나고 반드시 desktop으로 되돌릴 것**
  (사용자 화면이 잘려 보입니다)
- `npx next lint`는 ESLint 미설정이라 대화형 프롬프트가 뜹니다. 설정 추가는 사용자 확인 필요

### 1-5. 사용자 커뮤니케이션

- 사용자는 **짧게 여러 건씩** 요청하고, 작업 중에도 새 요청이 끼어듭니다.
  하던 것부터 끝내고 순서대로 처리하세요.
- **"커밋 배포 ㄱㄱ"** 요청이 잦습니다. 검증 → 커밋 → push까지 한 번에.
- **같은 요청을 반복하면 배포가 안 된 것입니다.** 이번에 탭 순서 변경을 3번 요청받았는데,
  코드는 고쳤지만 배포를 안 해서 사용자 화면에 반영되지 않았던 것이 원인이었습니다.
  → 로컬 확인만 하고 끝내지 말고 **배포까지** 하거나, 배포 전임을 명확히 알릴 것

---

## 2. 이번 세션에서 완료한 것 (결과물 전부)

커밋 19개, 2026-08-01 06:38 ~ 19:21.

### 2-1. 업체(기관) 마스터 DB 구축 — `955d78f`

`C:\Users\linea\Desktop\요양기관 상세정보\중요)전국장기요양기관 상세정보_업체정보.xlsx`
(11개 시트)를 별도 테이블로 저장하고 기존 시설에 병합했습니다.

- **`Institution` 테이블 43,943행** (기관번호 PK) — `prisma/schema.prisma`, `scripts/import-institutions.mjs`
- 시설 매핑 결과 (2026-08-01 실측)

| 항목 | 보유 시설 수 |
|---|---|
| 프로그램 운영 | 27,557곳 |
| 인력현황(요양보호사 수) | 26,717곳 ← **"인력 전부 0" 문제 해결** |
| 근속현황 | 26,717곳 |
| 기관정보(홈페이지·교통편·운영시간·주차·배상보험) | 26,717곳 |
| 비급여비용 | 28,844곳 |
| 치매전담실 | 372곳 |

### 2-2. SEO 대개편 — `955d78f` `e6ac1a5` `a2abd53` `75fa03f` `55cb735`

외부 SEO 오딧 지적사항을 전부 반영했습니다.

- **시설 상세 서버 렌더링(SSR)** — 예전엔 클라이언트 fetch라 28,000여 페이지가 빈 HTML로
  색인될 뻔했습니다. `page.tsx`(서버) + `FacilityDetailClient.tsx`(클라이언트)로 분리
- **홈 실데이터 전환** — 크롤러가 "등록된 시설 0곳"을 보던 문제. 서버에서 집계해 전달
- **중복 시설 16쌍 정리**, mock 데이터 색인 제외
- **`/search` noindex** (필터 조합이 무한이라 색인하면 크롤 예산 낭비)
- **지역 페이지 1,100여 개 사전 생성(SSG)** + 지역별 고유 통계·FAQ 차별화 + 페이지네이션
  (김해시 방문요양 180곳이 24곳만 보이고 끊기던 문제 → `?page=N` 실링크)
- **요양 가이드 10편** `/guide` — 카테고리별 SVG 일러스트 포함
- **서비스별 독립 랜딩 4개** `/services/hospital|home|housekeeping|family`
- **사이트맵 분리** — 인덱스 + `static`/`guides`/`regions`/`facilities-1~3`
  (Next 14의 `generateSitemaps`는 인덱스를 안 만들어줘서 라우트 핸들러로 직접 구성)
- **신뢰 페이지 신설** — `/about`(운영 주체·원칙), `/data-policy`(데이터 출처·안심지수 설명)

### 2-3. 돌보다 AI기반 안심지수 고도화 — `f21bfa4` `1f091fe` 외

- 평가 세부점수 **17,639건** 반영 (레이더 차트·평균 대비 막대 활성화)
- **지역 평균 대비 비교** 신설 — `/api/facilities/score-context`
- **가중치를 숫자 대신 "반영 비중"으로 표기** (핵심/높게/보조) — 아래 3-3 참고
- 기술서: [docs/ai-score-spec.md](docs/ai-score-spec.md)

### 2-4. 근무환경 지수 — 매니저용 신규 자산 — `f21bfa4`

**같은 데이터를 매니저 관점으로 뒤집은 것이 핵심입니다.**
보호자에게 "어느 시설이 모시기 좋은가"(안심지수)를 답하는 데이터로,
매니저에게 "어느 시설이 일하기 좋은가"를 답합니다.

- 5영역: 업무 강도 35 / 근속 안정성 30 / 의료 지원 15 / 팀 규모 10 / 기관 신뢰 10
- **구간 기준은 실제 분포의 사분위수** — 어르신/요양보호사 중앙값 3.4명(법정기준 2.3명),
  1년이상 근속률 중앙값 60%
- 커버리지 **11,470/11,696곳(98%)**
- 화면: `/mypage/sitter/workplaces`, API: `/api/sitter/workplaces`
- 기술서: [docs/work-index-spec.md](docs/work-index-spec.md)

### 2-5. 프로그램 정밀 검색 — 미활용 12.3만 행 활용 — `d14c5e3`

프로그램명이 자유 텍스트라 **고유 명칭이 52,589개**였습니다
(인지프로그램·인지활동·인지기능프로그램·힘뇌체조가 전부 같은 것).

- 키워드 규칙으로 **12개 태그** 정규화, 커버리지 **94.3%**, 주기 파싱 95.4%
- **pgvector 대신 규칙 기반을 택한 이유**: 비용 0원, "왜 이 태그가 붙었는지" 설명 가능,
  키워드 추가로 즉시 개선. 94.3%면 충분한데 남은 5.7% 때문에 비용·불투명성을 감수할 이유 없음
- 시설별 태그를 `extra.programTags`에 미리 계산해 저장 (**12,119곳**)
- **필터는 서버에서** — 클라이언트에서 걸면 받아온 300건 안에서만 찾게 돼
  원예(312곳) 같은 희귀 태그는 결과가 비어버립니다
- 기술서: [docs/program-taxonomy-spec.md](docs/program-taxonomy-spec.md)

### 2-6. 유사 시설 추천 엔진 — `d971f9b` `c99d0a7`

**보호자가 대안을 찾을 땐 항상 이유가 있다**는 통찰에서 출발했습니다.
"자리가 없네", "너무 머네", "비싸네" → 단순 목록이 아니라 **의도별 탭 + 현재 시설 대비 차이**.

- 5가지 의도 탭 (화면 순서): **비슷한 곳 → 안심지수 높은 곳 → 자리 있는 곳 → 비용 낮은 곳 → 더 가까운 곳**
- 델타 배지: `빈자리 5자리` `월 6만원 낮음` `안심지수 +7`
- **거리 가드** — 제주 시설에 순천(185km)을 추천하던 문제. 30km 우선, 3곳 미만이면 60→150km
- 2단계 검색(최근접 400곳 후보 → 정밀 채점)으로 0.2~0.5초
- **첫 화면분 12개는 서버 렌더링** — 클라이언트 fetch만 하면 인접 시설 링크가 초기 HTML에
  없어 크롤러가 타고 갈 수 없습니다 (이 SEO 후퇴를 `c99d0a7`에서 복구)

### 2-7. 로그인 이후 영역 — `7a96f11` `01d606d`

- **마이페이지 대시보드** — 보호자는 진행 중 요청 상태, 매니저는 프로필 완성도·레벨·새 일자리
- **프로필 완성도 게이지** (사진·자기소개·경력·자격증·활동지역·정산계좌 6항목)
- **매니저 레벨** 🌱새싹 → 🌿성장 → 🌟프로 → 👑베테랑
- **매니저 가이드** `/mypage/sitter/tips` 5편 (자기소개 작성법·지원 타이밍·첫날 체크리스트·
  치매 돌봄·정산과 세금)
- **매니저 메뉴 2열 그리드** — 가로 스크롤이라 6개 중 2개만 보이던 문제 해결

### 2-8. 데이터 신뢰성 — `55cb735`

- **비급여 이상값 처리** (`lib/feeQuality.ts`) — "1일 1원" 같은 값은 숫자 대신
  **"시설 문의 필요"**로 표시. 원본은 고치지 않고 이상하다는 사실만 알림
- **평가등급 표기 통일** — 공단 시설은 배지·척도 모두 A~E (위는 A등급, 아래는 1등급이던 혼재 해소)
- 등급 색 통일 — **1등급 코랄 · 2등급 골드 · 3등급 민트**, 숫자와 막대가 항상 같은 색

### 2-9. 버그 수정

| 버그 | 내용 |
|---|---|
| 비급여 중복 key | 같은 항목명(상급침실사용료) 중복 시 React key 충돌 → 인덱스 조합 key |
| 검색 하이드레이션 불일치 | 렌더링 중 `sessionStorage`를 읽어 서버/클라이언트 결과가 달라짐 → effect로 이동 |
| 죽은 코드 | `relatedPool`(24행 조회 후 `.length`만 사용), `relatedFacilities`(거리 계산 후 버림) 등 제거 |
| SEO 후퇴 | 유사 시설 클라이언트 fetch 전환으로 초기 HTML 링크 0개 → 서버 렌더링 복구로 12개 |

### 2-10. 법적 문서 강화 — `bdd5cc5` `c99d0a7`

- **개인정보처리방침 제10조 신설** — 무단 수집·크롤링 금지 및 법적 조치
- **이용약관 제9조** 금지행위 구체화 (크롤러·봇·헤드리스 브라우저, 기술적 보호조치 우회,
  취득 정보로 유사 서비스 개발·판매, 직거래 유도)
- **이용약관 제10조** 지식재산권 — 자체 지표까지 권리 범위 포함, 저작권법상 데이터베이스제작자
  보호 명시, 5개 법령 법정형 고지
- **이용약관 제12조·제13조 신설** — 돌봄 매칭에서 회사의 지위(플랫폼일 뿐, 고용관계 없음),
  손해배상(변호사 비용 포함 전액)
- **매니저약관 제5조·제6조** — 경력 진실성, 개인정보 비밀유지(활동 종료 후에도), 학대 금지,
  세금 자기책임 / 회사는 사용자(고용주) 아님

> ⚠️ 법령의 법정형만 실제 조문에서 인용했으나 **법률 자문은 아닙니다.**
> 실제 분쟁 대응을 염두에 둔다면 변호사 검토를 권합니다.

---

## 3. 확정된 결정사항과 규칙

### 3-1. 절대 하지 않기로 한 것

| 금지 | 이유 |
|---|---|
| 주민등록번호 수집 | 원천징수·배상보험 인프라가 없어 개인정보보호법 위반 소지 |
| "계약서" 표현 | 법적 구속력이 있는 것처럼 보이면 안 됨 → **"돌봄 확인서"**, "법적 계약서 아님" 명시 |
| 우리가 계산한 가격 표시 | 결제 인프라 없음. 보호자가 직접 입력한 희망 사례비 전달은 OK |
| 매년 바뀌는 금액 안내 | 고시로 변경됨. 구조만 설명하고 금액은 공식 홈페이지로 |
| 없는 데이터 지어내기 | "아직 공개되지 않았어요"로 표시. **가짜 점수·추정치 생성 금지** |
| 케어닥 워딩·구성 베끼기 | 사용자가 여러 번 명시적으로 요구 |
| 푸터 SEO 링크 완전히 숨기기 | 검색엔진 페널티. 10px/투명도 50%가 하한선 |
| 지수 가중치를 화면에 숫자로 공개 | 아래 3-3 참고 |

### 3-2. 톤앤매너

- **존댓말 + "~해요체".** 어르신·보호자가 읽기 쉽게
- 브랜드 색: `primary`(코랄 #FF6250), `royal`(보라 #7C42BD), `ivory`(배경 #FFFBF3),
  `mint`, `peach`, `accent`(골드), `ink`(텍스트)
- 헤더 워드마크 "돌보다" — 로고 옆, 코랄 단색, `text-base`(모바일 18.5px)
- 파괴적 액션은 숨긴다 (회원탈퇴는 화면 한참 아래 10px 흐린 글씨, 확인 단계에서 큰 버튼 = 취소)
- 로딩 중 빈 화면 금지 — `PageLoader` 사용

### 3-3. 지수 공개 수위 — 중요

**가중치를 숫자로 노출하지 않고 "반영 비중"(핵심/높게/보조)으로 표기합니다.**

이유: 퍼센트를 나열하면 우리가 설계한 지표가 아니라 계산표처럼 보이고,
반대로 아예 가리면 "뭘 숨기나" 싶어 신뢰를 잃습니다. **블러 처리는 택하지 않았습니다** —
요양 정보 서비스에서 평가 방식을 가리면 프리미엄이 아니라 불신 신호가 되고,
브랜드 슬로건("정보 비대칭 없는 투명한 돌봄")과 모순되기 때문입니다.

- 화면: "돌보다가 자체 설계한 6개 영역 지표", 영역별 `핵심 반영`/`높게 반영`/`보조 반영`
- `/data-policy`: "세부 배점과 계산식은 자체 설계한 기술이라 비중 수준만 공개.
  다만 어떤 데이터를 쓰는지·없을 때 어떻게 처리하는지는 모두 공개"
- **정확한 수치는 `docs/*.md` 내부 기술서에만** 유지
- 참고: GitHub 저장소가 공개라 코드에는 수치가 있습니다. 이 조치는 복제 차단이 아니라
  **브랜드 포지셔닝**입니다

### 3-4. 데이터 출처 표기

기관명 수준으로만 밝힙니다 (데이터셋 이름 나열 금지).
→ 국민건강보험공단 / 건강보험심사평가원 / 보건복지부 고시

### 3-5. 핵심 자산 관리 규칙 (CLAUDE.md에 명시됨)

- **`lib/dolbodaScore.ts`(안심지수)** 수정 시 [docs/ai-score-spec.md](docs/ai-score-spec.md)
  매핑 표·변경 이력 동시 갱신. **가중치는 사용자 확인 없이 변경 금지**
- **`lib/workIndex.ts`(근무환경 지수)** 수정 시 [docs/work-index-spec.md](docs/work-index-spec.md)
  동시 갱신. 구간 기준은 실제 분포 사분위수라 임의 변경 금지
- **기존 기능 제거는 반드시 확인받고 진행** (이 규칙이 다른 모든 판단보다 우선)

---

## 4. 아직 안 끝난 작업과 다음 할 일

### 4-1. 진행 중이던 것 (사용자가 중지시킴)

**앱(안드로이드·iOS) 배포 준비** — 사용자가 "나중에 안드로이드 애플에 다 배포할 것"이라고
했고, 기반 작업을 시작했다가 중지했습니다.

- ✅ **앱 아이콘 3종 생성 완료** (커밋 안 됨, 워킹트리에 있음)
  - `public/icon-192.png` (7.4KB), `public/icon-512.png` (36.3KB),
    `public/apple-touch-icon.png` (6.6KB)
  - 로고에 브랜드 배경(#FFFBF3)을 깔고 14% 여백을 준 형태 (투명 배경은 OS마다 다르게 보임)
- ❌ **아직 안 한 것**
  - `public/manifest.json` — 앱 이름·아이콘·`display: standalone`·theme_color
  - `app/layout.tsx`에 `viewport`(노치 대응 `viewport-fit=cover`)·`themeColor`·`appleWebApp`
  - `globals.css`에 앱스러운 CSS — `overscroll-behavior`(당길 때 튕김 방지),
    터치 하이라이트 제거, safe-area 대응
  - WebView 래핑 방식 결정 (Capacitor / React Native WebView / PWA(TWA))

### 4-2. 무거운 파일 (리팩터링 후보)

| 파일 | 줄 수 |
|---|---|
| `app/mypage/sitter/jobs/page.tsx` | 600 |
| `app/mypage/sitter/register/page.tsx` | 532 |
| `app/mypage/sitter/profile/page.tsx` | 446 |
| `app/facility/[id]/FacilityDetailClient.tsx` | 1,100여 |

### 4-3. 사용자 결정이 필요한 것

1. **정식 도메인** (dolboda.kr 등) — 정하면 `lib/siteConfig.ts`의 `SITE_URL` 한 곳만 바꾸면
   canonical·sitemap·OG가 전부 반영됩니다. Vercel 주소 → 새 도메인 301 리디렉션도 함께 설정
2. **GA4 또는 네이버 애널리틱스** — 계정 생성이 필요합니다
3. **서치콘솔·네이버 재제출** — `/sitemap.xml` 주소 그대로 제출하면 인덱스가 하위 6개를 안내

### 4-4. 데이터 대기 중 (파일 받으면 즉시 반영 가능)

- **행정처분(위반사실 공표)** — UI(경고 배너·카드 배지·안심지수 감점)는 완성돼 있고
  데이터가 없으면 아무것도 안 뜨는 구조. 파일을 `C:\Users\linea\Desktop\장기요양 행정처분`에
  넣고 `node scripts/import-admin-actions.mjs --write`

### 4-5. 매니저 리텐션 — 다음 후보 (사용자와 논의했으나 미착수)

사용자가 "일자리가 없어도 매니저가 들어올 콘텐츠"를 요구했고, 근무환경 지수를 만들었습니다.
다음 후보로 논의된 것들:

1. **채용 수요 신호** (추천) — 법정 배치기준(요양원 1:2.3)과 실제 인력을 비교해
   "지금 사람이 부족한 곳"을 산출. 근속 데이터의 1년 미만 비율(이직 잦음)과 결합.
   ⚠️ "법 위반"이라 단정하면 명예훼손 소지 → "인력이 여유롭지 않아 보여요" 수준으로
2. **통근 분석** — 기관정보의 교통편 텍스트("지하철 3호선 경복궁역에서 7212번 버스")가
   전혀 활용되지 않고 있습니다. 좌표 27,697곳과 결합 가능
3. **보수교육·자격 관리** — 요양보호사는 매년 8시간 법정 의무. 알림 인프라 선행 필요
4. **지역 인력 시장 리포트** — 시군구별 직종 인원·이직률

### 4-6. 시간이 해자를 만드는 것 (지금 시작 안 하면 못 만듦)

**시설 변화 추적(시계열 스냅샷)** — `pg_cron`이 Supabase에서 사용 가능합니다.
매일 정원·현원·대기·등급을 스냅샷으로 쌓으면 6개월 뒤 "이 시설은 보통 3주면 자리가 난다" 같은
입소 가능성 예측이 열립니다. **과거는 소급해서 만들 수 없으므로 시작이 빠를수록 유리합니다.**

---

## 5. 파일 구조 (이번 세션에 추가·변경된 것 위주)

```
CLAUDE.md                          ★ 최우선 작업 규칙 (안심지수·근무환경 지수 관리 규칙 포함)
HANDOVER.md                        2차 세션까지의 문서
HANDOVER-2026-08-01.md             ★ 이 문서 (최신)

docs/
  ai-score-spec.md                 ★ 안심지수 기술서 (매핑 표·변경 이력)
  work-index-spec.md               ★ 근무환경 지수 기술서
  program-taxonomy-spec.md         ★ 프로그램 분류 체계 기술서

lib/
  dolbodaScore.ts                  ★ 안심지수 (가중치 변경 금지)
  workIndex.ts                     ★ 근무환경 지수 (매니저용)
  programTaxonomy.ts               ★ 프로그램 12개 태그 분류
  similarity.ts                    ★ 유사 시설 추천 (의도별 랭킹·거리 가드)
  feeQuality.ts                    비급여 이상값 검증
  facilityStats.ts                 홈·API 공유 집계 캐시
  sitemapData.ts                   사이트맵 분리 데이터
  sitterProgress.ts                매니저 프로필 완성도·레벨
  managerTips.ts                   매니저 가이드 5편
  serviceLanding.ts                서비스별 랜딩 콘텐츠
  guides.ts                        요양 가이드 10편
  regionData.ts                    지역 집계 (캐시 활용)

app/
  page.tsx                         홈 (서버 집계 → HomeClient)
  facility/[id]/page.tsx           ★ 서버: 시설 + 유사시설 첫 화면분 계산
  facility/[id]/FacilityDetailClient.tsx   클라이언트 렌더링
  search/page.tsx + SearchPageClient.tsx   검색 (noindex 메타 + 클라이언트)
  guide/ + guide/[slug]/           요양 가이드
  services/[slug]/                 서비스별 랜딩 4개
  about/ data-policy/              신뢰 페이지
  sitemap.xml/route.ts             사이트맵 인덱스
  sitemaps/[slug]/route.ts         하위 사이트맵
  mypage/sitter/workplaces/        ★ 일하기 좋은 시설
  mypage/sitter/tips/              ★ 매니저 가이드
  api/facilities/similar/          유사 시설
  api/facilities/score-context/    지역 평균·인근 병원
  api/sitter/workplaces/           매니저 근무지 탐색

components/
  MyPageShell.tsx                  ★ 매니저 메뉴 (모바일 2열 그리드 / 데스크톱 사이드바)
  MyPageDashboard.tsx              ★ 마이페이지 대시보드
  SimilarFacilities.tsx            ★ 유사 시설 캐러셀 (의도 탭)
  DolbodaScoreCard.tsx             안심지수 카드
  GuideIllustration.tsx            가이드 SVG 일러스트
  DetailVisuals.tsx                등급 척도·레이더·비교 막대

scripts/
  import-institutions.mjs          업체 마스터 (실행 완료)
  build-program-tags.mts           프로그램 태그 생성 (실행 완료, 12,119곳)
  check-program-tags.mts           분류 커버리지 점검
  import-admin-actions.mjs         행정처분 (파일 대기 중)
  import-evaluations.mjs           평가 세부점수 (실행 완료, 17,639건)
```

---

## 6. DB 현황 (2026-08-01 실측)

| 항목 | 수치 |
|---|---|
| Facility 전체 | 28,844 |
| Institution (업체 마스터) | 43,943 |
| 평가 세부점수 보유 | 17,639 |
| 근속현황 보유 | 26,717 |
| 프로그램 태그 보유 | 12,119 |

---

## 7. 검증 절차 (커밋 전 체크리스트)

1. `npx tsc --noEmit` 통과
2. **dev 서버를 끄고** `npm run build` — 끝나면 `rm -rf .next` 후 dev 재시작
3. 브라우저 375px에서 실제 측정 (짐작 금지)
   - 가로 스크롤 없음, 44px 미만 터치 타겟 없음, 하단 탭바에 가려지는 요소 없음
4. 콘솔 경고 확인 (`did not match`, `same key` 등 하이드레이션·key 문제)
5. 세션 기능은 테스트 세션으로 검증 후 **테스트 데이터 삭제**

```js
// 테스트 세션 만들기 (scripts/에 임시 파일로)
await prisma.user.upsert({ where: { id: "test-user-x" }, create: { id: "test-user-x", name: "장테스트", email: "x@example.com" }, update: {} });
await prisma.session.upsert({ where: { id: "test-session-x" }, create: { id: "test-session-x", sessionToken: "testtoken-x", userId: "test-user-x", expires: new Date("2027-01-01") }, update: {} });
// 매니저 기능을 보려면 sitterProfile도 upsert (nickname/nationality/regions/agreedAt 필수)

// 브라우저에서
document.cookie = "authjs.session-token=testtoken-x; path=/; max-age=86400";

// 끝나면 반드시 삭제 (cascade)
await prisma.user.delete({ where: { id: "test-user-x" } });
```

6. 커밋 메시지는 한국어로 **무엇을·왜** 바꿨는지. 끝에
   `Co-Authored-By: Claude <noreply@anthropic.com>`
