# 돌보다 인수인계 — 15차 세션 (2026-08-05 저녁 ~ 08-06 새벽)

이 문서만 읽고 이어서 작업할 수 있게 썼습니다. 14차 직후 이어진 **33커밋 대형 세션**
(`80b86f9` → `34972ef`, 전부 푸시·Vercel 배포 라인). 몸통은 여섯입니다:
① 14차 감사에서 나온 오류 정정 + 상태 지도 스펙 승격 ② 고도화 로드맵 3종 + 리텐션
(최근 본 시설·지역 실사진) ③ **돌봄일지 2·3차 + 가족 공유 + 정책 확정(보관 3년·독려 선)**
④ **돌봄 포인트 전체(1·2단계+별 상단 노출)** ⑤ **시설찾기 지도 앱형 재구성 + 모바일
메뉴 개편 + 탭바 5탭** ⑥ 푸터 노출 계열 버그 4건 근절 + **전남광주 행정통합 대응**.

이전: [14차](HANDOVER-2026-08-05-14차.md). 5차 §1·§3, 10차(알림), 12차(B2B), 13차(사진
파이프라인) 여전히 유효. **5차 §7 상태 지도는 [docs/care-flow-spec.md](docs/care-flow-spec.md)로
승격됐고(이 세션), 지원자 정렬이 "별 우선"으로 바뀌었으니 반드시 승격본을 볼 것.**

---

## ⚡ 다음 세션에서 바로 시작할 일

1. **Resend 첫 배치 확인(8/6 아침)** — 사용자가 GitHub 시크릿 `RESEND_API_KEY`·`MAIL_FROM`을
   등록 완료(8/5 밤, 스크린샷 확인). **09:00 배치가 첫 정상 가동**이다.
   resend.com/emails 로그 + 충북 공고(8/3 등록·지원 0건)에 "지원자 없는 요청 넛지"
   실발송 여부. **예약 리마인드(resend-first-batch-check, 09:30)가 자동으로 안내한다.**
   비어 있으면 Actions 탭에서 send-* 워크플로 5개 로그 확인.
2. **폰 검증 대기 목록(누적)** — 두 계정에 테스트 포인트 각 10,000P 지급돼 있어
   바로 가능(§6 참조): ⭐별 구매→지원자 카드 배지+**상단 노출**, ⚡우선 요청→일자리
   목록 상단, 지도 모드(시트 3단 물리·클러스터·이 지역 재검색 — 카카오라 프로덕션
   전용), 카드→상세 공유 요소 모핑, 모바일 메뉴 시트(☰ 메뉴), 탭바 5탭, 일지
   신기능(조건 카드·14일 스트립·내역서 인쇄·가족 공유 초대→수락), 가이드 진행 바.
3. **개발 다음 순위(사용자 합의)**: ① **B2B 판매 전 필수 실태 대조**(시설 소식 발행·
   지역 배너·결제 자동화 중 12차 이후 남은 것 — 메모리 항목은 일부 낡음, 코드 대조
   먼저) ② GA4/네이버 애널리틱스(측정 0 — 계정 생성은 사용자와 함께) ③ 다음 지역
   사진 zip(오면 최우선, 13차 파이프라인) ④ **카카오톡 발송은 사용자가 "마지막에"로
   명시** — 다른 백로그 소진 전 착수 금지.

## 0. ★ 사용자가 직접 해야 할 일

- ✅ ~~GitHub 시크릿~~ — 완료(8/5). 8/6 발송 로그 확인만 남음(⚡1).
- 🟡 서치콘솔 canonical — 8/6 10:00 예약 리마인드 있음.
- 🟡 네이버 로그인 프로덕션(Vercel env+검수) · `ADMIN_USER_IDS` · R2 env 5종 — 이월.
- 🆕 **사용자가 시작해둔 백그라운드 작업 칩 세션**("새 일자리 알림 사각지대") — 같은
  수정을 이 세션이 이미 배포했으니(80b86f9) **그 세션은 닫고 결과물은 버릴 것**(병합 금지).

## 1. 이 세션에서 한 일 (주제별)

### 1-1. 14차 검증·정정 + 스펙 승격 (80b86f9 · 8d4b499)
- **14차 인수인계의 오류 실측 정정**: "일일 동기화 멈춤"은 틀림 — FacilitySnapshot에
  8/3~8/5 매일 적재 확인(DIRECT_URL은 8/2부터 등록돼 있었음). 진짜 빠진 건 RESEND 2종.
- **새 일자리 알림 사각지대**: 크론 09:00 + 당일 창이라 09시 이후 공고가 영영 누락 →
  기본 창을 어제~오늘 이틀로(중복은 발송 기록 유니크가 차단).
- **상태 전이 지도 승격**: 5차 §7 → [docs/care-flow-spec.md](docs/care-flow-spec.md)
  (API 4개 실측 기준 재작성, 역경매·취소 알림·advisory lock 반영). care-log-spec
  §9-3(확인서 stats=1 — 열람≠읽음) 신설, 스탬프 3종 정정.
- **Prisma 베이스라인**: `prisma/migrations/0_init` 생성 + 드리프트 0 확인 +
  `migrate resolve --applied` 실행(프로덕션 `_prisma_migrations` 생성). ⚠️ 이후
  스키마가 또 늘어(포인트·가족공유 등) **베이스라인이 낡음** — 전환 시
  [prisma/migrations/README.md](prisma/migrations/README.md)의 체크섬 절차 필수.

### 1-2. 고도화 로드맵 3종 + 리텐션 (9c8b6f7 · c36156c · 86ada99 · da283e1 · 272a92d)
- 매니저 등록 위저드 2단계에 성별·연령대(스텝 수 유지, POST 화이트리스트).
- 미선정·취소 메일에 "지금 지원할 수 있는 비슷한 공고" 최대 3건 동봉(lib/otherOpenJobs).
- **지원자 없는 요청 넛지** — 배치 알림 8종째(alert-system-spec §3-14, OpenRequestNudge
  요청당 평생 1회, --write인데 키 없으면 기록도 안 남기고 중단).
- 홈 "최근 본 시설" — 로컬 전용 기록(서버 저장 안 함 — 열람 이력은 민감 흔적),
  `ids=` 조회에 view=card 지원(카드 화면 페이로드 슬리밍).
- 지역 SEO 1,100여 페이지에 실사진 썸네일 + JSON-LD image.

### 1-3. 돌봄일지 2·3차 + 가족 공유 + 정책 (84aead5 · 4f31b67 · 03ad02d · 2547045)
- **반응의 실적화**: 🙏😊💪 누적이 지원자 카드·공개 프로필 숫자로, 매니저 지난 기록에
  반응 배지. 셈법은 사건 단위(guardianReaction != null 행 수).
- 보호자 뷰 **최근 14일 점 스트립**(§4-5 "빈 날은 빈 날로" 첫 구현, 시작 전 날짜 제외).
- **합의 조건 카드**(요금 분쟁 예방 — 양쪽이 같은 사례비·기간을 매일 봄, 확정 제안
  우선) + **기록 내역서 인쇄**(브라우저 인쇄→PDF, 인쇄 전용 헤더+비고용 고지문).
- **가족 공유(§9-5)**: 초대 링크+로그인 수락(비로그인 링크 금지 — 민감정보), 읽기
  전용(읽음 표시·반응 없음), 활성 5명·7일 만료·revokedAt 보존, 조건 카드에
  "가족 N명" 양쪽 표시. `/care-request/care-log/join?token=`.
- **정책 확정(사용자 위임)**: 보관 = 종료 후 3년 파기(시효 3종 정합, 실적 집계는
  보존) — 개인정보처리방침 8/5 개정. 독려 = **법적 거리 선 안에서만**(§10-5:
  지시·채근·패널티 금지, 허용은 반응 인용·혜택 가시화·부담 축소) — 체크인 메일을
  "보호자님이 소식을 기다리고 있어요"로 재작성+자율 고지.

### 1-4. ★ 돌봄 포인트 전체 (ab79b02 · 72bb285 · f4b3e53 · e4e5ca6 · 2596a29 등)
[docs/points-spec.md](docs/points-spec.md)가 단일 진실 공급원. 요체:
- **적립(감사 조건 — 완료 아님)**: 후기 도착 1,000P · 5점 +500P · 반응 50P/기록
  (요청당 10회) · 후기 작성 500P. 일지 작성 적립은 의도적 제외(형식적 기록 방지).
- **사용**: ⭐돌봄 스타 별 5,000P/7일 — 지원자 카드·공개 프로필 배지 +
  **지원자 목록 상단 우선 노출**(2026-08-06 사용자 확정 — 초기 "정렬 불변" 원칙 대체,
  그 안은 선착순). ⚡우선 요청 2,000P/7일 — 일자리 목록 상단 고정+배지.
- **추천인 코드**: 8자(혼동 문자 제외), ?ref= 링크를 ReferralCatcher(루트 레이아웃)가
  주워 로그인 시 귀속. **가입 즉시 양쪽 지급**(추천인 2,000P·신규 1,000P — "첫 완료
  대기"에서 사용자 결정으로 완화, 유령 계정 실익 없음 논거). 14일 귀속 창·1인 1회.
- **법적 장치**: 약관 제12조의2(현금 교환 불가·24개월 소멸·회수·비양도), 원장 합=잔액,
  사용은 advisory lock 트랜잭션. 금액 변경 전 points-spec §1 필독(CLAUDE.md 등재).
- 단위는 ×10 상향본이 최종(1P≈1원 멘탈모델 — 사용자 결정).

### 1-5. 시설찾기 지도 + 모바일 메뉴 + 탭바 (3c11920 · 8e5140a · 30ef528)
- **SearchMapView**: 전체 화면 지도 + 3단 스냅(22/52/90dvh) 물리 시트(13차 스프링
  상수 이식, 드래그 중 setState 0), 클러스터러+이미지 마커, 선택은 CustomOverlay 1개
  재사용(1회 팝, 펄스 금지), 이동 중 요청 0 → idle 300ms 후 "이 지역에서 다시 검색"
  (AbortController+영역 캐시 20), programMove 가드로 무한 루프 차단, 저채도는 컨테이너
  CSS 필터(카카오 웹 SDK엔 스타일 API 없음). dynamic import라 목록 방문엔 0바이트.
  시트 "상세"는 반드시 ViewGate 경유(8e5140a — 일반 <a>면 **비회원 열람 제한까지
  우회**된다).
- **모바일 메뉴**: 햄버거 아이콘 → **"☰ 메뉴" 라벨 필 버튼** + 우측 280ms 풀시트
  (4그룹: 시설 찾기/돌봄 준비/돌봄 매칭/내 계정 + 회사·약관 푸터 흡수). 50~70대
  타깃 사양(제목 lg/600·행 60px+·터치 48px+). **복귀 스위치**: Header.tsx
  `USE_SHEET_MENU=false` 한 줄 — 옛 드롭다운 코드 보존됨.
- **탭바**: 가이드 추가 → 5탭(홈·시설찾기·등급테스트·가이드·마이), 높이 72→59px.
- 등급테스트는 전 단계 탭바 숨김(경로 기반 — 14차의 승강 로직 45줄 제거, 사용자 지시).
- **공유 요소 전환**은 14차 인프라가 전 카드에 이미 배선돼 있었음(카드 사진→히어로,
  이름→제목, 스켈레톤 앵커·450ms 상한). 뒤로가기 역모핑은 Next 14 App Router가
  popstate를 감쌀 수 없어 불가(Next 15 실험 영역 — 업그레이드 때 열림).

### 1-6. 푸터 계열 버그 4건 + 광주 (524f6a1 · 1bfd143 · 7497674 · a5f64ce · 0470c22)
- 같은 뿌리("본문이 잠깐 짧아지는 순간") 4건: 검색 복귀 2회·지도 복귀·알림 설정 로딩.
  개별 수정(재주장 루프·목록 언마운트 금지·페인트 전 스페이서) 후 **전역 일반해**:
  루트 레이아웃이 본문을 `min-height:100dvh`로 감쌈(CLAUDE.md 등재). 짧은 페이지에서
  푸터가 폴드 아래로 가는 건 의도된 맞바꿈.
- **전남광주통합특별시**(2026 행정 통합, 실측 2,365곳 — 일일 동기화가 조용히 반영):
  "일하기 좋은 시설 광주 탭 0곳"의 원인. `lib/regions.ts`에 광주 5개 자치구
  분리 헬퍼(splitMergedJeonnamGwangju) 신설 — 일하기 좋은 시설(광주 269·전남 608
  실측 분리)·shortAddress·지역 알림 스크립트 3곳 수정. **지역 관련 코드를 새로 짤 땐
  반드시 이 헬퍼를 쓸 것.**
- 기타: 마이페이지 매니저 대표 카드 = 일자리 관리(한 차례 스왑 후 사용자 최종 확정),
  돌봄일지 관리는 얇은 줄 첫 번째. 강조 칩("돌보다 단독")은 텍스트 열 안 배치(절대
  배치 2회 실패). 얇은 줄 라벨 한 줄 고정(힌트가 말줄임). 공개 프로필 안내에 "보호자
  제안 가능" 문구. 가이드 가독성(진행 바·목차·읽는 시간 — JS 0). 소개서 PDF 3판
  (스크래치, 로고 임베드·실사진 전국 가정본).

## 2. 사용자 방침 (이 세션 신규 — 영구)
1. **매니저와 법적 거리 최대 확보**(메모리 manager-legal-distance 등재) — 지시·채근·
   평가·패널티 금지, 증빙류에 비고용 고지. 매니저 대상 문구는 전부 이 기준 검수.
2. **별의 상단 노출은 허용**(2026-08-06) — 단 신뢰 데이터(실적·후기·지수) 자체는 팔지 않는다.
3. 추천 보상은 가입 즉시(유령 계정 실익 없음 논거). 카카오톡 발송은 **마지막**.

## 3. 이 세션에서 배운 함정 (다음 세션 필독)
1. **dev 서버 켠 채 `npm run build` 금지** — 같은 `.next`를 덮어써 dev가 전 라우트
   500(MODULE_NOT_FOUND)으로 죽는다(CLAUDE.md 등재). 빌드 후 dev 재시작.
2. **본문이 짧아지는 순간 푸터 노출 계열** — 전역 100dvh 래퍼가 일반해(CLAUDE.md).
   재발하면 개별 스페이서 전에 래퍼 생존부터 확인.
3. **행정구역 개편이 일일 동기화로 조용히 들어온다** — 전남광주통합특별시가 전례.
   지역 접두어를 새로 짜지 말고 lib/regions.ts 헬퍼 사용. 시도 표기 분포 점검 쿼리:
   `SELECT split_part(address,' ',1), count(*) FROM "Facility" GROUP BY 1`.
4. **globals.css의 전역 폰트 상향 시스템** — text-[10~15px]·xs·sm이 한 단계씩 크게
   재정의돼 있다(40~60대 타깃). 체계 밖 값(13.5px 등)을 쓰면 오히려 주변보다 작아진다.
5. **node -e 등 외부 프로세스의 파일 쓰기는 dev HMR이 못 볼 수 있다** — 화면이 안
   바뀌면 파일부터 확인(디스크가 진실), 빌드로 최종 검증.
6. **route.ts에 핸들러 외 export 금지**(Next 빌드 에러) — 공용 함수는 lib로.
7. **인라인 PDF 뷰어는 같은 파일명을 캐시한다** — 재전송은 v2·v3 파일명으로.
8. **바텀시트 제스처 소유권**: 목록 스크롤은 full 스냅에서만 열고, 그 외엔 시트가
   전부 가져간다 — 겹치면 이중 스크롤.
9. Prisma **resolve --applied 후 스키마 변경** 시 베이스라인 재생성엔
   `_prisma_migrations` 행 삭제→재resolve 필요(README에 절차).
10. 계정 목록에 `_race_*` 테스트 잔재 9행(5차 경쟁 조건 검증) — 정리 후보.

## 4. 커밋·배포 상태
**미커밋 0. 33커밋(80b86f9→34972ef) 전부 푸시** — Vercel 자동 배포(중간 시점들
프로덕션 라이브 실측: 가이드·지역 실사진·번들 검사 다수). 사용자가 시작한 작업 칩
세션(§0)만 닫으면 됨.

## 5. DB 변경 (전부 db push로 프로덕션 반영, 가산형)
- 신설 모델: `PointLedger` `OpenRequestNudge` `CareLogFamilyAccess` `ReferralCode`
  `ReferralAttribution`
- 필드 추가: `SitterProfile.ribbonUntil` · `CareRequest.boostUntil` · `User.createdAt`
  (⚠️ 기존 회원 createdAt은 추가 시점 날짜 — 실가입일 아님, 스키마 주석 참조)
- `_prisma_migrations` 테이블 생성(resolve --applied) — §1-1 주의 참조
- 데이터: **테스트 포인트 지급 2건** — 운영자·교덩 각 +10,000P
  (kind=admin_grant, refId=test-2026-08-06). 회수하려면 해당 원장 행 삭제.

## 6. 로컬 환경 메모
- 예약 작업 2개: `resend-first-batch-check`(8/6 09:30, 1회성) ·
  기존 `seo-canonical-check-reminder`(8/6 10:00).
- 테스트 계정: 운영자(cms63hwkr…, 매니저 역할·닝닝이 프로필) / 교덩(cmsdccyxf…,
  보호자 역할) — 각 10,000P 보유. 별·우선 요청 교차 검증 시나리오는 최근 대화 참조.
- 소개서 PDF·HTML은 세션 스크래치(임시)에 있음 — 재생성은 HTML 수정 후 헤드리스
  크롬 print-to-pdf(카드뉴스와 같은 방식). 로고는 public/logo.png 임베드.
- dev 서버 여러 개가 3000 외 포트로 뜨는 상태였음 — 폰 테스트(192.168.219.204:3000)는
  기존 서버 재시작 필요. 카카오 지도는 등록 도메인(프로덕션)에서만 열림(5차 §1-1).
