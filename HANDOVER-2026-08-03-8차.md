# 돌보다 인수인계 — 8차 세션 (2026-08-03)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다.
> 이전: [7차](HANDOVER-2026-08-03-7차.md) · 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)는 여전히 유효합니다.

---

## 0. ★ 사용자가 직접 해야 할 일 (코드로는 못 하는 것)

### 0-1. 🔴 GitHub Actions 시크릿 등록 — **이걸 해야 알림이 실제로 나갑니다**

저장소 → Settings → Secrets and variables → Actions → New repository secret

| 시크릿 | 값 | 없으면 |
|---|---|---|
| `RESEND_API_KEY` | `.env.local`의 같은 값 | 사건만 세고 메일은 안 나감 (워크플로는 안 깨짐) |
| `MAIL_FROM` | `.env.local`의 같은 값 (선택) | `onboarding@resend.dev`로 발송 |

`DIRECT_URL`은 기존 수집 워크플로에 이미 등록돼 있으면 그대로 쓰입니다.
**아직 안 넣었다면 `DIRECT_URL`도 필요합니다** (7차 §0에서 이월된 항목).

등록 후 Actions 탭 → `send-facility-alerts` → Run workflow로 수동 검증 가능
(날짜 지정 입력칸 있음).

### 0-2. 🟡 Vercel `ADMIN_USER_IDS` — 7차에서 이월, 확인 필요

계정 id `cms63hwkr0000n8hqpkhkik63`. 등록·재배포했는지 확인.
이 값은 `/admin` 접근 + **자기 글 지원 허용(운영자 예외)** 두 곳에서 쓰입니다.

### 0-3. 🟡 공공데이터 제공신청 (권장 순위)

1. **장기요양기관 평가 세부항목 점수** ⭐ — 지금 28,860건 중 3건뿐. UI는 이미 만들어져 있어 데이터만 오면 바로 켜짐
2. 요양기관 폐업·휴업·지정취소 이력 — "이 법인이 과거에 폐업시킨 적 있다"는 등급으로 안 보이는 신호
3. 요양급여 청구 통계(시설 단위) — 실제 의료 처치 강도
4. 지역별 대기자 통계 · 5. 노인 인구 통계(B2B 상품용)

### 0-4. 🟡 이월 항목 (7차에서 계속)

- 공단 사진 zip 재다운로드
- 서치콘솔 canonical 확인 (8/6 예약 리마인드)

---

## 1. 이 세션에서 한 일

### 1-A. 🔴 버그 수정 (중요도 순)

**① 매니저 서명이 아예 불가능하던 문제** (`b9eb5bd`)
`findMatchedContext`가 보호자 쪽을 먼저 찾고 **있으면 매니저 쪽은 아예 보지 않았다.**
한 계정이 보호자이자 매니저일 때(역할 전환 기능) 보호자로 서명을 마치면 이후
매니저 합의서가 영영 안 잡히고 409로 막혔다. → 양쪽을 다 조회하고 **아직 내 서명이
비어 있는 쪽**을 고르도록 수정. 실측으로 `viewer: "sitter"` 전환 확인.

**② 자기 글에 자기가 지원되던 문제** (`a47f21b`, `9de81bc`)
지원 생성 API가 `guardianId`를 안 봤다. DB 확인 결과 **지원 3건 중 2건이 자기 글 지원**,
그중 1건은 매칭확정 + 합의서까지 생성돼 있었다. ①번 서명 버그도 사실 이 데이터에서
비롯된 증상이었다.
→ API에서 400 차단 + 목록에서도 제외. **단 운영자 계정은 예외**(`isAdminUser`) —
계정 하나로 흐름 전체를 테스트할 수 있어야 해서. `NODE_ENV`가 아니라 운영자 판별로
연 이유는 프로덕션에서 확인이 필요하기 때문.
→ 잘못된 데이터는 삭제함(지원 2·합의서 1, 매칭됐던 요청은 OPEN으로 복구).

**③ 요양인정번호 10자리 초과 입력** — 입력 단계에서 숫자만 10자리로 자름
**④ 복지용구 상담 내역 items가 배열이 아닐 때 화면이 죽을 수 있던 것** — 방어 추가
**⑤ 홈 통계 카운트업이 안 돌던 것** — `requestAnimationFrame`은 화면이 합성되지 않으면
콜백이 아예 안 불린다. `setInterval` 계단식으로 교체 + 감지 마진 0으로 좁힘
**⑥ 일자리 탭 아래 회색 바** — `overflow-x-auto`의 가로 스크롤바였다(360px에서 탭 4개가
넘침). `.no-scrollbar` 유틸 추가 + 여백 축소

### 1-B. 근무환경 지수 — 구조적 편향 제거 (`85d0d20`, `ea1dbd8`)

**유형별 법정 배치기준이 다른데 하나의 구간표를 쓰고 있었다.**

| | 요양원 | 주야간보호 |
|---|---|---|
| 법정 기준 | 1:2.3 | 1:7 |
| 실측 중앙값 | 2.3 | 6.6 |
| 수정 전 "매우 좋음" | 36% | **0.07% (4곳)** |
| 수정 후 | 23.1% | **13.3% (720곳)** |

둘 다 법을 지키는데 주야간보호만 낮은 점수를 받고 있었다. 각 유형의 실측 사분위수로
구간을 분리(기존 방법론 유지). 두 유형의 "좋음" 비율이 49.5% vs 49.8%로 나란해졌다.

**초기 이직 신호 신설** — "6개월 안에 그만둔 비율"(weight 2).
1년 근속률은 오래된 시설일수록 유리해서 "지금 사람이 나가는가"를 못 본다.
공단 원본(`Institution.data` 43,943곳 100% 보유)에 있던 3·6개월 세분값이
`Facility`로 옮겨질 때 `under1y`로 합쳐져 버려지고 있었다.
→ 매퍼 수정 + `scripts/backfill-tenure-detail.mjs`로 **26,717곳 백필**, 11,471곳 적용.

> ⚠️ 매퍼(`map-institutions-to-facilities.mjs`)를 통째로 재실행하면 안 된다.
> 정원·현원·대기·등급은 일일 수집이 더 최신 값으로 갱신하고 있어서 옛 값으로 되돌아간다.
> 그래서 tenure만 표적 백필하는 스크립트를 따로 만들었다.

**가중치(35/30/15/10/10)는 변경하지 않았다.** [docs/work-index-spec.md](docs/work-index-spec.md) 갱신 완료.

### 1-C. 찜 알림 — 1·2단계 완성 (`908fcb7`, `97c756e`, `2be7856`)

**문제**: 알림 재료는 매일 만들어지는데(일일 수집이 빈자리·등급변동을 이미 감지)
받을 사람 명단이 서버에 없었다. 찜·알림설정이 전부 localStorage.

**1단계 — 서버 이전**
- `FacilityFavorite`(찜 + 알림 스위치 한 행), `RegionInterest` 신설
- `/api/favorites` (GET·POST·PATCH), `/api/favorites/import`
- 비로그인은 localStorage 유지, 로그인하면 **병합**(덮어쓰기 아님).
  로컬을 정답으로 삼으면 새 기기의 빈 localStorage가 서버 찜을 통째로 지운다
- 공개 API를 그대로 둬서 사용처 6곳 무수정

**2단계 — 발송**
- `scripts/send-facility-alerts.mjs` — 스냅샷에서 사건을 **다시 계산**한다
  (수집 내부 값에 의존 안 함 → 재실행 안전)
- `AlertDelivery` — **발송 전에** 행을 만들고 unique 제약을 잠금으로 쓴다.
  발송 후 기록하면 그 사이 재실행에서 중복이 난다

**발송 정책 4가지 확정**
| 항목 | 결정 |
|---|---|
| 시각 | **오전 9시 KST** (수집 새벽 4시와 워크플로 분리) |
| 묶기 | **사람당 한 통** (사건 7건 → 1통 실측) |
| 등급 하락 | **보낸다, 단 "올랐어요/내려갔어요"로 문구 구분** |
| 최소 간격 | **같은 시설 7일** (`sentAt` 기준) |

### 1-D. 문서

- [docs/company-profile.md](docs/company-profile.md) — 회사소개서(에이지테크 포지셔닝, 수치는 전부 실측)
- [docs/favorite-alert-spec.md](docs/favorite-alert-spec.md) — 찜 알림 설계 (§7이 확정 정책으로 갱신됨)
- [docs/care-log-spec.md](docs/care-log-spec.md) — **돌봄일지 기획서 (미구현)**

### 1-E. 모바일 (`b748392`)

- 44px 미만 터치 타깃 10곳 통일
- `fixed bottom-0` 전체 스캔 → **시설 상세 하단 바에 safe-area 누락 발견·수정**
  (트래픽이 가장 많은 화면인데 노치 기기 홈 인디케이터와 겹칠 뻔했다)
- CLAUDE.md에 **새 UI 작업 원칙 4개** 추가 (문장 단위 줄바꿈·컴포넌트 size variant·
  시맨틱 반응형·모바일 우선)
- 확인된 것: 루트 폰트가 18.5px라 **iOS 입력 자동확대 문제는 없다**(17.76px ≥ 16px)

---

## 2. 다음에 할 일 (우선순위)

### 2-1. 구글 로그인 ← **사용자가 다음으로 지목**

카카오는 이메일 제공 동의가 꺼져 있으면 이메일을 안 준다(실측에서 재현).
그래서 **알림을 보낼 주소가 없는 계정이 생긴다.** 구글 로그인이 붙으면 상당수 해소된다.
`components/AuthModal.tsx`에 이미 구글 버튼이 "준비 중"으로 있다(`READY_PROVIDERS`에서 제외).

### 2-2. 알림 3단계

- 이메일 없는 계정 안내 (`/notifications`에 "알림을 받으려면 이메일이 필요해요")
- 관심 지역 신규 시설 알림
- 웹푸시 → 앱 FCM

### 2-3. 돌봄일지 — 설계 완료, **착수는 매칭이 돌기 시작한 뒤**

현재 매칭확정 0건·합의서 0건·매니저 3명. 만들어도 쓸 사람이 없다.
기획서에 30초 안에 끝나는 구조 + 빠른 알림(1탭) 설계까지 되어 있다.

### 2-4. 리텐션 로드맵 (순위)

1. **입소 대기 예측** — 시계열이 이미 쌓임. 경쟁사가 6개월은 못 따라온다. B2B 상품 직결
2. 알림 인프라 (1·2단계 완료, 3단계 남음)
3. 돌봄일지

---

## 3. 이 세션에서 배운 함정

1. **`daily-nhis-sync.mjs`는 plain node로 도는 `.mjs`라 TS 모듈을 못 붙인다.**
   알림 발송도 `.mjs`로 만들었다. 대신 수집 내부 값에 의존하지 않고 스냅샷에서
   사건을 다시 계산하게 해서 결합을 끊었다.
2. **매퍼 재실행은 일일 수집 결과를 되돌린다.** 표적 백필 스크립트를 따로 만들 것.
3. **중복 발송 방지는 "발송 전 기록"이어야 한다.** 발송 후 기록하면 그 사이 재실행에서 중복.
4. **Windows에 python이 없다.** 문서 일괄 치환은 sed 또는 Edit 도구로.
5. **React StrictMode(dev)가 API를 2번 호출한다.** 마이페이지에서 같은 API가 2~4번
   나가는 건 dev 전용이고 프로덕션엔 없다 — 호출처는 각각 한 곳뿐인 걸 확인했다.

---

## 4. ⚠️ 미커밋 상태 (7차에서 계속 보류)

§1-I(B2B 입점 절차 개편) 코드가 **여전히 working tree에만 있다**:
`app/admin/AdminClient.tsx` · `app/api/admin/inquiries/route.ts` ·
`app/api/business-inquiry/route.ts` · `app/business/BusinessInquiryForm.tsx` ·
`app/business/console/page.tsx` · `app/business/page.tsx` · `lib/businessPlans.ts` ·
`app/business/apply/`(미추적)

**사용자가 "나중에 다시 할게"로 보류한 상태.** 이번 세션에서도 건드리지 않았다.
임의로 커밋·배포하지 말 것.

---

## 5. DB 변경 (전부 프로덕션 반영 완료)

| 모델 | 용도 |
|---|---|
| `WelfareConsultRequest` | 복지용구 상담 신청 |
| `FacilityFavorite` | 찜 + 시설별 알림 스위치 |
| `RegionInterest` | 관심 지역 |
| `AlertDelivery` | 알림 발송 기록·중복 방지 |
| `CareProfile.birthDate` / `.ltcCertValidFrom` | 요양인정번호 등록 화면에서 수집 |

⚠️ `birthDate`는 "이름·생년월일은 안 받는다"는 기존 원칙의 **예외**다(사용자 결정).
개인정보처리방침 수집 항목에 반영 완료.
