# 돌보다 인수인계 — 12차 세션 (2026-08-04)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다.
> 11차와 같은 날 이어진 세션입니다. 11차는 **체감 속도·UI·버그**였고,
> 이번은 **기업회원(B2B) 전면 개편**입니다 — 요금 최적화 하나로 시작했다가
> 명칭 변경, 대시보드 고도화, 색상 팔레트 버그 발견, 그리고 영업·운영용
> 콘텐츠 아티팩트 7종 제작까지 이어졌습니다.
>
> 이전: [11차](HANDOVER-2026-08-04-11차.md) · 10차는 **알림 시스템**(안 건드림, 이번 세션 내내
> "알림 쪽은 손대지 말라"는 지시를 받아 지켰습니다) · 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름
> 상태 전이 지도)은 여전히 유효합니다.

---

## ⚡ 다음 세션에서 바로 시작할 일

**이 세션에서 커밋을 하나도 안 했습니다.** 35개 파일 수정 + 10개 신규 파일이 전부
working tree에만 있습니다(§4). 다음 세션 첫 일은:

1. **이 문서(§1)를 훑어 변경 목록이 사용자 의도와 맞는지 확인**
2. **B2B 관련 커밋만 먼저 만들지, 전체를 한 번에 커밋할지 사용자에게 확인** — B2B
   입점 개편(7차부터 보류)과 이번 세션 변경분이 같은 파일들(`app/business/*`,
   `app/admin/AdminClient.tsx`, `app/api/admin/inquiries/route.ts`)에 섞여 있어
   분리 커밋이 까다롭습니다. §4 참고.
3. **미착수 아티팩트 이어서**: 이메일 템플릿 라이브러리(§2-2, 사용자가 "추가
   기능좀"에서 골랐던 두 항목 중 아직 안 만든 것) — 사용자가 원하면 제작.
4. 비용 계산기(8번째 아티팩트)는 이 문서 작성 직후 검증·발행까지 끝냈습니다
   (§1-I 표 8번). 로컬 검증용으로 띄운 Node 서버를 정리하며 `taskkill /IM node.exe`를
   무차별로 돌렸는데 — 이게 **머신에 떠 있던 다른 node 프로세스까지 전부 죽였을
   수 있습니다**(예: 사용자가 따로 띄워둔 dev 서버). 다음 세션 시작 시 필요한
   서버가 죽어있으면 이게 원인일 수 있으니 다시 띄우면 됩니다.

---

## 0. ★ 사용자가 직접 해야 할 일 (9차부터 계속 이월)

11차에서도 확인 못 했습니다. 여전히 그대로입니다.

- 🔴 **네이버 로그인 프로덕션 반영** — Vercel `AUTH_NAVER_ID`·`AUTH_NAVER_SECRET` 등록 +
  네이버 개발자센터 검수. 안 하면 배포본에서 네이버 버튼이 죽습니다.
- 🟡 **GitHub Actions 시크릿**(`DIRECT_URL`·`RESEND_API_KEY`·`MAIL_FROM`) — 알림
  워크플로 4개가 전부 여기 걸려 있습니다.
- 🟡 Vercel `ADMIN_USER_IDS`, 공공데이터 제공신청, 공단 사진 zip 재다운로드,
  서치콘솔 canonical 확인(**8/6 예약 — 이제 이틀 남았습니다**)

---

## 1. 이 세션에서 한 일

### 1-A. "비슷은 시설" 조회 최적화 (원래 요청했던 작업)

11차 문서가 "다음 세션 바로 시작할 일"로 못박아둔 것부터 시작했습니다.

**진단이 처음 예상과 달랐습니다** — 11차는 `LIKE '%사직로%'`가 인덱스를 못 타서
2만 8천 행을 스캔한다고 추정했는데, 직접 `EXPLAIN ANALYZE`로 재보니 실제 DB 실행
시간은 **1ms 안팎**이었습니다. 진짜 원인은: 상세 페이지(`page.tsx`)와 API 라우트
(`/api/facilities/similar`)가 **거의 같은 일을 하는 조회를 따로 두 벌** 갖고 있었고,
상세 페이지 쪽은 최적화가 하나도 안 된 옛날 버전(`SELECT *`, 같은 시군구로만 후보를
좁혀 결과가 2~7곳뿐)이었습니다. 그래서:

- `lib/similarFacilities.ts` 신설 — 조회·채점·캐시 로직을 API 라우트에서 뽑아내
  단일 소스로 만듦(하루 캐시, `unstable_cache`)
- `app/facility/[id]/SimilarSection.tsx` 신설 — 이 로직을 쓰는 서버 컴포넌트,
  `<Suspense>`로 감싸 본문과 분리 스트리밍
- `app/facility/[id]/page.tsx`에서 중복 조회 코드 전부 삭제, 슬롯으로 교체
- `app/api/facilities/similar/route.ts`는 이제 `lib/similarFacilities.ts`를
  가져다 쓰기만 함(중복 제거)

**검증**: 프로덕션 빌드로 60일 전/후 실제 시설 다수 curl 스트리밍 응답 측정,
"비슷한 시설" 섹션이 본문보다 늦게 도착하는 것·SEO용 최종 HTML에 링크가 그대로
포함되는 것 확인.

### 1-B. 추가로 짚은 작은 버그 3개 (요청하지 않았지만 발견해서 고침)

사용자가 "더 살펴보고 검증 체크 자세히"라고 했을 때 발견:

1. `app/search/loading.tsx`가 로고 로더만 있어서, 로딩 중 화면 아래 SEO용 안내문
   ("전국 요양병원·요양원…")이 위로 올라와 보였습니다 → `FacilityCardSkeleton.tsx`
   신설(실제 카드 그리드 모양), `SearchPageClient.tsx`의 빈 로딩 상태에도 재사용.
2. `components/FacilityCard.tsx`에서 시설명·주소가 `line-clamp-2`라 길면 2줄까지
   허용됐는데, 특정 조합("의료법인승인의료재단동창원요양병원" + 긴 주소)에서
   카드 높이가 들쭉날쭉해졌습니다 → `truncate`(1줄+말줄임)로 변경.
3. `lib/sitterProfileContext.tsx`의 `isSitter`가 로딩 중 `null`인 동안 마이페이지
   역할 카드가 "매니저 시작"(미등록용 문구)을 잠깐 보여줬다가 실제 값으로 바뀌는
   깜빡임 → localStorage에 계정별 마지막 확인값을 캐싱해 `isSitterHint`로 우선
   노출(§3-1 참고, `useIsomorphicLayoutEffect.ts` 신설).

### 1-C. 마이페이지 보호자 화면 그라데이션

"보라색 그라데이션이 잘 안 보인다"는 지적 → 실제로 3곳(보호자 프로필 카드,
돌봄 신청 내역 카드, 진행 중인 돌봄 요청 카드)이 완전 흰 배경이었습니다.
`from-royal-50 to-white`로 옅게 추가(`app/mypage/care-profile/page.tsx`,
`components/CareRequestDetail.tsx`, `components/MyPageDashboard.tsx`).

### 1-D. B2B — "확인전화" 절차 완전 제거

사용자 지시: 시설 인증 절차에서 전화 확인을 없애고 전부 이메일로. 문구·주석
전체를 훑어서 "확인전화"라는 표현이 남은 곳을 0건으로 만들었습니다(`grep`으로
최종 확인). 관련 파일: `lib/businessPlans.ts`(SIGNUP_STEPS), `app/business/page.tsx`,
`app/business/BusinessInquiryForm.tsx`, `app/api/business-inquiry/route.ts`,
`app/admin/AdminClient.tsx`(승인 UI의 mailto 템플릿을 서류 요청용으로 교체),
`app/api/admin/inquiries/route.ts`.

### 1-E. B2B — "시설 운영자" → "기업회원" 전면 개칭

사용자 지시: "시설도 전부 기업회원으로 분류하자." 랜딩·신청폼·콘솔·개인정보처리방침·
푸터·메타 제목·JSON-LD까지 전부 바꿨습니다.

**예외 하나** — 정보 정정 요청(§1-F 하단) 관련 문구는 "시설 운영자"를 그대로
유지했습니다(사용자 지시: "오류정정 하는건 시설 운영자 라는 표현으로 해놓자").
지금 코드에 남은 "시설 운영자" 2곳은 전부 이 예외입니다
(`app/api/business/correction/route.ts:8`, `FacilityDetailClient.tsx:311`).

### 1-F. 기업회원 대시보드 고도화

콘솔(`/business/console`)에 실제로 없던 것들을 추가:

- **`CompanyOverview`** — 시설을 2곳 이상 관리하는 기업회원에게 탭 위에 전체 합산
  요약(조회수·상담 신청)을 먼저 보여줌. 시설 1곳뿐이면 안 그림.
- **`Delta` 배지** — "지난 30일 대비" 증감(▲/▼ % 또는 "신규"). `page.tsx`가 60일치를
  받아 최근 30일 vs 그 이전 30일로 나눠 계산.
- **`ViewTrendChart`** — 최근 30일 조회수 막대 그래프. 탭·호버로 그날 정확한
  숫자가 캡션에 뜸. `components/ViewTrendChart.tsx`로 뽑아내 콘솔·월간 보고서가
  공유(월간 보고서의 기존 `Sparkline`은 데스크톱 hover 전용 `title` 툴팁이라 폰에서
  안 먹혔는데, 공유하면서 자동으로 고쳐짐).
- **`lib/deltaPct.ts`** — 콘솔의 "지난 30일 대비"와 월간 보고서의 "지난달 대비"가
  각자 갖고 있던 % 계산을 하나로 합침. **창(window)이 다르니 헷갈리지 말 것**
  (rolling 30일 vs 캘린더 월) — `docs/business-console-spec.md` §5에 표로 정리.
- **`page.tsx` 최적화** — 조회수·상담·소식·스폰서 4개 배열을 시설당 `.filter()`하던
  것(O(F×V))을 `facilityId`로 미리 그룹핑하는 걸로 바꿈(O(V)).
- **`/business/console`·`/business/console/report`에 `loading.tsx` 추가** — 둘 다
  여러 테이블 병렬 조회하는 `force-dynamic` 라우트인데 없었습니다.

전부 mock 데이터로 6가지 극단 케이스(정상 증가/감소/신규/양쪽0/완전동일/빈 시리즈)
렌더링 검증 완료.

### 1-G. tailwind 색상 팔레트 버그 발견·수정 (예상 밖 수확)

Delta 배지 색이 안 먹는 걸 조사하다가, 이 프로젝트의 `ink` 팔레트가 원래
900/700/500/300/100만 정의돼 있고 **400이 없다**는 걸 발견했습니다. 기본
Tailwind 회색 팔레트엔 400이 있어서 습관적으로 썼겠지만, 커스텀 팔레트라 CSS가
아예 안 만들어져서 **전 프로젝트 33개+ 파일**이 조용히 부모 색을 상속하고
있었습니다. 이어서 200/600/800(ink)·50(ivory)도 같은 문제로 빠진 걸 추가로
찾았습니다.

`tailwind.config.ts` 한 파일에 5개 색 단계만 추가해서 전부 고쳤습니다(기존 값은
안 건드림, 순수 추가). `/about`·`/grade-helper` 등에서 수정 전후 색을 실측
대조해 확인. **이 변경은 화면 33곳+에 퍼져 있어서 커밋 전에 실기기로 한 번
훑어보길 권합니다** — 대부분 "더 옅어지는" 방향이라 안전할 가능성이 높지만
전부 눈으로 확인은 못 했습니다.

### 1-H. `docs/business-console-spec.md` 신설

기업회원 콘솔·대시보드 기능서. 다른 핵심 자산 문서(`ai-score-spec.md` 등)와
같은 급으로 CLAUDE.md에 참조 규칙 추가. 이 세션의 변경 이력, 두 가지 "증감"
지표의 차이, 알려진 제약(정정요청 상태추적 없음, 중복신청 방지 없음)까지 정리.

### 1-I. 기업회원 대상 콘텐츠 아티팩트 7종 (claude.ai 비공개 저장)

사용자가 "다른 페이지 기업 페이지를 더 생성해보자 추천"으로 시작 → 매번 추천하고
사용자가 고르는 식으로 진행했습니다. **전부 실제 소스 문서/코드에서 콘텐츠를
가져왔습니다 — 지어낸 숫자·후기 없음.**

디자인 시행착오: 처음엔 세리프+골드+원장(ledger) 스타일로 만들었다가 "촌스럽다"는
피드백을 받고 **다크 글래스모피즘 + 바이올렛(#ad95ff)/시안(#4be8d6) 액센트 +
Pretendard+모노스페이스**로 완전히 갈아엎었습니다. 이후 "색깔·글자가 잘 안 보인다"는
지적에 실제 WCAG 대비를 계산해보니 `text-faint`가 2.98:1(기준 4.5:1 미달)이었던
걸 발견해 6.43:1로 올렸습니다. 마지막엔 "더 고급스럽게"라는 요청으로 6페이지
전부에 그레인 텍스처·카드 인셋 하이라이트·페이드형 구분선·숫자 글로우를 추가하는
크래프트 패스를 한 번 더 돌렸습니다.

| # | 페이지 | URL | 소스 |
|---|---|---|---|
| 1 | 요금제 | `claude.ai/code/artifact/7ce645e7-0054-417e-a4da-dc715a16824c` | `lib/businessPlans.ts` |
| 2 | 회사소개 | `claude.ai/code/artifact/2c89ac59-63a3-4252-bd40-24204b03b19e` | `docs/company-profile.md` |
| 3 | 가입 가이드 | `claude.ai/code/artifact/f12f5952-212f-4672-8a80-b626166b9135` | `businessPlans.ts` SIGNUP_STEPS + `business/page.tsx` FAQ |
| 4 | B2B 포지션 비교 | `claude.ai/code/artifact/19d2b212-8cca-41e1-9341-5059a8ab6530` | `docs/competitor-caredoc-b2b.md` |
| 5 | 기능 비교 매트릭스 | `claude.ai/code/artifact/bee9b57e-9e57-402c-ba78-7bef893c3f18` | `businessPlans.ts` + `facilityOwner.ts` |
| 6 | 안심지수 방법론 | `claude.ai/code/artifact/082a1c8e-4adf-439d-bf9e-5f45797f946a` | `docs/ai-score-spec.md` |
| 7 | B2B 대응 매뉴얼(내부용) | `claude.ai/code/artifact/09e3771e-cd4f-43b4-ba61-04e812512d2d` | 실제 코드의 상태 파이프라인 + FAQ 근거 |
| 8 | 플랜 비용 계산기 | `claude.ai/code/artifact/2da21918-1ddb-4714-a426-a416b5d3d173` | `lib/businessPlans.ts` (사용자가 넣은 건수로 나누기만 함, 예측 없음) |

**5번(기능 비교 매트릭스) 제작 중 진짜 버그 하나 찾음**: 실버타운 플랜의 "지역
스폰서 노출" 포함 여부를 처음엔 미포함으로 표시했는데, 코드를 다시 보니 실버타운은
"지역 프리미엄의 모든 기능 포함"이고 프리미엄은 "플러스+스폰서의 모든 기능
포함"이라 **상속상 포함이 맞았습니다** — 수정.

**7번(대응 매뉴얼)은 정직성 검증**: "해지/환불" 질문에 뭐라고 답할지 쓰려다
`FacilitySubscription`을 다루는 라우트를 grep해보니 승인(생성) 하나뿐, 해지
전용 UI가 없다는 걸 확인 → 없는 정책을 지어내지 않고 "현재 미구현" 배지로
정직하게 표시.

**8번(비용 계산기)은 이 문서를 쓴 직후 검증·발행까지 완료했습니다** — 로컬
서버로 렌더링·콘솔에러 확인 후 슬라이더 최솟값(1건)·기본값(5건)·최댓값(30건)
세 지점에서 건당 비용 계산이 정확한지 대조했고(예: 5건일 때 플러스 49,000÷5=9,800원
정확히 일치), 복붙 잔재였던 미사용 `@property --border-angle` 선언도 제거
후 발행했습니다. 위 표의 8번 항목이 최종 URL입니다.

**추가로: 이후 세션에서 8개 페이지 전체에 공용 네비게이션 바·스크롤 리빌
연출을 얹었고, 기능 비교 매트릭스엔 플랜 컬럼 클릭 하이라이트 인터랙션을
추가했습니다(§9 참고).** 이 작업 중 재발행 실수가 하나 있었습니다 — 압축(컨텍스트
정리) 이후 새 세션에서 시작했기 때문에 `Artifact` 도구가 "이 대화가 발행한
적 없는 URL"로 인식해, 7개 페이지를 같은 URL에 덮어쓰는 대신 새 URL 7개를
새로 만들어버렸습니다. 원래 URL로 다시 정확히 덮어써서 위 표의 URL은 전부
최종적으로 맞습니다만, **잘못 생성된 중복 아티팩트 7개가 claude.ai/code/artifacts에
남아있습니다** — 같은 제목으로 뜰 텐데, 사용자가 원하면 수동으로 지워야 합니다
(이 세션 도구에는 삭제 기능이 없음).

**이메일 템플릿 라이브러리**(사용자가 추천 후보로 골랐던 두 번째 항목)는 아직
시작 안 했습니다 — §2-2 참고.

---

## 2. 다음에 할 일

### 2-1. ~~비용 계산기 검증·발행~~ — 완료

이 문서 작성 직후 검증·발행까지 끝냈습니다. §1-I 표의 8번 참고.

### 2-2. 이메일 템플릿 라이브러리 (아직 시작 안 함)

서류요청(이미 `AdminClient.tsx`에 mailto 템플릿 있음, 재사용)·승인완료 안내·
서류반려·정정요청 확인·스폰서 대기 안내 등 실제 B2B 흐름의 이메일 스니펫 모음.
7번(대응 매뉴얼)과 짝을 이루는 자료로 기획됐었습니다.

### 2-3. 이 세션 변경분 커밋 여부 결정

35개 수정 + 10개 신규 파일이 전부 미커밋입니다(§4). 사용자와 상의해서 커밋
범위·타이밍 정할 것.

### 2-4. tailwind 색상 수정 실기기 확인 (§1-G)

33개+ 파일에 퍼진 변경이라 커밋 전에 한 번 훑어보길 권함.

### 2-5. 11차에서 이월된 것들

- 취소 알림(요청 취소 시 지원자 이메일 안 나감) — **알림 쪽이라 이번 세션엔 손 안 댐**
- 나머지 라우트 `loading.tsx` 점검
- 리텐션 로드맵: 입소 대기 예측(1순위, 미착수)

---

## 3. 이 세션에서 배운 함정

1. **"진단"을 코드 읽기만으로 확정하지 말 것.** 11차가 추정한 "LIKE라 인덱스를
   못 탄다"는 실제로 `EXPLAIN ANALYZE`를 돌려보니 틀렸다(1ms). 진짜 병목은
   "같은 일을 하는 조회가 두 벌 있었다"는 구조 문제였다. 실측 없이 넘겨짚은
   진단을 그대로 밀고 나갔으면 엉뚱한 최적화를 했을 것이다.
2. **커스텀 Tailwind 팔레트는 기본 팔레트의 단계(50/100/.../900)를 전부 가정하면
   안 된다.** 이 프로젝트의 `ink`는 홀수 100단위만 있고 짝수(400/600/800)와
   50(ivory)이 원래 없었다 — 습관적으로 쓴 클래스가 조용히 부모 색을 상속하며
   실패한다. 의심 가면 `getComputedStyle`로 실제 색을 찍어봐야 한다(육안으로는
   "어차피 다크모드니까 비슷해 보여서" 못 잡는다).
3. **디자인 방향은 "예쁘다"의 기준이 사람마다 다르다 — 되도록 빨리 피드백을
   받아라.** 세리프+골드+원장 스타일을 처음부터 공들여 만들었는데 "촌스럽다"
   한 마디에 전면 재설계했다. 완성도를 높이기 전에 방향(팔레트·서체 계열)부터
   작게 보여주고 확인받는 편이 나았을 것 같다.
4. **"예쁘게/고급스럽게"는 실제로 측정 가능한 항목으로 쪼개서 처리하는 게
   낫다.** 명암 대비(WCAG 계산)·그림자 깊이·구분선 처리·타이포 트래킹처럼
   구체적으로 나누면 "느낌"이 아니라 체크리스트로 검증할 수 있다.
5. **이 자동화 브라우저 패널은 `document.visibilityState === "hidden"`으로
   동작한다.** 표준 브라우저 정책상 백그라운드 탭은 애니메이션·rAF가 멈춘다 —
   CSS `@property` 각도 애니메이션이나 `scroll-left` 키프레임이 "안 움직이는
   것처럼" 보여도 코드가 틀린 게 아니라 이 환경의 특성이다(11차의 View
   Transition 미확인 문제와 같은 계열). `resize_window`도 실제 `window.innerWidth`에
   항상 반영되는 건 아니었다 — 이럴 땐 CSSOM(`document.styleSheets`)으로
   미디어쿼리 규칙 자체가 올바른지 확인하는 우회로가 있다.
6. **Bash 도구가 일시적으로(분류기 문제로) 막히면 PowerShell로 같은 git 명령을
   그대로 돌릴 수 있다.** 세션이 끊기지 않고 이어갈 수 있는 유용한 대체 경로.
7. **없는 데이터로 "이렇게 하면 이만큼 좋아진다"는 절대 지어내지 말 것.** 비용
   계산기를 설계할 때 실제로 유혹이 컸다("플러스로 올리면 상담 전환율 40%
   증가" 같은 그럴듯한 숫자). 검증된 데이터가 없으면 계산기는 사용자가 입력한
   값을 정직하게 나누기만 하고, 그 사실을 화면에도 명시했다.
8. **로컬 검증 서버를 정리할 때 `taskkill /IM node.exe`처럼 이름으로만 잡는
   명령은 쓰지 말 것.** 내가 띄운 PID만이 아니라 머신에 떠 있는 다른 모든
   node 프로세스(사용자의 dev 서버 포함)까지 다 죽인다. `run_in_background`로
   띄운 프로세스는 해당 세션/PID를 정확히 지정해서 끄거나, 애초에 포트 충돌
   나면 그때 정리하는 식으로 접근해야 한다.

---

## 4. ⚠️ 미커밋 상태

**이 세션에서 커밋을 하나도 만들지 않았습니다.** 다음 두 종류가 섞여 있습니다:

**(A) 7차부터 이월된 B2B 입점 절차 개편**(사용자가 "나중에 다시 할게"로 보류) —
이번 세션에서 그 위에 §1-D~F 작업을 계속 얹었기 때문에, 이제 순수하게 "7차 때
보류한 부분만" 분리하기는 어렵습니다.

**(B) 이번 세션(12차) 전체 작업** — §1의 모든 내용.

```
 M CLAUDE.md
 M app/about/page.tsx
 M app/admin/AdminClient.tsx
 M app/api/admin/inquiries/route.ts
 M app/api/business-inquiry/route.ts
 M app/api/business/banner/route.ts
 M app/api/business/consults/route.ts
 M app/api/business/content/route.ts
 M app/api/business/correction/route.ts
 M app/api/business/photos/route.ts
 M app/api/business/posts/route.ts
 M app/api/facilities/similar/route.ts
 M app/api/facilities/view/route.ts
 M app/business/BusinessInquiryForm.tsx
 M app/business/console/ConsoleClient.tsx
 M app/business/console/page.tsx
 M app/business/console/report/page.tsx
 M app/business/page.tsx
 M app/facility/[id]/FacilityDetailClient.tsx
 M app/facility/[id]/page.tsx
 M app/mypage/care-profile/page.tsx
 M app/privacy/page.tsx
 M app/search/SearchPageClient.tsx
 M app/search/loading.tsx
 M components/CareRequestDetail.tsx
 M components/FacilityCard.tsx
 M components/Footer.tsx
 M components/MyPageDashboard.tsx
 M components/MyPageRoleCard.tsx
 M lib/businessPlans.ts
 M lib/monthlyReport.ts
 M lib/mypageRoleContext.tsx
 M lib/sitterProfileContext.tsx
 M prisma/schema.prisma
 M tailwind.config.ts
?? app/business/apply/
?? app/business/console/loading.tsx
?? app/business/console/report/loading.tsx
?? app/facility/[id]/SimilarSection.tsx
?? components/FacilityCardSkeleton.tsx
?? components/ViewTrendChart.tsx
?? docs/business-console-spec.md
?? lib/deltaPct.ts
?? lib/similarFacilities.ts
?? lib/useIsomorphicLayoutEffect.ts
```

**임의로 커밋·배포하지 말 것.** 사용자 확인 먼저.

아티팩트 8개(§1-I, §2-1)는 git과 무관하게 claude.ai에 비공개로 이미 저장돼
있습니다(계산기 1개만 발행 대기).

---

## 9. 이후 세션 추가 작업 — 아티팩트 8종 기술·인터랙션 고도화

"컨셉(다크 글래스모피즘)은 유지하면서 기술 고도화·인터랙션 개선"을 요청받아
8개 페이지 전체에 다음을 추가했습니다.

- **공용 네비게이션 바** — 7개 고객용 페이지(요금제·회사소개·가입가이드·
  경쟁사비교·기능매트릭스·안심지수·비용계산기) 상단에 sticky 바를 추가해
  서로 한 클릭에 오갈 수 있게 했습니다. 내부용 대응 매뉴얼은 성격이 달라
  (고객에게 보여주는 페이지가 아님) 이 바에는 넣지 않고, 대신 원래 있던
  하단 "참고 자료" 링크 목록을 4개→7개(전부)로 채웠습니다.
- **스크롤 리빌** — `IntersectionObserver` 기반으로 아래로 스크롤하면 섹션이
  살짝 떠오르며 나타나는 연출. `prefers-reduced-motion: reduce`가 켜져 있으면
  즉시 다 보이게 폴백합니다(애니메이션 없이). **이 자동화 브라우저 환경은
  기본값으로 `prefers-reduced-motion: reduce`가 true로 잡혀서, 폴백 경로만
  검증했고 실제 스크롤 애니메이션 자체는 육안으로 확인 못 했습니다** — 로직상
  타당하고 폴백 경로가 정상 동작하는 건 확인했지만, 일반 사용자 환경에서
  한 번 실물로 봐주시면 좋겠습니다.
- **기능 비교 매트릭스 컬럼 하이라이트** — 플랜 컬럼 헤더를 클릭하면 그
  컬럼만 강조되고 나머지는 옅어집니다(25개 기능 행 전부 정확히 반응하는 것
  확인 — 클릭 시 focused 25개, dimmed 100개, 다시 클릭하면 둘 다 0으로
  복귀). 키보드 접근(`role="button"`, Enter/Space)도 넣었습니다.

**재발행 실수 관련**: 위 §2-1에 적었듯, 압축 이후 세션이라 `Artifact` 도구가
같은 대화에서 발행한 적 없는 URL로 인식해 7개 페이지가 처음엔 새 URL로
발행됐습니다. `WebFetch`로 각 URL의 소유권을 확인한 뒤 `url` 파라미터로
원래 주소에 다시 정확히 덮어써서 지금은 URL이 전부 맞지만, 실수로 생성된
중복 아티팷트 7개가 정리 안 된 채 남아있습니다.

**로컬 검증 중 또 한 번의 `taskkill` 사고**: 이번엔 `taskkill /IM node.exe`
대신 `netstat`으로 실제 포트를 점유한 PID만 찾아서 그 PID 하나만 죽였습니다
(§3-8에서 배운 대로). 이번엔 다른 프로세스를 건드리지 않았습니다.

## 5. DB 변경

**없음.** 이번 세션은 화면·성능·콘텐츠 작업이라 스키마를 건드리지 않았습니다.
(`prisma/schema.prisma`의 변경은 §1-D 관련 주석 수정 3곳뿐 — 모델·필드는 그대로.)
