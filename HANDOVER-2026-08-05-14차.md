# 돌보다 인수인계 — 14차 세션 (2026-08-05)

이 문서만 읽고 이어서 작업할 수 있게 썼습니다. 13차(사진 파이프라인·바텀시트)에 바로
이어진 세션이고, **13차까지의 미커밋 상태를 전부 해소하고 17커밋을 배포**한 대형
세션입니다. 몸통은 다섯입니다: ① 메인페이지 고급화 완주(스펙 1·2·3단계 전부) ②
경기도 사진 완료 + 대표사진 재선정 ③ 돌봄 흐름 감사(버그 12건) ④ 리텐션 기능 4종 +
돌봄일지 고도화 5종 ⑤ **역경매 매칭**(제안 사례비·성별·연령대·재제안).

이전: [13차](HANDOVER-2026-08-05-13차.md) — 사진 파이프라인·바텀시트.
5차 §7(상태 지도)·10차(알림)·11차(체감속도)·12차(B2B) 여전히 유효.
알림은 [alert-system-spec](docs/alert-system-spec.md)이 단일 진실 공급원(§3-13까지 늘어남).

---

## ⚡ 다음 세션에서 바로 시작할 일

1. **실기기(폰) 검증 대기 목록** — 이 세션은 브라우저 팬이 화면 합성을 안 하는 상태라
   (rAF·IO·스크롤 이벤트·smooth scroll 전부 프레임에 묶임 — §5-1 함정) 아래는 코드
   검증까지만 됐다. 같은 와이파이에서 `http://192.168.219.204:3000` (dev 서버를 켰다면):
   - 메인 배너 손맛(스와이프·패럴랙스·View Transition 상세 전환)
   - 검색 바텀시트(13차분) + 중간 스냅 하단 페이드
   - 등급테스트: 문항 진행 중 탭바 숨김 → 맨 아래서 등장 → CTA 바 승강, 위로 올리면 재숨김
   - 사진 모달 뒤로가기(상세 유지), 제안 수정 시트, 찜 스낵바 원탭
2. **고도화 로드맵 합의된 다음 3개** (사용자가 "1→2→3 추천" 안내까지 받은 상태,
   착수 지시는 아직):
   ① 매니저 등록 5단계에 성별·연령대 스텝(지금은 프로필 관리에서만 입력 가능)
   ② 미선정 메일에 "같은 지역·유형의 새 OPEN 공고" 동봉
   ③ 지원자 0명 3일 경과 시 보호자에게 조건 완화 제안 알림(배치 알림 8종째)
   그다음 카카오톡 메시지 발송(talk_message 권한 이미 확보 — 대형).
3. **다음 지역 사진 zip** — 사용자가 준비하면 13차 파이프라인 그대로:
   inventory → convert → extras → upload --write. **분류기에 사람 감점이 들어갔으므로
   (photo-classify.mjs) 새 지역은 재정렬(photo-rehero) 없이도 처음부터 올바르다.**

## 0. ★ 사용자가 직접 해야 할 일 (이월 + 신규)

- 🔴 **GitHub Actions 시크릿** (`DIRECT_URL`·`RESEND_API_KEY`) — **사용자가 "나중에
  따로 노티해달라"고 명시(2026-08-05). 메모리 리마인드 등재됨.** 이게 없으면 일일
  동기화 + 배치 알림 5종이 프로덕션에서 전부 멈춰 있다. 즉시형 알림 6종(지원·확정·
  미선정·완료·일지 도착·취소)은 API 안에 있어 시크릿 없이도 동작 중.
- 🔴 네이버 로그인 프로덕션(Vercel env + 검수) — 이월.
- 🟡 Vercel `ADMIN_USER_IDS` — 이게 있어야 프로덕션 /admin(입점 신청 탭)이 열린다.
- 🟡 Vercel R2 env 5종 — 사진 "표시"엔 불필요(절대경로), 콘솔 업로드 R2 전환 시 필수.
- 🟡 서치콘솔 canonical 확인 — **8/6 예약 리마인드가 바로 내일이다.**
- 🆕 **원본 zip 2개 삭제 가능** — 5개 조건 전부 충족 확인 후 공식 통보 완료(§1-2).
  `photo-data/*/files/`도 삭제 가능, **jsonl 6개(inventory·manifest·extras × 2)는 영구 보관.**

## 1. 이 세션에서 한 일 (17커밋: 05b950c → aa7cbd0)

### 1-1. 메인페이지 모바일 고급화 — 스펙 1·2·3단계 완주 (05b950c)
- **HeroBanner 전면 재작성**: 진짜 `<a href>` 카드(ViewGate 유지, 수정키 새 탭 허용),
  활성 감지 3티어(scrollsnapchanging → IO → rAF), 자동 7초+정지 6종+**일시정지 버튼**,
  세그먼트 진행 표시(opacity·scaleX만), 키보드 화살표, 스와이프 연동 이미지 ±9px
  (**CSS 스크롤 드리븐** `animation-timeline: view(inline)` — JS 0, 미지원=정지 이미지).
- **View Transition 상세 전환 재도입** ([lib/navTransition.ts](lib/navTransition.ts) +
  NavTransitionWatcher): 예전 되돌림(53baa3b)의 원인("새 화면을 420ms 붙잡음")을
  **라우트 커밋(=loading.tsx 스켈레톤) 시점 종료 + 450ms 상한**으로 구조 제거.
  이름은 클릭한 카드에만(중복 안전), 도착지는 상세 스켈레톤의 제목·사진 바.
  모든 이동이 viewGateContext 한 곳을 지나므로 래핑도 한 곳.
- 실사진 우선 히어로(getHeroSlides — photo 있는 1등급 우선, 기호 시작 상호 배제,
  캐시 v3), 내주변·점수높은 위치 대기 스켈레톤, content-visibility(하단 2섹션),
  StatsStrip digit 정렬(§5-3 함정)·민트 신뢰 라벨, InfoTooltip flush(320px 넘침).

### 1-2. 사진 — 경기도 완료 + 대표 재선정 (e4739b3)
- 업로드 완료: 1of2 2,509 + 2of2 3,563 = **6,056곳** (photos-facilities 실측 6,056).
- zip 삭제 5조건(변환·extras·업로드·화면검증·jsonl보관) 전부 확인 → 삭제 승인 통보.
- **photo-rehero.mjs(신규)**: 캡션 점수로 대표사진 재선정, 375곳 교체(6.2%).
  멱등 확인(재실행 0곳). 원 순서는 manifest.jsonl에 보존.
- 분류기(photo-classify)에 같은 기준 이식: 사람·행사 감점, 지도·명함·로고·화장실 감점,
  "프로그램실" 중화(neutralizeRoomNames), stripName 공유 export.

### 1-3. 돌봄 흐름 감사 — 버그 12건 (725d494)
5차 §7 지도 대조로 전 API·화면 추적: 수정 시 날짜 역전 가드, **중복 요청 생성을
보호자별 advisory xact lock으로 직렬화**(부분 유니크 인덱스는 db push에서 소리 없이
사라질 수 있어 제외 — §5-2 함정), 합의서 동시 첫 서명 409, 일지 careDate 형식 검증,
무한 로더 3곳 제거(+오류 배너·재시도), 상세 버튼 try/finally, 개인정보 최소화 2건
(mine의 보호자 이름, open의 23필드 select 슬리밍), 위저드 렌더당 JSON.parse 제거.

### 1-4. 사용자 UI 피드백 — 총 9건 수정 (725d494 · be04495 · 2144fd8)
숫자 삐뚤(baseline=박스 바닥 함정 → 전 글자 1em 박스 통일), 비슷한 시설 스와이프
1번 튕김(**스냅 컨테이너 안 조건부 DOM 삽입 → 재스냅** — RoadviewThumb 상시 마운트
+opacity), 사진 모달 뒤로가기→메인 이탈(**닫힘=popstate 단일 경로**, §5-4 StrictMode
함정 회피), 마이페이지 기록 메뉴가 탈퇴 아래(셸 구조 버그 → GuardianRecordsMenu 분리),
필터 시트 하단 페이드, 매니저 역할 카드 순서+"매니저 프로필" 워딩, 툴팁 화면 밖
클램프(+화살표 역보정), 안심지수 칩 → **시설명 줄 오른쪽 끝**, 등급테스트 탭바
(인트로·결과 노출 / 진행 중 숨김 / **바닥 도달 시 등장+CTA 승강+히스테리시스** —
lib/tabBarVisibility 신규, §5-5 함정).
+ 보호자 퍼널 "시터"→"매니저" 통일 14곳, 비로그인 랜딩에 합의서·일지 항목
(**"계약서" 표현은 의도적으로 회피** — care-agreement-spec §1).

### 1-5. 리텐션 4종 (d59de10 · 87c5e47 · e8ddcfe · 97c405c)
- **찜 스낵바**: 찜 직후 "빈자리 알림 켜기" 원탭(자동 켜기 금지 — 수신동의).
  비로그인도 로컬 저장→로그인 병합.
- **일지 도착 알림(§3-12)**: 하루 첫 기록(정정 제외)에 보호자 메일. 중복 방지는
  "하루 1원본" 유니크 가드가 대신. 특이사항 메일과 두 통 금지.
- **비교함 가족 공유**: ?ids= 수신부는 이미 있었고 보내는 쪽만 신설.
  3단 폴백(네이티브 시트→클립보드→주소창 replaceState) — lib/shareLink.ts로 공용화.
- **매니저 공개 프로필 /manager/[id]**: 옵트인(publicProfileAt=동의 시각) 필수,
  끄면 1분 내 내려감(revalidate 60), 실명·연락처·계좌·국적·후기 본문 비노출.
  프로필 관리에 켜기/공유/미리보기/끄기.

### 1-6. 조사 + 돌봄일지 고도화 5종 (94284e7 · d8d3b8c · c01c06f · 1dd68e3)
경쟁사 4사(케어닥·케어네이션·이로움돌봄·케어링) 리텐션 구조 + 페인포인트 조사
(요지: 케어네이션=기록이 돈이 되는 구조/역경매, 케어링=직영+매일 공유,
이로움=정보도구형, **4사 모두 보호자→매니저 감정 보상 없음**; 보호자 1위 불만=추가
요금·정보 비대칭, 매니저=처우+**인정 부재**).
- **보호자 원탭 반응**(🙏😊💪, care-log-spec §9-1) — 읽음 확인의 다음 단계, 4사에 없는
  차별 장치. 매니저 오늘 카드에 반응 배지.
- 보호자 카드에 **복약·배변** 노출(기록은 받는데 표시 누락이었음).
- **일지 사진(§9-2)**: 스키마 예약분 실행 — capture 카메라 유도, 1280px WebP 클라
  축소, 우리 저장소 URL만 수용(주입 차단), sitter-photos 버킷 care-log/ 경로(§10-1 해소),
  동의 문구 상시.
- **확인서 일지 요약(§9-3)**: "기록 N일·특이사항 N건·사진 N장" — **읽음 처리 없는
  stats=1 모드**를 따로 만든 게 핵심(확인서 열람≠일지 읽음).
- **후기 화면 일지 요약**(§273 계획 실행): "기록 18일 · 식사 잘하신 날 15일".
- 복지용구 **잔액 계산기**(이로움 벤치마크 — 직접 입력 기준 명시, 연도별 초기화).

### 1-7. 역경매 매칭 (320624a · 74603ab)
- `CareRequestApplication` += proposedAmount·proposedUnit·message —
  지원 시트에서 제안 사례비(보호자 희망가 참고 표시)+한마디. **단위는 지원 시점 고정**
  (요청이 OPEN 중 수정돼도 제안 뜻 불변). 상한 100만원(오타 방지).
- `SitterProfile` += gender·ageBand — 프로필 관리에서 밴드 버튼만(정확 나이 안 받음),
  "보호자가 고를 때 참고" 안내. 서버 화이트리스트.
- 보호자 지원자 카드: 제안 배지 강조 + 한마디 인용 + 성별·연령대 + **돌봄일지 N일**
  (원본만 세서 정확 — 일지가 실적이 되는 연결).
- **합의서 정합**: 확정된 제안 금액이 있으면 그 값을 봉인(§1 선 유지 — 기록만).
- **재제안**: 지원완료+OPEN 동안만(PATCH action=propose, 확정 후 409). 지원 현황 탭에
  내 제안 표시+수정 시트. [id] PATCH를 액션별 권한 분기로 재편.
- 새 지원자 메일에 제안 금액·한마디 동봉. 일지 화면에 동기부여 박스(지어낸 수치 금지 —
  "기록일 수가 실적으로 보인다"는 사실만).

### 1-8. 시나리오 감사 2차 + 결정 (1dd68e3 · aa7cbd0)
전체 여정(등록→지원→재제안→확정→합의서→일지→완료→후기/취소 분기) 추적:
- **취소 알림(§3-13)** — 알림 스펙 §6-5의 마지막 빈 곳 실행. 미선정과 구분되는 문구
  ("지원 내용에 문제가 있었던 게 아니에요").
- 지원자 목록 정렬 명시(먼저 지원한 순 — DB 순서 의존 제거).
- **사용자 결정 명문화**: 취소된 돌봄의 합의서 비노출은 의도된 동작(스펙에 못 박음 —
  다음 세션이 "고치지" 말 것). 정정 시 보호자 반응 리셋도 의도 유지.

## 2. 다음에 할 일
§⚡ 참조. 추가 백로그: 카톡 발송(대형) · 웹푸시 · 지역 SEO 페이지에 실사진 ·
Prisma 마이그레이션 베이스라인(스키마를 계속 db push로 늘리는 중 — 이 세션만 6필드) ·
최근 본 시설 · dev 스트리밍 404 상태코드(전 라우트 공통, 낮음) ·
사진 서명 URL 업그레이드(민감도 오르면) · GPS/정산/AI 요약은 **법적 검토 선결**(변경 금지).

## 3. 이 세션에서 배운 함정 (다음 세션 필독)

1. **숨김 브라우저 팬은 프레임이 없다** — rAF·IntersectionObserver·scroll 이벤트·
   CSS smooth scroll 전부 멈추거나 유실된다. scrollTo는 `behavior:'instant'`로 강제해야
   움직이고, 그래도 scroll "이벤트"는 렌더링 스텝에 묶여 안 올 수 있다. 물리·스크롤
   검증은 실기기 몫으로 명확히 구분할 것(13차 §5의 백그라운드 탭 함정의 확장판).
2. **db push 워크플로에선 스키마에 못 적는 DB 객체(부분 유니크 인덱스 등)를 만들지
   말 것** — 다음 db push가 소리 없이 지울 수 있다. 대신 advisory xact lock 같은
   앱 레벨 직렬화를 쓴다(care-requests POST에 전례).
3. **StrictMode 이중 실행에서 effect 정리에 부수효과(history.back 등)를 넣으면 자기
   자신을 파괴한다** — 사진 모달이 열리자마자 닫혔다(실측). 정리에서는 리스너만 걷고,
   닫힘 경로를 popstate 하나로 통일했다. 같은 계열: **setState updater 안에서 외부
   스토어를 만지면 갱신이 유실된다**(updater는 순수해야 — 등급테스트 탭바에서 실측).
4. **overflow:hidden inline-block의 baseline은 글자 기준선이 아니라 박스 바닥** —
   숫자 슬롯과 평문을 섞으면 삐뚤어진다. 전부 같은 1em 박스로 통일하면 기준 자체가
   사라진다(StatsStrip).
5. **스냅 컨테이너 안에서 조건부 DOM 삽입 금지** — 브라우저가 직전 스냅 대상으로
   재스냅해 스크롤이 튕긴다(비슷한 시설 캐러셀에서 실측). 상시 마운트+opacity 전환
   +contain으로 격리.
6. **스트리밍 라우트의 404는 generateMetadata에서 notFound를 던져야** 상태코드가
   진짜 404가 된다 — 본문 렌더 중엔 이미 200이 나갔다(전 라우트 공통 특성).
7. **캡션 기반 사진 판정 3함정**: 시설명 속 낱말 오탐(stripName 필수), 감점 회피처로
   프로그램 사진이 승격되는 역효과(영역 서열+승격 금지), "오시는 길"은 대부분 지도다.
8. **prisma generate 후 dev 서버 재시작**(10차 §5-4 재확인 — 이 세션에서 3회 반복).

## 4. 커밋·배포 상태
**미커밋 0. 전부 배포 라인에 있음.** 이 세션 17커밋(05b950c→aa7cbd0) + Vercel 자동
배포(05b950c·725d494는 프로덕션 라이브 실측 확인, 이후 커밋들은 자동 배포 진행).
첫 커밋(05b950c)이 12·13차 이월분 78파일을 포함한다.

## 5. DB 변경 (전부 가산형·무중단, db push로 프로덕션 반영 완료)
- `SitterProfile` += `publicProfileAt`(공개 동의 시각) · `gender` · `ageBand`
- `CareLog` += `guardianReaction` (photos는 기존 예약 필드를 사용 시작)
- `CareRequestApplication` += `proposedAmount` · `proposedUnit` · `message`
- 데이터 변경: `Facility.extra.photos/photoItems` 6,056곳 채워짐 + 375곳 대표 순서 교체,
  `hero-slides` 캐시 키 v3.

## 6. 로컬 환경 메모
- 원본 zip 2개·photo-data files/ — **삭제 승인됨**(jsonl 6개만 보관).
- `.env.local.bak-*`은 gitignore에 추가돼 커밋 안 됨(시크릿 포함 주의).
- dev 서버는 세션 종료 시점에 preview로 떠 있었음(3000). LAN 폰 테스트 시 재기동 필요할 수 있음.
- 이 세션에서 쓴 검증용 스크래치: 운영자 테스트 프로필(닝닝이)을 공개 프로필 테스트에
  잠시 켰다가 **비공개로 원복**해둠 — 공개는 소유자가 프로필 관리에서 직접 켜는 것.
