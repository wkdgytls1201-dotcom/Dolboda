# 작업 규칙

- **기존 기능 제거는 반드시 확인받고 진행할 것.** 리팩터링·정리 과정에서 이미 있던 기능(화면, API, 필드, 동작)을 없애야 할 것 같으면 먼저 사용자에게 물어보고, 승인 전에는 제거하지 않는다. 이 규칙은 다른 모든 판단보다 우선한다.
- **앱 성능 최적화가 최우선 목표 중 하나다.** 새 화면·기능을 만들 때마다 번들 크기, 응답 페이로드 크기, 불필요한 리렌더·재요청을 항상 의식할 것. "일단 되게 만들고 나중에 최적화"가 아니라 만들 때부터 가볍게 만든다.
- **SEO·GEO(AI 검색 노출)도 최우선 목표 중 하나다.** 새 페이지를 추가할 때 메타데이터·구조화 데이터(JSON-LD)·sitemap 반영을 기본 작업 범위에 포함시킨다. 나중에 따로 붙이는 게 아니라 처음부터 같이 간다.
- **최대한 가볍게(lightweight) 만들 것.** 불필요한 의존성 추가, 큰 클라이언트 번들, 과도한 이미지·데이터 전송을 피한다. 같은 결과를 낼 수 있으면 더 무거운 방법보다 가벼운 방법을 고른다.
- **본문이 잠깐 짧아지는 순간(로딩·전환·복귀)에 푸터가 올라와 보이는 계열 버그** — 전역 일반해가 이미 깔려 있다: 루트 레이아웃이 본문을 `min-height:100dvh`로 감싼다(2026-08-05, 같은 버그 4번 재발 후). 새 화면에서 이 계열이 또 보이면 개별 스페이서보다 이 래퍼가 살아 있는지부터 확인.
- **새 라우트를 만들면 `loading.tsx`를 같이 만든다.** 없으면 서버 응답이 올 때까지 화면이 이전 상태로 멈춰 있어 "눌렀는데 아무 일도 안 일어난다"로 보이고, 본문이 빈 사이 푸터가 화면 위로 올라온다(실제로 두 증상 다 겪었다 — 11차 §1-B). 목록·상세처럼 모양이 정해진 화면은 로고 로더보다 **실제 레이아웃 모양의 뼈대**가 훨씬 빠르게 느껴진다.
- **dev 서버가 떠 있는 채로 `npm run build`를 돌리지 말 것.** 둘이 같은 `.next`를 쓰기 때문에 프로덕션 빌드가 dev 청크를 덮어써 dev 서버가 전 라우트 500(MODULE_NOT_FOUND)으로 죽는다(2026-08-05 실측). 빌드 검증이 필요하면 dev를 내리고 하거나, 빌드 후 dev를 재시작한다.
- **렌더 도중 `localStorage`·`sessionStorage`·`window`를 읽지 말 것.** 서버에는 그 값이 없어 하이드레이션이 깨지고 화면이 통째로 다시 그려진다. 복원은 `useLayoutEffect`에서 하고(깜빡임 없음), 복원 전에는 저장 effect가 돌지 않게 막는다(초기값이 저장분을 덮어써 데이터가 날아간다). 같은 버그를 검색 페이지·등급테스트에서 두 번 겪었다.

## UI 제작 원칙 (앞으로 새로 만드는 화면부터 적용, 2026-08-03)

- **긴 한글 문장은 문장 단위로 줄바꿈한다.** 브라우저 기본 줄바꿈에 맡기면 조사·어절 중간에서 끊겨 어색해진다. 문장·구 경계에서 끊어지는 파서(또는 `word-break: keep-all` + 의미 단위 수동 개행)를 써서, 화면 폭이 바뀌어도 문장이 이상한 자리에서 끊기지 않게 한다.
- **재사용 컴포넌트는 처음부터 크기(size) variant를 props로 연다.** 아이콘 배지·카드 같은 에셋을 만들 때 sm/md/lg 같은 뎁스 구조로 설계해두면, 나중에 다른 화면에서 다른 크기로 쓸 때 새로 만들지 않고 그대로 재사용할 수 있다.
- **레이아웃 반응형은 뷰포트 폭이 아니라 의미 단위 기준으로 짠다.** 숫자로 정한 브레이크포인트보다 "이 정보 덩어리가 한 줄에 있어야 뜻이 통하는가"를 먼저 판단하고 그 경계에서 줄을 바꾼다.
- **모바일이 최우선이다(안드로이드·iOS 앱화 예정).** 새 화면은 360~430px 모바일 뷰포트를 기준으로 먼저 만들고 데스크톱은 나중에 확장한다. 터치 타깃 44px 이상, safe-area(`env(safe-area-inset-*)`) 대응을 기본으로 한다.

- **우리가 직접 쌓는 데이터로 지표·리포트를 만들기 전엔 [docs/data-assets-spec.md](docs/data-assets-spec.md)를 먼저 읽을 것.**
  무엇을 쌓고 있고(자리 시계열·조회수·비급여 기준값·돌봄 기록), 어디에 쓰며,
  **무엇을 팔지 않는지**(§5 — 신뢰 데이터 자체는 팔지 않는다)가 정리돼 있다.
  공통 원칙 둘: **표본이 얇으면 지표를 만들지 않는다**(null → 섹션 숨김),
  **관측된 사실과 추정을 문장에서 구분한다**. 자리 추이 지표의 임계값을 바꾸려면
  [docs/vacancy-insight-spec.md](docs/vacancy-insight-spec.md) §4(하면 안 되는 것)를 먼저 볼 것.
- **돌보다 공공데이터 기반 안심지수(`lib/dolbodaScore.ts`)는 핵심 자산이다.** 가중치·신호 매핑·보정 로직을 바꿀 때는 반드시 [docs/ai-score-spec.md](docs/ai-score-spec.md)의 매핑 표와 변경 이력을 함께 갱신하고, 사용자 확인 없이 가중치를 바꾸지 않는다.
- **돌봄 포인트(`lib/pointsConfig.ts`)는 법적 설계가 걸린 기능이다.** 금액·지급 조건을 바꾸기 전에 [docs/points-spec.md](docs/points-spec.md) §1을 읽을 것 — 환전·현금성 금지, "완료가 아니라 보호자의 감사에 지급"이 선이다(별의 우선 노출은 2026-08-06 사용자 결정으로 허용됨 — 신뢰 데이터 자체는 팔지 않는다). 값 변경 시 그 문서와 약관 제12조의2를 함께 갱신한다.
- **근무환경 지수(`lib/workIndex.ts`)도 같은 급의 자산이다.** 매니저용 지표로, 수정 시 [docs/work-index-spec.md](docs/work-index-spec.md)를 함께 갱신한다. 구간 기준은 실제 데이터 분포(사분위수)에서 뽑은 값이라 임의로 바꾸지 않는다.

- **돌봄 합의서(`lib/careAgreement.ts`)는 법적 포지션이 걸린 기능이다.** 조항 문구나 흐름을 바꾸기 전에 반드시 [docs/care-agreement-spec.md](docs/care-agreement-spec.md) §1을 읽을 것. 회사는 "표준 양식 제공 + 서명 기록 보관"까지가 선이고, 이 선을 넘어 계약 당사자가 되면 직업안정법상 유료직업소개사업 등록 문제가 생긴다.
- **상태를 바꾸는 API에는 반드시 현재 상태 가드를 넣을 것.** 화면에서 버튼을 숨기는 것만으로는 막히지 않는다. 돌봄 흐름의 단계별 상태·주체·가드는 [docs/care-flow-spec.md](docs/care-flow-spec.md)의 표를 먼저 대조한다(2026-08-05에 5차 §7에서 승격 — 역경매·취소 알림 반영된 최신본).
- **알림(이메일 발송) 관련 코드를 건드리기 전엔 [docs/alert-system-spec.md](docs/alert-system-spec.md)를 먼저 읽을 것.** 알림 스크립트가 4개(`scripts/send-*.mjs`)로 나뉘어 있고 "사람당 메일 한 통"·"발송 전 기록"·"지역 표기는 REGIONS 17개 기준" 같은 전역 원칙이 있다 — 새 알림을 아무 스크립트에나 끼워 넣거나 이 원칙을 깨면 중복 발송·조용한 실패로 이어진다.
- **기업회원 콘솔·대시보드(`/business/console`)를 고칠 땐 [docs/business-console-spec.md](docs/business-console-spec.md)를 먼저 읽을 것.** 콘솔의 "최근 30일 대비" 증감과 월간 성과 보고서의 "지난달 대비" 증감은 **창(rolling 30일 vs 캘린더 월)이 다르다** — 계산 함수(`lib/deltaPct.ts`)는 공유하지만 섞어 쓰면 숫자가 안 맞는다. 정보 정정 요청 관련 문구만 "기업회원" 대신 "시설 운영자"를 쓰는 것도 의도된 예외다.

## 인수인계 문서 (최신순)

**새 세션은 [HANDOVER-2026-08-06-16차.md](HANDOVER-2026-08-06-16차.md)부터 읽으세요.**

⚡ **16차 문서 맨 위 "다음 세션에서 바로 시작할 일"을 먼저 확인하세요** —
① Resend 발송 로그 확인(아직 미확인 — `AlertDelivery` 0행) ② **iOS Safari 실기기
검수 미완**(사용자·나 모두 아이폰 없음, 코드 방어는 전부 넣음 — §3-A 체크리스트)
③ 자리 인사이트는 **8/9(관측 7일차)부터** 화면에 뜬다 ④ 개발 순위: GA4 → 사진 zip,
**카카오톡은 사용자가 "마지막"으로 명시.**
**미커밋 0 — 14커밋 전부 배포 라인.** 16차 §3(함정 11개 — 부분 실패가 데이터를
오염시킨다, 같은 조회 복붙 금지, 화면·서버가 같은 판단 함수를 쓸 것, tailwind config는
재시작 필요 등)과 §6(검증 시나리오 122케이스 목록)은 코드 작업 전 필독.
**★ 원본 사진 zip 3개 삭제 가능** — 16차 §0.

[15차](HANDOVER-2026-08-06-15차.md)는 **돌봄 포인트·지도 앱형·모바일 메뉴·일지 2·3차**
입니다(§2 사용자 방침 — 매니저 법적 거리·별 상단 노출 허용 — 과 §3 함정 10개는 여전히 유효:
dev 켠 채 build 금지, 전남광주 통합 표기, 전역 폰트 상향 시스템 등).
[14차](HANDOVER-2026-08-05-14차.md)는 **메인 고급화 + 경기도 사진 + 돌봄 흐름 감사 +
역경매**입니다(§3 함정 8개 여전히 유효 — 숨김 팬 검증 한계, db push에서 수동 인덱스
금지, StrictMode 정리 부수효과, 스냅 컨테이너 DOM 삽입 금지 등).
[13차](HANDOVER-2026-08-05-13차.md)는 사진
파이프라인 상세(순서·인코딩 함정) — 다음 지역 zip을 처리한다면 13차 §1-A·§3 필독.
[12차](HANDOVER-2026-08-04-12차.md)는 **기업회원(B2B) 전면 개편** — B2B 콘솔·요금제·명칭을
건드린다면 12차와 [docs/business-console-spec.md](docs/business-console-spec.md)를 같이 보세요.
[11차](HANDOVER-2026-08-04-11차.md)는 **체감 속도·UI·버그**, [10차](HANDOVER-2026-08-04-10차.md)는
**알림 시스템**이라 성격이 다릅니다 — 알림 쪽을 건드린다면 10차와
[docs/alert-system-spec.md](docs/alert-system-spec.md)(§3-13까지, 단일 진실 공급원)를 같이 보세요.
[5차](HANDOVER-2026-08-02-5차.md)의 **§1(함정)·§3(규칙)** 두 절도 여전히 유효합니다.
5차 §7의 상태 전이 지도는 [docs/care-flow-spec.md](docs/care-flow-spec.md)로 승격됐습니다(2026-08-05) —
역경매·취소 알림이 반영된 그쪽이 최신이니 5차 §7 표는 더 이상 대조 기준으로 쓰지 마세요.

그 이전 문서는 배경 확인용으로만 참고: [9차](HANDOVER-2026-08-04-9차.md) → [8차](HANDOVER-2026-08-03-8차.md) → [7차](HANDOVER-2026-08-03-7차.md) → [6차](HANDOVER-2026-08-02-6차.md) → [4차](HANDOVER-2026-08-02.md) → [3차](HANDOVER-2026-08-01.md) → [2차](HANDOVER.md)

- **시설 사진 파이프라인(`scripts/photo-import.mjs`·`lib/r2Storage.ts`)을 건드리기 전엔
  13차 §1-A·§3을 먼저 읽을 것.** 순서가 생명이다: inventory → convert → extras →
  upload. extras가 끝나기 전에 upload를 돌리면 여분 사진이 조용히 누락된다. 원본 zip
  삭제 판단은 5개 조건(변환·extras·업로드 완료 + 화면 검증 + jsonl 3종 보관) 전부
  충족 후 사용자에게 명시적으로 알린 다음에만.
