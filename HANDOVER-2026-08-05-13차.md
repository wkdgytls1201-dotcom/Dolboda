# 돌보다 인수인계 — 13차 세션 (2026-08-05)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다.
> 12차(B2B 개편·아티팩트)에 바로 이어진 세션이고, 이번 세션의 몸통은 셋입니다:
> **① 시설 사진 대량 파이프라인(R2) — 실제 가동 중**, **② 검색 필터 물리형 바텀시트**,
> **③ 메인페이지 모바일 고급화(진행 중 — 배너 리워크 직전에 중단)**.
>
> 이전: [12차](HANDOVER-2026-08-04-12차.md) — B2B·아티팩트 8종. 5차 §1·§3·§7, 10차(알림),
> 11차(체감속도) 여전히 유효.

---

## ⚡ 다음 세션에서 바로 시작할 일

1. **R2 업로드 완료 확인** — 13차 세션 종료 시점(1of2: 1,220 / 2of2: 1,700, 프로세스
   2개 생존)에 백그라운드로 계속 돌던 중이었다. **업로드는 OS 프로세스라 세션과
   무관하게 계속 돈다.** PC가 안 꺼졌다면 완료됐을 것. 확인:
   ```
   node -e "const fs=require('fs');for(const d of['경기도_1of2','경기도_2of2']){let n=0;try{n=JSON.parse(fs.readFileSync('C:/Users/linea/photo-data/'+d+'/upload-progress.json','utf8')).length}catch(e){};console.log(d,n)}"
   ```
   - 1of2가 2,509 미만이거나 2of2가 3,563 미만이면 **그냥 upload를 다시 돌리면 이어서 한다**:
     `node scripts/photo-import.mjs upload "C:/Users/linea/photo-data/경기도_1of2" --write`
     (2of2도 동일. 중단 재개 안전 — upload-progress.json이 지킨다)
   - 완료 후 무작위 시설 몇 곳 상세페이지에서 사진 확인 → **그때 사용자에게 "원본 zip
     지워도 된다"고 말해줄 것** (사용자가 명시적으로 이 확인을 기다리고 있음. §3-1 조건 참조)
2. **메인페이지 HeroBanner — ⚠️ 사용자가 "다른 곳(다른 세션)에서 배너 작업은 하겠다"고
   명시함(2026-08-05).** 이 라인에서는 HeroBanner.tsx를 건드리지 말 것(충돌 위험).
   §2-1의 명세는 그 다른 세션이 참고할 자료다. StatsStrip·Reveal 리워크는 이미 이
   라인에서 끝나 미커밋 상태로 있으니, 다른 세션이 같은 파일을 만지면 겹칠 수 있음을
   사용자에게 알릴 것.
3. 사용자가 폰으로 바텀시트 손맛 테스트 예정 — 같은 와이파이에서
   `http://192.168.219.204:3000/search` (dev 서버 0.0.0.0 바인딩·방화벽 허용 확인 완료).
   피드백 오면 BottomSheet.tsx 상수 3개(강성 420/감쇠 40/플릭 0.5) 튜닝.

---

## 0. ★ 사용자가 직접 해야 할 일 (이월)

- 🔴 **네이버 로그인 프로덕션 반영** — Vercel env + 네이버 검수. 안 하면 배포 후 버튼 죽음.
- 🟡 GitHub Actions 시크릿(DIRECT_URL 등) — 일일 동기화·알림 4종이 전부 여기 걸림.
- 🟡 Vercel ADMIN_USER_IDS, 공공데이터 제공신청, 서치콘솔 canonical(8/6 예약 — 내일!).
- 🆕 **R2 프로덕션 반영** — 로컬 `.env.local`에만 R2 키 5개가 있다. **Vercel에도 같은
  5개 환경변수를 등록해야 배포본에서 사진이 뜬다** (R2_ACCOUNT_ID, R2_ACCESS_KEY_ID,
  R2_SECRET_ACCESS_KEY, R2_BUCKET=facility-photos, R2_PUBLIC_URL). 사진 URL 자체는
  DB에 절대경로로 저장돼서 프론트는 키 없이도 표시되지만, 콘솔 업로드 API가 R2를 쓰게
  바꾸는 날엔 필수. ⚠️ r2.dev 공개 URL은 개발용 속도 제한이 있어 **트래픽 늘면
  img.dolboda.kr 커스텀 도메인 연결 필요**(§3-4).

## 1. 이 세션에서 한 일

### 1-A. 시설 사진 대량 파이프라인 — 이 세션의 최대 성과

**공단 사진 zip(경기도 2개, 총 72GB)을 받아 분류→리사이징→R2 업로드→DB 반영까지
실제 가동시켰다.** 상세페이지·검색카드에 실사진이 뜨는 것까지 검증 완료.

- **데이터 형식(실측)**: `41_경기도/{시설명}({기관기호})/{시설명}_{카테고리}_{번호}_{설명}.jpg`
  - 기관기호가 폴더명에 있어 DB `extra.instCode`와 **정확 매칭**(경기도 6,509곳 중 93% 매칭)
  - 카테고리 3종뿐: 시설구조·전경 / 시설설비상태 / 프로그램
  - zip 파일명 인코딩은 **UTF-8이 정답** (CP949 아님 — §3-2)
- **스크립트** (`scripts/`):
  - `photo-classify.mjs` — 캡션 키워드로 9영역(`FacilityPhotoArea`) 분류. **캡션에서
    시설명을 먼저 제거**해야 함("정원"노인요양원→outdoor 오분류 실측·수정). BMP 등
    실패 시 다음 후보로 넘어가는 heroCandidates/backup 구조.
  - `photo-import.mjs` — 4개 서브커맨드, 전부 중단·재개 안전:
    - `inventory` — 이미지 안 읽고 전체 목록만 jsonl로 (원본 지운 뒤의 유일한 기록)
    - `convert` — 시설당 최대 12장(hero1+갤러리8+프로그램3) 선별 변환. manifest.jsonl에
      srcPath 기록(extras가 정확히 제외하는 데 필요)
    - `extras` — 선별 안 된 나머지 40%를 낮은 화질로 변환(e*.webp) — "나중에 장수
      늘리기" 대비 보관용. **convert 완료 후에만 돌릴 것**(§3-3)
    - `upload` — R2 업로드 + DB(`Facility.extra.photos/photoItems`) 반영. extras는
      R2에만 올리고 DB엔 안 넣음
- **화질 확정(사용자 결정 — 원본 안 보관하므로 영구)**: 선별분 1000px/q72(장당 ~50KB),
  프로그램·여분 720px/q52(~25KB). 경기도 전체 ≈ 3.3GB, 전국 환산 ≈ 13.5GB → R2 월 ~70원.
- **저장소 = Cloudflare R2** (`lib/r2Storage.ts`, aws4fetch 서명): 트래픽 요금 0이 결정
  이유. 키는 `.env.local`에 있음(대화에 노출 안 함). 버킷 `facility-photos`, 공개 URL
  `https://pub-89b83aebf5a7456880545455a4cf1bb6.r2.dev`.
- **갤러리 UI 교체**: `FacilityPhotoGallery.tsx`(대표 1장만 첫 화면, "사진 N장 보기"
  버튼) + `FacilityPhotoModal.tsx`(신규 — 영역 탭·그리드·라이트박스, 팝업 열기 전 추가
  다운로드 0건). 실측: 상세페이지 R2 요청 1건(50KB)뿐.
- **카드 페이로드 수정**: `lib/facilityRepo.ts` toCardFacility가 photos를 12장 전부
  내려보내던 것 → **첫 장만**(300건 목록에서 URL 3,600개 낭비 방지).
- **진행 상태**: 변환·inventory·extras 전부 완료. 업로드만 진행 중(⚡ 참조).
  로컬 산출물: `C:/Users/linea/photo-data/경기도_{1of2,2of2}/` — **inventory.jsonl·
  manifest.jsonl·extras.jsonl 3파일은 영구 보관 필수**(합계 20MB 미만, 원본 지운 뒤
  유일한 사진 목록 기록).

### 1-B. 검색 필터 물리형 바텀시트 (완료·검증됨)

- `components/BottomSheet.tsx` **신규** — 3단 스냅(닫힘/중간60%/전체92%), 위치는
  ref+rAF로만 구동(드래그 중 React 리렌더 0), 임계감쇠 스프링, 속도 플릭(0.5px/ms,
  1.6 이상이면 전체→닫힘 스킵), 상단 러버밴드 15%, 콘텐츠 스크롤 소유권(중간에서
  위로=확장, 전체+최상단에서 아래로=하강), 애니메이션 중 터치 시 즉시 인계,
  visualViewport 대응, ESC/포커스트랩/복귀, reduced-motion 폴백, 데스크톱(sm+)은
  기존 중앙 모달. 합성 이벤트로 물리 전 분기 검증함.
- `components/FilterBar.tsx` **전면 재작성** — draft(임시)↔적용 상태 분리. 시트 안
  변경은 "N곳 보기" 눌러야 적용, 닫으면 폐기. 실시간 결과 수: 서버 범위(유형·프로그램
  태그)가 달라지면 그 범위로 가볍게 재조회(로딩 중 이전 숫자 유지), 0곳이면 버튼
  비활성+안내. 시트에 안심지수·정렬 섹션 추가(스펙 순서).
- `lib/facilityFilters.ts` — SearchPageClient의 필터 파이프라인을 순수 함수로 추출
  (`filterFacilityList`/`filterAndSortFacilityList`) — 목록과 임시 카운트가 같은 규칙 공유.
- `lib/useFlipGrid.ts` **신규** — 필터 적용 시 살아남은 카드 FLIP 이동(WAAPI,
  화면 근처만, 40장 초과 시 생략, reduced-motion 제외).

### 1-C. 메인페이지 모바일 고급화 (진행 중 — 사용자의 대형 스펙)

사용자가 매우 상세한 스펙을 줬다(원문은 이 대화에만 있음 — 요지: CSS Scroll Snap 배너,
숫자 digit reel, 카드 진입 공통화, View Transition, SEO 훼손 금지, 새 라이브러리 금지,
transform/opacity만, 1~3단계 우선순위).

**끝난 것**:
- 0단계 분석 완료 — 구조는 §2-1 참고
- `components/StatsStrip.tsx` **재작성** — 0부터 카운트업(스펙 금지) 제거. 서버 숫자
  상시 표시 + 마지막 3자리만 짧게(0.55s) 구르는 digit reel(aria엔 최종 숫자만,
  tabular-nums+1ch 고정폭으로 CLS 0, reduced-motion 시 정지). 카드 3장 → 구분선 있는
  단일 패널 + 아이콘 + "공공데이터 기준 · 매일 갱신" 라벨(daily-nhis-sync 실재하므로 진실).
- `components/Reveal.tsx` — 카드마다 만들던 IntersectionObserver를 **모듈 레벨 공유
  옵저버 1개**로 통합(등장 후 unobserve). IO 없는 웹뷰에선 숨기지 않는 가드 추가.

**중단 지점 — HeroBanner.tsx 재작성 직전**. §2-1에 명세.

### 1-D. B2B 아티팩트 8종 후속 개선 (완료)

12차에서 만든 아티팩트에 이번 세션 추가분:
- 공용 스티키 네비게이션(7개 고객용 페이지, 요금제를 맨 오른쪽으로), 스크롤 리빌
- 모바일 표 UX: 첫 컬럼 sticky + scroll-snap + 우측 페이드 + "옆으로 밀기" 힌트
  (경쟁사비교·기능매트릭스)
- 기능 매트릭스 **데이터 재정의**(사용자 지시): 시설소개영상 삭제, 플러스에서 프로그램
  소개·상세통계 해제, 프리미엄에 홈·가이드배너 추가, 전담배정·분기미팅 행 삭제 →
  **요금제 페이지 카드도 이에 맞춰 수정**(실버타운 중복 항목 정리 포함)
- "케어닥" → "경쟁사" 표기 통일(비교 페이지·대응 매뉴얼)
- 대응 매뉴얼: status 코드 노출을 사람 제목+작은 코드 태그로, "말할 것/근거"를
  라벨-위/본문-아래 구조로 재배치, 문장 단위 개행
- 안심지수 방법론: 등급 스케일 라벨 겹침 → 구간 폭 비례 flex로 구조적 해결
- 전 페이지 word-break:keep-all, 요금제 카드 그리드 align-items:start
- ⚠️ 재발행 실수 1회: 컨텍스트 압축 후 세션이라 `url` 파라미터 없이 발행해 **중복
  아티팩트 7개가 생성됐다가 원본 URL로 재발행으로 복구**. 잘못 생성된 7개는
  claude.ai/code/artifacts에 남아 있음(사용자가 수동 삭제해야; 세션 도구에 삭제 없음).

### 1-E. 기타

- 시설 상세 워딩(사용자 지시): 섹션 제목 "돌보다 공공데이터 기반 안심지수" →
  **"공공데이터 기반 안심지수"**, 카드 배지 → **"돌보다 AI기반 안심지수"**
  (FacilityDetailClient.tsx·DolbodaScoreCard.tsx 두 곳만; data-policy 등 다른 페이지는
  의도적으로 유지)
- 사진 갤러리용 스키마는 **이미 lib/types.ts에 있었다** — FacilityPhotoArea 9종·
  PHOTO_AREA_ORDER("메인은 반드시 외관" 주석 포함). 새로 만들지 않고 채웠다.
- devDeps 추가: sharp, yauzl / deps: aws4fetch

## 2. 다음에 할 일

### 2-1. HeroBanner.tsx 재작성 (중단 지점 — 명세)

현재 파일의 문제(분석 완료): ①카드가 `role="button"` div — **실제 `<a href>` 아님(SEO
스펙 위반)** ②onScroll마다 전체 children 순회+setIndex ③자동슬라이드 5초·탭 비활성/
화면 밖 정지 없음 ④비활성 opacity 0.5(과함) ⑤점 표시가 width 전환.

만들 것: ①`<a href="/facility/id">` + onClick preventDefault→ViewGate(스와이프 10px
이상이면 이동 취소, 중복 이동 가드) ②활성 판정을 IO(root=track, threshold 0.6)로
③자동슬라이드 7초+, document.hidden·배너 화면 밖(IO)·상호작용 후 12초·reduced-motion
정지 ④scale 0.97/opacity 0.85 ⑤진행 표시 transform 기반 ⑥키보드 화살표. 이미지
mountedSlides(index 기반 마운트)·fetchPriority 첫 장 high는 **유지**(이미 잘 돼 있음).
2단계로 View Transition(클릭 카드에만 view-transition-name 동적 부여, 미지원 폴백),
3단계 스와이프 이미지 미세 이동은 성능 확인 후.

이후: 내주변 위치 대기 스켈레톤(고정 높이), FacilityCard 터치 피드백(active:scale-[0.985])
확인, 검증(§29 태스크: tsc·빌드·320~430px·reduced-motion·Lighthouse 전후).

### 2-2. 사진 파이프라인 나머지

- 업로드 완료 확인 → 사용자에게 원본 삭제 OK 통보(⚡1)
- **다음 지역 zip 처리 절차(확립됨)**: 사용자가 zip을 로컬에 놓음 →
  `inventory` → `convert` → `extras` → `upload --write` → 로컬 files/ 삭제(jsonl 3종은
  보관) → 사용자에게 원본 삭제 OK. 지역당 약 1~2시간(변환 30분+업로드 60분+).
- 콘솔 소유자 업로드 API(`app/api/business/photos/route.ts`)는 아직 Supabase — R2로
  통일할지 결정 필요(supabaseStorage와 r2Storage 인터페이스 거의 동일하게 맞춰둠).
- FacilityCardSkeleton/카드에 사진 유무 섞일 때 레이아웃 확인.

### 2-3. 12차에서 이월

이메일 템플릿 라이브러리(아티팩트 9번째 후보), tailwind 팔레트 수정 실기기 확인,
커밋 범위·타이밍 결정(이제 50파일+ 미커밋).

## 3. 이 세션에서 배운 함정

1. **"원본을 지워도 되냐"는 질문에는 체크리스트로 답한다** — 변환완료+extras완료+
   업로드완료+화면검증+jsonl보관 5개를 조건으로 명시하고, 사용자에게 "지워도 될 때
   내가 말해주겠다"고 약속했다. 전부 충족 전에 지우면 복구 불가.
2. **공단 zip 파일명은 UTF-8이다.** unzip 기본(CP437 해석)이 깨진 걸 보고 CP949로
   변환하면 이중으로 망가진다. ZIP 로컬 헤더의 원시 바이트를 직접 읽어 확인했다 —
   .NET ZipFile은 기본으로 올바르게 읽는다. yauzl도 문제없음.
3. **extras는 convert가 끝난 뒤에만.** upload가 시작 시점의 extras.jsonl만 읽으므로,
   extras 진행 중에 upload를 돌리면 이후 생성분이 영영 안 올라가고 재실행해도
   progress에 막혀 건너뛴다. 실제로 한 번 저질렀다가 0곳 시점에 잡아서 무사.
4. **r2.dev 공개 URL은 개발용 — 속도 제한 있음.** 실서비스 트래픽엔 커스텀 도메인
   (img.dolboda.kr)을 R2 버킷에 연결해야 한다. URL 치환은 DB 일괄 UPDATE로 가능.
5. **백그라운드 탭에서는 setTimeout이 심하게 지연된다** — 합성 이벤트로 "천천히
   드래그"는 재현되지만 "중간 속도 플릭"은 재현 불가(타이머가 밀려 속도가 0에 수렴).
   동기 디스패치는 반대로 초고속이 된다. 물리 코드의 속도 분기를 자동화로 다
   검증하려 하지 말고, 경계 상수만 다른 동일 코드 경로임을 확인하면 충분.
6. **setPointerCapture는 try/catch로 감싼다** — 합성 이벤트·일부 브라우저에서
   NotFoundError를 던져 드래그 핸들러 전체가 죽을 수 있다.
7. **Cloudflare 가입 직후 API 토큰 발급은 "Please verify your email"에 막힌다** —
   이메일 인증을 해도 **로그인 세션이 미인증 상태를 캐시**하므로 로그아웃→재로그인해야
   풀린다. Access Key/Secret은 생성 화면에서만 보이고 목록에선 다시 못 본다(Roll로 재발급).
8. **대량 사진을 붙이면 목록 API 페이로드를 다시 봐야 한다** — 카드는 첫 장만 쓰는데
   photos 배열 전체(12 URL)가 300건×12개씩 실려 나가고 있었다. toCardFacility에서 잘랐다.
9. **긴 파이프라인은 단계마다 progress 파일** — convert/extras/upload 전부
   재개 가능하게 만든 덕에 PC 절전·중단·실수 재시작에도 데이터 손실이 없었다.

## 4. ⚠️ 미커밋 상태

**이 세션에서도 커밋 0회.** 12차 분(§12차-4) + 이번 세션 분이 겹쳐 50파일+.
이번 세션 신규 파일: `components/BottomSheet.tsx`, `components/FacilityPhotoModal.tsx`,
`lib/r2Storage.ts`, `lib/useFlipGrid.ts`, `scripts/photo-classify.mjs`,
`scripts/photo-import.mjs`, `scripts/photo-dryrun.mjs`, `scripts/photo-contactsheet.mjs`,
이 문서. 주요 수정: FilterBar(전면)·StatsStrip(전면)·FacilityPhotoGallery(전면)·
Reveal·SearchPageClient·facilityFilters·facilityRepo·DolbodaScoreCard·
FacilityDetailClient·.gitignore(tmp-photo-*)·package.json(sharp/yauzl/aws4fetch).

**임의로 커밋·배포하지 말 것.** 특히 배포 전에 Vercel R2 env(§0)와 프로덕션 DB에
사진 URL이 이미 들어가고 있다는 점(업로드가 프로덕션 DB에 직접 씀)을 같이 볼 것.

## 5. DB 변경

**스키마 변경 없음.** 데이터 변경 있음: `Facility.extra`에 `photos`(string[])·
`photoItems`({url,area,caption}[])가 업로드된 시설부터 채워지는 중(경기도 ~6,000곳
예정). 기존 필드 위에 스프레드로 얹으므로 다른 extra 값은 보존된다.

## 6. 로컬 환경 메모

- `.env.local`에 R2_* 5개 추가됨(+백업 .env.local.bak-*). 절대 커밋 금지(이미 gitignore).
- `C:/Users/linea/photo-data/` — 지역별 산출물. files/는 업로드 후 지워도 되고
  **jsonl 3종은 영구 보관**.
- 원본 zip: `C:/Users/linea/Desktop/건강보험 중요자료/01_경기도_1of2(완료).zip`,
  `C:/Users/linea/Desktop/02_경기도_2of2.zip` — **아직 지우지 말 것**(⚡1 완료 후).
- dev 서버가 LAN에 열려 있음: http://192.168.219.204:3000 (같은 와이파이 폰 테스트용).
