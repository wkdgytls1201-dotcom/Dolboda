# 돌보다 인수인계 — 9차 세션 (2026-08-04)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다.
> 이전: [8차](HANDOVER-2026-08-03-8차.md) · 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)는 여전히 유효합니다.

---

## 0. ★ 사용자가 직접 해야 할 일 (코드로는 못 하는 것)

### 0-1. 🔴 네이버 로그인 프로덕션 반영 — **안 하면 배포 후 네이버 버튼이 그대로 죽습니다**

- Vercel 프로젝트 → Settings → Environment Variables에 `AUTH_NAVER_ID` · `AUTH_NAVER_SECRET`
  등록(`.env.local`과 같은 값) 후 재배포
- 네이버 개발자센터에서 **검수(서비스 URL·Callback URL을 프로덕션 도메인으로) 완료됐는지 확인**.
  이 세션 중 사용자가 콘솔에서 직접 로그인 테스트까지는 했지만, 검수 상태 자체는 코드로 확인 불가
- 서비스 URL·콜백 URL에 로컬(`localhost:3000`)만 등록돼 있다면 프로덕션 콜백
  (`https://<프로덕션도메인>/api/auth/callback/naver`)도 추가 등록 필요

### 0-2. 🟡 이월 항목 (8차·7차에서 계속)

- GitHub Actions 시크릿(`RESEND_API_KEY`, `MAIL_FROM`) — 등록 여부 미확인. 안 넣으면
  알림 발송 워크플로가 사건만 세고 메일은 안 나감(안 깨짐)
- Vercel `ADMIN_USER_IDS` — 등록·재배포 확인
- 공공데이터 제공신청(장기요양기관 평가 세부항목 점수 최우선)
- 공단 사진 zip 재다운로드
- 서치콘솔 canonical 확인(8/6 예약 리마인드)

### 0-3. 🟢 테스트 데이터 정리(선택)

이번 세션에서 네이버 테스트 계정("교덩")으로 돌봄요청 마법사 실계정 완주 검증을 하며
실제 `CareRequest`가 1건 생성됨(서울·병원간병·2026-08-04~17). 실서비스 데이터가 아니므로
필요하면 지워도 됨 — 사용자가 요청하기 전까지는 건드리지 않았음.

---

## 1. 이 세션에서 한 일

### 1-A. 네이버 로그인 추가 (`c832b08`)

카카오는 이메일 제공 동의가 꺼져 있으면 이메일을 안 준다 — 알림을 보낼 주소가 없는 계정이
생기는 근본 원인이었다(8차 §2-1에서 지목했던 항목). 네이버는 이메일이 기본 제공이라 채택.

- `auth.ts`에 `Naver` 프로바이더 추가, `allowDangerousEmailAccountLinking: true`,
  JWT/세션에 `provider` 노출(`"kakao"`/`"naver"`, 옛 토큰은 `"kakao"`로 폴백)
- `components/AuthModal.tsx` — 네이버 버튼을 카카오보다 위에 배치(이메일 확보율이 더 높아서)
- `app/mypage`, `app/privacy`, `app/terms`에 프로바이더별 표기 반영
- 구글 로그인은 이번 세션에서 검토했다가 **다시 뺐다** — 카카오+네이버 두 개로 충분하다는
  사용자 판단(8차 §2-1의 "구글 로그인 다음 우선순위"는 이걸로 종료)

### 1-B. 법적 문구 · 개인정보방침 정비 (`9243777`)

- `components/GradeTest.tsx` — "돌보다가 직접 연결해드리는 돌봄 인력" 식 문구를
  "돌보다에서 보호자와 매니저를 연결해드리는 서비스"로 통일(다른 화면과 같은 패턴).
  회사가 계약 당사자가 아니라 연결자라는 포지션은 [docs/care-agreement-spec.md](docs/care-agreement-spec.md) §1과
  같은 이유 — 직업안정법상 유료직업소개사업 등록 문제를 피하는 선
- `app/privacy/page.tsx` — 개인정보 국외 이전 §5-1 신설(Resend Inc./미국, Telegram FZ-LLC/UAE)
- 복지용구 상담 신청 화면 — 로그인 요구, "회원가입 없이 신청 가능" 문구 재검토(사용자 지시 반영)

### 1-C. 돌봄일지 1단계 착수 (`fcd4d88`)

**[docs/care-log-spec.md](docs/care-log-spec.md) §9의 "매칭이 돌기 시작한 뒤 착수" 판단을 사용자가 직접 뒤집었다**
("돌봄일지부터 ㄱㄱ") — 2026-08-04 기준 매칭확정 0건인 상태에서 그대로 진행.

- `CareLog`·`CareQuickNote` 모델 신설(append-only 정정 방식, `CareAgreement`와 같은 이유로
  관계 제약 없음 — 상대방 계정이 지워져도 기록은 남는다)
- `/api/care-log`, `/api/care-log/quick-note` API, `/care-request/care-log` 화면
  (매니저 작성 뷰·보호자 열람 뷰를 역할별로 분기)
- 설계 대비 의도적으로 줄인 부분(사진 업로드 보류, 달력 대신 리스트, 정정본 나란히 보기 보류 등)은
  [docs/care-log-spec.md](docs/care-log-spec.md) §0-1 표에 정리해뒀다

### 1-D. 인터랙션·애니메이션 전수 점검 (`2d8daf8` ~ `2257b65`, 약 12개 커밋)

사용자 지시: "인터렉티브 애니메이션 하나씩 점검 및 개선 고도화 — 최대한 사용하기 쉽게
중요한건 눈에띄게". 마이페이지에서 시작해 사이트 전역으로 확장. 순서: 마이페이지 →
돌봄 흐름(합의서·돌봄일지·요청 마법사) → 시설 상세 → 홈 → 등급테스트·가이드 → 로딩 속도 실측
→ 알림·찜(사용자가 지정한 순서 "1,,3,4,2").

체크리스트(전 화면 공통): 터치 피드백(`active:` — 앱이 웹뷰로 감싸질 예정이라 `hover:`가
안 먹는다), 44px 미만 터치 타깃(CLAUDE.md 자체 규칙), `prefers-reduced-motion` 대응,
선택 상태가 눈에 띄는지.

**실계정 검증 중 찾은 진짜 버그**(코드 리뷰가 아니라 실제 로그인 후 재현):

| 버그 | 원인 | 커밋 |
|---|---|---|
| 미매칭 지원자에게 돌봄대상자 실명 노출 | `open` 요청 API가 `recipientName`을 안 뺐음(어느 화면도 안 쓰는데 응답엔 있었음) | `35abd5d` |
| 돌봄일지 정정이 화면에 안 보임 | `!correctsId` 필터가 정정 기록(항상 `correctsId` 있음)을 통째로 숨김 | `35abd5d` |
| 정정 폼이 빈 채로 열림 | 버튼 클릭 시 기존 값으로 프리필하는 코드가 없었음 | `35abd5d` |
| 비로그인 시 알림 화면 무한 로딩 | 로딩 체크가 비로그인 체크보다 먼저라 `data`가 영원히 `undefined` | `35abd5d` |
| `score-context` API가 페이지 1회 로드에 서버까지 최대 2번 나감(800행 스캔 엔드포인트) | `useEffect`가 `cancelled` 불리언만 썼지 실제 `fetch`를 취소 안 함 → StrictMode 이중 마운트가 서버까지 두 번 감 | `2257b65` |
| 홈 카드 등장 애니메이션이 "움직임 줄이기" 설정을 무시 | `Reveal` 컴포넌트가 `.animate-*` 클래스가 아니라 순수 Tailwind transition을 JS로 토글해서, globals.css의 일괄 해제 블록에 안 걸림 | `a58b95c` |
| 돌봄요청 마법사가 새로고침하면 1단계로 리셋 | 순수 `useState`라 상태가 안 남음 | `031dad8` |
| NavCard(마이페이지) 카드 높이 177px로 부풀어 있었음 | `auto-rows-fr` 그리드가 2줄 텍스트 카드 하나 때문에 행 전체를 늘림 | `9340bdf` |

그 외 44px 미만 터치 타깃 20여 곳, `active:` 피드백 누락 15곳 이상을 화면별로 수정.
`components/CareFormFields.tsx`의 칩 선택 스타일을 옅은 배경에서 solid fill로 바꿔
"선택된 것이 눈에 띄게" 지시를 전역 반영(`CareRequestWizard`, `care-profile` 등 자동 전파).
자세한 화면별 내역은 각 커밋 메시지 참고(설명이 상세해서 여기 요약이 아니라 `git log`가
1차 자료).

### 1-E. 알림·찜 화면 점검 — 결과 없음(정상)

`/notifications`·`/favorites` 모두 콘솔 에러 0, 가로 스크롤 0, 44px 미만 0.
`components/Switch.tsx`는 이미 이번 세션 내내 다른 화면에 새로 적용하던 44px 히트박스
패턴을 처음부터 갖추고 있어서 손대지 않음.

관심 지역 17개를 권역별(수도권/충청권/호남권/영남권/강원·제주)로 묶어 가독성을 높이는
실험을 했으나 **사용자가 기존 플랫 나열이 더 낫다고 판단해 되돌림**. 관련 코드
(`lib/regions.ts`의 `REGION_GROUPS`, `app/notifications/page.tsx`의 그룹 렌더링)는
커밋 없이 원상복구 완료 — working tree에 흔적 없음.

---

## 2. 다음에 할 일 (우선순위)

### 2-1. 알림 3단계

- 이메일 없는 계정 안내(`/notifications`에 "알림을 받으려면 이메일이 필요해요") — 네이버
  로그인이 붙어 이메일 미보유 계정 비율은 줄었지만 완전히 없어진 건 아님
- 관심 지역 신규 시설 알림(현재는 관심 지역 저장까지만 있고 발송 로직 없음)
- 웹푸시 → 앱 FCM

### 2-2. 돌봄일지 2단계

1단계(확인 일지·빠른 알림)까지 나왔다. 매칭확정이 여전히 0건이라 실사용 검증은 아직
못 했음 — 매칭이 처음 뜨는 순간이 진짜 검증 시점. 다음 단계 후보: 사진 업로드,
정정 기록 나란히 보기, id 기반 라우팅(동시 매칭 다건 대응).

### 2-3. 돌봄요청 마법사 재검증

5단계 마법사 sessionStorage 임시저장은 코드·저장소 레벨 검증까지만 했고, "요청이 없는
새 계정으로 마법사를 열어서 임시저장이 실제로 이어지는지"는 아직 확인 못 함(두 테스트
계정 다 이미 요청이 있어서 마법사 진입 자체가 막힘). 새 계정 확보되면 확인.

### 2-4. 리텐션 로드맵 (8차와 동일 순위 유지)

1. 입소 대기 예측(B2B 직결)
2. 알림 인프라 3단계
3. 돌봄일지 2단계

---

## 3. 이 세션에서 배운 함정

1. **`cancelled` 불리언 플래그는 진짜 취소가 아니다.** React 18 StrictMode(dev)가 effect를
   두 번 실행할 때, `AbortController` 없이는 실제 fetch가 서버까지 두 번 간다 — 무거운
   엔드포인트(800행 스캔)에서는 진짜 비용이 든다. 새 `useEffect` fetch는 항상
   `AbortController`로 쓸 것.
2. **Tailwind 마진 유틸의 우선순위는 className 문자열 순서로 보장되지 않는다.** 44px
   터치 타깃을 만들 때 `-m-2 mt-1` 같은 마진 상쇄 트릭 대신 `min-h-[44px]` + `items-center`를
   쓸 것 — 애매하지 않다.
3. **`.animate-*` 클래스 기반 reduced-motion 해제는 JS로 transition 클래스를 토글하는
   컴포넌트(`Reveal` 등)엔 안 걸린다.** 그런 컴포넌트는 자체적으로
   `matchMedia("(prefers-reduced-motion: reduce)")`를 체크해야 한다.
4. **append-only 정정 데이터(`correctsId`)를 다루는 화면은 항상 "최신 버전 찾기" 헬퍼가
   필요하다.** `!correctsId` 필터만 쓰면 정정 기록 자체가 안 보이는 버그가 생긴다
   (`CareAgreement` 패턴과 같은 구조라 `CareLog`에서도 반복됐다).
5. **자동화 환경에서 JS `.click()`을 연속으로 여러 번 한 호출에 몰아 쓰면 React 배치
   업데이트와 씹힌다.** 한 번 클릭 → `wait` → 결과 확인, 클릭 하나당 툴 호출 하나로
   나눌 것.
6. **`nid.naver.com` 등 자격증명 입력 도메인은 브라우저 툴에서 직접 열 수 없다.** 로그인은
   사용자가 실제 UI로 직접 해야 하고, 세션 토큰을 코드로 위조하는 것도 막혀 있다(시도 안 함).
7. **Naver OAuth는 CSRF 토큰이 실린 POST가 필요하다.** `/api/auth/signin/naver`로 직접
   GET하면 `UnknownAction` 에러 — 실제 로그인 버튼 클릭 플로우로만 테스트 가능.

---

## 4. ⚠️ 미커밋 상태 (계속 보류, 8차·7차와 동일)

B2B 입점 절차 개편 코드가 여전히 working tree에만 있다:
`app/admin/AdminClient.tsx` · `app/api/admin/inquiries/route.ts` ·
`app/api/business-inquiry/route.ts` · `app/business/BusinessInquiryForm.tsx` ·
`app/business/console/page.tsx` · `app/business/page.tsx` · `lib/businessPlans.ts` ·
`app/business/apply/`(미추적)

**사용자가 "나중에 다시 할게"로 보류한 상태.** 이번 세션에서도 건드리지 않았다.
임의로 커밋·배포하지 말 것.

---

## 5. DB 변경 (전부 프로덕션 반영 완료)

| 모델 | 용도 |
|---|---|
| `CareLog` | 돌봄일지 — 매니저 작성, 보호자 열람, append-only 정정 |
| `CareQuickNote` | 돌봄일지 빠른 알림(1탭, 5종 + 커스텀) |

`Naver` 로그인은 스키마 변경 없음(NextAuth가 `Account` 테이블에 프로바이더별로 그대로 쌓음).
