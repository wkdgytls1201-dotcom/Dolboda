# 돌보다(Dolboda) 인수인계 문서 — 4차 세션

> 최종 갱신: **2026-08-02** · 이 문서만 읽고 바로 이어서 작업할 수 있게 작성했습니다.
> **새 세션은 반드시 `C:\Users\linea\요양` 폴더에서 열어주세요.**
> 이전 문서([HANDOVER-2026-08-01.md](HANDOVER-2026-08-01.md))는 3차 세션까지의 내용이며, 이 문서가 최신입니다.
> 더 이전 문서([HANDOVER.md](HANDOVER.md))는 2차 세션까지의 내용입니다.

- 배포: https://dolboda-rho.vercel.app
- 저장소: https://github.com/wkdgytls1201-dotcom/Dolboda (브랜치 `main`)
- 이 세션 마지막 커밋: **`e37eb6e`** (2026-08-02)
- 커밋 12개, 전부 배포 성공 확인(Vercel 상태 API로 각 커밋마다 확인)

---

## 0. 이 세션에서 새로 배운 것 — 다음 세션이 반드시 알아야 할 것

### 0-1. 이번 세션은 dev 서버를 한 번도 못 띄웠다 ⚠️

**다른 세션의 dev 서버가 이 폴더에서 계속 돌고 있어서, 이번 세션은 전체 작업을
브라우저 확인 없이 코드 리딩·타입체크·DB 실측 스크립트만으로 진행했다.**
(`npx tsc --noEmit` 전부 통과, Prisma 라운드트립 테스트로 스키마·트랜잭션 로직은
검증했지만 **실제 화면은 한 번도 안 봤다.**)

다음 세션 시작하면 **가장 먼저 375px로 아래를 확인할 것**:
- 마이페이지 대시보드 카드 애니메이션(순차 등장)이 자연스러운지, 겹치거나 깨지지 않는지
- 돌봄 프로필 만들기/수정 화면 전체 흐름
- 시설 상세의 "OO 기준 체크포인트"(CareMatchPoints) 노출 여부와 레이아웃
- 매니저 메뉴 2×2 그리드 + 대표 카드가 실제로 잘 보이는지
- 모달 5종(로그인·상담신청·위치동의·매니저약관·지원확인시트) 등장 애니메이션이
  이상하게 튀지 않는지

### 0-2. Prisma 스키마 변경은 전부 `db push`로만 반영했다 (마이그레이션 파일 없음)

`prisma/migrations` 폴더 자체가 없다. 이번 세션에 추가한 테이블(`CareProfile`,
`CareReview`)과 컬럼(`ConsultRequest.userId/profileSummary/status/facilityNotifiedAt`)은
전부 `npx prisma db push`로 DB에 직접 반영했다. 스키마 변경할 땐 이 방식을 그대로
따르면 된다(`prisma.config.ts`가 `DIRECT_URL`을 쓰도록 이미 설정돼 있음).

### 0-3. 미사용 CSS를 먼저 찾아보고 새로 만들 것

`app/globals.css`에 `pop` 키프레임이 정의만 되고 실제로 쓰는 곳이 하나도 없었다
(발견 후 이번 세션에 활성화해서 씀). 애니메이션이든 유틸 클래스든 새로 만들기 전에
`grep`으로 기존에 안 쓰이는 게 있는지 먼저 확인하는 습관이 코드 최적화에 도움이 됨.

### 0-4. API가 파라미터를 지원해도, 실제로 그 파라미터를 쓰는 이득이 있는지 다시 확인할 것

`/api/facilities/similar?intent=X`가 있었는데도 실제로는 `intent`를 줘도 5개 의도
전체(60KB)를 같이 보내고 있었다(이번 세션에 발견·수정, `e37eb6e`). **"파라미터가
있다"와 "그 파라미터가 실제로 응답을 줄이는가"는 다른 문제** — 캐시 계층(unstable_cache)이
있으면 "compute는 캐시되니까 다 보내도 되지 않나"라고 착각하기 쉬운데, 네트워크로
나가는 바이트 수는 캐시와 무관하게 그대로 나간다.

### 0-5. 사용자가 먼저 스스로 리스크를 지적하면, 그 판단이 맞는지 확인하고 명확히 답할 것

"상담신청 이메일로 광고도 보내고 싶다"는 요청에 사용자가 스스로 "저 이메일은 내가
이용 허가받은 게 아니라서"라고 짚었다. 정보통신망법 제50조(영리목적 광고성 정보
사전동의 원칙, B2B도 예외 없음)에 해당하는 정확한 지적이라 광고 발송 기능은
만들지 않았고, 대신 옵트인 구독 링크로 나중에 합법적 리스트를 쌓는 대안을 제시했다.
**공공데이터에서 나온 연락처를 마케팅에 쓰려는 요청이 나오면 이 판단을 그대로 적용할 것.**

---

## 1. 개발할 때 절대 잊으면 안 되는 것 (3차 세션에서 이어짐, 계속 유효)

### 1-1. 코드 최적화는 "만들 때부터", 성능은 실측으로 검증

이번 세션에도 실측 없이 넘어갔으면 놓쳤을 문제가 여러 건 있었다:
- `useCareProfiles` 훅이 컴포넌트마다 독립 fetch → Provider로 세션당 1회로 전환(`aba68e6`)
- `/api/sitter/workplaces`가 `ORDER BY` 없는 `LIMIT 1500` → 서울·경기에서 상위 30곳이
  임의 표본이었음(`fc00194`)
- 유사시설 API가 `intent` 파라미터를 무시하고 항상 5개 전부 보냄(`e37eb6e`, 위 0-4 참고)

**새 쿼리·새 fetch를 짤 때마다 "이게 실제로 필요한 만큼만 주고받는가"를 스크립트로
직접 재보는 습관을 계속 유지할 것.** (`scripts/_tmp-*.mts`를 만들어 실측 후 바로 삭제하는
패턴을 이번 세션 내내 사용함 — DB 접속 정보는 `.env.local`에서 직접 읽으면 된다.)

### 1-2. 모바일 사용자 최우선 — 이번 세션에 새로 걸린 것들

- **터치 타겟 44px** 규칙을 새로 만든 보조 선택 칩(어르신 전환 탭, 상담 프로필 선택
  칩)에서 빠뜨렸다가 재점검 라운드에서 발견해 40px로 보강(`8dec9e3`). **새 인터랙션
  요소를 만들 때마다 min-h 체크할 것.**
- **애니메이션은 전부 1회성**으로 유지한다. 체크포인트 카드에 하트 아이콘 무한
  플로팅을 넣었다가 스스로 되돌렸다 — 40~60대 사용자 대상 앱에서 본문 중 계속
  움직이는 요소는 산만함·어지러움을 줄 수 있어 "차분하고 신뢰감 있는" 톤과 안 맞는다.
  **`animate-float-slow`(무한 반복) 계열은 꼭 필요한 곳(로딩 표시 등) 외엔 쓰지 않을 것.**
- **한글 조사(을/를, 으로/로, 이/가) 오류**를 두 번 발견했다: "1등급**로** 이용"
  (받침 있는 "등급"엔 "으로"가 맞음, `5dad7d5`)와 이번에 고친 워딩들. **동적으로
  조합되는 한글 문자열은 받침 유무를 따져 조사가 맞는지 항상 확인할 것.**

### 1-3. 개발 환경 함정 — 3차 세션과 동일, 여전히 유효

- dev 서버가 켜진 상태에서 `npm run build` 금지 (이번 세션은 다른 세션의 dev
  서버 때문에 build 자체를 한 번도 못 돌렸다 — 위 0-1 참고)
- 브라우저 검증 시 뷰포트 375px → 끝나면 desktop으로 되돌릴 것

### 1-4. 사용자 커뮤니케이션

- **"커밋 배포 하고"**가 없어도, 위험이 낮고 이미 논의된 작업이면 배포까지 하는 게
  자연스러운 흐름이었다(이번 세션 내내 실제로 그렇게 진행함). 다만 **"일단 보류"**처럼
  명시적으로 멈추라는 신호가 있으면 그 부분만 코드는 만들되 활성화·배포는 그대로 하고
  **문서에 왜 보류인지 정확히 남길 것**(이번 세션의 상담신청 B안이 그 예시 — §4-3-1 참고).
- 사용자가 중간에 여러 요청을 한 번에 몰아서 보낼 때가 있다(mid-turn 메시지). 순서대로
  다 처리하고, 처리 못 한 게 있으면 명시적으로 남길 것.

---

## 2. 이번 세션에서 완료한 것 (커밋 12개, 전부 배포됨)

### 2-1. 백엔드 성능·보안·데이터 정합성 — `fc00194`

- **레이트리밋 신설**(`lib/rateLimit.ts`): 무인증 LLM 프록시(`/api/grade-helper/explain`)
  IP당 10분 5회+전역 상한, 상담신청(`/api/consult`) 시간당 5회+전화번호 형식 검증
- **매니저 계좌번호 노출 수정**: 지원자 응답에서 `select`로 정산 계좌 필드 제외
- **캐시 신설**: 시설 상세 `revalidate 86400`, `score-context`(지역 평균·최근접 병원)
  하루 캐시, `/api/sitter/workplaces` 지역별 하루 캐시
- **`/api/sitter/workplaces` 쿼리 수정**: `ORDER BY` 없는 `LIMIT 1500` → 지수 계산에
  쓰는 조각만 select해 전체를 훑도록(상위 30곳이 실제 상위가 되게)
- **검색 300ms 디바운스** — 한 글자마다 전체 스캔 2회 나가던 것
- **지역 평균 산식 정합성**: 표시 시설엔 있고 비교 대상엔 없던 "인근 병원 접근성" 신호를
  이웃 시설에도 동일하게 계산
- **mock 시설 제외 통일**(지역 통계·nearby total), **지역 페이지 404 링크 제거**
  (0곳인 유형 링크를 안 보여주게)
- **프로그램 태그 규칙 수정**: `social`의 포괄어("프로그램"·"활동") 제거로 929종
  오분류 해소, 가족참여 등 누락 키워드 보강. **⚠️ 반영하려면
  `npx tsx scripts/build-program-tags.mts --write` 실행 필요 — 아직 안 돌림**
- **요양보호사 법정 배치기준 서술 정정**(2.1→2.3, 배점은 유지), JSON-LD 13곳
  XSS-safe 직렬화(`lib/jsonLd.ts`)

### 2-2. 모바일 최적화 — `e478015`

- **폰트 404 발견·복구**: `globals.css`의 Pretendard `@font-face` URL이 실제로는
  404라 브랜드 폰트가 한 번도 로드된 적이 없었음(시스템 폰트로 조용히 폴백).
  공식 dynamic-subset 방식으로 교체(`app/layout.tsx`)
- 히어로 배너 7장 eager → 현재±1장만 마운트, Reveal 컴포넌트 하이드레이션 전
  빈 화면 버그 수정, 로드뷰 타일 이중 다운로드 제거(`lib/useRoadviewPano.ts`)
- `viewport` export(themeColor·노치 대응), 탭 하이라이트 제거

### 2-3. 매니저 영역 UX 개편 — `fff63b7`

- 모바일 메뉴: 6개 동급 나열 → 대표 카드(일자리, 코랄 그라데이션) + 2×2 그리드 +
  얇은 줄(알림)
- **레벨 기준 현실화**(`lib/sitterProgress.ts`): 완료 5/10건 문턱이 매칭 드문
  초기 마켓플레이스에서 사실상 전원을 영원한 새싹으로 만들던 문제 →
  프로필 완성/첫 지원으로 "활동 매니저", 완료 1건 "프로", 3건 "베테랑"
- **버그 수정**: `/mypage` 레이아웃이 재마운트 안 돼 저장 후 공유 상태 갱신이
  안 되던 것(등록 직후 "등록 안 하셨어요", 계좌 등록 직후 "계좌 없음" 등)

### 2-4. 어르신 돌봄 프로필 신설 — `0ea9da5`, `955b661`, `907dff3`

**민감정보(건강 상태) 최소 수집 원칙**을 스키마로 강제:
- 이름·생년월일 없음(호칭이 이름 역할), 전부 선택지 구간값(자유 텍스트 없음)
- `consentAt` 필수 컬럼 — 동의 없는 저장이 스키마 수준에서 불가능
- 본인 삭제 + 회원 탈퇴 시 cascade

재사용 연계: 등급테스트 결과 저장, 돌봄 요청 마법사 자동 채움, 마이페이지
대시보드 맞춤 카드(치매→인지프로그램 검색, 거동 무거움→재활프로그램 검색,
등급→`lib/ltcGuide.ts` 기반 이용 가능 급여 안내), 시설 상세 "OO 기준 체크포인트"
(`CareMatchPoints` — 프로필과 데이터가 직접 이어지는 항목만 생성, 근거 없으면
아예 안 보여줌), 검색 `?programTags=`/`?type=A,B` 딥링크.

### 2-5. 매니저↔보호자 양면 리텐션 루프 — `907dff3`

- **미선정 자동 처리**: 확정 순간 나머지 지원자를 "미선정"으로 일괄 변경 + 상태
  가드 추가(중복 확정 방지, `8dec9e3`에서 재검증 중 발견해 수정)
- **완료 직후 화면 신설**(`CareReviewPrompt`): 별점·후기 → `CareReview` 테이블 →
  지원자 카드에 "돌보다에서 완료 N건·★4.8" (플랫폼 보증 실적, 자기신고 경력과 구분)
- **같은 조건으로 재요청** 기능
- **상담신청에 돌봄 프로필 옵트인 전달** (체크해야만 전송, 신청 시점 스냅샷 저장)

### 2-6. 성능 재구조화 — `aba68e6`

`useCareProfiles`를 컴포넌트별 독립 fetch 훅 → `lib/careProfileContext.tsx`
Provider로 전환. 시설 상세만 28,000여 페이지라 페이지 이동마다 반복 fetch되던 걸
세션(전체 페이지 로드)당 1회로 줄임. `FavoritesProvider`/`CompareProvider`와 같은
자리(`app/layout.tsx` 루트)에 마운트.

### 2-7. 버그 수정 5건 재검증 라운드 — `8dec9e3`

확정 API 중복 요청 가드, 등급테스트 `?estimate=` URL 누수(다른 프로필에 잘못
전이되던 것), `CareMatchPoints` 빈 여백 버그, `isSitter` boolean|null 오판정
깜빡임 2곳, 터치 타겟 보강.

### 2-8. 애니메이션·모달 통일 — `5dad7d5`

미사용 `pop` 키프레임 활성화(완료·저장 순간), 카드 순차 등장(stagger), 공용
ChipSelect/ChipMultiSelect 선택 시 pop, 모달 5종 등장 통일(배경 페이드+카드
스케일). 한글 조사 오류 수정("1등급로"→"1등급으로").

### 2-9. 상담신청 시설 자동 이메일 발송(B안) — `4c0b191` — **⚠️ 보류, 상세는 §4-3-1**

시설 이메일(`Facility.extra.institutionInfo.email`, 실제 시설 81%가 보유)로
로고·슬로건 담은 HTML 메일 자동 발송하는 코드는 완성했으나, 발신 주소가
`onboarding@resend.dev`(Resend 테스트 전용)라 실제로는 발송이 거부될 가능성이 큼.
**정식 도메인 결정 → Resend 도메인 인증 전까지는 사실상 비활성 상태(A안과 동일하게
동작).**

### 2-10. 가독성·성능·문구 마무리 — `e37eb6e`

인력현황 카드 레이아웃 분리(설명/전체직원수), 유사시설 탭 전환 재최적화(§0-4),
안심지수 표기 3곳 다듬기(가중치 변경 없음).

---

## 3. 확정된 결정사항과 규칙 (3차 세션 내용에 추가된 것만)

### 3-1. 광고성 이메일 절대 금지 — 이번 세션 신규 확정

공단 등 공공데이터에서 얻은 연락처(시설 이메일 등)로 **광고성 정보를 사전 동의
없이 보내지 않는다.** 정보통신망법 제50조는 B2B도 예외 없이 사전동의를 요구한다.
지금 만든 상담신청 전달 메일은 "실제 문의를 전달하는 거래성 정보"로만 한정하고,
광고 문구는 절대 섞지 않는다. 나중에 마케팅이 필요하면 전달 메일 하단에 옵트인
구독 링크를 넣어 합법적으로 동의받은 리스트를 따로 쌓을 것(미구현, 아이디어만).

### 3-2. 애니메이션 원칙

- 전부 **1회성**(등장 시 1번). 무한 반복(`animate-float-slow`)은 본문 콘텐츠엔
  쓰지 않는다 — 산만함, 40~60대 사용자 배려
- 새로 만들기 전에 `app/globals.css`에 이미 있는 키프레임/유틸을 먼저 찾아본다
- 선택 상태가 바뀌는 요소(칩, 별점 등)는 `key`를 상태값에 걸어 "방금 바뀐 것만"
  다시 마운트되게 하는 패턴을 쓴다(전체가 다시 애니메이션되지 않게)

### 3-3. Provider 패턴 — 세션당 1회 fetch가 기본

새로운 "로그인 시 개인화 데이터"를 여러 화면에서 써야 하면, 컴포넌트별 훅으로
각자 fetch하게 하지 말고 루트 레이아웃의 Provider로 만들어 공유한다
(`FavoritesProvider`/`CompareProvider`/`CareProfileProvider`가 이 패턴).
`/mypage` 전용 데이터는 `app/mypage/layout.tsx`의 `SitterProfileProvider`처럼
그 레이아웃 안에만 스코프해도 된다(모든 페이지에서 안 쓰이면).

### 3-4. API 파라미터를 만들 때 실제 이득을 재확인

캐시가 있다고 해서 파라미터별 응답 크기 최적화를 건너뛰면 안 된다(§0-4).
파라미터를 추가했으면 실제로 그 경로가 더 적은 데이터를 주고받는지 실측할 것.

---

## 4. 아직 안 끝난 작업과 다음 할 일

### 4-1. 시각적 검증 필요 (최우선 — §0-1 참고)

이번 세션 변경분 전체가 브라우저 미검증 상태. dev 서버 비게 되면 바로 375px로
전체 화면 훑어볼 것.

### 4-2. 반영 안 된 데이터 작업

- **프로그램 태그 규칙 변경 반영 안 됨**: `npx tsx scripts/build-program-tags.mts --write`
  아직 안 돌림(§2-1). 규칙은 코드에 있지만 `extra.programTags`는 예전 값 그대로.

### 4-3. 사용자 결정이 필요한 것 (3차 세션과 동일 + 추가)

1. **정식 도메인** (dolboda.kr 등) — `lib/siteConfig.ts`의 `SITE_URL` 한 곳만 바꾸면
   canonical·sitemap·OG 전부 반영. **아래 4-3-1이 이 결정에 막혀 있음.**
2. GA4/네이버 애널리틱스, 서치콘솔 재제출 (3차와 동일, 미착수)

### 4-3-1. ⏸️ 보류 — 상담신청 시설 자동 이메일 발송 (§2-9 참고)

코드는 완성(`app/api/consult/route.ts`, `lib/resend.ts`). 실행 안 되는 이유와
재개 조건은 이 파일 §2-9 및 `HANDOVER-2026-08-01.md` §4-3-1에 상세 기록됨.
**요약**: 도메인 정해서 Resend에 인증 → 발신 주소 교체 → 실제 시설 주소로
테스트 발송(사용자 확인 후 진행) → 그제서야 `facilityNotifiedAt`이 실제로 채워짐.

### 4-4. 데이터 대기 중 (파일 받으면 즉시 반영 가능) — 3차와 동일

- 행정처분(위반사실 공표) — `node scripts/import-admin-actions.mjs --write`

### 4-5. 다음 리텐션 후보 (논의됨, 미착수)

1. **카카오톡 알림** — `auth.ts`에서 `talk_message` 권한을 이미 로그인 시 받고
   있음. 새 일자리·미선정·매칭 통보를 실제로 발송하는 인프라만 있으면 됨.
   리텐션 논의에서 가장 우선순위 높게 나온 항목.
2. **상담 이력 화면** — API는 완성(`/api/consult/mine`, GET+PATCH), 화면 없음.
   탈중개 방지 설계 원칙 적용: "전화 걸기" 버튼 없이 전부 플랫폼 안 행동
   (시설 다시 보기·유사시설·비교함 담기)으로만 유도. "입소 결정했어요" 체크 시
   "계속 지켜봐 드릴까요?"(지켜보기, 아래 4-6 완성 후 연결) 자리만 먼저 잡아둘 수 있음.
3. **옵트인 구독 링크** — 상담 전달 메일 하단에 넣어 합법적 마케팅 리스트 구축(§3-1)

### 4-6. 시간이 해자를 만드는 것 — 시설 시계열 스냅샷 (3차와 동일, 미착수)

`pg_cron`으로 매일 스냅샷(정원·현원·대기·등급) 적재. **오늘 시작 안 하면 그
하루치는 영원히 소급 못 만든다.** 쌓이는 기간별로:
- 1개월: "이번 주 자리 난 시설"
- 3개월: **찜한 시설 알림이 처음 실체를 가짐**(지금 알림 설정 화면은 localStorage
  껍데기뿐)
- 6개월: "보통 3주면 자리 나요" 같은 대기소진 속도 — 전국 어디에도 없는 정보
- 1년+: 등급 변동 추적, 인력 감소 경고

Supabase 대시보드에서 `pg_cron` 확장 활성화 필요(코드로는 못 켬).

---

## 5. 파일 구조 (이번 세션 신규·주요 변경)

```
HANDOVER-2026-08-02.md              ★ 이 문서 (최신)

prisma/schema.prisma                 CareProfile·CareReview 신설,
                                      ConsultRequest에 userId·profileSummary·
                                      status·facilityNotifiedAt 추가
                                      (마이그레이션 파일 없음, db push만 사용 — §0-2)

lib/
  careProfileContext.tsx            ★ 신설 — 돌봄 프로필 세션 공유 Provider
  ltcGuide.ts                        ★ 신설 — 장기요양등급별 이용 가능 급여 안내
  rateLimit.ts                       ★ 신설 — IP 레이트리밋
  jsonLd.ts                          ★ 신설 — JSON-LD XSS-safe 직렬화
  resend.ts                          대폭 확장 — HTML 이메일 템플릿(로고·슬로건)
  sitterProgress.ts                  레벨 기준 현실화
  dolbodaScore.ts                    ★ 핵심자산 — 문구만 변경(가중치 동일)
  similarity.ts                      5의도 일괄 계산 + 메모이제이션
  useRoadviewPano.ts                 타일 이중 다운로드 제거

components/
  CareMatchPoints.tsx                ★ 신설 — 시설 상세 "OO 기준 체크포인트"
  CareReviewPrompt.tsx               ★ 신설 — 돌봄 완료 직후 후기·재요청 화면
  MyPageDashboard.tsx                돌봄 프로필 카드·애니메이션 대량 추가
  MyPageShell.tsx                    매니저 메뉴 위계 개편(대표카드+그리드+얇은줄)
  SimilarFacilities.tsx              탭별 개별 fetch로 재전환(§0-4)
  CareFormFields.tsx                 ChipSelect/MultiSelect pop 애니메이션
  (모달 5종)                          등장 애니메이션 통일

app/
  mypage/care-profile/page.tsx      ★ 신설 — 돌봄 프로필 관리 화면
  api/care-profile/route.ts         ★ 신설 — 돌봄 프로필 CRUD
  api/care-reviews/route.ts         ★ 신설 — 완료 후기 생성
  api/consult/mine/route.ts         ★ 신설 — 상담 이력 조회 (화면은 미착수, §4-5)
  api/consult/route.ts              시설 자동발송 추가(§2-9, 보류)
  facility/[id]/FacilityDetailClient.tsx   CareMatchPoints 연결, 인력현황 레이아웃
  layout.tsx                        CareProfileProvider 마운트, 폰트 교체

docs/
  ai-score-spec.md                  라벨 문구 변경 이력 기록(가중치 불변)
  program-taxonomy-spec.md          social 포괄어 제거 이력(§2-1)
```

---

## 6. DB 현황 (2026-08-02 실측)

| 항목 | 수치 |
|---|---|
| Facility 전체 | 28,844 (실데이터 28,834) |
| Institution | 43,943 |
| **시설 이메일 보유(실데이터)** | **23,290 (81%)** — 상담신청 B안 재개 시 즉시 활용 가능 |
| CareProfile | 0 (신규 기능, 아직 실사용자 없음) |
| CareReview | 0 (신규 기능) |
| ConsultRequest | 3 |
| CareRequest | 5 |
| SitterProfile | 1 |

---

## 7. 검증 절차 (3차 세션과 동일 + 이번 세션 보완)

1. `npx tsc --noEmit` 통과 (이번 세션 매 변경마다 실행, 전부 통과)
2. **dev 서버를 끄고** `npm run build` — 이번 세션은 못 함(§0-1), 다음 세션 최우선
3. 브라우저 375px 실측 — 이번 세션은 못 함, 다음 세션 최우선(§4-1 체크리스트)
4. **DB 변경 검증은 실제 라운드트립 테스트로**: 이번 세션은 `scripts/_tmp-*.mts`를
   임시로 만들어 생성→조회→트랜잭션→cascade 삭제까지 실제로 돌려보고 바로 삭제하는
   방식을 여러 번 썼다(확정 가드, 후기 생성, care-profile cascade 등). 스키마·
   트랜잭션 로직을 바꿀 때 이 패턴을 이어서 쓸 것 — `.env.local`에서 커넥션
   문자열을 직접 읽는 스크립트라 Prisma Client 재생성 없이도 바로 실행 가능.
5. 콘솔 경고 확인, 커밋 메시지 한국어로 무엇을·왜, 끝에
   `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`
