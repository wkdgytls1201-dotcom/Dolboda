# 돌보다 인수인계 — 16차 세션 (2026-08-06)

이 문서만 읽고 이어서 작업할 수 있게 썼습니다. 15차 직후 이어진 **21커밋 세션**
(`5fac819` → `c53aa91`, 전부 푸시·Vercel 배포 라인). 몸통은 여덟입니다:
① SEO 실태 대조 후 진짜 갭만 보강 ② **폐업 추적·집계 신설**(missingSince) ③ **돌봄일지
경우의 수 감사 — 실버그 6건** + 고도화 4종 ④ **서울 사진 3,189곳 업로드 완료**
⑤ **데이터 자산 상품화 4종 + 기술서 2건** ⑥ **모바일 마감 — 57페이지 순회 + 호버 폴리시**
⑦ **알림 메일 6종 공용 셸 + 실발송 최초 성공** ⑧ **사진 활용 3종(캡션 SEO·영역 재분류·실사진 필터)**

이전: [15차](HANDOVER-2026-08-06-15차.md). 5차 §1·§3, 10차(알림), 12차(B2B),
13차(사진 파이프라인) 여전히 유효. **새 기술서 [docs/data-assets-spec.md](docs/data-assets-spec.md)와
[docs/vacancy-insight-spec.md](docs/vacancy-insight-spec.md)는 지표·리포트를 만들기 전 필독.**

---

## ⚡ 다음 세션에서 바로 시작할 일

1. **🔴 전국 사진 zip — 최우선.** 사용자가 "며칠 내로 다 들어온다"고 했다(2026-08-06 밤).
   경북·강원 zip(`04_경상북도_강원특별자치도.zip`, 26GB)을 한 번 받았으나 **파일이 잘려
   처리 못 했다** — EOCD·중앙 디렉터리가 통째로 없었다(§3-12 진단법). **사용자가 다시
   받는 중**이니, zip이 보이면 크기·끝부분부터 확인하고 13차 순서대로
   (inventory → convert → extras → upload) 돌릴 것. 지역당 2시간 안팎.
   ★ 사진이 32% → 90%대가 되면 오늘 만든 것 대부분(캡션 SEO·실사진 필터·영역 분류·
     OG 실사진)의 효과가 3배 가까이 커진다. 다른 무엇보다 이게 먼저다.
2. **iOS Safari 실기기 검수** — 사용자·나 모두 아이폰이 없어 미검증으로 남았다.
   코드 방어는 전부 넣었으므로 위험도는 낮다. **BrowserStack 무료 30분**이면 §3-A의
   4항목을 다 볼 수 있다. iOS 앱화 심사 때 어차피 필요하니 그때 몰아서도 된다.
3. **자리 인사이트는 8/9(관측 7일차)부터 화면에 뜬다** — 8/6 기준 관측 5일이라 지표가
   null이라 안 보인다. 8/9 이후 시설 상세에서 실물 확인할 것(`docs/vacancy-insight-spec.md`).
4. 개발 순위(사용자 합의): ① 사진 zip(위) ② **GA4/네이버 애널리틱스 — 측정이 0이라
   오늘 만든 것들이 실제로 쓰이는지 알 방법이 없다.** 계정 생성은 사용자와 함께 해야 하고
   (사용자가 "계정생성은 나중에"라고 미뤄둠), 측정 ID만 받으면 코드는 바로 붙일 수 있다.
   ③ B2B 판매 전 실태 점검(§7 참고 — 급하지 않다) ④ **카카오톡은 사용자가 "마지막"으로 명시**.

> ✅ **Resend는 이제 검증 끝났다**(16차 마지막에 실증). 15차부터 "발송 로그 확인"이
> 숙제로 남아 있었는데, 강제 발송 테스트로 사건 감지 → 대상 매칭 → `noreply@dolboda.kr`
> 발신 → **실제 수신 확인**(스팸함 아님) → `AlertDelivery` 기록까지 전 구간이 돌았다.
> 이 알림은 그전까지 한 번도 실제로 나간 적이 없었다(`AlertDelivery` 0행이었다).

## 0. ★ 사용자가 직접 해야 할 일

- 🔴 **`MAIL_FROM`을 `noreply@send.dolboda.kr`로 바꿀 것** (`.env.local` + GitHub Secrets 양쪽).
  현재 `noreply@dolboda.kr`로 보내는데, Resend에 인증된 도메인은 `send.dolboda.kr`이라
  **SPF가 어긋난다.** 지금은 DKIM이 통과해서 메일이 정상 도착하지만(실측), 나중에
  DMARC를 `p=quarantine`으로 올리는 순간 **전 발송이 스팸함으로 간다.**
  값만 바꾸면 되고 코드 수정은 필요 없다 — 6개 스크립트 전부 `MAIL_FROM`을 읽는다.
- 🔴 **경북·강원 zip 재다운로드** — 받은 파일이 잘려 있었다(§3-12). 사용자가 다시 받는 중.
- 🔴 **원본 zip 3개 삭제 가능** — 서울(32GB)·경기 2개. 13차 §3-1의 5개 조건 전부 충족
  (변환·extras·업로드·화면검증·jsonl 보관). 약 100GB 회수.
  ⚠️ `C:/Users/linea/photo-data/*/`의 **jsonl 3종은 영구 보관**(원본 지운 뒤 유일한 기록).
  `files/` 폴더는 업로드 끝나 삭제 가능.
- 🟡 **GA4 계정 생성 + 측정 ID(`G-XXXXXXX`) 전달** — 사용자가 "계정생성은 나중에"로
  미뤄둠. ID만 오면 코드는 바로 붙는다.
- 🟡 서치콘솔 canonical · 네이버 로그인 프로덕션(Vercel env+검수) · `ADMIN_USER_IDS` ·
  R2 env 5종 — 이월.
- 🟡 **기관 마스터를 다시 임포트하면** `node --env-file=.env.local scripts/build-fee-benchmarks.mjs`도
  같이 돌릴 것(비급여 비교 기준값이 낡는다 — 파일 머리말에도 명시).

---

## 1. 이 세션에서 한 일 (주제별)

### 1-1. SEO 실태 대조 + 갭 보강 (`5fac819`)

외부 SEO 진단을 받아 **코드와 전수 대조**했다. 1~3순위(사이트맵 인덱스 분리·검색
파라미터 noindex·페이지네이션 self-canonical·구조화 데이터·aggregateRating 미사용)는
**이미 전부 구현돼 있었다.** 진짜 갭만 채웠다:
- 메인 H1에 핵심 검색어: "소중한 부모님" → **"부모님께 맞는 요양원·요양병원"**
- 가이드 바이라인(작성 주체·최근 검토일·근거자료) + Article author.
  `Guide.reviewer`는 **필드만 열어뒀다 — 실존 검수자가 생기기 전엔 채우지 않는다**(가짜 검수자 금지)
- **`/editorial-policy` 신설** — 작성 원칙·검토 주기·광고 구분·정정 절차
- 지역+유형 2페이지 이후 FAQ 본문·JSON-LD 생략(중복 억제)
- 사이트맵 `regions`(허브 244) / **`region-services`(지역+유형 897)** 분리 —
  서치콘솔에서 어느 묶음이 문제인지 구분. **인덱스 주소 그대로라 재제출 불필요**

### 1-2. ★ 폐업 추적·집계 신설 (`54cc9c5` · `d2b7c4d` · `46306e1`)

사용자 질문("몇 달 뒤에 특정 지역에서 몇 곳 폐업했는지 알 수 있나")에서 시작.
**답은 "당시엔 불가"였다** — 일일 동기화가 개수만 텔레그램으로 알리고 어디에도 안 남겼다.

- `Facility.missingSince`(date) 추가. 파일에서 사라지면 그날 날짜를 찍고, **다시 나타나면
  null 복구**(하루치 누락 자동 치유). 행은 삭제하지 않는다.
- `lib/facilityPresence.ts` — **7일 연속 사라진 시설만** 사이트맵 제외 + 상세 noindex,follow
  + "공개 자료에서 확인되지 않아요" 안내. 데이터·페이지는 보존.
- `scripts/closure-report.mjs` — 기간·시도·**시군구·읍면동**·유형·월별 집계 + TSV.
  `--addr 진영읍`, `--list`(정확한 주소 포함), `--confirmed`(7일+만).
- **소급 기록 161곳**(2026-08-06 실측: 전남 42·경기 29·서울 16 …).

> ⚠️ **이 기능의 가장 위험한 실패 모드를 `46306e1`에서 막았다.** 일일 동기화는 지역
> 다운로드가 실패해도 계속 진행하는데(설계 의도), 그러면 그 지역 시설 **수천 곳이
> 통째로 "사라짐"으로 보여** missingSince가 대량으로 찍히고 7일 뒤 검색에서 사라진다.
> 전체 시설 수 가드(20,000)는 이걸 못 잡는다(한두 지역이 빠져도 합계는 그 위에 남는다).
> `missingSkipReason()`이 ① 지역 실패가 하나라도 있으면 ② 사라짐이 임계(전체 2%,
> 최소 300)를 넘으면 **기록을 건너뛴다.** 재등장 복구는 근거가 확실하므로 항상 실행.

**한계(사용자에게 이미 안내함)**: 기록은 **2026-08-06부터** 쌓인다. 그 이전 폐업은
공단이 이력을 주지 않아 복원 불가. "폐업 확정"이 아니라 "공단 자료에서 사라짐"이다
(상호 변경·이전도 잡힌다).

### 1-3. ★ 돌봄일지 경우의 수 감사 — 실버그 6건 (`33b7a6f` · `39e2eaa`)

사용자가 예로 든 두 경우("시작 전에 일지를 쓴다", "완료 후에 쓴다")가 **실제 데이터에
이미 있었다**(각 4건). 상태·시점·권한 축으로 전수 감사한 결과:

| # | 문제 | 실측 | 수정 |
|---|---|---|---|
| 1 | 돌봄 시작 전 날짜로 작성 | **4건** | 날짜 창(시작일~종료일) |
| 2 | 완료 후 무기한 작성 | **4건** | 종료 후 **7일 유예**(사용자 결정) |
| 3 | **완료 건이 진행 중 건을 가림** | 잠재 | 진행 중 우선 선택 |
| 4 | 정정으로 하루 1원본 우회 | 잠재 | 정정은 같은 날짜만 |
| 5 | 남의 돌봄 사진 삽입 | 잠재 | URL을 내 건 폴더로 한정 |
| 6 | 밀린 날짜 일괄 작성 시 메일 폭탄 | 잠재 | 오늘·어제만 발송 |

**3번이 특히 위험했다**: 세 라우트(일지·빠른알림·사진)가 각자 `findFirst orderBy
createdAt desc`를 복붙해 최신 1건만 집었다. 매니저가 진행 중 A와 완료된 B를 가지면
최신인 B가 잡혀 **오늘 A의 기록이 완료된 B에 들어간다**. 보호자는 진행 중 1건 제한
(advisory lock)이지만 **매니저 쪽엔 상한이 없어** 두 번째 일을 맡는 순간 터진다.
→ `lib/careLogContext.ts` 단일 구현으로 통일.

계산은 `lib/careLog.ts`의 `careLogWindow()`/`careDateError()`(순수 함수) — **화면과 서버가
같은 함수를 쓴다.** 완료 처리일도 반영한다(예정일보다 일찍 완료하면 그날이 끝).

### 1-4. 돌봄일지 고도화 4종 (`8030d7a`)

- **여러 돌봄 건 동시 진행**: 역할별 목록을 내려보내고 `requestId`로 고른다. 상단 전환
  칩(진행 중 🟢/종료 ⚪). 쓰기 요청은 보고 있던 건 id를 실어 보내고, **서버는 내 목록에
  없는 id면 기본값으로 조용히 바꾸지 않고 거절한다**(엉뚱한 건에 쓰이는 것이 막으려던 문제).
- **주간 요약 메일**(alert-system-spec §3-15, 배치 9종째): 월요일 09:00만.
  기록 0건인 주는 **보내지 않는다** — 빈 요약은 매니저에 대한 무언의 지적이 된다.
  발송 기록 `CareLogWeeklyDigest`(돌봄건×주).
- **보호자 화면**: 특이사항·사진 필터(해당 날 없으면 칩 자체를 숨김) + 식사·컨디션
  분포 막대(5일 이상 쌓였을 때만). 이미 받아온 목록만 쓰는 순수 클라이언트 계산.
- **지각 작성 모드**(`59ce2fb`, 로그인 순회에서 발견): 유예 기간에 폼이 멀쩡히 떠 있는데
  **무조건 오늘 날짜로 저장을 시도해 100% 400**을 맞았다. 대상 날짜를 `logWindow.max`로
  잡고, 안내 배너·카드 제목·버튼이 그 날짜로 바뀐다. 빠른 알림은 숨긴다("방금 식사하셨어요"는
  지금의 소식이라 지각 작성이 없다).

### 1-5. 서울 사진 파이프라인 완료

13차 순서 그대로(inventory → convert → extras → upload):

| 단계 | 결과 |
|---|---|
| inventory | 3,416곳 / 32,337장 |
| convert | 3,192곳 / 18,195장 / 967MB (DB 매칭 93.4%) |
| extras | 1,277곳 / 12,193장 / 392MB |
| **upload** | **3,189곳 / 화면용 18,164장 + 보관용 12,166장** |

검증: 서울 시설 3,598곳 중 **3,182곳(88.4%)에 사진**. 무작위 4곳 R2 URL 전부 200
(webp 30~185KB), 상세페이지 렌더 확인. → **원본 zip 삭제 가능**(§0).

### 1-6. ★ 데이터 자산 상품화 4종 + 기술서 (`556c3e3` · `d1680a5` · `a00298e`)

전수 조사에서 **매일 쌓이는데 화면 어디에도 안 쓰이는** 데이터가 나왔다.

**① 자리 추이 + 인사이트** — `FacilitySnapshot`(28,504행, 8/2부터 매일 289~394행)이
알림 스크립트만 읽고 있었다. 공단은 **오늘 값만** 공개하므로 "자리가 잘 나나",
"대기 얼마나 기다리나"는 우리가 매일 모아야만 답할 수 있다.
- `lib/vacancyTrend.ts` — forward-fill(스냅샷은 **바뀐 시설만** 적재해 날짜가 듬성듬성.
  이걸 안 하면 "기록 없는 날 = 자리 0"으로 잘못 읽힌다)
- `lib/vacancyInsight.ts` — 회전 횟수·만실 비율·대기 소진 속도·예상 대기.
  **예측이 함부로 안 나오는 것이 핵심**: 관측 7일 미만이면 지표 자체를 안 만들고,
  예측은 14일+ AND 대기>0 AND 실제 감소 AND 잡음 아님 **4조건 전부** 충족 시에만.
- 설계·한계는 [docs/vacancy-insight-spec.md](docs/vacancy-insight-spec.md) — **§4(하면 안 되는 것) 필독**

**② 비급여 벤치마크** — `scripts/build-fee-benchmarks.mjs` → `lib/feeBenchmarks.generated.ts`
(시도 17 × 유형 2 × 항목 4 = 109개, 9KB). **런타임 조회 0**(페이지가 매번 집계하면
경기도 1.2초, 28,000페이지가 각자 그럴 수 없다). 평균이 아니라 **중앙값**(상급침실료
극단값이 평균을 끌어올린다).
- 보호자: 시설 상세에 "평균보다 45% 높아요"
- 시설(B2B): 월간 보고서 "비급여 비용 · 지역 비교"

**③ 조회→상담 전환율** — `consults.conversionPct`. "노출을 늘릴 문제인가, 페이지를
손볼 문제인가"를 가른다. 조회 30회 미만이면 null(한 건이 수치를 흔든다).

**④ 반응 종류별 분포**(매니저 공개 프로필) — 🙏😊💪 막대.
> ★ **"기록률 92%" 같은 성실도 점수는 의도적으로 만들지 않았다.** 회사가 매니저를
> 평가하는 지표라 법적 거리 방침 위반이다. 보호자가 누른 버튼의 집계만 보여준다.
> 0인 종류를 숨기는 것도 같은 이유(없는 것을 강조하면 감점처럼 읽힌다).

**기술서 [docs/data-assets-spec.md](docs/data-assets-spec.md) 신설** — 무엇을 쌓고
(규모 실측) / 어디에 쓰고 / **§5 무엇을 팔지 않는지**를 표로 못박았다. CLAUDE.md에 등재.

### 1-7. 모바일 마감 (`44cd8c2` · `6f37753` · `59ce2fb` · `8cf53ff`)

**⚠️ 실기기 검수는 못 했다** — 헤드리스 Chromium이라 뷰포트 에뮬레이션까지만.
Android Chrome은 **같은 엔진이라 사실상 검증**됐고, iOS Safari(WebKit)는 미검증.

**순회 — 57페이지 @ 360px(보급형 기준), 가로 넘침 최종 0**
- 공개 26 + 매니저 22 + 보호자 9 (로그인은 사용자가 직접 해주심)
- 넘침 2건 발견·수정:
  - **개인정보처리방침 표**(383px > 360px) — `table-layout:auto`에서 긴 한글이 컬럼을
    밀어냄. `LegalPage` 래퍼에 `table-fixed` + 셀 `overflow-wrap:anywhere` → **약관·방침
    4페이지 한 번에 해결**
  - **지역 페이지 시설명**(396px) — 공단 원본에 띄어쓰기 없는 긴 이름
    (`"(A)"+1건강드림실버케어통합재가센터`). 전역 `word-break:keep-all`이 못 끊음.
    시설명에 `min-w-0` + `anywhere`(truncate는 정보가 사라져 줄바꿈 선택)

**모달 스크롤 체이닝(실버그)** — `AuthModal`·`ConsultModal`은 배경 스크롤 잠금이
**아예 없었고**, 시트 2곳은 `overflow:hidden` 복붙본만 있었다(iOS에선 그것만으로 안
멈추고 닫을 때 페이지가 맨 위로 튄다). → `lib/useBodyScrollLock.ts` 신설(스크롤 위치
복원 포함), 4곳 통일 + `overscroll-contain` + `85vh→85dvh`.

**버튼 손맛** — `hover:`를 500곳 넘게 쓰는데 `hoverOnlyWhenSupported`가 꺼져 있었다.
터치 기기는 탭 뒤에도 `:hover`를 유지하므로 **폰에서 버튼이 눌린 채 굳었다.**
tailwind.config 한 줄로 호버 규칙 65개가 전부 `@media (hover:hover)` 안으로 이동.
추가로 버튼 커서(브라우저 기본이 화살표였다)·전역 focus-visible 링(전체 앱에서 2파일뿐이었다)·
CTA 1px 리프트(호버 기기만) + 누름 반응(터치 포함, 60ms).

**B2B 그래프 고급화** — 납작한 막대 30개 → **면적 그래프**(그라데이션 + 선 + 평균
점선 + 선택 안내선 + 0.9초 그리기 애니메이션). 차트 라이브러리 없이 인라인 SVG —
recharts류면 이 화면 하나에 100KB+, **공유 번들 87.4kB 그대로**.

### 1-8. ★ 알림 메일 6종 공용 셸 + 실발송 최초 성공 (`214e053` · `593f1b8`)

**발단**: 사용자가 "아침에 시설 알림이 안 오던데"라고 물었다. 원인은 **정상**이었다 —
알림은 "새 시설이 조건에 맞게 등록됐을 때"만 나가는데 그날 해당 사건이 없었다.
확인 차 강제 발송을 돌렸고(사용자 승인 후), 그 과정에서 **이 알림이 서비스 시작 이래
한 번도 실제로 나간 적이 없다**는 걸 알게 됐다(`AlertDelivery` 0행).

- **전 구간 실증**: 사건 감지 → 저장 검색 매칭 → Resend API → **실제 수신**(스팸함 아님)
  → `AlertDelivery` 기록. 15차부터 이월돼 있던 "Resend 확인" 숙제가 이걸로 닫혔다.
- **`scripts/lib/emailShell.mjs` 신설** — 6개 스크립트가 각자 인라인 HTML을 갖고 있어
  메일마다 생김새가 달랐다. `renderEmail()`/`infoCard()`로 통일:
  - 코랄 그라데이션 히어로(`#FF6250 → #FF8A72 → #FFB07E`) + **Outlook용 `bgcolor` 폴백**
    (Outlook은 CSS 그라데이션을 무시해 배경이 하얗게 뜬다)
  - 로고 · 슬로건("공공데이터로 찾는 믿을 수 있는 요양시설") · 돌보다 소개 블록
    (`withIntro` 옵션 — 첫 접촉 성격의 메일만 켠다)
  - **프리헤더**(받은편지함 미리보기 문구) · 불릿프루프 CTA 버튼(테이블 기반)
- **배치형 6개 스크립트 전부 이관**: `send-facility-alerts` · `send-care-log-reminders` ·
  `send-grade-test-reminders` · `send-sitter-job-alerts` · `send-open-request-nudges` ·
  `send-care-log-weekly`.
- ⚠️ **즉시형(매칭 확정·지원자 도착 등 API 라우트에서 나가는 메일)은 아직 안 옮겼다** —
  `lib/` 쪽 발송 경로를 쓴다. 다음에 알림을 손볼 때 같이 통일하면 좋다(급하진 않다).
- `SITE_SLOGAN`은 `lib/siteConfig.ts`와 `scripts/lib/emailShell.mjs`에 **사본이 둘** 있다
  (순수 node라 ts를 못 읽는다). 문구를 바꾸면 **양쪽 다** 고칠 것 — 주석에도 적어뒀다.

> ⚠️ **SPF 불일치를 이때 발견했다** — §0 첫 항목. 지금은 도착하지만 DMARC를 올리면
> 전 발송이 스팸으로 간다. 값 하나만 바꾸면 된다.

### 1-9. ★ 사진 활용 3종 (`70480b4` · `1191441` · `c53aa91`)

서울 3,189곳 사진이 들어왔는데 **"올려두기만" 한 상태**였다. 이미 가진 자산에서
더 뽑아내는 쪽으로 3가지를 했다(사용자 승인 후 순서대로).

**① 캡션을 SEO로** (`70480b4`) — 공단 원본 사진에는 캡션이 있는데 어디에도 안 쓰였다.
- 카드 페이로드에 `photoCaption`(첫 장만) 추가 → 썸네일 `alt`가 "○○요양원 시설 사진"
  대신 실제 캡션("2층 프로그램실 원예활동" 같은)으로. 구글 이미지 검색에 걸린다.
- 시설 상세 JSON-LD `image`에 실사진 6장, OG 카드도 **실사진 1장 + 기본 OG**.
  카톡·페북 공유 미리보기가 일러스트에서 실물로 바뀐다.
- ⚠️ **스톡 이미지는 `alt=""`를 유지했다** — 그 시설 사진이 아닌데 시설명을 붙이면
  검색엔진에도 스크린리더에도 거짓말이 된다. 코드 주석에 이유를 남겼다.

**② 영역 재분류** (`1191441`) — 사진의 21.6%가 `etc`로 뭉뚱그려져 있었다.
- `bath`/`care`/`office` 영역 신설 + 분류 규칙 보강 → **6,910장 이동**, etc 21.6% → 8%대
- `heroScore`에서 목욕(-50)·사무(-40) 감점 — 대표 사진이 화장실이면 안 된다
- ⚠️ **첫 규칙 초안이 오분류를 냈고 표본 검증에서 잡았다**: "족욕 프로그램",
  "목욕데이 봉사"가 `program` → `bath`로 42장 끌려갔다. **활동 우선 규칙**을 앞에 두고
  "프로그램실"은 방으로 보호해서 해결. **표본을 눈으로 안 봤으면 그대로 나갔다.**
- 재실행 가능: `node --env-file=.env.local scripts/reclassify-photo-areas.mjs`

**③ 실사진 필터** (`c53aa91`) — 보호자에게 "실물을 본 곳"과 "안 본 곳"은 무게가 다르다.
- `hasPhotoOnly` 필터를 시트·칩·배지 카운트·**저장 검색**(요약 문구·저장 판정·API 파싱)까지
  전 구간 연결. 한 곳이라도 빠지면 저장했다 불러올 때 조건이 조용히 사라진다.
- 검증: 도봉 검색 232곳 → 206곳, 적용 후 카드 30장 전부 실사진. 경계 6건 통과.
- 지금은 크게 좁히는 필터지만(사진 32%), **전국 사진이 들어오면 기본 기대치**가 된다.

---

## 2. 사용자 방침 (이 세션 신규)

1. **돌봄 종료 후 일지 유예 = 7일**(2026-08-06 결정). "어제 것을 오늘 쓰는" 정당한 지각
   작성은 살리고, 한참 뒤의 기록 생성은 막는다.
2. 15차 방침 전부 유효: 매니저 법적 거리 최대 확보 · 별의 상단 노출 허용(신뢰 데이터
   자체는 안 판다) · 추천 보상 가입 즉시 · **카카오톡은 마지막**.

## 3. 이 세션에서 배운 함정 (다음 세션 필독)

1. **"계속 진행하는" 배치는 부분 실패가 데이터를 오염시킨다.** 일일 동기화의 지역
   실패 → 대량 폐업 오탐이 전례. 전체 개수 가드는 부분 실패를 못 잡는다 —
   **실패 여부를 따로 기억해 파생 기록을 건너뛰어야** 한다.
2. **같은 조회를 여러 라우트에 복붙하면 결함도 복제된다.** 일지 계열 세 라우트가
   전부 같은 `findFirst desc` 결함을 갖고 있었다. 컨텍스트 찾기는 lib 하나로.
3. **화면과 서버가 같은 판단 함수를 써야 한다.** 유예 기간 폼이 100% 실패한 원인이
   "서버는 창을 아는데 화면은 몰랐다"였다.
4. **Tailwind `hoverOnlyWhenSupported`는 기본이 꺼짐** — 터치 기기에서 호버가 굳는다.
   config 변경은 **dev 서버 재시작해야** 반영된다(HMR이 안 잡는다 — 실측).
5. **`button`의 브라우저 기본 커서는 화살표다.** Tailwind preflight도 안 바꾼다.
6. **긴 한글은 `word-break:keep-all`과 만나면 컨테이너를 밀어낸다.** 공단 원본에
   띄어쓰기 없는 시설명이 실재한다. 좁은 셀·카드에선 `overflow-wrap:anywhere`로 예외를 둔다.
7. **모달에 body 잠금이 없으면 iOS에서 닫을 때 페이지가 맨 위로 튄다.**
   `overflow:hidden`만으론 부족 — 스크롤 위치를 기억했다 복원해야 한다.
8. **표본이 얇을 때 지표를 만들면 거짓말이 된다.** 3~4일치로 "회전율"을 말하면
   공공데이터 갱신 지연(8/3에 변동 5건뿐이던 날이 실재)을 추세로 착각한다.
9. **`prisma.$queryRaw`에서 문자열 날짜와 DateTime을 직접 비교하면 42883**.
   `to_char(... AT TIME ZONE 'Asia/Seoul','YYYY-MM-DD')`로 맞춘다.
10. **`.mjs` 스크립트에서 export를 쓰면 import 시 main이 돌아간다** — 직접 실행 판별
    (`import.meta.url` vs `process.argv[1]`)을 넣어야 테스트에서 안전하다.
11. **React key를 항목명으로 잡으면 중복이 난다** — 한 시설에 "상급침실사용료"가
    1인실·2인실로 두 번 오는 경우가 실재(155,000·310,000원).
12. **대용량 zip은 잘려서 도착할 수 있다.** 경북·강원 26GB가 그랬다 — yauzl이
    `End of central directory record signature not found`. **마지막 1MB를 훑어
    EOCD·ZIP64 EOCD·중앙 디렉터리를 찾아보면 즉시 판별**된다(셋 다 없으면 잘린 것).
    ⚠️ **부분 복구를 시도하지 말 것** — 중앙 디렉터리가 없으면 어떤 사진이 빠졌는지
    알 방법이 없어 **조용히 누락**된다. 다시 받는 게 유일한 답이다.
13. **🔴 dev 서버를 켠 채 `npm run build`를 돌리면 화면 전체가 먹통이 된다.**
    CLAUDE.md 1번 함정인데 **이 세션에서 내가 밟았다.** 프로덕션 빌드가 dev 청크를
    덮어써 React가 하이드레이트되지 않았고, 증상은 500이 아니라 **"버튼이 아무 반응이
    없다"**였다(HTML은 멀쩡히 보인다). "필터가 안 열린다"로 한참 헤맸다.
    → 빌드 전에 dev를 내리고, 빌드 후 재시작. **증상이 '클릭이 안 먹는다'면 이걸 먼저 의심.**
14. **분류 규칙을 바꿨으면 결과 표본을 눈으로 볼 것.** 사진 재분류에서 그럴듯한 규칙이
    42장을 엉뚱한 곳으로 옮겼다(§1-9 ②). 개수만 보면 "잘 옮겨졌다"로 보인다.
15. **내가 세운 가설도 수치로 확인하고 말할 것.** 사진 개선 제안에서 "사진 1장뿐인
    시설 1,485곳에 extras를 붙일 수 있다"고 했는데, 실측하니 **해당 0곳**이었다
    (extras는 12장 상한을 넘긴 시설에만 존재). 제안 단계에서 세어봤어야 했다.

### 3-A. iOS 실기기 체크리스트 (미검증 — 사용자 확인 필요)

1. 로그인/상담 모달 열고 **뒤 페이지 안 밀리는지**, 닫을 때 **원래 스크롤 위치인지**
2. 주소창 접힘/펼침 시 모달이 화면을 넘지 않는지(85dvh)
3. 입력창 탭할 때 **화면 확대 없는지**(폰트 상향 시스템이 막고 있음 — 확인만)
4. 하단 고정 CTA가 홈 인디케이터에 안 가리는지
5. (Android 보급형) 지도 바텀시트 드래그·시설 상세 스크롤 프레임

## 4. 커밋·배포 상태

**미커밋 0. 21커밋(`5fac819`→`c53aa91`) 전부 푸시** — Vercel 자동 배포.
66파일 +4,345줄 −311줄. 매 커밋 전 프로덕션 빌드 통과 확인.

> 이 문서는 `0489a32`(14커밋 시점)에 처음 쓰고, 이후 5커밋을 반영해 갱신했다.
> 갱신분: `214e053` `593f1b8`(메일 셸) · `70480b4` `1191441` `c53aa91`(사진 3종).

## 5. DB 변경 (전부 db push로 프로덕션 반영, 가산형)

- 필드 추가: **`Facility.missingSince`**(date, 인덱스) — 폐업 추정 시작일
- 신설 모델: **`CareLogWeeklyDigest`**(`[careRequestId, weekOf]` 복합 PK) — 주간 요약 발송 기록
- 데이터:
  - `missingSince` **소급 161곳**(2026-08-06)
  - 서울 사진 **3,189곳** `extra.photos`/`photoItems` 반영
  - 테스트 데이터 정리 1건 — 기간과 기록이 어긋난 돌봄(일지 4건)의 요청 기간·완료일을
    실제 기록에 맞춤(시작 08-07→08-04, 완료 08-02→08-05). **기록은 진술이라 안 건드렸다.**
  - **사진 영역 재분류 6,910장**(`FacilityPhoto.area` 갱신 — `program`은 보호). 되돌리려면
    `scripts/reclassify-photo-areas.mjs`의 규칙을 고쳐 다시 돌리면 된다(멱등).
  - `AlertDelivery` **첫 실데이터 기록**(그전까지 0행)

## 6. 검증 자산 (다음 세션이 재활용할 것)

시나리오 테스트를 스크래치에 만들어 돌렸다. **레포에는 없다**(테스트 러너가 없는
프로젝트라 임시로 작성). 같은 영역을 건드리면 다시 만들 가치가 있다:

| 대상 | 케이스 | 무엇을 잡았나 |
|---|---|---|
| 일지 날짜 창 | 21 | 시작 전·돌봄 중·종료 직후·유예 경계·조기/지연 완료·월말·연말 걸침 |
| 동기화 안전장치 | 10 | 임계 경계(551/552)·지역 실패·표기 개편형 대량 사라짐·작은 DB 하한 |
| 자리 추이 | 16 | 관측 0·1회·듬성듬성 forward-fill·입력 순서 무관·현원>정원·창 밖 관측 |
| 자리 인사이트 | 27 | 신뢰도 경계·예측 4조건·회전 세는 규칙·문장 생성 |
| 차트 좌표 | 41 | 값 전부 0(나눗셈 0)·데이터 1·2개·극단 스파이크·만 단위 |
| 비급여 기준값 | 7 | 없는 항목·지역·주소 결손·요양병원 형식·17개 시도 상식 범위 |
| 실사진 필터 | 6 | 사진 1장·여러 장·빈 배열·필드 없음·undefined·null |

**합계 128케이스 전부 통과.**

## 6-A. B2B 실태 점검이란 (사용자 질문에 답한 내용)

사용자가 "실태점검은 뭐지"라고 물어 설명한 것 — **다음 세션이 착수할 수 있게 남긴다.**

유료 기능 테이블이 **전부 0행**이다: `SponsorPlacement` · `FacilityBanner` ·
`FacilityPost` · `FacilityContent` · `AdEvent`. 즉 B2B 유료 기능은 **프로덕션에서
한 번도 실제로 돌아본 적이 없다.** 점검은 [docs/business-console-spec.md](docs/business-console-spec.md)의
**요금제가 약속한 것**과 **실제 구현**을 한 줄씩 대조하는 작업이다:

- 요금제별로 켜지는 기능이 실제로 켜지는가 / 만료 시 실제로 꺼지는가
- 콘솔 지표가 데이터 0행에서도 깨지지 않는가(빈 상태 화면)
- 노출 카운트·클릭 집계가 실제로 쌓이는가(`AdEvent` 경로)
- 결제·해지 흐름에 상태 가드가 있는가(CLAUDE.md 원칙 — 화면에서 버튼 숨기는 건 방어가 아니다)

**급하지 않다** — 첫 유료 고객을 받기 전까지는 실사용자 피해가 없다. 다만 **첫 고객을
받는 그날 전에는 반드시** 끝나 있어야 한다.

## 7. 로컬 환경 메모

- 테스트 계정: 운영자(`cms63hwkr…`, 매니저·닝닝이) / 교덩(`cmsdccyxf…`, 보호자·매니저 겸함).
  각 10,000P 보유. **카카오 로그인은 등록 도메인인 `localhost:3000`에서만 된다** —
  다른 포트에서 시도하면 KOE006. 세션 쿠키는 포트를 무시하므로 3000에서 로그인한 뒤
  다른 포트의 dev 서버에서도 그대로 쓰인다(이 세션에서 그렇게 검증함).
- `photo-data/서울특별시/` — jsonl 3종(inventory 8.43MB·manifest 4.46MB·extras 0.97MB) 보관 필수.
- 사진 zip은 `C:/Users/linea/Desktop/건강보험 중요자료/`에 온다. **`04_경상북도_강원특별자치도.zip`은
  잘린 파일이라 쓰면 안 된다** — 사용자가 다시 받는 중. 새로 받은 zip은 §3-12로 먼저 검증.
- 메일 발송 스크립트는 전부 `scripts/lib/emailShell.mjs`를 쓴다. 새 알림을 추가할 때
  인라인 HTML을 다시 쓰지 말 것(생김새가 갈라진다 — 그래서 이번에 통일했다).
- dev 서버가 여러 포트에 떠 있을 수 있다. **tailwind.config·prisma schema 변경은
  재시작 필요**(HMR이 안 잡는다).
- 카카오 지도는 등록 도메인(프로덕션)에서만 열린다(5차 §1-1).
