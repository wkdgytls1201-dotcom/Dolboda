# 돌보다 인수인계 — 10차 세션 (2026-08-04)

> 이 문서만 읽고 이어서 작업할 수 있게 썼습니다. 9차와 같은 날 이어진 세션이라
> 겹치는 배경 설명은 줄이고 **9차 이후 새로 한 일**만 상세히 적습니다.
> 이전: [9차](HANDOVER-2026-08-04-9차.md) · 5차의 §1(함정)·§3(규칙)·§7(돌봄 흐름 상태 전이 지도)는 여전히 유효합니다.

---

## 0. ★ 사용자가 직접 해야 할 일 (코드로는 못 하는 것)

### 0-1. 🔴 9차에서 이월 — 네이버 로그인 프로덕션 반영

아직 확인 안 됐으면 최우선입니다.
- Vercel 환경변수에 `AUTH_NAVER_ID`·`AUTH_NAVER_SECRET` 등록 후 재배포
- 네이버 개발자센터 검수(서비스 URL·Callback URL이 프로덕션 도메인으로 등록됐는지) 확인
- 프로덕션 콜백: `https://<프로덕션도메인>/api/auth/callback/naver`

### 0-2. 🟡 GitHub Actions 시크릿 — 이번 세션에서 워크플로가 4개로 늘었습니다

이번 세션에 **새 워크플로 3개**가 추가됐습니다 (`send-care-log-reminders`,
`send-grade-test-reminders`, `send-sitter-job-alerts`). 전부 기존
`send-facility-alerts`와 **같은 시크릿**(`DIRECT_URL`·`RESEND_API_KEY`·`MAIL_FROM`)을
씁니다 — 저장소 시크릿은 워크플로별이 아니라 저장소 전체에 등록되는 것이라, 9차
§0-1에서 이미 등록했다면 **추가 등록은 필요 없습니다**. 다만 9차 시점에도 등록
여부가 미확인 상태였으니, 아직이라면 네 워크플로가 전부 "사건만 세고 메일은
안 나가는" 상태일 겁니다.

등록됐는지 확인하려면: Actions 탭 → 네 워크플로 각각 → `workflow_dispatch`로 수동
실행 → 로그에서 "이메일없음"·"발송 0통"이 아니라 실제 "발송 N통"이 찍히는지 확인.
(단, 셋은 지금 **대상 자체가 0건**이라 시크릿이 등록돼 있어도 발송 로그를 보려면
실제로 조건에 맞는 데이터가 생겨야 합니다. `send-sitter-job-alerts`만은 예외 —
2026-08-04 기준 실제로 매칭되는 공고·매니저가 있어서, 시크릿만 등록돼 있으면
바로 발송 로그를 볼 수 있습니다 — §1-I 참고.)

### 0-3. 🟡 그 외 9차에서 이월된 것 (아직 확인 안 됐으면)

- Vercel `ADMIN_USER_IDS` — 계정 id `cms63hwkr0000n8hqpkhkik63`
- 공공데이터 제공신청(장기요양기관 평가 세부항목 점수 최우선)
- 공단 사진 zip 재다운로드
- 서치콘솔 canonical 확인 (8/6 예약 리마인드 — 이틀 남음)

### 0-4. 🟢 테스트 데이터 (참고, 급하지 않음)

이번 세션에서도 임시 테스트 계정을 만들어 썼지만 **전부 세션 안에서 직접 삭제**했습니다
(`test-guardian-reminder@example.invalid` 등 `.invalid` 도메인 계정 2개 + 딸린
CareRequest·Application·SitterProfile·CareProfile). DB에 남은 흔적 없습니다.
9차에서 언급한 네이버 테스트 계정("교덩")의 실제 CareRequest 1건은 이번 세션에서도
그대로 뒀습니다 — 필요하면 지워도 됩니다.

---

## 1. 이 세션(9차 이후)에서 한 일

> 이 문서는 세션 도중 한 번 커밋됐다가(`6d379d3`), 사용자 요청으로 **같은
> 문서에 계속 이어서** 갱신되고 있습니다. §1-I가 그 뒤에 추가된 부분입니다.

### 1-A. 인터랙션 전수 점검 마무리 (`2a82eaa`, `6bb5df5`, `38b8223`)

9차 시점에 "알림·찜 화면 점검"까지 끝난 상태였는데, 사용자가 계속 이어가자고
해서 안 본 영역을 마저 훑었습니다.

**전역 내비게이션** (`2a82eaa`) — 가장 중요한 발견. `components/MobileTabBar.tsx`
(모든 페이지 하단 탭바)와 `components/Header.tsx`(모바일 햄버거 드롭다운)가
**색상 전환만 있고 `active:` 눌림 피드백이 아예 없었습니다.** 웹뷰 앱화 예정이라
`hover:`가 안 먹는다는 원칙을 세션 내내 다른 화면에 적용해왔는데, 정작 모든
페이지에 뜨는 주 내비게이션 자체를 놓치고 있었습니다. `active:scale-90` +
옅은 배경으로 통일.

**매니저(시터) 쪽 화면 5개** (`6bb5df5`) — `jobs`·`register`·`settlements`·`tips`·
`workplaces`. `register`(5단계 가입 마법사)에 문제가 특히 많았습니다: 뒤로가기
버튼 44px 미만, 국적 칩·자격증 삭제/추가 버튼 눌림 피드백 없음, 활동 지역
그리드도 마찬가지. 흥미로운 점 — 활동 지역 그리드는 **프로필 관리 화면
(`app/mypage/sitter/profile/page.tsx`)엔 이미 `active:scale-95`가 있는데, 이
신규 가입 화면엔 복사가 안 돼 있었습니다.** 같은 UI를 두 곳에 만들면서 한쪽만
고친 전형적인 드리프트.

검증 방법: 실계정으로 가입 마법사를 실제로 3~4단계까지 진행하며 자격증
추가/삭제 버튼 히트박스를 44×44px로 정확히 측정(음수 마진으로 확장한 게
레이아웃을 안 깨는지 스크린샷 대조), 제출은 안 하고 이탈해 테스트 데이터를
안 남겼습니다.

**검색 페이지 상단바** (`38b8223`) — `FilterBar.tsx` 본체(칩·시트 내부)는 이미
잘 돼 있었는데, 정렬 버튼 3개·리스트/지도 뷰 전환·검색어 지우기(×)·"내 위치로
정렬" 버튼이 전부 `hover:`만 있고 `active:`가 없었습니다. FilterBar 시트의
닫기(X)·초기화 버튼도 마찬가지.

### 1-B. 버그 수정 — 관심 지역 "전남"·"광주" 저장이 조용히 실패 (`ec1fc07`)

**이번 세션에서 가장 심각했던 발견.** 저장 검색조건 기능을 만들다가 지역
매핑을 다시 보게 됐는데, `/api/favorites`의 PATCH가 `lib/regionSeo.ts`의
`REGION_SEO`(16개, "전남"·"광주"를 "전남광주" 하나로 합침)로 지역명을
검증하고 있었습니다. 그런데 `/notifications` 화면은 `lib/regions.ts`의
`REGIONS`(17개, 전남·광주 분리)로 칩을 그립니다. 그래서:

1. 사용자가 "전남" 또는 "광주" 칩을 누르면
2. 클라이언트는 즉시 켜진 것처럼 보여주고(`lib/alertPreferencesContext.tsx`의
   낙관적 업데이트)
3. 서버는 `REGION_SEO.some(r => r.slug === region)`이 `false`라 **400을 냄**
4. 그런데 `fetch()`는 400 같은 실패 **응답**을 reject하지 않는다 —
   `.catch()`가 안 걸려서 클라이언트가 실패를 전혀 몰랐다
5. 결과: 화면·localStorage엔 켜진 채로 영원히 남지만, `RegionInterest` 행은
   **평생 안 만들어짐**. 그 사용자는 "전남 관심 지역 켰다"고 믿지만 실제로는
   알림이 절대 안 갑니다.

`REGION_SEO` 대신 `REGIONS`로 검증하도록 API를 고치고, 클라이언트도 `res.ok`를
확인해 실패 시 실제로 롤백하도록 함께 고쳤습니다(같은 클래스의 버그가 다른
곳에 또 숨어있어도 이번처럼 조용히 넘어가지 않게).

**실계정 검증**: "전남" 토글 → PATCH 200 확인 → 새로고침 후에도 solid
스타일 유지되는 것으로 서버 저장 실제 확인 → 원복.

⚠️ **이 지역 taxonomy 이중화 문제(REGIONS 17개 vs REGION_SEO 16개)는 완전히
해소된 게 아닙니다.** `app/api/sitter/workplaces/route.ts`의 `prefixesFor`
함수가 같은 문제를 이미 한 번 우회해뒀고(주석에 명시), 이번에
`scripts/send-facility-alerts.mjs`에도 `REGION_PREFIXES`라는 이름으로 세
번째 사본이 생겼습니다(TS lib를 순수 node 스크립트가 import 못 해서 어쩔 수
없이 복제). **다음에 지역 관련 코드를 새로 짤 땐 반드시 `REGIONS`(17개, 광주·
전남 분리)를 기준으로 잡을 것** — `REGION_SEO`는 SEO 랜딩 페이지(`/region/...`)
전용이지 관심 지역·활동 지역 표기의 정답이 아닙니다.

### 1-C. 알림 3단계 마무리 — 이메일 없는 계정 안내 (`1087d3b`)

- `/notifications`에 이메일 없는 계정용 경고 배너(카카오 재로그인 유도)
- `auth.ts`에 `signIn` 콜백 추가 — Auth.js가 이미 연결된 카카오 계정으로
  재로그인할 때 DB의 기존 `User` 행을 그대로 쓰고 갱신 안 한다는 걸 소스
  (`@auth/core`의 `handle-login.js`)에서 직접 확인했습니다. 배너만 있으면
  "다시 로그인하세요" 안내가 실제로는 아무 효과 없는 거짓 안내가 될
  뻔했습니다. 재로그인 시 프로필에 새로 이메일이 왔는데 DB엔 없으면 그때
  채워 넣도록 고쳤습니다(이미 있는 이메일은 절대 안 덮어씀).
- ⚠️ 이메일 없는 실제 카카오 계정이 없어서 이 `signIn` 콜백은 e2e 검증을
  못 했습니다. 다음에 그런 계정이 생기면 확인 필요.

### 1-D. 알림 3단계 마무리 — 관심 지역 신규 시설 알림 (`25dd557`)

`scripts/send-facility-alerts.mjs`에 세 번째 사건 판정 추가: `Facility.createdAt`이
오늘(KST)인 시설을 찾아 주소에서 시/도를 뽑고, `RegionInterest`에 그 지역을
등록한 사람 전원에게 보냅니다.

- 별도 스크립트로 안 만든 이유: "사람당 메일 한 통" 원칙을 지키려면 빈자리·
  등급변동·신규등록을 같은 `byUser` 묶음에 넣어야 합니다.
- **검증 중 발견**: `Facility.createdAt`은 `timestamp without time zone`
  컬럼입니다. SQL의 `AT TIME ZONE 'Asia/Seoul'`로 날짜 경계를 확인하면
  **오히려 틀린 결과**가 나옵니다(이미 UTC 인스턴트로 저장된 값을 로컬
  시간으로 잘못 재해석해서 한 번 더 시프트됨). JS `Date` 산술(±9시간)로
  직접 UTC 경계를 계산하는 지금 방식이 맞다는 걸 두 방법을 대조해서
  확인했습니다.
- 실제 DB에서 대량 시설이 생성된 날(2026-07-30, 23,817곳)로 드라이런해
  날짜 경계·지역 매핑 검증. 주소에 시/도가 아예 없는 35건만 정상적으로
  건너뜀 확인.

### 1-E. 저장 검색조건 리마인드 (`a4bcbbe`)

리텐션 강화용 신규 기능. `/search` 필터 시트에서 조건(유형·등급·거리·진료과목·
프로그램태그·빈자리만·공공데이터확인·안심지수우수)을 저장하면(`SavedSearch`
모델, 최대 5개/인), 그 조건에 맞는 새 시설이 등록될 때 알려줍니다.

**새 파일**: `app/api/saved-searches/route.ts`(GET·POST), `app/api/saved-searches/[id]/route.ts`
(DELETE), `lib/facilityFilters.ts`, `lib/savedSearch.ts`. **수정**:
`components/FilterBar.tsx`(저장 버튼), `app/notifications/page.tsx`(목록·삭제 UI).

- **매칭 범위를 의도적으로 줄임** — `send-facility-alerts.mjs`(순수 node
  스크립트)가 값싸게 재현 가능한 조건(유형·등급·진료과목·프로그램태그·
  빈자리·공공데이터확인)만 매칭합니다. `maxDistanceKm`(사용자 위치를 서버에
  안 둠)·`goodScoreOnly`(안심지수는 즉석 계산이라 순수 node에서 재현 비용이
  큼 — §근무환경지수와 같은 문제, 아래 §2-1 참고)는 저장은 해두되 매칭에서는
  건너뜁니다. "놓치는 것보다 조금 넓게 잡는 게 낫다"는 판단.
- **아키텍처 정리** — `FacilityFilters` 타입·`EMPTY_FILTERS`를 `FilterBar.tsx`
  (클라이언트 컴포넌트)에서 `lib/facilityFilters.ts`(순수 타입 파일)로
  옮겼습니다. 서버 API가 클라이언트 컴포넌트 파일에서 값을 import하면 그
  파일 전체("use client" + 아이콘 + AuthModal)가 서버 함수 번들에 딸려
  들어가는 걸 막기 위해서입니다.
- **메일 폭탄 방지** — 조건이 넓으면(유형 하나만) 대량 등록일에 수천 건이
  한꺼번에 매칭될 수 있습니다(실측: 5,071건). 메일당 카드 20개로 자르고
  "외 N건 더"를 안내. `AlertDelivery`는 잘린 것 포함 전부 기록해 내일
  다시 안 옵니다.
- **개발 중 트러블슈팅**: 스키마 push·generate 후 dev 서버를 재시작 안 해서
  `prisma.savedSearch`가 `undefined`로 500 에러가 났습니다 — Next dev
  서버는 `@prisma/client` 재생성을 자동으로 못 집어서 수동 재시작이
  필요합니다(이 프로젝트에서 몇 번째 반복되는 함정 — §3 참고).
- 실계정 검증: "요양원" 필터 저장 → 200 확인 → `/notifications` 목록
  반영 확인 → 삭제까지 실계정에서 확인. 대량 등록일로 드라이런해 5,071건
  매칭·수신자 1명 정상 계산(`--write`는 안 돌림).

### 1-F. 돌봄일지 정기 체크인 (`05c6d6f`)

진행 중인 매칭 돌봄(`CareRequest.status === "MATCHED"`이고 오늘이 기간 안)의
마지막 일지로부터 3일 이상 지나면 매니저에게 메일. 3일 쿨다운.

**새 파일**: `scripts/send-care-log-reminders.mjs`,
`.github/workflows/send-care-log-reminders.yml`. **새 모델**: `CareLogReminder`
(요청 하나당 한 행, `sentAt`만 계속 갱신 — `AlertDelivery`처럼 날짜별로 안 쌓는
더 단순한 쿨다운 설계).

별도 스크립트로 둔 이유: 대상(매니저)과 판정 기준이 보호자 쪽 찜·관심지역
알림과 완전히 다르고 수신자가 거의 안 겹칩니다.

⚠️ **2026-08-04 기준 매칭확정 CareRequest가 실서비스에 0건이라 실사용
데이터로 검증 못 했습니다.** 임시 테스트 계정(`.invalid` 이메일) 2개로
매칭확정 요청을 하나 만들어 대상 탐지 로직만 확인(정상 1건 탐지)하고 즉시
삭제했습니다. 실제 이메일 발송(`--write`)까지는 검증 안 함(진짜 매칭이
생기면 `workflow_dispatch`로 수동 확인 필요).

### 1-G. 등급테스트 재검사 리마인드 (`f66d4a4`)

처음엔 "등급테스트 결과가 서버에 전혀 저장이 안 된다"고 판단해서 새 저장소가
필요하다고 봤는데, 다시 확인하니 **틀린 판단이었습니다** — 결과 화면의
"프로필에 저장" 버튼을 타면 이미 `CareProfile.estimatedBand`·`estimatedAt`에
서버 저장되고 있었습니다(`app/api/care-profile/route.ts`). 그래서 새 모델
없이 그 값만 보면 됐습니다.

대상: `estimatedAt`이 90일(약 3개월) 이상 지났고, 아직 실제 장기요양등급
(`ltcGrade`)이 없는 프로필. 이미 확정 등급을 받았으면 "예상"을 다시 볼
이유가 없어서 제외. 쿨다운 30일.

**새 파일**: `scripts/send-grade-test-reminders.mjs`,
`.github/workflows/send-grade-test-reminders.yml`. **새 모델**: `GradeTestReminder`
(CareLogReminder와 같은 설계).

⚠️ 예상 구간을 90일 전에 저장해둔 사람이 실서비스에 아직 없어 검증 못
했습니다. 테스트 계정으로 `estimatedAt`을 100일 전으로 둔 `CareProfile`을
만들어 탐지(1건) 확인 → `ltcGrade`를 실제 값으로 채워 제외되는지(0건)까지
확인 → 삭제.

### 1-H. 리텐션 아이디어 중 보류한 것 — 매니저 근무환경지수 알림

사용자가 처음 요청한 "매니저 쪽 근무환경지수 알림"(지수가 오르면 매니저에게
알림)은 **구현하지 않았습니다.** 두 가지 진짜 이유:

1. 근무환경지수(`lib/workIndex.ts`)는 매일 스냅샷이 안 쌓이고 그때그때
   `Facility` 원본에서 즉석 계산되는 값이라, "어제보다 올랐다"를 알려면
   전용 일별 스냅샷 테이블을 새로 만들거나 200줄짜리 계산 로직을 순수
   node 스크립트에 통째로 복제해야 합니다.
2. **더 근본적으로**, 매니저 쪽엔 알림 수신자 명부 자체가 없습니다.
   `/mypage/sitter/notifications`의 토글이 `localStorage`에만 저장돼서,
   보호자 쪽처럼 서버가 "누가 원하는지"를 모릅니다.

사용자가 "A(매니저 알림 인프라)는 다음에, B(저장 검색조건)부터"로 순서를
정해서 이 시점엔 안 건드렸는데, **같은 세션 안에서 바로 이어서 §1-I로
처리했습니다.**

### 1-I. 매니저 알림 인프라 서버 이전 + "새 일자리 알림" 발송 (`2897351`, `e714b9e`)

§1-H에서 보류했던 것을 사용자가 "이어서 만들어"로 바로 이어갔습니다.

**서버 이전** (`2897351`) — `SitterNotificationPref` 모델 신설(사람당 한 행,
`userId`를 그대로 기본키로 씀 — `SitterProfile`처럼 별도 id를 만들 필요가 없어서).
`/api/sitter/notification-prefs`(GET·PATCH)를 보호자 쪽 `/api/favorites`와 같은
구조로 만들고, `app/mypage/sitter/notifications/page.tsx`가 `localStorage` 대신
이 API를 쓰도록 다시 짰습니다. 클라이언트는 `res.ok`를 직접 확인해 저장 실패
시 롤백합니다 — §1-B에서 겪은 "`fetch()`는 400을 reject 안 한다" 교훈을
이번엔 처음부터 반영했습니다.

**새 일자리 알림** (`e714b9e`, `scripts/send-sitter-job-alerts.mjs`) — 오늘
새로 등록된 `CareRequest`(상태 OPEN, 매니저 입장에선 "채용 공고") 중 매니저
활동 지역(`SitterProfile.regions`)과 겹치는 걸 찾아, `newJob` 알림을 켜둔
사람에게 메일로 보냅니다.

- **관심 지역 알림보다 훨씬 간단했습니다** — `CareRequest.region`과
  `SitterProfile.regions`가 **둘 다 처음부터** `lib/regions.ts`의
  `REGIONS`(17개) 표기를 그대로 씁니다. 시설 알림처럼 주소를 파싱해 지역을
  추론할 필요가 없어서, §1-B·§3-4에서 반복된 "지역 taxonomy 이중화" 문제
  자체가 안 생겼습니다.
- **쿨다운이 필요 없습니다** — 공고가 "오늘 새로 생김"은 평생 한 번뿐인
  사건이라, `SitterJobAlertDelivery`(userId+careRequestId 조합을 기본키로
  삼아 중복 발송만 막음)만으로 충분합니다. 날짜별 기록이나 며칠 단위 쿨다운
  개념 자체가 없습니다.
- **사용자가 명시적으로 요청한 것**: 스크립트 전체에 초보자도 따라 읽을 수
  있을 만큼 자세한 주석. 판정 로직 각 단계가 "무엇을·왜" 하는지, DB 기록
  순서가 왜 그래야 하는지까지 문장으로 풀어 썼습니다 — 다음 사람이 코드만
  보고도 흐름을 따라갈 수 있게 하는 게 목적입니다.
- **검증**: 임시 테스트 계정을 만들 필요도 없이 **실제 프로덕션 데이터로 바로
  확인됐습니다** — 2026-08-04 당일 실제로 등록된 공고 1건이 활동 지역이
  겹치는 매니저 3명과 매칭되고, 그중 이메일이 있는 1명에게 정상적으로 갈
  준비가 된 것을 드라이런으로 확인했습니다(`--write`는 안 돌려 실제 발송은
  안 함).

⚠️ **아직 안 된 것 — "매칭 진행 알림"(`SitterNotificationPref.matchUpdate`)**.
토글은 서버에 저장되지만 발송 로직이 없습니다. 이건 다른 알림들과 성격이
달라서(매일 새벽 배치가 아니라 지원 상태가 바뀌는 **그 순간** 나가야 자연스러움)
배치 스크립트가 아니라, 지원 상태를 바꾸는 API 라우트(매칭확정·미선정 처리하는
곳)에 발송을 직접 끼워 넣는 다른 모양의 작업이 될 겁니다. §2-1 참고.

---

## 2. 다음에 할 일 (우선순위)

### 2-1. 매칭 진행 알림(matchUpdate) 발송 — §1-I에서 미룬 것

`SitterNotificationPref.matchUpdate` 토글은 이미 서버에 저장되는데 실제 발송
로직이 없습니다. 배치 스크립트가 아니라 **지원 상태를 바꾸는 API 라우트에
직접 발송을 끼워 넣는** 다른 모양의 작업입니다. `CareRequestApplication.status`가
"매칭확정"·"미선정"으로 바뀌는 지점(아마 `app/api/care-requests/[id]/route.ts`
근처 — 정확한 위치는 재확인 필요)을 찾아서, 그 상태 변경이 성공한 직후 해당
매니저에게 메일을 보내면 됩니다. 배치 알림들과 달리 **쿨다운·중복방지가
자연스럽게 해결됩니다**(한 번 바뀐 상태는 다시 안 바뀌니까 — 다만 API가
같은 요청으로 두 번 불릴 가능성은 여전히 고려해야 합니다).

### 2-2. 네 알림 워크플로의 첫 실사용 검증

`send-care-log-reminders`·`send-grade-test-reminders`는 대상 0건 상태로
로직만 확인했습니다. `send-facility-alerts`·`send-sitter-job-alerts`는 실제
데이터로 매칭까지는 확인했지만 `--write`(진짜 발송)는 아직 아무것도 안
돌렸습니다. 실제 사용자 데이터가 쌓이면(매칭 발생, 저장 검색조건 실사용자
등) `workflow_dispatch`로 수동 실행해 진짜 이메일이 나가는지 한 번씩 확인
필요합니다.

### 2-3. 웹푸시 → 앱 FCM

알림 인프라 로드맵의 마지막 단계. 네이티브 앱 래핑이 되어야 의미가 있습니다.

### 2-4. 리텐션 로드맵 (남은 것)

1. **입소 대기 예측** — 여전히 1순위, 아직 손 안 댐
2. 돌봄일지 2단계(사진 업로드 등) — 여전히 매칭 0건이라 검증 불가한 상태
3. 근무환경지수 변화 감지 알림 — §1-H에서 보류. 즉석 계산 지표라 일별
   스냅샷 없이는 값싸게 못 만든다는 문제가 안 풀렸습니다.

---

## 3. 이 세션에서 배운 함정

1. **`fetch()`는 HTTP 실패 응답(400·500 등)을 reject하지 않는다.** `.catch()`만
   믿으면 서버 에러를 완전히 놓친다 — 항상 `res.ok`를 직접 확인할 것.
   (§1-B의 "전남"·"광주" 버그가 몇 주간 안 들킨 직접적 원인.)
2. **Prisma 스키마를 push·generate한 뒤엔 dev 서버를 반드시 재시작해야 한다.**
   Next.js dev 서버는 `@prisma/client` 재생성을 자동으로 못 집어서, 새 모델을
   `prisma.xxx`로 부르면 `undefined`째 500이 난다. 이번 세션에서 최소 두 번
   반복(9차 이전에도 있었던 패턴).
3. **같은 UI를 두 화면에 복사하면 한쪽만 고치고 잊기 쉽다.** 매니저 활동
   지역 그리드가 프로필 관리 화면엔 `active:scale-95`가 있는데 신규 가입
   화면엔 없었던 것처럼. 비슷한 컴포넌트를 새로 만들 때 기존 화면과 diff를
   한 번 대조해볼 것.
4. **지역 표기가 두 가지 taxonomy로 나뉘어 있다는 걸 계속 잊는다.**
   `lib/regions.ts`의 `REGIONS`(17개, 광주·전남 분리, 관심지역/활동지역의
   정답)와 `lib/regionSeo.ts`의 `REGION_SEO`(16개, "전남·광주" 통합, SEO
   랜딩 페이지 전용)를 헷갈리면 조용히 실패하는 코드가 나온다. 이번 세션에만
   두 번(API 검증 버그, 발송 스크립트 매핑) 부딪혔다.
5. **즉석 계산되는 지표(근무환경지수·안심지수)는 "변화 감지" 알림을 값싸게
   못 만든다.** 일별 스냅샷이 따로 없으면 전용 스냅샷 테이블을 새로 만들거나
   계산 로직을 발송 스크립트에 복제해야 한다 — 둘 다 가볍지 않은 결정이라
   섣불리 시작하지 말고 먼저 스코프를 사용자와 맞출 것.
6. **"저장이 안 될 것 같다"는 가정은 코드를 직접 확인하기 전엔 결론 내리지
   말 것.** 등급테스트 재검사 리마인드를 처음엔 "새 저장소 필요"로 오판했다가,
   `app/api/care-profile/route.ts`를 실제로 열어보고서야 이미 저장되고
   있다는 걸 알았다. 추측으로 스코프를 크게 잡기 전에 관련 API 코드를
   먼저 읽을 것.

---

## 4. ⚠️ 미커밋 상태 (계속 보류, 9차·8차·7차와 동일)

B2B 입점 절차 개편 코드가 여전히 working tree에만 있습니다:
`app/admin/AdminClient.tsx` · `app/api/admin/inquiries/route.ts` ·
`app/api/business-inquiry/route.ts` · `app/business/BusinessInquiryForm.tsx` ·
`app/business/console/page.tsx` · `app/business/page.tsx` · `lib/businessPlans.ts` ·
`app/business/apply/`(미추적)

**사용자가 "나중에 다시 할게"로 보류한 상태.** 이번 세션에서도 건드리지
않았습니다. 임의로 커밋·배포하지 말 것.

---

## 5. DB 변경 (전부 프로덕션 반영 완료)

| 모델 | 용도 |
|---|---|
| `SavedSearch` | 저장한 검색조건(`FacilityFilters` JSON, 최대 5개/인) |
| `CareLogReminder` | 돌봄일지 체크인 알림 쿨다운(요청당 1행, `sentAt` 갱신) |
| `GradeTestReminder` | 등급테스트 재검사 알림 쿨다운(프로필당 1행, `sentAt` 갱신) |
| `SitterNotificationPref` | 매니저 알림 설정(`userId`가 그대로 기본키, `newJob`·`matchUpdate`) |
| `SitterJobAlertDelivery` | 새 일자리 알림 중복 방지(`userId`+`careRequestId` 조합이 기본키) |

기존 모델 필드 변경은 없음(`CareProfile.estimatedBand`·`estimatedAt`은 이미
있던 필드를 재활용). `User`에 `savedSearches SavedSearch[]`·
`sitterNotificationPref SitterNotificationPref?` 관계 추가.

---

## 6. GitHub Actions 워크플로 현황 (전부 09:00 KST)

| 워크플로 | 스크립트 | 대상 |
|---|---|---|
| `send-facility-alerts` | `scripts/send-facility-alerts.mjs` | 보호자 — 찜한 시설 빈자리·등급변동, 관심 지역·저장 검색조건 신규 시설 |
| `send-care-log-reminders` | `scripts/send-care-log-reminders.mjs` | 매니저 — 진행 중인 매칭 돌봄, 일지 3일 이상 없음 |
| `send-grade-test-reminders` | `scripts/send-grade-test-reminders.mjs` | 보호자 — 등급테스트 예상 구간 90일 이상 경과, 실제 등급 미확정 |
| `send-sitter-job-alerts` | `scripts/send-sitter-job-alerts.mjs` | 매니저 — 활동 지역에 새 돌봄 공고(CareRequest) 등록 |

넷 다 시크릿은 동일(`DIRECT_URL`·`RESEND_API_KEY`·`MAIL_FROM`), `workflow_dispatch`로
`date` 입력받아 수동 실행 가능. **모든 알림 채널은 이메일뿐**입니다 — 인앱
알림함(`/notifications`의 "알림함" 섹션)은 아직 껍데기고, 카카오톡 발송도
아직 안 씀(`auth.ts`가 로그인 시 `talk_message` 권한은 미리 받아뒀지만
실제로는 사용 안 함 — 나중에 채널 추가할 때 이 구조 그대로 이메일 발송
옆에 카카오 발송만 추가하면 되게 짜여 있음).
